# 外描边设置

设置组件外描边（outline）样式。外描边绘制在组件的外侧，不影响布局，不会占用组件本身大小。

## Functions

### public func outline( width!: Length, color!: ResourceColor = Color.BLACK, radius!: Length = 0.vp, style!: OutlineStyle = OutlineStyle.SOLID ): This

```cangjie
public func outline( width!: Length, color!: ResourceColor = Color.BLACK, radius!: Length = 0.vp, style!: OutlineStyle = OutlineStyle.SOLID ): This
```
功能： 设置外描边样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 命名参数。 外描边宽度，不支持百分比。 说明： width为必设项，否则不显示外描边。 | width | - |
| ResourceColor | 否 | 命名参数。 外描边颜色。 | color | Color.BLACK |
| Length | 否 | 命名参数。 外描边圆角半径，不支持百分比。 说明： 最大生效值：组件width/2 + outlineWidth或组件height/2 + outlineWidth。 | radius | 0.vp |
| OutlineStyle | 否 | 命名参数。 外描边样式。 | style | OutlineStyle.SOLID |

### public func outlineColor(value: ResourceColor): This

```cangjie
public func outlineColor(value: ResourceColor): This
```
功能： 设置元素的外描边颜色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 元素的外描边颜色。 初始值：Color.BLACK | value | - |

### public func outlineColor( top!: ResourceColor = Color.BLACK, right!: ResourceColor = Color.BLACK, bottom!: ResourceColor = Color.BLACK, left!: ResourceColor = Color.BLACK): This

```cangjie
public func outlineColor( top!: ResourceColor = Color.BLACK, right!: ResourceColor = Color.BLACK, bottom!: ResourceColor = Color.BLACK, left!: ResourceColor = Color.BLACK): This
```
功能： 设置元素的外描边颜色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 否 | 命名参数。 上侧外描边颜色。 | top | Color.BLACK |
| ResourceColor | 否 | 命名参数。 右侧外描边颜色。 | right | Color.BLACK |
| ResourceColor | 否 | 命名参数。 下侧外描边颜色。 | bottom | Color.BLACK |
| ResourceColor | 否 | 命名参数。 左侧外描边颜色。 | left | Color.BLACK |

### public open func outlineRadius(value: Length): This

```cangjie
public open func outlineRadius(value: Length): This
```
功能： 设置元素的外描边圆角半径。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 元素的外描边圆角半径，不支持百分比。 初始值：0。 最大生效值：组件width/2 + outlineWidth或组件height/2 + outlineWidth。 | value | - |

### public func outlineRadius( topLeft!: Length = 0.vp, topRight!: Length = 0.vp, bottomLeft!: Length = 0.vp, bottomRight!: Length = 0.vp): This

```cangjie
public func outlineRadius( topLeft!: Length = 0.vp, topRight!: Length = 0.vp, bottomLeft!: Length = 0.vp, bottomRight!: Length = 0.vp): This
```
功能： 设置元素的外描边圆角半径。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 否 | 命名参数。 左上角圆角半径。 | topLeft | 0.vp |
| Length | 否 | 命名参数。 右上角圆角半径。 | topRight | 0.vp |
| Length | 否 | 命名参数。 左下角圆角半径。 | bottomLeft | 0.vp |
| Length | 否 | 命名参数。 右下角圆角半径。 | bottomRight | 0.vp |

### public open func outlineStyle(value: OutlineStyle): This

```cangjie
public open func outlineStyle(value: OutlineStyle): This
```
功能： 设置元素的外描边样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| OutlineStyle | 是 | 元素的外描边样式。 初始值：OutlineStyle.SOLID | value | - |

### public func outlineStyle( top!: OutlineStyle = OutlineStyle.SOLID, right!: OutlineStyle = OutlineStyle.SOLID, bottom!: OutlineStyle = OutlineStyle.SOLID, left!: OutlineStyle = OutlineStyle.SOLID): This

```cangjie
public func outlineStyle( top!: OutlineStyle = OutlineStyle.SOLID, right!: OutlineStyle = OutlineStyle.SOLID, bottom!: OutlineStyle = OutlineStyle.SOLID, left!: OutlineStyle = OutlineStyle.SOLID): This
```
功能： 设置元素的外描边样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| OutlineStyle | 否 | 命名参数。 上侧外描边样式。 | top | OutlineStyle.SOLID |
| OutlineStyle | 否 | 命名参数。 右侧外描边样式。 | right | OutlineStyle.SOLID |
| OutlineStyle | 否 | 命名参数。 下侧外描边样式。 | bottom | OutlineStyle.SOLID |
| OutlineStyle | 否 | 命名参数。 左侧外描边样式。 | left | OutlineStyle.SOLID |

### public open func outlineWidth(value: Length): This

```cangjie
public open func outlineWidth(value: Length): This
```
功能： 设置元素的外描边宽度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 元素的外描边宽度。初始值：0 。 说明： 不支持百分比。 外描边效果width为必设项。 | value | - |

### public func outlineWidth( top!: Length = 0.vp, right!: Length = 0.vp, bottom!: Length = 0.vp, left!: Length = 0.vp): This

```cangjie
public func outlineWidth( top!: Length = 0.vp, right!: Length = 0.vp, bottom!: Length = 0.vp, left!: Length = 0.vp): This
```
功能： 设置外描边的宽度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 否 | 命名参数。 上侧外描边宽度。 | top | 0.vp |
| Length | 否 | 命名参数。 右侧外描边宽度。 | right | 0.vp |
| Length | 否 | 命名参数。 下侧外描边宽度。 | bottom | 0.vp |
| Length | 否 | 命名参数。 左侧外描边宽度。 | left | 0.vp |


## Examples

```cangjie
package ohos_app_cangjie_entry

import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 public func build() {
 Column() {
 Row(30) {
 // 线段
 Text('DASHED')
 .backgroundColor(Color.PINK)
 .outlineStyle(OutlineStyle.DASHED)
 .outlineWidth(5.vp)
 .outlineColor(Color.BLUE)
 .outlineRadius(10.vp)
 .width(120.vp)
 .height(120.vp)
 .textAlign(TextAlign.Center)
 .fontSize(16)
 // 点线
 Text('DOTTED')
 .backgroundColor(Color.PINK)
 .outline(width: 5.vp, color: Color.GREEN, radius: 10.vp, style: OutlineStyle.DOTTED)
 .width(120.vp)
 .height(120.vp)
 .textAlign(TextAlign.Center)
 .fontSize(16)
 }
 .width(300.vp)
 .height(150)

 Text('.outlineWidth/outlineColor/outlineRadius/outlineStyle')
 .backgroundColor(Color.PINK)
 .fontSize(20.vp)
 .width(300.vp)
 .height(300.vp)
 .textAlign(TextAlign.Center)
 .outlineWidth(left: 3, right: 6, top: 10, bottom: 15)
 .outlineColor(left: Color.ORANGE, right: Color.BLUE, top: Color.RED, bottom: Color.GREEN)
 .outlineRadius(topLeft: 10, topRight: 20, bottomLeft: 40, bottomRight: 80)
 .outlineStyle(
 left: OutlineStyle.DOTTED,
 right: OutlineStyle.DOTTED,
 top: OutlineStyle.SOLID,
 bottom: OutlineStyle.DASHED
 )
 }
 .height(700.vp)
 .width(380.vp)
 .borderWidth(1.vp)
 }
}
```

