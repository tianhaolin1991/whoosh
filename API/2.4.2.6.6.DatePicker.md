# DatePicker

日期选择器组件，用于根据指定日期范围创建日期滑动选择器。

## Component Constructors

### public init( start!: DateTime = DateTime.of(year: 1970, month: Month.of(1), dayOfMonth: 1), end!: DateTime = DateTime.of(year: 2100, month: Month.of(12), dayOfMonth: 31), selected!: DateTime = DateTime.now() )

```cangjie
public init( start!: DateTime = DateTime.of(year: 1970, month: Month.of(1), dayOfMonth: 1), end!: DateTime = DateTime.of(year: 2100, month: Month.of(12), dayOfMonth: 31), selected!: DateTime = DateTime.now() )
```
功能： 根据指定范围的DateTime创建可以选择日期的滑动选择器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| DateTime | 否 | 命名参数。 指定选择器的起始日期。 | start | DateTime.of(year: 1970, month: Month.of(1), dayOfMonth: 1) |
| DateTime | 否 | 命名参数。 指定选择器的结束日期。 | end | DateTime.of(year: 2100, month: Month.of(12), dayOfMonth: 31) |
| DateTime | 否 | 命名参数。 设置选中项的日期。 | selected | DateTime.now() |

### public init( start!: DateTime = DateTime.of(year: 1970, month: Month.of(1), dayOfMonth: 1), end!: DateTime = DateTime.of(year: 2100, month: Month.of(12), dayOfMonth: 31), selected!: (DateTime, (DateTime) -> Unit) )

```cangjie
public init( start!: DateTime = DateTime.of(year: 1970, month: Month.of(1), dayOfMonth: 1), end!: DateTime = DateTime.of(year: 2100, month: Month.of(12), dayOfMonth: 31), selected!: (DateTime, (DateTime) -> Unit) )
```
功能： 根据指定范围的DateTime创建可以选择日期的滑动选择器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| DateTime | 否 | 命名参数。 指定选择器的起始日期。 | start | DateTime.of(year: 1970, month: Month.of(1), dayOfMonth: 1) |
| DateTime | 否 | 命名参数。 指定选择器的结束日期。 | end | DateTime.of(year: 2100, month: Month.of(12), dayOfMonth: 31) |
| (DateTime,(DateTime)->Unit) | 是 | 命名参数。 设置选中项的日期。该参数支持@Binder双向绑定变量。 | selected | - |


## Functions

### public func disappearTextStyle( color!: ResourceColor = 0xff182431, size!: Length = 14.fp, weight!: FontWeight = FontWeight.Regular, family!: String = "HarmonyOS Sans", style!: FontStyle = FontStyle.Normal ): This

```cangjie
public func disappearTextStyle( color!: ResourceColor = 0xff182431, size!: Length = 14.fp, weight!: FontWeight = FontWeight.Regular, family!: String = "HarmonyOS Sans", style!: FontStyle = FontStyle.Normal ): This
```
功能： 设置所有选项中最上和最下两个选项的文本样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 否 | 命名参数。 文本颜色。 | color | 0xff182431 |
| Length | 否 | 命名参数。 文本尺寸。不支持百分比设置。 | size | 14.fp |
| FontWeight | 否 | 命名参数。 字体粗细。 | weight | FontWeight.Regular |
| String | 否 | 命名参数。 字体列表。 | family | "HarmonyOS Sans" |
| FontStyle | 否 | 命名参数。 字体样式。 | style | FontStyle.Normal |

### public func disappearTextStyle( color!: ResourceColor = 0xff182431, size!: Length, weight!: FontWeight = FontWeight.Regular, family!: AppResource, style!: FontStyle = FontStyle.Normal ): This

```cangjie
public func disappearTextStyle( color!: ResourceColor = 0xff182431, size!: Length, weight!: FontWeight = FontWeight.Regular, family!: AppResource, style!: FontStyle = FontStyle.Normal ): This
```
功能： 设置所有选项中最上和最下两个选项的文本样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 否 | 命名参数。 文本颜色。 | color | 0xff182431 |
| Length | 是 | 命名参数。 文本尺寸。不支持百分比设置。 | size | - |
| FontWeight | 否 | 命名参数。 字体粗细。 | weight | FontWeight.Regular |
| AppResource | 是 | 命名参数。 字体列表。 | family | - |
| FontStyle | 否 | 命名参数。 字体样式。 | style | FontStyle.Normal |

### public func lunar(isLunar: Bool): This

```cangjie
public func lunar(isLunar: Bool): This
```
功能： 设置弹窗的日期是否显示农历。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 日期是否显示农历。 - true：展示农历。 - false：不展示农历。 初始值：false。 | isLunar | - |

### public func selectedTextStyle( color!: ResourceColor = 0xff007dff, size!: Length = 20.vp, weight!: FontWeight = FontWeight.Medium, family!: String = "HarmonyOS Sans", style!: FontStyle = FontStyle.Normal ): This

```cangjie
public func selectedTextStyle( color!: ResourceColor = 0xff007dff, size!: Length = 20.vp, weight!: FontWeight = FontWeight.Medium, family!: String = "HarmonyOS Sans", style!: FontStyle = FontStyle.Normal ): This
```
功能： 设置选中项的文本样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 否 | 命名参数。 文本颜色。 | color | 0xff007dff |
| Length | 否 | 命名参数。 文本尺寸。不支持百分比设置。 | size | 20.vp |
| FontWeight | 否 | 命名参数。 字体粗细。 | weight | FontWeight.Medium |
| String | 否 | 命名参数。 字体列表。 | family | "HarmonyOS Sans" |
| FontStyle | 否 | 命名参数。 字体样式。 | style | FontStyle.Normal |

### public func selectedTextStyle( color!: ResourceColor = 0xff007dff, size!: Length, weight!: FontWeight = FontWeight.Medium, family!: AppResource, style!: FontStyle = FontStyle.Normal ): This

```cangjie
public func selectedTextStyle( color!: ResourceColor = 0xff007dff, size!: Length, weight!: FontWeight = FontWeight.Medium, family!: AppResource, style!: FontStyle = FontStyle.Normal ): This
```
功能： 设置选中项的文本样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 否 | 命名参数。 文本颜色。 | color | 0xff007dff |
| Length | 是 | 命名参数。 文本尺寸。不支持百分比设置。 | size | - |
| FontWeight | 否 | 命名参数。 字体粗细。 | weight | FontWeight.Medium |
| AppResource | 是 | 命名参数。 字体列表。 | family | - |
| FontStyle | 否 | 命名参数。 字体样式。 | style | FontStyle.Normal |

### public func textStyle( color!: ResourceColor = 0xff182431, size!: Length = 16.fp, weight!: FontWeight = FontWeight.Regular, family!: String = "HarmonyOS Sans", style!: FontStyle = FontStyle.Normal ): This

```cangjie
public func textStyle( color!: ResourceColor = 0xff182431, size!: Length = 16.fp, weight!: FontWeight = FontWeight.Regular, family!: String = "HarmonyOS Sans", style!: FontStyle = FontStyle.Normal ): This
```
功能： 设置选中项的文本样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 否 | 命名参数。 文本颜色。 | color | 0xff182431 |
| Length | 否 | 命名参数。 文本尺寸。不支持百分比设置。 | size | 16.fp |
| FontWeight | 否 | 命名参数。 字体粗细。 | weight | FontWeight.Regular |
| String | 否 | 命名参数。 字体列表。 | family | "HarmonyOS Sans" |
| FontStyle | 否 | 命名参数。 字体样式。 | style | FontStyle.Normal |

### public func textStyle( color!: ResourceColor = 0xff182431, size!: Length, weight!: FontWeight = FontWeight.Regular, family!: AppResource, style!: FontStyle = FontStyle.Normal ): This

```cangjie
public func textStyle( color!: ResourceColor = 0xff182431, size!: Length, weight!: FontWeight = FontWeight.Regular, family!: AppResource, style!: FontStyle = FontStyle.Normal ): This
```
功能： 设置选中项的文本样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 否 | 命名参数。 文本颜色。 | color | 0xff182431 |
| Length | 是 | 命名参数。 文本尺寸。不支持百分比设置。 | size | - |
| FontWeight | 否 | 命名参数。 字体粗细。 | weight | FontWeight.Regular |
| AppResource | 是 | 命名参数。 字体列表。 | family | - |
| FontStyle | 否 | 命名参数。 字体样式。 | style | FontStyle.Normal |

### public func onDateChange(callback: (DatePickerResult) -> Unit): This

```cangjie
public func onDateChange(callback: (DatePickerResult) -> Unit): This
```
功能： 选择日期时触发该事件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( DatePickerResult )->Unit | 是 | 返回选中的时间，年月日为选中的日期，时分取决于当前系统时间的时分，秒恒为00。 | callback | - |


## Classes

### MyFont

class MyFont
```cangjie
public class MyFont { public var size: Length public var weight: FontWeight public var family: String public var style: FontStyle public init(size!: Length = 16.fp, weight!: FontWeight = FontWeight.Normal, family!: String = "HarmonyOS Sans", style!: FontStyle = FontStyle.Normal) public init(size!: Length = 16.fp, weight!: FontWeight = FontWeight.Normal, family!: AppResource, style!: FontStyle = FontStyle.Normal) }
```
功能： 设置字体初始化格式样式。

#### var family

```cangjie
public var family: String
```
功能： 字体系列。初始值：HarmonyOS Sans。
Type: String; Readable: true; Writable: true

#### var size

```cangjie
public var size: Length
```
功能： 字体尺寸。
Type: Length; Readable: true; Writable: true

#### var style

```cangjie
public var style: FontStyle
```
功能： 字体风格。
Type: FontStyle; Readable: true; Writable: true

#### var weight

```cangjie
public var weight: FontWeight
```
功能： 字体粗细。
Type: FontWeight; Readable: true; Writable: true


### PickerTextStyle

class PickerTextStyle
```cangjie
public class PickerTextStyle { public PickerTextStyle( public let color: ResourceColor, public let font: MyFont ) }
```
功能： 字体样式配置类型。

#### let color

```cangjie
public let color: ResourceColor
```
功能： 文本颜色。
Type: ResourceColor; Readable: true; Writable: false

#### let font

```cangjie
public let font: MyFont
```
功能： 文本样式。
Type: MyFont; Readable: true; Writable: false


### DatePickerResult

class DatePickerResult
```cangjie
public class DatePickerResult { public DatePickerResult( public let year: Int64, public let month: Int64, public let day: Int64 ) }
```
功能： 记录日期选择器弹窗的选择结果。

#### let day

```cangjie
public let day: Int64
```
功能： 选中日期的日。
Type: Int64; Readable: true; Writable: false

#### let month

```cangjie
public let month: Int64
```
功能： 选中日期的月。
Type: Int64; Readable: true; Writable: false

#### let year

```cangjie
public let year: Int64
```
功能： 选中日期的年。
Type: Int64; Readable: true; Writable: false



## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*
import std.time.DateTime
import std.time.Month

@Entry
@Component
class EntryView {
 @State var isLunar: Bool = false
 @State var selectedDate: DateTime = DateTime.of(year: 2024, month: Month.of(4), dayOfMonth: 28)
 @State var resultedDate: DateTime = DateTime.of(year: 2024, month: Month.of(4), dayOfMonth: 28)

 func build() {
 Column() {
 Button("切换公历农历")
 .backgroundColor(0x2788D9)
 .onClick({
 event => this.isLunar = !this.isLunar
 })
 .width(200.vp)

 DatePicker(
 start: DateTime.of(year: 2012, month: Month.of(8), dayOfMonth: 8),
 end: DateTime.of(year: 2045, month: Month.of(8), dayOfMonth: 8),
 selected: @Binder(this.selectedDate)
 )
 .disappearTextStyle(color: Color.GRAY, size: 16.fp, weight: FontWeight.Bold)
 .textStyle(color: 0xff182431, size: 18.fp, weight: FontWeight.Normal)
 .selectedTextStyle(color: 0xff0000FF, size: 26.fp, weight: FontWeight.Regular)
 .lunar(this.isLunar)
 .onDateChange(
 { res =>
 this.resultedDate = DateTime.of(year: res.year, month: Month.of(res.month), dayOfMonth: res.day)
 AppLog.info(
 "select current date is: " + res.year.toString() + "-" + res.month.toString() + "-" +
 res.day.toString())
 })
 .margin(top: 30)
 }.width(100.percent)
 }
}
```

