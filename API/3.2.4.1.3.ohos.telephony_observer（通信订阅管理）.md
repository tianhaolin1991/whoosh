# ohos.telephony_observer（通信订阅管理）

本模块提供订阅管理功能，可以订阅/取消订阅的事件包括：网络状态变化、信号状态变化、通话状态变化、蜂窝数据链路连接状态、蜂窝数据业务的上下行数据流状态、SIM状态变化。

```cangjie
import kit.TelephonyKit.*
```
## Classes

### CallStateInfo

class CallStateInfo
```cangjie
public class CallStateInfo { public CallStateInfo( public let state: CallState, public let number: String ) }
```
功能： 通话状态相关信息。

#### let number

```cangjie
public let number: String
```
功能： 电话号码。
Type: String; Readable: true; Writable: false

#### let state

```cangjie
public let state: CallState
```
功能： 通话类型。
Type: CallState; Readable: true; Writable: false


### DataConnectionStateInfo

class DataConnectionStateInfo
```cangjie
public class DataConnectionStateInfo { public DataConnectionStateInfo( public let state: DataConnectState, public let network: RadioTechnology ) }
```
功能： 数据连接状态相关信息。

#### let network

```cangjie
public let network: RadioTechnology
```
功能： 网络类型。
Type: RadioTechnology; Readable: true; Writable: false

#### let state

```cangjie
public let state: DataConnectState
```
功能： 数据连接状态。
Type: DataConnectState; Readable: true; Writable: false


### ObserverOptions

class ObserverOptions
```cangjie
public class ObserverOptions { public ObserverOptions( public var slotId!: Int32 = 0 ) }
```
功能： 电话相关事件订阅参数。

#### var slotId

```cangjie
public var slotId: Int32 = 0
```
功能： 卡槽ID。 - 0：卡槽1 - 1：卡槽2。
Type: Int32; Readable: true; Writable: true


### SimStateData

class SimStateData
```cangjie
public class SimStateData { public SimStateData( public let cardType: CardType, public let state: SimState, public let reason: LockReason ) }
```
功能： SIM卡类型和状态。

#### let cardType

```cangjie
public let cardType: CardType
```
功能： SIM卡类型。
Type: CardType; Readable: true; Writable: false

#### let reason

```cangjie
public let reason: LockReason
```
功能： SIM卡锁类型。
Type: LockReason; Readable: true; Writable: false

#### let state

```cangjie
public let state: SimState
```
功能： SIM卡状态。
Type: SimState; Readable: true; Writable: false


### TelephonyObserver

class TelephonyObserver
```cangjie
public class TelephonyObserver {}
```
功能： 通信事件订阅类，提供各种静态方法如： on/off 等订阅/取消订阅事件。



## Enums

### LockReason

enum LockReason
```cangjie
public enum LockReason { | SIM_NONE | SIM_PIN | SIM_PUK | SIM_PN_PIN | SIM_PN_PUK | SIM_PU_PIN | SIM_PU_PUK | SIM_PP_PIN | SIM_PP_PUK | SIM_PC_PIN | SIM_PC_PUK | SIM_SIM_PIN | SIM_SIM_PUK | ... }
```
功能： SIM卡锁类型。

#### SIM_NONE

```cangjie
SIM_NONE
```
功能： 无锁。

#### SIM_PC_PIN

```cangjie
SIM_PC_PIN
```
功能： 组织PIN锁。

#### SIM_PC_PUK

```cangjie
SIM_PC_PUK
```
功能： 组织PUK锁。

#### SIM_PIN

```cangjie
SIM_PIN
```
功能： PIN锁。

#### SIM_PN_PIN

```cangjie
SIM_PN_PIN
```
功能： 网络PIN锁。

#### SIM_PN_PUK

```cangjie
SIM_PN_PUK
```
功能： 网络PUK锁。

#### SIM_PP_PIN

```cangjie
SIM_PP_PIN
```
功能： 服务提供商PIN锁。

#### SIM_PP_PUK

```cangjie
SIM_PP_PUK
```
功能： 服务提供商PUK锁。

#### SIM_PUK

```cangjie
SIM_PUK
```
功能： PUK锁。

#### SIM_PU_PIN

```cangjie
SIM_PU_PIN
```
功能： 子网PIN锁。

#### SIM_PU_PUK

```cangjie
SIM_PU_PUK
```
功能： 子网PUK锁。

#### SIM_SIM_PIN

```cangjie
SIM_SIM_PIN
```
功能： SIM PIN锁。

#### SIM_SIM_PUK

```cangjie
SIM_SIM_PUK
```
功能： SIM PUK锁。


### ObserverEventType

enum ObserverEventType
```cangjie
public enum ObserverEventType <: ToString { | NetworkStateChange | SignalInfoChange | CallStateChange | CellularDataConnectionStateChange | CellularDataFlowChange | SimStateChange | IccAccountInfoChange | ... }
```
功能： 事件类型。

#### CallStateChange

```cangjie
CallStateChange
```
功能： 通话状态变化事件。

#### CellularDataConnectionStateChange

```cangjie
CellularDataConnectionStateChange
```
功能： 蜂窝数据链路连接状态变化事件。

#### CellularDataFlowChange

```cangjie
CellularDataFlowChange
```
功能： 蜂窝数据业务的上下行数据流状态变化事件。

#### IccAccountInfoChange

```cangjie
IccAccountInfoChange
```
功能： 卡账户变化事件。

#### NetworkStateChange

```cangjie
NetworkStateChange
```
功能： 网络状态变化事件。

#### SignalInfoChange

```cangjie
SignalInfoChange
```
功能： 信号状态变化事件。

#### SimStateChange

```cangjie
SimStateChange
```
功能： sim状态更改事件。



