# 框架接口

此页面记录UI框架使用的公开接口，应用开发者禁止使用，否则会产生无法预期的结果。

## Functions

### public func bind(builder: (CustomView) -> ViewBuilder, thisView: CustomView)

```cangjie
public func bind(builder: (CustomView) -> ViewBuilder, thisView: CustomView)
```
功能： 用于将@Builder修饰的函数与自定义组件对象进行绑定。详情见 bind函数使用 。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( CustomView )-> ViewBuilder | 是 | @Builder修饰的函数类型。 | builder | - |
| CustomView | 是 | 当前自定义组件对象（一般为this）。 | thisView | - |

### public func bind<T1>(builder: (CustomView, ObservedProperty<T1>) -> ViewBuilder, thisView: CustomView)

```cangjie
public func bind<T1>(builder: (CustomView, ObservedProperty<T1>) -> ViewBuilder, thisView: CustomView)
```
功能： 用于将@Builder修饰的函数与自定义组件对象进行绑定。详情见 bind函数使用 。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( CustomView , ObservedProperty <T1>)-> ViewBuilder | 是 | @Builder修饰的函数类型。 | builder | - |
| CustomView | 是 | 当前自定义组件对象（一般为this）。 | thisView | - |

### public func bind<T1, T2>(builder: (CustomView, ObservedProperty<T1>, ObservedProperty<T2>) -> ViewBuilder, thisView: CustomView)

```cangjie
public func bind<T1, T2>(builder: (CustomView, ObservedProperty<T1>, ObservedProperty<T2>) -> ViewBuilder, thisView: CustomView)
```
功能： 用于将@Builder修饰的函数与自定义组件对象进行绑定。详情见 bind函数使用 。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( CustomView , ObservedProperty <T1>, ObservedProperty <T2>)-> ViewBuilder | 是 | @Builder修饰的函数类型。 | builder | - |
| CustomView | 是 | 当前自定义组件对象（一般为this）。 | thisView | - |

### public func bind<T1, T2, T3>(builder: (CustomView, ObservedProperty<T1>, ObservedProperty<T2>, ObservedProperty<T3>) -> ViewBuilder, thisView: CustomView)

```cangjie
public func bind<T1, T2, T3>(builder: (CustomView, ObservedProperty<T1>, ObservedProperty<T2>, ObservedProperty<T3>) -> ViewBuilder, thisView: CustomView)
```
功能： 用于将@Builder修饰的函数与自定义组件对象进行绑定。详情见 bind函数使用 。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( CustomView , ObservedProperty <T1>, ObservedProperty <T2>, ObservedProperty <T3>)-> ViewBuilder | 是 | @Builder修饰的函数类型。 | builder | - |
| CustomView | 是 | 当前自定义组件对象（一般为this）。 | thisView | - |

### public func getResourceBool(res: AppResource): Bool

```cangjie
public func getResourceBool(res: AppResource): Bool
```
功能： 获取资源对象对应的布尔资源值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 资源对象。 | res | - |

**Returns:**

| type | description |
| --- | --- |
| Bool | 资源对象对应的布尔资源值。 |

### public func getResourceColor(res: AppResource): Color

```cangjie
public func getResourceColor(res: AppResource): Color
```
功能： 获取资源对象对应颜色资源的值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 资源对象。 | res | - |

**Returns:**

| type | description |
| --- | --- |
| Color | 资源对象对应颜色对象。 |

### public func getResourceDouble(res: AppResource): Float64

```cangjie
public func getResourceDouble(res: AppResource): Float64
```
功能： 获取资源对象对应的浮点资源值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 资源对象。 | res | - |

**Returns:**

| type | description |
| --- | --- |
| Float64 | 浮点资源值。 |

### public func getResourceInt32(res: AppResource): Int32

```cangjie
public func getResourceInt32(res: AppResource): Int32
```
功能： 获取资源对象对应的整型资源值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 资源对象。 | res | - |

**Returns:**

| type | description |
| --- | --- |
| Int32 | 整型资源值。 |

### public func getResourceLength(res: AppResource): Length

```cangjie
public func getResourceLength(res: AppResource): Length
```
功能： 获取资源对象对应的Length资源值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 资源对象。 | res | - |

**Returns:**

| type | description |
| --- | --- |
| Length | Length资源值。 |

### public func getResourceMedia(res: AppResource): String

```cangjie
public func getResourceMedia(res: AppResource): String
```
功能： 获取图片资源对应的字符串资源值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 资源对象。 | res | - |

**Returns:**

| type | description |
| --- | --- |
| String | 字符串图片资源值。 |

### public func getResourceString(res: AppResource): String

```cangjie
public func getResourceString(res: AppResource): String
```
功能： 获取资源对象对应的字符串资源值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 资源对象。 | res | - |

**Returns:**

| type | description |
| --- | --- |
| String | 字符串资源值。 |

### public func getResourcePluralString(content: PluralResource): String

```cangjie
public func getResourcePluralString(content: PluralResource): String
```
功能： 获取资源对象对应的单复数字符串资源值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| PluralResource | 是 | 资源对象。 | content | - |

**Returns:**

| type | description |
| --- | --- |
| String | 单复数字符串资源值。 |

### public func getResourceStringArray(res: AppResource): Array<String>

```cangjie
public func getResourceStringArray(res: AppResource): Array<String>
```
功能： 获取资源对象对应的字符串数组资源值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 资源对象。 | res | - |

**Returns:**

| type | description |
| --- | --- |
| Array<String> | 字符串数组资源值。 |

### public func getResourceUInt32(res: AppResource): UInt32

```cangjie
public func getResourceUInt32(res: AppResource): UInt32
```
功能： 获取资源对象对应的无符号整型资源值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 资源对象。 | res | - |

**Returns:**

| type | description |
| --- | --- |
| UInt32 | 无符号整型资源值。 |

### public func hasContainer(): Bool

```cangjie
public func hasContainer(): Bool
```
功能： 判断是否在容器上下文中，UI框架使用。

**Returns:**

| type | description |
| --- | --- |
| Bool | 判断结果。 |

### public func loadNativeView(view: CustomView): Bool

```cangjie
public func loadNativeView(view: CustomView): Bool
```
功能： UI框架使用的基础函数。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| CustomView | 是 | - | view | - |

**Returns:**

| type | description |
| --- | --- |
| Bool | - |

### public func throwNativeError(msg: String): Unit

```cangjie
public func throwNativeError(msg: String): Unit
```
功能： 异常抛出接口。内部接口，UI框架使用。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 错误信息。 | msg | - |


## Interfaces

### ArrayLike

interface ArrayLike
```cangjie
public interface ArrayLike<T> { prop size: Int64 operator func [](idx: Int64, value!: T): Unit operator func [](idx: Int64): T }
```
功能： 数组类型。内部接口，框架使用。

#### prop size

```cangjie
prop size: Int64
```
功能： 数组大小。
Type: Int64; Readable: true; Writable: false

#### operator func [](idx: Int64, value!: T): Unit

```cangjie
operator func [](idx: Int64, value!: T): Unit
```
功能： 在指定索引处写入数组元素。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int64 | 是 | 索引值。 | idx | - |
| T | 是 | 命名参数。 写入值。 | value | - |

#### operator func [](idx: Int64): T

```cangjie
operator func [](idx: Int64): T
```
功能： 通过索引获取数组元素。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int64 | 是 | 索引值。 | idx | - |

**Returns:**

| type | description |
| --- | --- |
| T | 数组元素。 |


### CStringExtend

interface CStringExtend
```cangjie
public interface CStringExtend { func free(): Unit func toStringOption(): ?String }
```
功能： 仓颉与C互操作字符串类型。内部接口，框架使用。

#### func free(): Unit

```cangjie
func free(): Unit
```
功能： 释放字符串内存。UI框架使用。

#### func toStringOption(): ?String

```cangjie
func toStringOption(): ?String
```
功能： 将CStringExtend类型对象转换为Option类型字符串。UI框架使用。

**Returns:**

| type | description |
| --- | --- |
| ?String | Option类型字符串。 |


### ComponentRender

interface ComponentRender
```cangjie
public interface ComponentRender { func initial(): Unit func update(): Unit }
```
功能： 组件渲染接口，UI框架使用。

#### func initial(): Unit

```cangjie
func initial(): Unit
```
功能： UI框架使用。

#### func update(): Unit

```cangjie
func update(): Unit
```
功能： UI框架使用。


### LengthProp

interface LengthProp
```cangjie
public interface LengthProp { prop px: Length prop vp: Length prop fp: Length prop percent: Length prop lpx: Length }
```
功能： 像素单位。UI框架使用。

#### prop fp

```cangjie
prop fp: Length
```
功能： 字体像素，与vp类似适用屏幕密度变化，随系统字体大小设置变化。
Type: Length; Readable: true; Writable: false

#### prop lpx

```cangjie
prop lpx: Length
```
功能： 视窗逻辑像素单位，l.px单位为实际屏幕宽度与逻辑宽度（通过designWidth配置）的比值，designWidth默认值为720。当designWidth为720时，在实际宽度为1440物理像素的屏幕上，1l.px为2.px大小。
Type: Length; Readable: true; Writable: false

#### prop percent

```cangjie
prop percent: Length
```
功能： 百分比类型，用于描述以percent像素单位为单位的长度。
Type: Length; Readable: true; Writable: false

#### prop px

```cangjie
prop px: Length
```
功能： 屏幕物理像素单位。
Type: Length; Readable: true; Writable: false

#### prop vp

```cangjie
prop vp: Length
```
功能： 屏幕密度相关像素，根据屏幕像素密度转换为屏幕物理像素，当数值不带单位时，默认单位vp。在实际宽度为1440物理像素的屏幕上，1vp约等于3px。 说明： vp与px的比例与屏幕像素密度有关。
Type: Length; Readable: true; Writable: false


### Observer

interface Observer
```cangjie
public interface Observer { func onStateUpdate(info: String, dependentElmtIds: ArrayList<Int64>): Unit func onStateUpdate(info: String): Unit func notifyRead(info: String): Unit func id(): Int64 func aboutToBeDeleted(): Unit }
```
功能： 持久化存储基类。内部接口，框架使用。

#### func aboutToBeDeleted(): Unit

```cangjie
func aboutToBeDeleted(): Unit
```
功能： 删除持久化存储对象，UI框架使用。

#### func id(): Int64

```cangjie
func id(): Int64
```
功能： 获取持久化存储对象id。

**Returns:**

| type | description |
| --- | --- |
| Int64 | 持久化存储对象id。 |

#### func notifyRead(info: String): Unit

```cangjie
func notifyRead(info: String): Unit
```
功能： UI框架使用。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | - | info | - |

#### func onStateUpdate(info: String, dependentElmtIds: ArrayList<Int64>): Unit

```cangjie
func onStateUpdate(info: String, dependentElmtIds: ArrayList<Int64>): Unit
```
功能： UI框架使用。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | - | info | - |
| ArrayList<Int64> | 是 | - | dependentElmtIds | - |

#### func onStateUpdate(info: String): Unit

```cangjie
func onStateUpdate(info: String): Unit
```
功能： UI框架使用。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | - | info | - |



## Classes

### BaseView

class BaseView
```cangjie
public abstract class BaseView { public init() }
```
功能： UI框架使用的组件基础类。


### CJEntry

class CJEntry
```cangjie
public class CJEntry {}
```
功能： 用于提供被Native调用的全局函数。


### CJResource

class CJResource
```cangjie
public class CJResource { public var id: Int64 public var ty: UInt32 public var params: String public init(id: Int64, ty: UInt32, params: String) }
```
功能： 系统资源，框架使用。

#### var id

```cangjie
public var id: Int64
```
功能： 资源的id值。
Type: Int64; Readable: true; Writable: true

#### var ty

```cangjie
public var ty: UInt32
```
功能： 资源的类型。
Type: UInt32; Readable: true; Writable: true

#### var params

```cangjie
public var params: String
```
功能： 其他资源参数。
Type: String; Readable: true; Writable: true


### CallbackCJWebResourceRequest

class CallbackCJWebResourceRequest
```cangjie
public class CallbackCJWebResourceRequest <: BaseCallBack {}
```
功能： 用于提供框架调用的回调函数。


### ContainerBase

class ContainerBase
```cangjie
public abstract class ContainerBase <: ViewBase {}
```
功能： UI框架使用的组件基础类。


### CustomView

class CustomView
```cangjie
public abstract class CustomView <: RemoteView & Observer { public let nativeView: View public var isReusable: Bool = false public init(parent: Option<CustomView>, localStorage: Option<LocalStorage>) }
```
功能： UI框架使用的组件基础类。

#### var isReusable

```cangjie
public var isReusable: Bool = false
```
功能： UI框架使用。
Type: Bool; Readable: true; Writable: true

#### let nativeView

```cangjie
public let nativeView: View
```
功能： UI框架使用。
Type: View; Readable: true; Writable: false


### FFIVectorBool

class FFIVectorBool
```cangjie
public class FFIVectorBool { public init(vec: VectorBoolHandle) public init(size: Int64) }
```
功能： FFI容器类型，供框架使用。


### FFIVectorCJInt64

class FFIVectorCJInt64
```cangjie
public class FFIVectorCJInt64 { public init(vec: VectorCJInt64Handle) public init(size: Int64) }
```
功能： FFI容器类型，供框架使用。


### FFIVectorFloat32

class FFIVectorFloat32
```cangjie
public class FFIVectorFloat32 { public init(vec: VectorFloat32Handle) public init(size: Int64) }
```
功能： FFI容器类型，供框架使用。


### FFIVectorFloat64

class FFIVectorFloat64
```cangjie
public class FFIVectorFloat64 { public init(vec: VectorFloat64Handle) public init(size: Int64) }
```
功能： FFI容器类型，供框架使用。


### FFIVectorInt32

class FFIVectorInt32
```cangjie
public class FFIVectorInt32 { public init(vec: VectorInt32Handle) public init(size: Int64) }
```
功能： FFI容器类型，供框架使用。


### FFIVectorInt64

class FFIVectorInt64
```cangjie
public class FFIVectorInt64 { public init(vec: VectorInt64Handle) public init(size: Int64) }
```
功能： FFI容器类型，供框架使用。


### FFIVectorString

class FFIVectorString
```cangjie
public class FFIVectorString { public init(vec: VectorStringHandle) public init(size: Int64) }
```
功能： FFI容器类型，供框架使用。


### FFIVectorUInt32

class FFIVectorUInt32
```cangjie
public class FFIVectorUInt32 { public init(vec: VectorUInt32Handle) public init(size: Int64) }
```
功能： FFI容器类型，供框架使用。


### FFIVectorUInt8

class FFIVectorUInt8
```cangjie
public class FFIVectorUInt8 { public init(vec: VectorUInt8Handle) public init(size: Int64) }
```
功能： FFI容器类型，供框架使用。


### HybridComponentBase

class HybridComponentBase
```cangjie
public open class HybridComponentBase <: SharedObject {}
```
功能： 混合组件基础类，供混合框架使用。


### InteractableView

class InteractableView
```cangjie
public open class InteractableView {}
```
功能： 组件基类，更多方法详见仓颉组件的通用事件相关章节。


### LocalStorageInterOp

class LocalStorageInterOp
```cangjie
public open class LocalStorageInterOp {}
```
功能： LocalStorage内部使用的类。


### MainThreadContext

class MainThreadContext
```cangjie
public class MainThreadContext <: ThreadContext {}
```
功能： 框架使用的线程上下文。


### Observable

class Observable
```cangjie
public open class Observable {}
```
功能： 框架状态管理使用的基础类。


### ObservedComplexAbstract

class ObservedComplexAbstract
```cangjie
public abstract class ObservedComplexAbstract <: Observable {}
```
功能： 框架状态管理使用的基础类。


### ObservedObject

class ObservedObject
```cangjie
public abstract class ObservedObject <: ObservedComplexAbstract {}
```
功能： 框架状态管理使用的基础类。


### ObservedPropertyAbstract

class ObservedPropertyAbstract
```cangjie
public abstract class ObservedPropertyAbstract <: Observable { public init(info: String) }
```
功能： 框架状态管理使用的基础类。


### PluralResource

class PluralResource
```cangjie
public class PluralResource { public var id: Int64 public var ty: UInt32 public var count: Int64 public var plural: Int64 public init(id: Int64, ty: UInt32, count: Int64, plural: Int64) }
```
功能： 复数资源，框架使用。

#### var id

```cangjie
public var id: Int64
```
功能： 资源的id值。
Type: Int64; Readable: true; Writable: true

#### var ty

```cangjie
public var ty: UInt32
```
功能： 资源的类型。
Type: UInt32; Readable: true; Writable: true

#### var count

```cangjie
public var count: Int64
```
功能： 指定单复数的数量。
Type: Int64; Readable: true; Writable: true

#### var plural

```cangjie
public var plural: Int64
```
功能： 复数值。
Type: Int64; Readable: true; Writable: true


### RemoteView

class RemoteView
```cangjie
public abstract class RemoteView{ public init() }
```
功能： UI框架使用的组件基础类。


### SubscriberManager

class SubscriberManager
```cangjie
public class SubscriberManager {}
```
功能： 框架状态管理使用的基础类。


### View

class View
```cangjie
public open class View <: ViewBase {}
```
功能： UI框架使用的基础类。


### ViewBase

class ViewBase
```cangjie
public open class ViewBase <: InteractableView & ComponentRender {}
```
功能： 组件基类，更多方法详见仓颉组件的通用属性、手势处理、动画相关章节。


### ViewBuilder

class ViewBuilder
```cangjie
public class ViewBuilder { public ViewBuilder(public let build: () -> Unit) }
```
功能： UI框架使用的基础类。

#### let build

```cangjie
public let build:() -> Unit
```
功能： UI框架使用。
Type: ()->Unit; Readable: true; Writable: false


### ViewStackProcessor

class ViewStackProcessor
```cangjie
public class ViewStackProcessor {}
```
功能： UI框架使用的基础类。


### ForEach

class ForEach
```cangjie
public class ForEach <: ComponentRender { public init(subcomponent: () -> Unit) }
```
功能： ForEach组件。


### If

class If
```cangjie
public class If <: ComponentRender { public init(subcomponent: () -> Unit) }
```
功能： if/else组件的定义结构体，供UI框架使用。


### SizeOptions

class SizeOptions
```cangjie
public class SizeOptions { public SizeOptions( public var width!: Length = 0.vp, public var height!: Length = 0.vp ) }
```
功能： 设置宽高尺寸。

#### var width

```cangjie
public var width: Length = 0.vp
```
功能： 宽度。
Type: Length; Readable: true; Writable: true

#### var height

```cangjie
public var height: Length = 0.vp
```
功能： 高度。
Type: Length; Readable: true; Writable: true



## Structs

### CArrInt32

struct CArrInt32
```cangjie
public struct CArrInt32 { public CArrInt32( public let head: CPointer<Int32>, public let size: Int64 ) }
```
功能： 框架内使用结构体。


### CJTouchTestInfo

struct CJTouchTestInfo (deprecated)
```cangjie
public struct CJTouchTestInfo {}
```
功能： 当前按压点所在组件的坐标系、id和尺寸相关信息。


### CTouchResult

struct CTouchResult (deprecated)
```cangjie
public struct CTouchResult {}
```
功能： 自定义事件分发结果，开发者通过返回结果来影响事件分发。


### ExternalString

struct ExternalString
```cangjie
public struct ExternalString { public init(value: String) }
```
功能： 框架内使用结构体。


### FFiCheckboxGroupResult

struct FFiCheckboxGroupResult
```cangjie
public struct FFiCheckboxGroupResult { public FFiCheckboxGroupResult( public let status: Int32, public let size: Int64, public let nameBuffer: CPointer<CString> ) }
```
功能： 多选框群组，用于控制多选框全选或者不全选状态。


### NativeDimension

struct NativeDimension
```cangjie
public struct NativeDimension { public NativeDimension( public let value: Float64, public let unit: Int32 ) }
```
功能： 框架内使用结构体。


### NativeLength

struct NativeLength
```cangjie
public struct NativeLength { public NativeLength( public let value: Float64, public let unitType: Int32 ) }
```
功能： 框架内使用结构体。


### NativeOffset

struct NativeOffset
```cangjie
public struct NativeOffset { public NativeOffset( public let dx: NativeLength, public let dy: NativeLength ) }
```
功能： 框架内使用结构体。


### NativeOptionBool

struct NativeOptionBool
```cangjie
public struct NativeOptionBool { public NativeOptionBool( public let hasValue: Bool, public let value: Bool ) }
```
功能： 框架内使用结构体。


### NativeOptionCArrInt32

struct NativeOptionCArrInt32
```cangjie
public struct NativeOptionCArrInt32 { public NativeOptionCArrInt32( public let hasValue: Bool, public let value: CArrInt32 ) }
```
功能： 框架内使用结构体。


### NativeOptionCString

struct NativeOptionCString
```cangjie
public struct NativeOptionCString { public NativeOptionCString( public let hasValue: Bool, public let value: CString ) }
```
功能： 框架内使用结构体。


### NativeOptionCallBack

struct NativeOptionCallBack
```cangjie
public struct NativeOptionCallBack { public NativeOptionCallBack( public let hasValue: Bool, public let value: Int64 ) }
```
功能： 框架内使用结构体。


### NativeOptionFloat32

struct NativeOptionFloat32
```cangjie
public struct NativeOptionFloat32 { public NativeOptionFloat32( public let hasValue: Bool, public let value: Float32 ) }
```
功能： 框架内使用结构体。


### NativeOptionFloat64

struct NativeOptionFloat64
```cangjie
public struct NativeOptionFloat64 { public NativeOptionFloat64( public let hasValue: Bool, public let value: Float64 ) }
```
功能： 框架内使用结构体。


### NativeOptionInt32

struct NativeOptionInt32
```cangjie
public struct NativeOptionInt32 { public NativeOptionInt32( public let hasValue: Bool, public let value: Int32 ) }
```
功能： 框架内使用结构体。


### NativeOptionInt64

struct NativeOptionInt64
```cangjie
public struct NativeOptionInt64 { public NativeOptionInt64( public let hasValue: Bool, public let value: Int64 ) }
```
功能： 框架内使用结构体。


### NativeOptionLength

struct NativeOptionLength
```cangjie
public struct NativeOptionLength { public NativeOptionLength( public let hasValue: Bool, public let value: NativeLength ) }
```
功能： 框架内使用结构体。


### NativeOptionUInt32

struct NativeOptionUInt32
```cangjie
public struct NativeOptionUInt32 { public NativeOptionUInt32( public let hasValue: Bool, public let value: UInt32 ) }
```
功能： 框架内使用结构体。


### NativeRectangle

struct NativeRectangle
```cangjie
public struct NativeRectangle { public NativeRectangle( let x: Float64, let xUnit: Int32, let y: Float64, let yUnit: Int32, let width: Float64, let widthUnit: Int32, let height: Float64, let heightUnit: Int32 ) }
```
功能： 框架内使用结构体。


### NativeResourceObject

struct NativeResourceObject
```cangjie
public struct NativeResourceObject { public NativeResourceObject( public let bundleName: CString, public let moduleName: CString, public let id: Int32, public let resType: Int32, public let paramsJsonStr: CString ) }
```
功能： 框架内使用结构体。


### NativeTextShadowV2

struct NativeTextShadowV2 (deprecated)
```cangjie
public struct NativeTextShadowV2 { public NativeTextShadowV2( let radius: Float64, let offsetX: Float64, let offsetY: Float64, let color: UInt32, let fill: Bool, let shadowType: UInt32 ) }
```
功能： 设置文字阴影效果，框架内使用结构体。


### NativeTextTimerShadow

struct NativeTextTimerShadow (deprecated)
```cangjie
public struct NativeTextTimerShadow { public NativeTextTimerShadow( let radius: Float64, let offsetX: Float64, let offsetY: Float64, let color: UInt32, let fill: Bool, let shadowType: UInt32 ) }
```
功能： 设置文字阴影效果，框架内使用结构体。



## Types

### VectorBoolHandle

type VectorBoolHandle
```cangjie
public type VectorBoolHandle = CPointer<Unit>
```
功能： UI框架使用。


### VectorCJInt64Handle

type VectorCJInt64Handle
```cangjie
public type VectorCJInt64Handle = CPointer<Unit>
```
功能： UI框架使用。


### VectorFloat32Handle

type VectorFloat32Handle
```cangjie
public type VectorFloat32Handle = CPointer<Unit>
```
功能： UI框架使用。


### VectorFloat64Handle

type VectorFloat64Handle
```cangjie
public type VectorFloat64Handle = CPointer<Unit>
```
功能： UI框架使用。


### VectorInt32Handle

type VectorInt32Handle
```cangjie
public type VectorInt32Handle = CPointer<Unit>
```
功能： UI框架使用。


### VectorInt64Handle

type VectorInt64Handle
```cangjie
public type VectorInt64Handle = CPointer<Unit>
```
功能： UI框架使用。


### VectorNativeTextTimerShadow (deprecated)

type VectorNativeTextTimerShadow (deprecated)
```cangjie
public type VectorNativeTextTimerShadow = CPointer<Unit>
```
功能： UI框架使用。


### VectorUInt32Handle

type VectorUInt32Handle
```cangjie
public type VectorUInt32Handle = CPointer<Unit>
```
功能： UI框架使用。


### VectorUInt8Handle

type VectorUInt8Handle
```cangjie
public type VectorUInt8Handle = CPointer<Unit>
```
功能： UI框架使用。



