# ohos.labels（标签信息）

标签说明。标签包括atomicservice（是否支持元服务）、crossplatform（是否支持跨平台）、deprecated（废弃版本）、form（是否支持在form中使用）、permission（所需权限）、since（api等级）、stagemodelonly（是否仅支持Stage模型）、syscap（所需系统能力）等信息。

```cangjie
import ohos.labels.*
```
## Interfaces

### PermissionValue

interface PermissionValue
```cangjie
public interface PermissionValue { operator func &(rhs: PermissionValue): PermissionValue operator func |(rhs: PermissionValue): PermissionValue }
```
功能： 用于处理权限的与或关系。

#### operator func &(rhs: PermissionValue): PermissionValue

```cangjie
operator func &(rhs: PermissionValue): PermissionValue
```
功能： 和另一个权限集的与。返回与操作后的权限集。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| PermissionValue | 是 | 与操作的另一个权限集。 | rhs | - |

**Returns:**

| type | description |
| --- | --- |
| PermissionValue | 此权限集与另一个权限集与后的权限集。 |

#### operator func |(rhs: PermissionValue): PermissionValue

```cangjie
operator func |(rhs: PermissionValue): PermissionValue
```
功能： 和另一个权限或。返回或操作后的权限。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| PermissionValue | 是 | 或操作的另一个权限集。 | rhs | - |

**Returns:**

| type | description |
| --- | --- |
| PermissionValue | 此权限集与另一个权限集或后的权限集。 |



## Classes

### APILevel

class APILevel
```cangjie
public class APILevel { public let level: UInt8 public let atomicservice: Bool public let crossplatform: Bool public let deprecated: UInt8 public let form: Bool public let permission:?PermissionValue public let stagemodelonly: Bool public let syscap: String public const init(level_val: UInt8, atomicservice!: Bool = false, crossplatform!: Bool = false, deprecated!: UInt8 = 0, form!: Bool = false, permission!: ?PermissionValue= None, stagemodelonly!: Bool = true, syscap!: String = "") }
```
功能： 标签的定义。标签以注解的形式打在api上。标签包括atomicservice（是否支持元服务）、crossplatform（是否支持跨平台）、deprecated（废弃版本）、form（是否支持在form中使用）、permission（所需权限）、since（api等级）、stagemodelonly（是否仅支持Stage模型）、syscap（所需系统能力）等信息。

#### let atomicservice

```cangjie
public let atomicservice: Bool
```
功能： 当前api是否支持元服务。
Type: Bool; Readable: true; Writable: false

#### let crossplatform

```cangjie
public let crossplatform: Bool
```
功能： 当前api是否支持跨平台。
Type: Bool; Readable: true; Writable: false

#### let deprecated

```cangjie
public let deprecated: UInt8
```
功能： 当前api的废弃版本，默认为0表示未废弃。
Type: UInt8; Readable: true; Writable: false

#### let form

```cangjie
public let form: Bool
```
功能： 当前api是否支持在form中使用。
Type: Bool; Readable: true; Writable: false

#### let level

```cangjie
public let level: UInt8
```
功能： 当前api的等级。
Type: UInt8; Readable: true; Writable: false

#### let permission

```cangjie
public let permission:?PermissionValue
```
功能： 当前api所需的权限信息。
Type: ? PermissionValue; Readable: true; Writable: false

#### let stagemodelonly

```cangjie
public let stagemodelonly: Bool
```
功能： 当前api是否仅支持Stage模型。
Type: Bool; Readable: true; Writable: false

#### let syscap

```cangjie
public let syscap: String
```
功能： 当前api所需的系统能力。
Type: String; Readable: true; Writable: false


### PermissionAnd

class PermissionAnd
```cangjie
public class PermissionAnd <: PermissionValue { public const init(lhs: PermissionValue, rhs: PermissionValue) }
```
功能： 表示若干个权限的与。


### PermissionOr

class PermissionOr
```cangjie
public class PermissionOr <: PermissionValue { public const init(lhs: PermissionValue, rhs: PermissionValue) }
```
功能： 表示若干个权限的或。



## Structs

### String

struct String
```cangjie
extend String <: PermissionValue {}
```
功能： 扩展PermissionValue接口，使用字符串表示单个权限。



