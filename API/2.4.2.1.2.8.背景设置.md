# 背景设置

设置组件的背景样式。

## Functions

### public func backdropBlur(value: Float64): This

```cangjie
public func backdropBlur(value: Float64): This
```
功能： 为组件添加背景模糊效果，可以自定义设置模糊半径和灰阶参数。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Float64 | 是 | 为当前组件添加背景模糊效果，入参为模糊半径，模糊半径越大越模糊，为0时不模糊。 | value | - |

### public open func backgroundColor(color: ResourceColor): This

```cangjie
public open func backgroundColor(color: ResourceColor): This
```
功能： 设置组件背景颜色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 组件的背景色。 | color | - |

### public func backgroundImage(src!: AppResource, repeat!: ImageRepeat = ImageRepeat.NoRepeat): This

```cangjie
public func backgroundImage(src!: AppResource, repeat!: ImageRepeat = ImageRepeat.NoRepeat): This
```
功能： 设置组件的背景图片。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 命名参数。 图片地址，支持网络图片资源和本地图片资源地址和Base64，不支持svg类型的图片。 | src | - |
| ImageRepeat | 否 | 命名参数。 设置背景图片的重复样式，默认不重复。当设置的背景图片为透明底色图片，且同时设置了backgroundColor时，二者叠加显示，背景颜色在最底部。 | repeat | ImageRepeat.NoRepeat |

### public func backgroundImage(src!: String, repeat!: ImageRepeat = ImageRepeat.NoRepeat): This

```cangjie
public func backgroundImage(src!: String, repeat!: ImageRepeat = ImageRepeat.NoRepeat): This
```
功能： 设置组件的背景图片。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 命名参数。 图片地址，支持网络图片资源和本地图片资源地址和Base64，不支持svg类型的图片。 | src | - |
| ImageRepeat | 否 | 命名参数。 背景图片的重复样式，默认不重复。当设置的背景图片为透明底色图片，且同时设置了backgroundColor时，二者叠加显示，背景颜色在最底部。 | repeat | ImageRepeat.NoRepeat |

### public func backgroundImageSize(imageSize: ImageSize): This

```cangjie
public func backgroundImageSize(imageSize: ImageSize): This
```
功能： 设置组件背景图片的宽高。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ImageSize | 是 | 背景图像的高度和宽度。 初始值：ImageSize.Auto。 | imageSize | - |

### public func backgroundImageSize(width!: Length = 0.vp, height!: Length = 0.vp): This

```cangjie
public func backgroundImageSize(width!: Length = 0.vp, height!: Length = 0.vp): This
```
功能： 设置组件背景图片的宽高。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 否 | 命名参数。 背景图片的宽度。 | width | 0.vp |
| Length | 否 | 命名参数。 背景图片的高度。 | height | 0.vp |

### public func backgroundImagePosition(x!: Length = 0.vp, y!: Length = 0.vp): This

```cangjie
public func backgroundImagePosition(x!: Length = 0.vp, y!: Length = 0.vp): This
```
功能： 设置背景图的位置。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 否 | 命名参数。 相对于组件左上角的x坐标。 | x | 0.vp |
| Length | 否 | 命名参数。 相对于组件左上角的y坐标。 | y | 0.vp |

### public func backgroundImagePosition(align: Alignment): This

```cangjie
public func backgroundImagePosition(align: Alignment): This
```
功能： 设置背景图的位置。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Alignment | 是 | 背景图在组件中显示位置。 | align | - |

### public func backgroundBlurStyle(value!: BlurStyle, options!: Option<BackgroundBlurStyleOptions> = None): This

```cangjie
public func backgroundBlurStyle(value!: BlurStyle, options!: Option<BackgroundBlurStyleOptions> = None): This
```
功能： 为当前组件提供一种在背景和内容之间的模糊能力，通过枚举值的方式封装了不同的模糊半径、蒙版颜色、蒙版透明度、饱和度、亮度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| BlurStyle | 是 | 命名参数。 背景模糊样式。模糊样式中封装了模糊半径、蒙版颜色、蒙版透明度、饱和度、亮度五个参数。 | value | - |
| BackgroundBlurStyleOptions | 否 | 命名参数。 背景模糊选项。 | options | None |

### public func backgroundImageResizable(slice: EdgeWidths): This

```cangjie
public func backgroundImageResizable(slice: EdgeWidths): This
```
功能： 设置背景图在拉伸时可调整大小的图像选项。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| EdgeWidths | 是 | 边框宽度类型，用于描述组件边框不同方向的宽度。 只有当bottom和right同时大于0时，该属性生效。默认单位：vp。 | slice | - |

### public func backgroundBrightness(rate: Float64, lightUpDegree: Float64): This

```cangjie
public func backgroundBrightness(rate: Float64, lightUpDegree: Float64): This
```
功能： 设置组件背景提亮效果，包含亮度变化速率和提亮程度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Float64 | 是 | 亮度变化速率。亮度变化速率越大，提亮程度下降速度越快。若rate为0，则lightUpDegree将不生效，即不会产生任何提亮效果。 初始值：0.0。 取值范围：[-1.0, 1.0]。 | rate | - |
| Float64 | 是 | 提亮程度。提亮程度越大，亮度提升程度越大。 初始值：0.0。 取值范围：(0.0, +∞)。 | lightUpDegree | - |

### public func backgroundEffect(value: BackgroundEffectOptions): This

```cangjie
public func backgroundEffect(value: BackgroundEffectOptions): This
```
功能： 设置组件背景属性，包含背景模糊半径，亮度，饱和度，颜色等参数。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| BackgroundEffectOptions | 是 | 组件背景属性包括：饱和度，亮度，颜色。 | value | - |

### public func background(builder: () -> Unit, align!: Alignment=Alignment.Center): This

```cangjie
public func background(builder: () -> Unit, align!: Alignment=Alignment.Center): This
```
功能： 设置组件背景。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| () -> Unit | 是 | 自定义背景。使用时结合 @Builder 和 bind 方法使用。 | builder | - |
| Alignment | 否 | 命名参数。 自定义背景与组件的对齐方式。 同时设置了background，backgroundColor，backgroundImage时，叠加显示，background在最上层。 | align | Alignment.Center |


## Classes

### BackgroundBlurStyleOptions

class BackgroundBlurStyleOptions
```cangjie
public class BackgroundBlurStyleOptions <: BlurStyleOptions { public let inactiveColor: Color public let policy: BlurStyleActivePolicy public init( colorMode!: ThemeColorMode = ThemeColorMode.SYSTEM, adaptiveColor!: AdaptiveColor = AdaptiveColor.DEFAULT, blurOptions!: BlurOptions = BlurOptions([0.0, 0.0]), scale!: Float32 = 1.0, policy!: BlurStyleActivePolicy = BlurStyleActivePolicy.ALWAYS_ACTIVE, inactiveColor!: ResourceColor = Color.TRANSPARENT ) }
```
功能： 背景模糊选项类型。

#### let inactiveColor

```cangjie
public let inactiveColor: ResourceColor
```
功能： 窗口失焦后，窗口内控件模糊效果会被移除，则使用inactiveColor作为控件背板颜色。
Type: ResourceColor; Readable: true; Writable: false

#### let policy

```cangjie
public let policy: BlurStyleActivePolicy
```
功能： 模糊激活策略。
Type: BlurStyleActivePolicy; Readable: true; Writable: false


### BackgroundEffectOptions

class BackgroundEffectOptions
```cangjie
public class BackgroundEffectOptions { public let adaptiveColor: AdaptiveColor public let blurOptions: BlurOptions public let brightness: Float64 public let color: Color public let inactiveColor: Color public let policy: BlurStyleActivePolicy public let radius: Float64 public let saturation: Float64 public init( adaptiveColor!: AdaptiveColor = AdaptiveColor.DEFAULT, blurOptions!: BlurOptions = BlurOptions([0.0, 0.0]), brightness!: Float64 = 1.0, color!: Color = Color.TRANSPARENT, inactiveColor!: Color = Color.TRANSPARENT, policy!: BlurStyleActivePolicy = BlurStyleActivePolicy.ALWAYS_ACTIVE, radius!: Float64, saturation!: Float64 = 1.0 ) }
```
功能： 背景效果参数。

#### let adaptiveColor

```cangjie
public let adaptiveColor: AdaptiveColor
```
功能： 背景模糊效果使用的取色模式，默认为DEFAULT。使用AVERAGE时color必须带有透明度，取色模式才生效。
Type: AdaptiveColor; Readable: true; Writable: false

#### let blurOptions

```cangjie
public let blurOptions: BlurOptions
```
功能： 灰阶模糊参数。
Type: BlurOptions; Readable: true; Writable: false

#### let brightness

```cangjie
public let brightness: Float64
```
功能： 亮度，取值范围：[0, +∞)。推荐取值范围：[0, 2]。
Type: Float64; Readable: true; Writable: false

#### let color

```cangjie
public let color: Color
```
功能： 颜色。
Type: Color; Readable: true; Writable: false

#### let inactiveColor

```cangjie
public let inactiveColor: Color
```
功能： 窗口失焦后，窗口内控件模糊效果会被移除，则使用inactiveColor作为控件背板颜色。
Type: Color; Readable: true; Writable: false

#### let policy

```cangjie
public let policy: BlurStyleActivePolicy
```
功能： 模糊激活策略。
Type: BlurStyleActivePolicy; Readable: true; Writable: false

#### let radius

```cangjie
public let radius: Float64
```
功能： 模糊半径，取值范围：[0, +∞)。初始值为0.0。
Type: Float64; Readable: true; Writable: false

#### let saturation

```cangjie
public let saturation: Float64
```
功能： 饱和度，取值范围：[0, +∞)。推荐取值范围：[0, 50]。
Type: Float64; Readable: true; Writable: false



## Enums

### BlurStyle

enum BlurStyle
```cangjie
public enum BlurStyle { | BACKGROUND_THIN | BACKGROUND_REGULAR | BACKGROUND_THICK | BACKGROUND_ULTRA_THICK | COMPONENT_ULTRA_THIN | COMPONENT_THIN | COMPONENT_REGULAR | COMPONENT_THICK | COMPONENT_ULTRA_THICK | Regular | NONE | Thin | Thick }
```
功能： 模糊设置。

#### BACKGROUND_THIN

```cangjie
BACKGROUND_THIN
```
功能： 近距景深模糊。

#### BACKGROUND_REGULAR

```cangjie
BACKGROUND_REGULAR
```
功能： 中距景深模糊。

#### BACKGROUND_THICK

```cangjie
BACKGROUND_THICK
```
功能： 远距景深模糊。

#### BACKGROUND_ULTRA_THICK

```cangjie
BACKGROUND_ULTRA_THICK
```
功能： 超远距景深模糊。

#### COMPONENT_ULTRA_THIN

```cangjie
COMPONENT_ULTRA_THIN
```
功能： 组件超轻薄材质模糊。

#### COMPONENT_THIN

```cangjie
COMPONENT_THIN
```
功能： 组件普通材质模糊。

#### COMPONENT_REGULAR

```cangjie
COMPONENT_REGULAR
```
功能： 组件普通材质模糊。

#### COMPONENT_THICK

```cangjie
COMPONENT_THICK
```
功能： 组件厚材质模糊。

#### COMPONENT_ULTRA_THICK

```cangjie
COMPONENT_ULTRA_THICK
```
功能： 组件超厚材质模糊。

#### NONE

```cangjie
NONE
```
功能： 关闭模糊。

#### Regular

```cangjie
Regular
```
功能： 普通厚度材质模糊。

#### Thin

```cangjie
Thin
```
功能： 轻薄材质模糊。

#### Thick

```cangjie
Thick
```
功能： 厚材质模糊。


### BlurStyleActivePolicy

enum BlurStyleActivePolicy
```cangjie
public enum BlurStyleActivePolicy { | ALWAYS_ACTIVE | ALWAYS_INACTIVE | FOLLOWS_WINDOW_ACTIVE_STATE }
```
功能： 模糊效果设置。

#### ALWAYS_ACTIVE

```cangjie
ALWAYS_ACTIVE
```
功能： 一直有模糊效果。

#### ALWAYS_INACTIVE

```cangjie
ALWAYS_INACTIVE
```
功能： 一直无模糊效果。

#### FOLLOWS_WINDOW_ACTIVE_STATE

```cangjie
FOLLOWS_WINDOW_ACTIVE_STATE
```
功能： 模糊效果跟随窗口焦点状态变化，非焦点不模糊，焦点模糊。

#### getValue

```cangjie
public getValue(): Int32
```
功能： 获取枚举的数值类型。



## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*
import kit.LocalizationKit.*

@Entry
@Component
class EntryView {
 func build() {
 Column(5) {
 Text("background color").fontSize(9).width(90.percent).fontColor(0xCCCCCC)
 Row().width(90.percent).height(50).backgroundColor(0xE5E5E5).border(width: 1.vp)

 Text("background image repeat along X").fontSize(9).width(90.percent).fontColor(0xCCCCCC)
 Row()
 .backgroundImage(src: @r(app.media.background), repeat: ImageRepeat.X)
 .backgroundImageSize(width: 250.px, height: 140.px)
 .width(90.percent)
 .height(70)
 .border(width: 1.vp)

 Text("background image repeat along Y").fontSize(9).width(90.percent).fontColor(0xCCCCCC)
 Row()
 .backgroundImage(src: @r(app.media.background), repeat: ImageRepeat.Y)
 .backgroundImageSize(width: 500.px, height: 120.px)
 .width(90.percent)
 .height(100)
 .border(width: 1.vp)

 Text("background image size").fontSize(9).width(90.percent).fontColor(0xCCCCCC)
 Row()
 .width(90.percent).height(150)
 .backgroundImage(src: @r(app.media.background), repeat: ImageRepeat.NoRepeat)
 .backgroundImageSize(width: 1000, height: 500)
 .border(width: 1.vp)

 Text("background fill the box(Cover)").fontSize(9).width(90.percent).fontColor(0xCCCCCC)
 // 不保准图片完整的情况下占满盒子
 Row()
 .width(200)
 .height(50)
 .backgroundImage(src: @r(app.media.background), repeat: ImageRepeat.NoRepeat)
 .backgroundImageSize(ImageSize.Cover)
 .border(width: 1.vp)

 Text("background fill the box(Contain)").fontSize(9).width(90.percent).fontColor(0xCCCCCC)
 // 保准图片完整的情况下放到最大
 Row()
 .width(200)
 .height(50)
 .backgroundImage(src: @r(app.media.background), repeat: ImageRepeat.NoRepeat)
 .backgroundImageSize(ImageSize.Contain)
 .border(width: 1.vp)

 Text("background image position").fontSize(9).width(90.percent).fontColor(0xCCCCCC)
 Row()
 .width(100)
 .height(50)
 .backgroundImage(src: @r(app.media.background), repeat: ImageRepeat.NoRepeat)
 .backgroundImageSize(width: 1000, height: 560)
 .backgroundImagePosition(x: -500, y: -300)
 .border(width: 1.vp)
 }.width(100.percent).height(100.percent).padding(top: 5)
 }
}
```

