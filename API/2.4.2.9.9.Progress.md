# Progress

进度条组件，用于显示内容加载或操作处理等进度。

## Component Constructors

### public init(option: ProgressOptions)

```cangjie
public init(option: ProgressOptions)
```
功能： 创建进度条组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ProgressOptions | 是 | 按进度条类型不同，设置不同属性的进度条组件参数。 | option | - |

### public init(value!: Float64, total!: Float64 = 100.0, `type`!: ProgressType = ProgressType.Linear)

```cangjie
public init(value!: Float64, total!: Float64 = 100.0, `type`!: ProgressType = ProgressType.Linear)
```
功能： 创建进度条组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Float64 | 是 | 命名参数。 指定当前进度值。设置小于0的数值时置为0.0，设置大于total的数值时置为total。 初始值：0.0 | value | - |
| Float64 | 否 | 命名参数。 指定进度总长。设置小于等于0的数值时置为100.0。 | total | 100.0 |
| ProgressType | 否 | 指定进度条类型。 | type | ProgressType.Linear |


## Functions

### public func color(baseColor: ResourceColor): This

```cangjie
public func color(baseColor: ResourceColor): This
```
功能： 设置进度条前景色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 进度条前景色。 初始值： - Capsule：'0x33007dff' - Ring：起始端：'0xff86c1ff'，结束端：'0xff254ff7' - 其他样式：'0xff007dff' | baseColor | - |

### public func color(value: Array<ColorStop>): This

```cangjie
public func color(value: Array<ColorStop>): This
```
功能： 设置进度条前景色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array< ColorStop > | 是 | 进度条前景色。 初始值： - Capsule：'0x33007dff' - Ring：起始端：'0xff86c1ff'，结束端：'0xff254ff7' - 其他样式：'0xff007dff' | value | - |

### public func style(strokeWidth!: Length = 10.vp, scaleCount!: Int32 = 120, scaleWidth!: Length = 2.vp): This

```cangjie
public func style(strokeWidth!: Length = 10.vp, scaleCount!: Int32 = 120, scaleWidth!: Length = 2.vp): This
```
功能： 设置进度条的样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 否 | 命名参数。 设置进度条宽度（不支持百分比设置）。 | strokeWidth | 10.vp |
| Int32 | 否 | 命名参数。 设置环形进度条总刻度数。 | scaleCount | 120 |
| Length | 否 | 命名参数。 设置环形进度条刻度粗细（不支持百分比设置），刻度粗细大于进度条宽度时，为系统默认粗细。 | scaleWidth | 2.vp |

### public func style(ringStyle: RingStyleOptions): This

```cangjie
public func style(ringStyle: RingStyleOptions): This
```
功能： 设置进度条Ring的样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| RingStyleOptions | 是 | 设置Ring的样式。 | ringStyle | - |

### public func style(eclipseStyle: EclipseStyleOptions): This

```cangjie
public func style(eclipseStyle: EclipseStyleOptions): This
```
功能： 设置进度条Eclipse的样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| EclipseStyleOptions | 是 | 设置Eclipse的样式。 | eclipseStyle | - |

### public func style(scaleRingStyle: ScaleRingStyleOptions): This

```cangjie
public func style(scaleRingStyle: ScaleRingStyleOptions): This
```
功能： 设置进度条ScaleRing的样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ScaleRingStyleOptions | 是 | 设置ScaleRing的样式。 | scaleRingStyle | - |

### public func style(progressStyle: ProgressStyleOptions): This

```cangjie
public func style(progressStyle: ProgressStyleOptions): This
```
功能： 设置进度条的基础样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ProgressStyleOptions | 是 | 仅可设置各类型进度条的基本样式。 | progressStyle | - |

### public func style(linearStyle: LinearStyleOptions): This

```cangjie
public func style(linearStyle: LinearStyleOptions): This
```
功能： 设置进度条Linear的样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| LinearStyleOptions | 是 | 设置Linear的样式。 | linearStyle | - |

### public func style(capsuleStyle: CapsuleStyleOptions): This

```cangjie
public func style(capsuleStyle: CapsuleStyleOptions): This
```
功能： 设置进度条Capsule的样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| CapsuleStyleOptions | 是 | 设置Capsule的样式。 | capsuleStyle | - |

### public func value(baseValue: Float64): This

```cangjie
public func value(baseValue: Float64): This
```
功能： 设置当前进度值。设置小于0的数值时置为0，设置大于total的数值时置为total。非法数值不生效。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Float64 | 是 | 当前进度值。 初始值：0 | baseValue | - |


## Classes

### CapsuleStyleOptions

class CapsuleStyleOptions
```cangjie
public class CapsuleStyleOptions { public var content: String public var font: Fonts public var borderWidth: Length public var borderColor: ResourceColor public var fontColor: ResourceColor public var showDefaultPercentage: Bool public var enableSmoothEffect: Bool public var enableScanEffect: Bool public init(content!: String = "HarmonyOS Sans", font!: Fonts = Fonts(), borderWidth!: Length = 1.vp, borderColor!: ResourceColor = Color(0x33007dff), fontColor!: ResourceColor = Color(0xff182431), showDefaultPercentage!: Bool = false, enableSmoothEffect!: Bool = true, enableScanEffect!: Bool = false) }
```
功能： Capsule的样式参数类型。

#### var borderColor

```cangjie
public var borderColor: ResourceColor
```
功能： 内描边颜色。
Type: ResourceColor; Readable: true; Writable: true

#### var borderWidth

```cangjie
public var borderWidth: Length
```
功能： 内描边宽度（不支持百分比设置）。
Type: Length; Readable: true; Writable: true

#### var content

```cangjie
public var content: String
```
功能： 文本内容，应用可自定义。
Type: String; Readable: true; Writable: true

#### var enableScanEffect

```cangjie
public var enableScanEffect: Bool
```
功能： 扫光效果的开关。
Type: Bool; Readable: true; Writable: true

#### var enableSmoothEffect

```cangjie
public var enableSmoothEffect: Bool
```
功能： 进度平滑动效的开关。开启平滑动效后设置进度，进度会从当前值渐变至设定值，否则进度从当前值突变至设定值。
Type: Bool; Readable: true; Writable: true

#### var font

```cangjie
public var font: Fonts
```
功能： 文本样式。
Type: Fonts; Readable: true; Writable: true

#### var fontColor

```cangjie
public var fontColor: ResourceColor
```
功能： 文本颜色。
Type: ResourceColor; Readable: true; Writable: true

#### var showDefaultPercentage

```cangjie
public var showDefaultPercentage: Bool
```
功能： 显示百分比文本的开关，开启后会在进度条上显示当前进度的百分比。设置了content属性时该属性不生效。
Type: Bool; Readable: true; Writable: true


### EclipseStyleOptions

class EclipseStyleOptions
```cangjie
public class EclipseStyleOptions { public var enableSmoothEffect: Bool public init(enableSmoothEffect!: Bool = true) }
```
功能： Eclipse的样式参数类型。

#### var enableSmoothEffect

```cangjie
public var enableSmoothEffect: Bool
```
功能： 进度平滑动效的开关。开启平滑动效后设置进度，进度会从当前值渐变至设定值，否则进度从当前值突变至设定值。
Type: Bool; Readable: true; Writable: true


### LinearStyleOptions

class LinearStyleOptions
```cangjie
public class LinearStyleOptions { public var strokeWidth: Length public var strokeRadius: Length public var enableSmoothEffect: Bool public var enableScanEffect: Bool public init(strokeWidth!: Length = 4.vp, strokeRadius!: Length = 2.vp, enableSmoothEffect!: Bool = true, enableScanEffect!: Bool = false) public init(strokeWidth!: Length = 4.vp, enableSmoothEffect!: Bool = true, enableScanEffect!: Bool = false) }
```
功能： Linear的样式参数。

#### var enableScanEffect

```cangjie
public var enableScanEffect: Bool
```
功能： 扫光效果的开关。
Type: Bool; Readable: true; Writable: true

#### var enableSmoothEffect

```cangjie
public var enableSmoothEffect: Bool
```
功能： 进度平滑动效的开关。开启平滑动效后设置进度，进度会从当前值渐变至设定值，否则进度从当前值突变至设定值。
Type: Bool; Readable: true; Writable: true

#### var strokeRadius

```cangjie
public var strokeRadius: Length
```
功能： 设置线性进度条圆角半径。
Type: Length; Readable: true; Writable: true

#### var strokeWidth

```cangjie
public var strokeWidth: Length
```
功能： 设置进度条宽度（不支持百分比设置）。
Type: Length; Readable: true; Writable: true


### ProgressConfiguration

class ProgressConfiguration
```cangjie
public class ProgressConfiguration { public var value: Float32 public var total: Float32 public init(value: Float32, total: Float32) }
```
功能： Progress的进度总长和进度值参数。

#### var total

```cangjie
public var total: Float32
```
功能： 进度总长。
Type: Float32; Readable: true; Writable: true

#### var value

```cangjie
public var value: Float32
```
功能： 当前进度值。
Type: Float32; Readable: true; Writable: true


### ProgressOptions

class ProgressOptions
```cangjie
public class ProgressOptions { public var value: Float64 public var total: Float64 public var progressType: ProgressType public init(value!: Float64, total!: Float64 = 100.0, progressType!: ProgressType = ProgressType.Linear) }
```
功能： 属性的进度条组件参数。

#### var progressType

```cangjie
public var progressType: ProgressType
```
功能： 指定进度条类型。
Type: ProgressType; Readable: true; Writable: true

#### var total

```cangjie
public var total: Float64
```
功能： 指定进度总长。设置小于等于0的数值时置为100。
Type: Float64; Readable: true; Writable: true

#### var value

```cangjie
public var value: Float64
```
功能： 指定当前进度值。设置小于0的数值时置为0，设置大于total的数值时置为total。
Type: Float64; Readable: true; Writable: true


### ProgressStyleOptions

class ProgressStyleOptions
```cangjie
public class ProgressStyleOptions { public var strokeWidth: Length public var scaleCount: Int32 public var scaleWidth: Length public var enableSmoothEffect: Bool public init(strokeWidth!: Length = 4.vp, scaleCount!: Int32 = 120, scaleWidth!: Length = 2.vp, enableSmoothEffect!: Bool = true) }
```
功能： 各类型进度条的基本样式。

#### var enableSmoothEffect

```cangjie
public var enableSmoothEffect: Bool
```
功能： 进度平滑动效的开关。开启平滑动效后设置进度，进度会从当前值渐变至设定值，否则进度从当前值突变至设定值。
Type: Bool; Readable: true; Writable: true

#### var scaleCount

```cangjie
public var scaleCount: Int32
```
功能： 设置环形进度条总刻度数。
Type: Int32; Readable: true; Writable: true

#### var scaleWidth

```cangjie
public var scaleWidth: Length
```
功能： 设置环形进度条刻度粗细（不支持百分比设置），刻度粗细大于进度条宽度时，为系统默认粗细。
Type: Length; Readable: true; Writable: true

#### var strokeWidth

```cangjie
public var strokeWidth: Length
```
功能： 设置进度条宽度（不支持百分比设置）。
Type: Length; Readable: true; Writable: true


### RingStyleOptions

class RingStyleOptions
```cangjie
public class RingStyleOptions { public var strokeWidth: Length public var shadow: Bool public var status: ProgressStatus public var enableSmoothEffect: Bool public var enableScanEffect: Bool public init(strokeWidth!: Length = 4.vp, shadow!: Bool = false, status!: ProgressStatus = ProgressStatus.PROGRESSING, enableSmoothEffect!: Bool = true, enableScanEffect!: Bool = false) }
```
功能： Ring的样式参数。

#### var enableScanEffect

```cangjie
public var enableScanEffect: Bool
```
功能： 扫光效果的开关。
Type: Bool; Readable: true; Writable: true

#### var enableSmoothEffect

```cangjie
public var enableSmoothEffect: Bool
```
功能： 进度平滑动效的开关。开启平滑动效后设置进度，进度会从当前值渐变至设定值，否则进度从当前值突变至设定值。
Type: Bool; Readable: true; Writable: true

#### var shadow

```cangjie
public var shadow: Bool
```
功能： 进度条阴影开关。
Type: Bool; Readable: true; Writable: true

#### var status

```cangjie
public var status: ProgressStatus
```
功能： 进度条状态，当设置为LOADING时会开启检查更新动效，此时设置进度值不生效。当从LOADING设置为PROGRESSING，检查更新动效会执行到终点再停止。
Type: ProgressStatus; Readable: true; Writable: true

#### var strokeWidth

```cangjie
public var strokeWidth: Length
```
功能： 设置进度条宽度（不支持百分比设置）。
Type: Length; Readable: true; Writable: true


### ScaleRingStyleOptions

class ScaleRingStyleOptions
```cangjie
public class ScaleRingStyleOptions { public var strokeWidth: Length public var scaleCount: Int32 public var scaleWidth: Length public var enableSmoothEffect: Bool public init(strokeWidth!: Length = 4.vp, scaleCount!: Int32 = 120, scaleWidth!: Length = 2.vp, enableSmoothEffect!: Bool = true) }
```
功能： ScaleRing的样式参数。

#### var enableSmoothEffect

```cangjie
public var enableSmoothEffect: Bool
```
功能： 进度平滑动效的开关。开启平滑动效后设置进度，进度会从当前值渐变至设定值，否则进度从当前值突变至设定值。
Type: Bool; Readable: true; Writable: true

#### var scaleCount

```cangjie
public var scaleCount: Int32
```
功能： 设置环形进度条总刻度数。
Type: Int32; Readable: true; Writable: true

#### var scaleWidth

```cangjie
public var scaleWidth: Length
```
功能： 设置环形进度条刻度粗细（不支持百分比设置），刻度粗细大于进度条宽度时，为系统默认粗细。
Type: Length; Readable: true; Writable: true

#### var strokeWidth

```cangjie
public var strokeWidth: Length
```
功能： 用于设置进度条宽度。
Type: Length; Readable: true; Writable: true



## Enums

### ProgressStatus

enum ProgressStatus
```cangjie
public enum ProgressStatus { | LOADING | PROGRESSING }
```
功能： Progress组件的进度条状态。

#### LOADING

```cangjie
LOADING
```
功能： 加载中。

#### PROGRESSING

```cangjie
PROGRESSING
```
功能： 进度更新中。


### ProgressStyle

enum ProgressStyle
```cangjie
public enum ProgressStyle { | Linear | Ring | Eclipse | ScaleRing | Capsule }
```
功能： Progress组件的样式类型。

#### Capsule

```cangjie
Capsule
```
功能： 胶囊样式，头尾两端圆弧处的进度展示效果与Eclipse相同；中段处的进度展示效果与Linear相同。高度大于宽度的时候自适应垂直显示。

#### Eclipse

```cangjie
Eclipse
```
功能： 圆形样式，显示类似月圆月缺的进度展示效果，从月牙逐渐变化至满月。

#### Linear

```cangjie
Linear
```
功能： 线性样式。

#### Ring

```cangjie
Ring
```
功能： 环形无刻度样式，环形圆环逐渐显示至完全填充效果。

#### ScaleRing

```cangjie
ScaleRing
```
功能： 环形有刻度样式，显示类似时钟刻度形式的进度展示效果。



## Examples

```cangjie
package ohos_app_cangjie_entry

import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 let scaleStyle0 = ScaleRingStyleOptions(strokeWidth: 15.vp, scaleCount: 15, scaleWidth: 5.vp,
 enableSmoothEffect: true)
 let scaleStyle1 = ScaleRingStyleOptions(strokeWidth: 20.vp, scaleCount: 20, scaleWidth: 5.vp,
 enableSmoothEffect: true)
 let scaleStyle2 = ScaleRingStyleOptions(strokeWidth: 20.vp, scaleCount: 30, scaleWidth: 3.vp,
 enableSmoothEffect: true)
 let ringStyle = RingStyleOptions(strokeWidth: 20.vp)
 func build() {
 Column(15) {
 Text("Linear Progress").fontSize(20).fontColor(0xCCCCCC).width(90.percent)
 Progress(value: 10.0, `type`: ProgressType.Linear).width(200)
 Progress(value: 20.0, total: 150.0, `type`: ProgressType.Linear)
 .color(Color.GRAY)
 .value(50.0)
 .width(200)

 Text("Eclipse Progress").fontSize(20).fontColor(0xCCCCCC).width(90.percent)
 Row(40) {
 Progress(value: 10.0, `type`: ProgressType.Eclipse).width(100)
 Progress(value: 20.0, total: 150.0, `type`: ProgressType.Eclipse)
 .width(100)
 .color(Color.GRAY)
 .value(50.0)
 }

 Text("ScaleRing Progress").fontSize(20).fontColor(0xCCCCCC).width(90.percent)
 Row(40) {
 Progress(value: 10.0, `type`: ProgressType.ScaleRing).width(100)
 Progress(value: 20.0, total: 150.0, `type`: ProgressType.ScaleRing)
 .color(Color.GRAY)
 .value(50.0)
 .width(100)
 .style(scaleStyle0)
 }

 Row(40) {
 Progress(value: 20.0, total: 150.0, `type`: ProgressType.ScaleRing)
 .color(Color.GRAY)
 .value(50.0)
 .width(100)
 .style(scaleStyle1)
 Progress(value: 20.0, total: 150.0, `type`: ProgressType.ScaleRing)
 .color(Color.GRAY)
 .value(50.0)
 .width(100)
 .style(scaleStyle2)
 }

 Text("Ring Progress").fontSize(20).fontColor(0xCCCCCC).width(90.percent)
 Row(40) {
 Progress(value: 10.0, `type`: ProgressType.Ring).width(100)
 Progress(value: 20.0, total: 150.0, `type`: ProgressType.Ring)
 .color(Color.GRAY)
 .value(50.0)
 .width(100)
 .style(ringStyle)
 }

 Text("Capsule Progress").fontSize(20).fontColor(0xCCCCCC).width(90.percent)
 Row(40) {
 Progress(value: 10.0, `type`: ProgressType.Capsule).width(100).height(50)
 Progress(value: 20.0, total: 150.0, `type`: ProgressType.Capsule)
 .color(Color.GRAY)
 .value(50.0)
 .width(100)
 .height(50)
 }
 }
 }
}
```
```cangjie
package ohos_app_cangjie_entry

import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 let colorStop0 = ColorStop(0X02fd03, 0.5)
 let colorStop1 = ColorStop(Color.BLUE, 1.0)
 let ringStyle0 = RingStyleOptions(strokeWidth: 20.vp)
 let ringStyle1 = RingStyleOptions(strokeWidth: 20.vp, shadow: true)
 func build() {
 Column(15) {
 Text("Gradient Color").fontSize(20).fontColor(0xCCCCCC).width(90.percent)
 Row(40) {
 Progress(value: 70.0, `type`: ProgressType.Ring)
 .width(100)
 .style(ringStyle0)
 .color([colorStop0, colorStop1])
 }
 Text("Shadow").fontSize(20).fontColor(0xCCCCCC).width(90.percent)
 Row(40) {
 Progress(value: 70.0, `type`: ProgressType.Ring).width(120).color(Color.BLUE).style(ringStyle1)
 }
 }
 }
}
```
```cangjie
package ohos_app_cangjie_entry

import ohos.state_manage.*
import ohos.state_macro_manage.*
import ohos.component.*
import ohos.base.*
import ohos.resource_manager.*

@Entry
@Component
class EntryView {
 let ringStyle0 = RingStyleOptions(strokeWidth: 20.vp, status: ProgressStatus.LOADING)
 let ringStyle1 = RingStyleOptions(strokeWidth: 20.vp, enableScanEffect: true)
 func build() {
 Column(15) {
 Text("Loading Effect").fontSize(20).fontColor(0xCCCCCC).width(90.percent)
 Row(40) {
 Progress(value: 0.0, `type`: ProgressType.Ring).width(100).style(ringStyle0).color(Color.BLUE)
 }
 Text("Shadow").fontSize(20).fontColor(0xCCCCCC).width(90.percent)
 Row(40) {
 Progress(value: 30.0, `type`: ProgressType.Ring).width(100).color(0X02fd03).style(ringStyle1)
 }
 }
 }
}
```
```cangjie
package ohos_app_cangjie_entry

import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 func build() {
 Column(15) {
 Row(40) {
 Progress(value: 100.0, total: 100.0, `type`: ProgressType.Capsule).width(100).height(50)
 .style(
 CapsuleStyleOptions(
 content: 'Installing...',
 font: Fonts(size: 13, style: FontStyle.Normal),
 borderColor: Color.BLUE,
 borderWidth: 1,
 fontColor: Color.GRAY,
 enableScanEffect: false,
 showDefaultPercentage: false))
 }
 }.width(100.percent).padding( top: 5 )
 }
}
```
```cangjie
package ohos_app_cangjie_entry

import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 @State
 var value: Float64 = 0.0

 func build() {
 Column(10) {
 Text('enableSmoothEffect: true').fontSize(9).fontColor(0xCCCCCC).width(90.percent).margin(5).margin( top: 20 )
 Progress( value: this.value, total: 100.0, `type`: ProgressType.Linear ).style(RingStyleOptions(strokeWidth: 10, enableSmoothEffect: true ))
 Text('enableSmoothEffect: false').fontSize(9).fontColor(0xCCCCCC).width(90.percent).margin(5)
 Progress( value: this.value, total: 100.0, `type`: ProgressType.Linear ).style(RingStyleOptions(strokeWidth: 10, enableSmoothEffect: false ))
 Button('value +10')
 .onClick{ =>
 this.value += 10.0
 }.width(75).height(15).fontSize(9)
 }.width(50.percent).height(100.percent).margin( left: 20 )
 }
}
```

