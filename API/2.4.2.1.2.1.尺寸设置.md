# 尺寸设置

设置组件的宽高、边距。

## Functions

### public func width(value: Length): This

```cangjie
public func width(value: Length): This
```
功能： 设置组件自身的宽度，缺省时使用元素自身内容需要的宽度。若子组件的宽大于父组件的宽，则会画出父组件的范围。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 组件宽度。 单位：vp。 | value | - |

### public func width<T>(value: Option<T>): This

```cangjie
public func width<T>(value: Option<T>): This
```
功能： 设置组件自身的宽度，参数值为None时使用元素自身内容需要的宽度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Option<T> | 是 | 组件宽度。 T为Int64、Float64、Length、AppResource类型。 | value | - |

### public func height(value: Length): This

```cangjie
public func height(value: Length): This
```
功能： 设置组件自身的高度，缺省时使用元素自身内容需要的高度。若子组件的高大于父组件的高，则会画出父组件的范围。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 组件高度。 单位：vp。 | value | - |

### public func height<T>(value: Option<T>): This

```cangjie
public func height<T>(value: Option<T>): This
```
功能： 设置组件自身的高度，参数值为None时使用元素自身内容需要的宽度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Option<T> | 是 | 组件高度。 T为Int64、Float64、Length、AppResource类型。 | value | - |

### public func size(width!: Length, height!: Length): This

```cangjie
public func size(width!: Length, height!: Length): This
```
功能： 设置组件的高宽。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 命名参数。 组件宽度。 单位：vp。 | width | - |
| Length | 是 | 命名参数。 组件高度。 单位：vp。 | height | - |

### public func padding(value: Length): This

```cangjie
public func padding(value: Length): This
```
功能： 设置内边距属性。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 组件的内边距，四个方向内边距同时生效。 单位：vp。 | value | - |

### public func padding(top!: Length = 0.vp, right!: Length = 0.vp, bottom!: Length = 0.vp, left!: Length = 0.vp): This

```cangjie
public func padding(top!: Length = 0.vp, right!: Length = 0.vp, bottom!: Length = 0.vp, left!: Length = 0.vp): This
```
功能： 设置内边距属性。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 否 | 命名参数。 上内边距，组件内元素距组件顶部的尺寸。 初始值： 0.vp。 | top | 0.vp |
| Length | 否 | 命名参数。 右内边距，组件内元素距组件右边界的尺寸。 初始值： 0.vp。 | right | 0.vp |
| Length | 否 | 命名参数。 下内边距，组件内元素距组件底部的尺寸。 初始值： 0.vp。 | bottom | 0.vp |
| Length | 否 | 命名参数。 左内边距，组件内元素距组件左边界的尺寸。 初始值： 0.vp。 | left | 0.vp |

### public func margin(value: Length): This

```cangjie
public func margin(value: Length): This
```
功能： 设置外边距属性。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 组件的外边距，四个方向内边距同时生效。 单位：vp | value | - |

### public func margin(top!: Length = 0.vp, right!: Length = 0.vp, bottom!: Length = 0.vp, left!: Length = 0.vp): This

```cangjie
public func margin(top!: Length = 0.vp, right!: Length = 0.vp, bottom!: Length = 0.vp, left!: Length = 0.vp): This
```
功能： 设置外边距属性。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 否 | 命名参数。 上内边距，组件顶部距组件外元素的尺寸。 | top | 0.vp |
| Length | 否 | 命名参数。 右内边距，组件右边界距组件外元素的尺寸。 | right | 0.vp |
| Length | 否 | 命名参数。 下内边距，组件底部距组件外元素的尺寸。 | bottom | 0.vp |
| Length | 否 | 命名参数。 左内边距，组件左边界距组件外元素的尺寸。 | left | 0.vp |

### public func layoutWeight(value: Int32): This

```cangjie
public func layoutWeight(value: Int32): This
```
功能： 设置组件的布局权重，使用该属性的组件在父容器（ Row / Column / Flex ）的主轴方向按照权重分配尺寸。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 父容器尺寸确定时，设置了layoutWeight属性的子元素与兄弟元素占主轴尺寸按照权重进行分配，忽略元素本身尺寸设置，表示自适应占满剩余空间。 说明： 仅在 Row / Column / Flex 布局中生效。可选值为大于等于0的数字，或者可以转换为数字的字符串。如果容器中有子元素设置了layoutWeight属性，且设置的属性值大于0，则所有子元素不会再基于flexShrink和flexGrow布局。 | value | - |

### public func constraintSize(minWidth!: Length = 0.vp, maxWidth!: Length = (Float64.Inf).vp, minHeight!: Length = 0.vp, maxHeight!: Length = (Float64.Inf).vp): This

```cangjie
public func constraintSize(minWidth!: Length = 0.vp, maxWidth!: Length = (Float64.Inf).vp, minHeight!: Length = 0.vp, maxHeight!: Length = (Float64.Inf).vp): This
```
功能： 设置约束尺寸，组件布局时，进行尺寸范围限制。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 否 | 命名参数。 元素最小宽度。 初始值：0.vp。 | minWidth | 0.vp |
| Length | 否 | 命名参数。 元素最大宽度。 初始值：(Float64.Inf).vp。 | maxWidth | (Float64.Inf).vp |
| Length | 否 | 命名参数。 元素最小高度。 初始值：0.vp。 | minHeight | 0.vp |
| Length | 否 | 命名参数。 元素最大高度。 初始值：(Float64.Inf).vp。 | maxHeight | (Float64.Inf).vp |


## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 func build() {
 Column() {
 Text("margin and padding:").fontSize(12).fontColor(0xCCCCCC).width(90.percent)
 Row() {
 // 宽度80 ,高度80 ,外边距20（蓝色区域），内边距10（白色区域）
 Row() {
 Row().size(width: 100.percent, height: 100.percent).backgroundColor(Color.GREEN)
 }
 .width(80)
 .height(80)
 .padding(10)
 .margin(20)
 .backgroundColor(Color.WHITE)
 }.backgroundColor(Color.BLUE)

 Text("constraintSize").fontSize(12).fontColor(0xCCCCCC).width(90.percent)
 Text("this is a Text.this is a Text.this is a Text.this is a Text.this is a Text.this is a Text.this is a Text.this is a Text.this is a Text.this is a Text.this is a Text.this is a Text.this is a Text.this is a Text.this is a Text")
 .width(90.percent)
 .constraintSize(maxWidth: 200.vp)

 Text("layoutWeight").fontSize(12).fontColor(0xCCCCCC).width(90.percent)
 // 父容器尺寸确定时，设置了layoutWeight的子元素在主轴布局尺寸按照权重进行分配，忽略本身尺寸设置。
 Row() {
 // 权重1，占主轴剩余空间1/3
 Text("layoutWeight(1)")
 .size(width: 30.percent, height: 110.vp).backgroundColor(0xFFEFD5).textAlign(TextAlign.Center)
 .layoutWeight(1)
 // 权重2，占主轴剩余空间2/3
 Text("layoutWeight(2)")
 .size(width: 30.percent, height: 110.vp).backgroundColor(0xF5DEB3).textAlign(TextAlign.Center)
 .layoutWeight(2)
 // 未设置layoutWeight属性，组件按照自身尺寸渲染
 Text("no layoutWeight")
 .size(width: 30.percent, height: 110.vp).backgroundColor(0xD2B48C).textAlign(TextAlign.Center)
 }.size(width: 90.percent, height: 140.vp).backgroundColor(0xAFEEEE)
 Text("calc:").fontSize(12).fontColor(0xCCCCCC).width(90.percent)
 Text("calc test")
 .fontSize(50)
 .fontWeight(FontWeight.Bold)
 .backgroundColor(0xFFFAF0)
 .textAlign(TextAlign.Center)
 .margin(50)
 .size(width: 90.percent, height: 10.percent)
 }.width(100.percent).margin(top: 5)
 }
}
```

