# ohos.unifiedDataChannel（标准化数据通路）

本模块为统一数据管理框架（Unified Data Management Framework,UDMF）的组成部分，针对多对多跨应用数据共享的不同业务场景，提供了标准化的数据通路，提供了标准化的数据接入与读取接口。同时对文本、图片等数据类型提供了标准化定义，方便不同应用间进行数据交互，减少数据类型适配的工作量。

```cangjie
import kit.ArkData.*
```
## Classes

### Options

class Options
```cangjie
public class Options { public Options( public var intention!: Intention, public var key!: String ) }
```
功能： UDMF提供的数据操作接口可选项。

#### var intention

```cangjie
public var intention: Intention
```
功能： 表示数据操作相关的数据通路类型。
Type: Intention; Readable: true; Writable: true

#### var key

```cangjie
public var key: String
```
功能： UDMF中数据对象的唯一标识符。
Type: String; Readable: true; Writable: true


### UnifiedData

class UnifiedData
```cangjie
public class UnifiedData { public init() public init(record: UnifiedRecord) }
```
功能： 表示UDMF统一数据对象，提供封装一组数据记录的方法。


### UnifiedRecord

class UnifiedRecord
```cangjie
public class UnifiedRecord { public init() public init(dtype: String, value: UnifiedDataChannelValueType) }
```
功能： 对UDMF支持的数据内容的抽象定义，称为数据记录。一个统一数据对象内包含一条或多条数据记录，例如一条文本记录、一条图片记录、一条HTML记录等。



## Enums

### Intention

enum Intention
```cangjie
public enum Intention { | DATA_HUB | DRAG | ... }
```
功能： UDMF已经支持的数据通路枚举类型。其主要用途是标识各种UDMF数据通路所面向的不同业务场景。

#### DATA_HUB

```cangjie
DATA_HUB
```
功能： 公共数据通路。

#### DRAG

```cangjie
DRAG
```
功能： 拖拽类型数据通道。


### ShareOptions

enum ShareOptions
```cangjie
public enum ShareOptions { | IN_APP | CROSS_APP | ... }
```
功能： UDMF支持的设备内使用范围类型枚举。

#### CROSS_APP

```cangjie
CROSS_APP
```
功能： 表示允许在本设备内跨应用使用。

#### IN_APP

```cangjie
IN_APP
```
功能： 表示允许在本设备同应用内使用。


### UnifiedDataChannelValueType

enum UnifiedDataChannelValueType
```cangjie
public enum UnifiedDataChannelValueType { | INTEGER32(Int32) | INTEGER64(Int64) | DOUBLE(Float64) | BOOLEAN(Bool) | STRING(String) | ARRAYBUFFER(Array<UInt8>) | PIXELMAP(PixelMap) | NULL | UNDEFINED | ... }
```
功能： 用于表示统一数据记录允许的数据字段类型。

#### ARRAYBUFFER(Array<UInt8>)

```cangjie
ARRAYBUFFER(Array<UInt8>)
```
功能： 表示Array<UInt8>的类型。

#### BOOLEAN(Bool)

```cangjie
BOOLEAN(Bool)
```
功能： 表示Bool的类型。

#### DOUBLE(Float64)

```cangjie
DOUBLE(Float64)
```
功能： 表示Float64的类型。

#### INTEGER32(Int32)

```cangjie
INTEGER32(Int32)
```
功能： 表示Int32的类型。

#### INTEGER64(Int64)

```cangjie
INTEGER64(Int64)
```
功能： 表示Int64的类型。

#### NULL

```cangjie
NULL
```
功能： 表示null。

#### PIXELMAP(PixelMap)

```cangjie
PIXELMAP(PixelMap)
```
功能： 表示PixelMap的类型。

#### STRING(String)

```cangjie
STRING(String)
```
功能： 表示String的类型。

#### UNDEFINED

```cangjie
UNDEFINED
```
功能： 表示undefined。



## Types

### GetDelayData

type GetDelayData
```cangjie
public type GetDelayData =(dtype: String) -> UnifiedData
```
**功能：**对UnifiedData的延迟封装，支持延迟获取数据。当前只支持同设备剪贴板场景，后续场景待开发。

| type | description | name | required |
| --- | --- | --- | --- |
| String | 作为延迟封装的标识。 | dtype | 是 |



