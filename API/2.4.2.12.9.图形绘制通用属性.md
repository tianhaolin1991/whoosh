# 图形绘制通用属性

图形绘制通用属性目前支持 Circle 、 Ellipse 、 Line 、 Polyline 、 Polygon 、 Path 、 Rect 、 Shape 等组件。

## Functions

### public func antiAlias(antiAlias: Bool): This

```cangjie
public func antiAlias(antiAlias: Bool): This
```
功能： 设置是否开启抗锯齿效果。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否开启抗锯齿效果。 true：开启抗锯齿；false：关闭抗锯齿。 初始值：true。 | antiAlias | - |

### public func fill(color: ResourceColor): This

```cangjie
public func fill(color: ResourceColor): This
```
功能： 设置填充区域的颜色，异常值按照初始值处理。与通用属性 foregroundColor 同时设置时，后设置的属性生效。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 填充区域颜色。初始值：Color.BLACK。 | color | - |

### public func fillOpacity(value: Float64): This

```cangjie
public func fillOpacity(value: Float64): This
```
功能： 设置填充区域透明度。取值范围是[0.0,1.0]，若给定值小于0.0，则取值为0.0；若给定值大于1.0，则取值为1.0，其余异常值按1.0处理。取值为1.0代表不透明，取值为0.0代表完全透明。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Float64 | 是 | 填充区域透明度。 初始值：1.0。 | value | - |

### public func fillOpacity(value: Int64): This

```cangjie
public func fillOpacity(value: Int64): This
```
功能： 设置填充区域透明度。取值范围是[0, 1]，若给定值小于0，则取值为0；若给定值大于1，则取值为1，其余异常值按1处理。取值为1代表不透明，取值为0代表完全透明。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int64 | 是 | 填充区域透明度。 初始值：1。 | value | - |

### public func fillOpacity(value: AppResource): This

```cangjie
public func fillOpacity(value: AppResource): This
```
功能： 设置填充区域透明度。取值范围是[0, 1]，若给定值小于0，则取值为0；若给定值大于1，则取值为1，其余异常值按1处理。取值为1代表不透明，取值为0代表完全透明。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 填充区域透明度。 | value | - |

### public func height(value: Length): This

```cangjie
public func height(value: Length): This
```
功能： 设置组件高度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 组件高度。 单位：vp。 | value | - |

### public func size(width!: Length, height!: Length): This

```cangjie
public func size(width!: Length, height!: Length): This
```
功能： 设置组件宽高。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 命名参数。 组件宽度。 单位：vp。 | width | - |
| Length | 是 | 命名参数。 组件高度。 单位：vp。 | height | - |

### public func stroke(color: ResourceColor): This

```cangjie
public func stroke(color: ResourceColor): This
```
功能： 设置边框颜色。默认没有边框。异常值不会绘制边框。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 边框颜色。 | color | - |

### public func strokeDashArray(dashArray: Array<Length>): This

```cangjie
public func strokeDashArray(dashArray: Array<Length>): This
```
功能： 设置边框间隙。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array< Length > | 是 | 边框间隙。初始值：[]，单位：vp。 取值范围≥0，异常值按照初始值处理。 | dashArray | - |

### public func strokeDashOffset(dashOffset: Length): This

```cangjie
public func strokeDashOffset(dashOffset: Length): This
```
功能： 设置边框绘制起点的偏移量。异常值按照初始值处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 边框绘制起点的偏移量。 初始值：0。 单位：vp。 | dashOffset | - |

### public func strokeLineCap(lineCap: LineCapStyle): This

```cangjie
public func strokeLineCap(lineCap: LineCapStyle): This
```
功能： 设置边框端点绘制样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| LineCapStyle | 是 | 边框端点绘制样式。 初始值：LineCapStyle.Butt。 | lineCap | - |

### public func strokeLineJoin(lineJoin: LineJoinStyle): This

```cangjie
public func strokeLineJoin(lineJoin: LineJoinStyle): This
```
功能： 设置边框拐角绘制样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| LineJoinStyle | 是 | 边框拐角绘制样式。 初始值：LineJoinStyle.Miter。 | lineJoin | - |

### public func strokeMiterLimit(miterLimit: Float64): This

```cangjie
public func strokeMiterLimit(miterLimit: Float64): This
```
功能： 设置斜接长度与边框宽度比值的极限值。斜接长度表示外边框外边交点到内边交点的距离，边框宽度即strokeWidth属性的值。该属性取值需在strokeLineJoin属性取值LineJoinStyle.Miter时生效。 该属性的合法值范围应当大于等于1.0，当取值范围在[0,1)时按1.0处理，其余异常值按初始值处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Float64 | 是 | 斜接长度与边框宽度比值的极限值。 初始值：4.0。 | miterLimit | - |

### public func strokeMiterLimit(miterLimit: Int64): This

```cangjie
public func strokeMiterLimit(miterLimit: Int64): This
```
功能： 设置斜接长度与边框宽度比值的极限值。斜接长度表示外边框外边交点到内边交点的距离，边框宽度即strokeWidth属性的值。该属性取值需在strokeLineJoin属性取值LineJoinStyle.Miter时生效。 该属性的合法值范围应当大于等于1，当取值范围在[0,1)时按1处理，其余异常值按初始值处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int64 | 是 | 斜接长度与边框宽度比值的极限值。 初始值：4。 | miterLimit | - |

### public func strokeOpacity(value: Float64): This

```cangjie
public func strokeOpacity(value: Float64): This
```
功能： 设置边框透明度。该属性的取值范围是[0.0, 1.0]，若给定值小于0.0，则取值为0.0；若给定值大于1.0，则取值为1.0，其余异常值按1.0处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Float64 | 是 | 边框透明度。 初始值：1.0。 | value | - |

### public func strokeOpacity(value: Int64): This

```cangjie
public func strokeOpacity(value: Int64): This
```
功能： 设置边框透明度。该属性的取值范围是[0, 1]，若给定值小于0，则取值为0；若给定值大于1，则取值为1，其余异常值按1处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int64 | 是 | 边框透明度。 初始值：1。 | value | - |

### public func strokeOpacity(value: AppResource): This

```cangjie
public func strokeOpacity(value: AppResource): This
```
**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 边框透明度。 | value | - |

### public func strokeWidth(value: Length): This

```cangjie
public func strokeWidth(value: Length): This
```
功能： 设置边框宽度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 边框宽度。 初始值：1。 默认单位：vp。 暂不支持百分比，百分比按照1px处理。 | value | - |

### public func width(value: Length): This

```cangjie
public func width(value: Length): This
```
功能： 设置组件宽度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 组件宽度。单位：vp。 | value | - |


