# RichEditor

支持图文混排和文本交互式编辑的组件。

## Component Constructors

### public init(controller: RichEditorController)

```cangjie
public init(controller: RichEditorController)
```
功能： 创建RichEditor组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| RichEditorController | 是 | 富文本控制器。 | controller | - |


## Functions

### public func bindSelectionMenu( spantype!: RichEditorSpanType = RichEditorSpanType.TEXT, content!: () -> Unit, responseType!: ResponseType = ResponseType.LongPress, options !: SelectionMenuOptions ): This

```cangjie
public func bindSelectionMenu( spantype!: RichEditorSpanType = RichEditorSpanType.TEXT, content!: () -> Unit, responseType!: ResponseType = ResponseType.LongPress, options !: SelectionMenuOptions ): This
```
功能： 设置自定义选择菜单。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| RichEditorSpanType | 否 | 命名参数。 指定选择菜单的类型。 | spantype | RichEditorSpanType.TEXT |
| ()->Unit | 是 | 命名参数。 指定选择菜单的内容。使用时结合 @Builder 和 bind 方法使用。 | content | - |
| ResponseType | 否 | 命名参数。 指定选择菜单的响应类型。 | responseType | ResponseType.LongPress |
| SelectionMenuOptions | 是 | 命名参数。 指定选择菜单的选项。 | options | - |

### public func copyOptions(copyOptions: CopyOptions): This

```cangjie
public func copyOptions(copyOptions: CopyOptions): This
```
功能： 设置文本内容支持复制粘贴的能力。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| CopyOptions | 是 | 复制粘贴的能力。 初始值：CopyOptions.LocalDevice。 | copyOptions | - |

### public func customKeyboard(builder!: () -> Unit): This

```cangjie
public func customKeyboard(builder!: () -> Unit): This
```
功能： 定义自定义键盘。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ()->Unit | 是 | 命名参数。 富文本编辑器的自定义键盘。使用时结合 @Builder 和 bind 方法使用。 | builder | - |

### public func aboutToIMEInput(callback: (RichEditorInsertValue) -> Bool): This

```cangjie
public func aboutToIMEInput(callback: (RichEditorInsertValue) -> Bool): This
```
功能： 输入法输入内容前，触发事件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( RichEditorInsertValue )->Bool | 是 | 回调函数，输入法输入内容前触发。 RichEditorInsertValue ：输入法将要输入内容信息。 true：组件执行添加内容操作。 false：组件不执行添加内容操作。 | callback | - |

### public func aboutToDelete(callback: (RichEditorDeleteValue) -> Bool): This

```cangjie
public func aboutToDelete(callback: (RichEditorDeleteValue) -> Bool): This
```
功能： 输入法删除内容前，触发事件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( RichEditorDeleteValue )->Bool | 是 | 回调函数，输入法删除内容前触发该回调 。 RichEditorDeleteValue ：准备删除的内容所在的文本Span信息。 true：组件执行删除操作。 false：组件不执行删除操作。 | callback | - |

### public func onDeleteComplete(callback: () -> Unit): This

```cangjie
public func onDeleteComplete(callback: () -> Unit): This
```
功能： 输入法完成删除后，触发事件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ()->Unit | 是 | 回调函数，订阅输入法完成删除时触发。 | callback | - |

### public func onDidChange(callback: (TextRange, TextRange) -> Unit): This

```cangjie
public func onDidChange(callback: (TextRange, TextRange) -> Unit): This
```
功能： 组件执行增删操作后，触发事件。文本实际未发生增删时，不触发该事件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( TextRange , TextRange )->Unit | 是 | 回调函数，件执行增删操作后，触发回调。文本实际未发生增删时，不触发该回调。参数：图文变化前后的内容范围。 | callback | - |

### public func onIMEInputComplete(callback: (RichEditorTextSpanResult) -> Unit): This

```cangjie
public func onIMEInputComplete(callback: (RichEditorTextSpanResult) -> Unit): This
```
功能： 输入法完成输入后，触发事件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( RichEditorTextSpanResult )->Unit | 是 | 回调函数，输入法完成输入后触发回调。 RichEditorTextSpanResult：输入法完成输入后的文本Span信息。 | callback | - |

### public func onPaste(callback: (PasteEvent) -> Unit): This

```cangjie
public func onPaste(callback: (PasteEvent) -> Unit): This
```
功能： 完成粘贴前，触发事件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( PasteEvent )->Unit | 是 | 回调函数，完成粘贴前，触发回调。 PasteEvent：定义用户粘贴事件。 | callback | - |

### public func onReady(callback: () -> Unit): This

```cangjie
public func onReady(callback: () -> Unit): This
```
功能： 富文本组件初始化完成后，触发事件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ()->Unit | 是 | 回调函数，富文本组件初始化完成后触发回调。 | callback | - |

### public func onSelect(callback: (RichEditorSelection) -> Unit): This

```cangjie
public func onSelect(callback: (RichEditorSelection) -> Unit): This
```
功能： 鼠标左键双击选中内容时，会触发事件；松开鼠标左键后，会再次触发事件。手指长按选中内容时，会触发事件；松开手指后，会再次触发事件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( RichEditorSelection )->Unit | 是 | 回调函数，鼠标左键按下选择，松开左键后触发回调。 用手指选择时，松开手指触发回调。 RichEditorSelection：选中的所有Span信息。 | callback | - |


## Classes

### PasteEvent

class PasteEvent
```cangjie
public class PasteEvent {}
```
功能： 定义用户粘贴事件。


### RichEditorController

class RichEditorController
```cangjie
public class RichEditorController {}
```
功能： RichEditor组件的控制器。


### RichEditorDeleteValue

class RichEditorDeleteValue
```cangjie
public class RichEditorDeleteValue { public RichEditorDeleteValue( public var offset: Int32, public var direction: RichEditorDeleteDirection, public var length: Int32, public var richEditorDeleteSpans: ArrayList<RichEditorSpanResult> ) }
```
功能： 删除操作的信息和被删除内容的信息。

#### var direction

```cangjie
public var direction: RichEditorDeleteDirection
```
功能： 表示删除操作的方向。
Type: RichEditorDeleteDirection; Readable: true; Writable: true

#### var length

```cangjie
public var length: Int32
```
功能： 表示删除内容长度。
Type: Int32; Readable: true; Writable: true

#### var offset

```cangjie
public var offset: Int32
```
功能： 表示删除内容的偏移位置。
Type: Int32; Readable: true; Writable: true

#### var richEditorDeleteSpans

```cangjie
public var richEditorDeleteSpans: ArrayList<RichEditorSpanResult>
```
功能： 表示删除的文本或者图片Span的具体信息。
Type: ArrayList< RichEditorSpanResult >; Readable: true; Writable: true


### LeadingMarginPlaceholder

class LeadingMarginPlaceholder
```cangjie
public class LeadingMarginPlaceholder { public init(pixelMap!: PixelMap, size!: (Length, Length)) }
```
功能： 前导边距占位符，用于表示文本段落左侧与组件边缘之间的距离。


### RichEditorParagraphStyle

class RichEditorParagraphStyle
```cangjie
public class RichEditorParagraphStyle { public var textAlign: TextAlign public var margin: Option<Length> public var placeholder: Option<LeadingMarginPlaceholder> public init(textAlign!: TextAlign = TextAlign.Start) public init(textAlign!: TextAlign = TextAlign.Start, leadingMargin!: Length) public init(textAlign!: TextAlign = TextAlign.Start, leadingMargin!: LeadingMarginPlaceholder) }
```
功能： 段落样式。

#### var textAlign

```cangjie
public var textAlign: TextAlign
```
功能： 表示文本段落在水平方向的对齐方式。
Type: TextAlign; Readable: true; Writable: true

#### var margin

```cangjie
public var margin: Option<Length>
```
功能： 表示外边距。
Type: Option< Length >; Readable: true; Writable: true

#### var placeholder

```cangjie
public var placeholder: Option<LeadingMarginPlaceholder>
```
功能： 表示文本段落缩进。
Type: Option< LeadingMarginPlaceholder >; Readable: true; Writable: true


### RichEditorImageSpanStyle

class RichEditorImageSpanStyle
```cangjie
public class RichEditorImageSpanStyle { public init(size!: (Length, Length), verticalAlign!: ImageSpanAlignment = ImageSpanAlignment.BASELINE, objectFit!: ImageFit = ImageFit.Cover) public init(verticalAlign!: ImageSpanAlignment = ImageSpanAlignment.BASELINE, objectFit!: ImageFit = ImageFit.Cover) }
```
功能： 图片样式。


### RichEditorTextStyle

class RichEditorTextStyle
```cangjie
public class RichEditorTextStyle { public init(fontColor!: Color = Color.BLACK, fontSize!: Length = 16.vp, fontStyle!: FontStyle = FontStyle.Normal, fontWeight!: FontWeight = FontWeight.Normal, fontFamily!: String = "HarmonyOS Sans", decoration!: TextDecoration = TextDecoration(`type`: TextDecorationType.None, color: Color.BLACK)) }
```
功能： 文本样式信息。


### TextDecoration

class TextDecoration
```cangjie
public class TextDecoration { public var `type`: TextDecorationType public var color: ResourceColor public init(`type`!: TextDecorationType, color!: ResourceColor = Color.BLACK) { this.`type` = `type` this.color = transAppResourceToResourceColor(color) } }
```
功能： 文本装饰样式及颜色类型。

#### var `type`

```cangjie
public var `type`: TextDecorationType
```
功能： 表示装饰线类型。
Type: TextDecorationType; Readable: true; Writable: true

#### var color

```cangjie
public var color: TextDecorationType
```
功能： 表示装饰颜色。
Type: TextDecorationType; Readable: true; Writable: true


### RichEditorImageSpanOptions

class RichEditorImageSpanOptions
```cangjie
public class RichEditorImageSpanOptions { public var offset: Int32 public var imageStyle: RichEditorImageSpanStyle public init(offset!: Int32 = Int32.Max, imageStyle!: RichEditorImageSpanStyle = RichEditorImageSpanStyle()) }
```
功能： 表示添加图片的偏移位置和图片样式信息的类型。

#### var offset

```cangjie
public var offset: Int32
```
功能： 表示添加图片的位置。省略时，添加到所有内容的最后。
Type: Int32; Readable: true; Writable: true

#### var imageStyle

```cangjie
public var imageStyle: RichEditorImageSpanStyle
```
功能： 表示图片样式信息的类型。省略时，使用系统默认图片信息。
Type: RichEditorImageSpanStyle; Readable: true; Writable: true


### RichEditorTextSpanOptions

class RichEditorTextSpanOptions
```cangjie
public class RichEditorTextSpanOptions { public init(offset!: Int32 = Int32.Max, style!: RichEditorTextStyle = RichEditorTextStyle()) }
```
功能： 添加文本的偏移位置和文本样式信息。


### SelectionMenuOptions

class SelectionMenuOptions
```cangjie
public class SelectionMenuOptions { public init(onAppear!: () -> Unit = { => }, onDisappear!: () -> Unit = { => }) }
```
功能： 菜单选项类型。


### RichEditorImageSpanResult

class RichEditorImageSpanResult
```cangjie
public class RichEditorImageSpanResult { public var size: (Float64, Float64) = (0.0, 0.0) public var verticalAlign: ImageSpanAlignment = ImageSpanAlignment.TOP public var objectFit: ImageFit = ImageFit.Auto public var spanPosition: RichEditorSpanPosition = RichEditorSpanPosition(0, (0, 0)) public var valuePixelMap: Option<PixelMap> = None public var valueResourceStr: String = "" public var imageStyle: RichEditorImageSpanStyleResult = RichEditorImageSpanStyleResult() public var offsetInSpan: (Int32, Int32) = (0, 0) public init(size: (Float64, Float64), verticalAlign: ImageSpanAlignment, objectFit: ImageFit) public init( size: (Float64, Float64), verticalAlign: ImageSpanAlignment, objectFit: ImageFit, spanPosition: RichEditorSpanPosition, valuePixelMap: Option<PixelMap>, valueResourceStr: String, imageStyle: RichEditorImageSpanStyleResult, offsetInSpan: (Int32, Int32) ) public init() }
```
功能： 表示后端返回的图片信息的类型。

#### var imageStyle

```cangjie
public var imageStyle: RichEditorImageSpanStyleResult
```
功能： 表示图片样式。
Type: RichEditorImageSpanStyleResult; Readable: true; Writable: true

#### var objectFit

```cangjie
public var objectFit: ImageFit
```
功能： 表示图片缩放类型。
Type: ImageFit; Readable: true; Writable: true

#### var offsetInSpan

```cangjie
public var offsetInSpan:(Int32, Int32)
```
功能： 表示Span里图片的起始和结束位置。
Type: (Int32, Int32); Readable: true; Writable: true

#### var size

```cangjie
public var size:(Float64, Float64)
```
功能： 表示图片的宽度和高度。
Type: (Float64, Float64); Readable: true; Writable: true

#### var spanPosition

```cangjie
public var spanPosition: RichEditorSpanPosition
```
功能： 表示Span位置。
Type: RichEditorSpanPosition; Readable: true; Writable: true

#### var valuePixelMap

```cangjie
public var valuePixelMap: Option<PixelMap>
```
功能： 表示图片内容。
Type: Option< PixelMap >; Readable: true; Writable: true

#### var valueResourceStr

```cangjie
public var valueResourceStr: String
```
功能： 表示图片资源id。
Type: String; Readable: true; Writable: true

#### var verticalAlign

```cangjie
public var verticalAlign: ImageSpanAlignment
```
功能： 表示图片垂直对齐方式。
Type: ImageSpanAlignment; Readable: true; Writable: true


### RichEditorImageSpanStyleResult

class RichEditorImageSpanStyleResult
```cangjie
public class RichEditorImageSpanStyleResult { public var size: (Float64, Float64) = (0.0, 0.0) public var verticalAlign: ImageSpanAlignment = ImageSpanAlignment.CENTER public var objectFit: ImageFit = ImageFit.Auto public var layoutStyle: RichEditorLayoutStyleResult = RichEditorLayoutStyleResult() }
```
功能： 后端返回的图片样式信息。

#### var layoutStyle

```cangjie
public var layoutStyle: RichEditorLayoutStyleResult
```
功能： 表示图片布局风格。
Type: RichEditorLayoutStyleResult; Readable: true; Writable: true

#### var objectFit

```cangjie
public var objectFit: ImageFit
```
功能： 表示图片缩放类型。
Type: ImageFit; Readable: true; Writable: true

#### var size

```cangjie
public var size:(Float64, Float64)
```
功能： 表示图片的宽度和高度。
Type: (Float64,Float64); Readable: true; Writable: true

#### var verticalAlign

```cangjie
public var verticalAlign: ImageSpanAlignment
```
功能： 表示图片垂直对齐方式。
Type: ImageSpanAlignment; Readable: true; Writable: true


### RichEditorInsertValue

class RichEditorInsertValue
```cangjie
public class RichEditorInsertValue { public var insertOffset: Int32 public var insertValue: String public var previewText: String = "" public init( insertOffset: Int32, insertValue: String ) public init( insertOffset: Int32, insertValue: String, previewText: String ) }
```
功能： 插入文本信息。

#### var insertOffset

```cangjie
public var insertOffset: Int32
```
功能： 表示插入的文本偏移位置。
Type: Int32; Readable: true; Writable: true

#### var insertValue

```cangjie
public var insertValue: String
```
功能： 表示插入的文本内容。
Type: String; Readable: true; Writable: true

#### var previewText

```cangjie
public var previewText: String
```
功能： 表示插入的预上屏文本内容。
Type: String; Readable: true; Writable: true


### RichEditorLayoutStyleResult

class RichEditorLayoutStyleResult
```cangjie
public class RichEditorLayoutStyleResult { public var borderRadius: String = "" public var margin: String = "" }
```
功能： 图片布局风格。

#### var borderRadius

```cangjie
public var borderRadius: String = ""
```
功能： 圆角类型，用于描述组件边框圆角半径。
Type: String; Readable: true; Writable: true

#### var margin

```cangjie
public var margin: String = ""
```
功能： 外边距类型，用于描述组件不同方向的外边距。
Type: String; Readable: true; Writable: true


### RichEditorSelection

class RichEditorSelection
```cangjie
public class RichEditorSelection { public RichEditorSelection( public var selection: (Int32, Int32), public var spans: ArrayList<RichEditorSpanResult> ) }
```
功能： 选中内容信息。

#### var selection

```cangjie
public var selection:(Int32, Int32)
```
功能： 表示选中范围。
Type: (Int32, Int32); Readable: true; Writable: true

#### var spans

```cangjie
public var spans: ArrayList<RichEditorSpanResult>
```
功能： 表示Span信息。
Type: ArrayList< RichEditorSpanResult >; Readable: true; Writable: true


### RichEditorSpanPosition

class RichEditorSpanPosition
```cangjie
public class RichEditorSpanPosition { public RichEditorSpanPosition( public var spanIndex: Int32, public var spanRange: (Int32, Int32) ) }
```
功能： Span位置信息。

#### var spanIndex

```cangjie
public var spanIndex: Int32
```
功能： 表示Span索引值。
Type: Int32; Readable: true; Writable: true

#### var spanRange

```cangjie
public var spanRange:(Int32, Int32)
```
功能： 表示Span内容在RichEditor内的起始和结束位置。
Type: (Int32, Int32); Readable: true; Writable: true


### RichEditorSpanResult

class RichEditorSpanResult
```cangjie
public class RichEditorSpanResult { public var spanType: SpanType public var textResult: Option<RichEditorTextSpanResult> public var imageResult: Option<RichEditorImageSpanResult> }
```
功能： Span信息类型。

#### var imageResult

```cangjie
public var imageResult: Option<RichEditorImageSpanResult>
```
功能： 表示image类型的结果。
Type: Option< RichEditorImageSpanResult >; Readable: true; Writable: true

#### var spanType

```cangjie
public var spanType: SpanType
```
功能： 表示Span的类型。
Type: SpanType; Readable: true; Writable: true

#### var textResult

```cangjie
public var textResult: Option<RichEditorTextSpanResult>
```
功能： 表示text类型的结果。
Type: Option< RichEditorTextSpanResult >; Readable: true; Writable: true


### RichEditorParagraphStyleResult

class RichEditorParagraphStyleResult
```cangjie
public class RichEditorParagraphStyleResult { public var textAlign: TextAlign = TextAlign.Start public var leadingMargin: (String, String) = ("", "") public var wordBreak: WordBreak = WordBreak.BreakWord public var lineBreakStrategy: LineBreakStrategy = LineBreakStrategy.GREEDY }
```
功能： 段落样式。

#### var textAlign

```cangjie
public var textAlign: TextAlign = TextAlign.Start
```
功能： 表示文本段落在水平方向的对齐方式。
Type: TextAlign; Readable: true; Writable: true

#### var leadingMargin

```cangjie
public var leadingMargin: (String, String) = ("", "")
```
功能： 表示文本段落缩进。
Type: (String， String); Readable: true; Writable: true

#### var wordBreak

```cangjie
public var wordBreak: WordBreak = WordBreak.BreakWord
```
功能： 表示断行规则。
Type: WordBreak; Readable: true; Writable: true

#### var lineBreakStrategy

```cangjie
public var lineBreakStrategy: LineBreakStrategy = LineBreakStrategy.GREEDY
```
功能： 表示折行规则。
Type: LineBreakStrategy; Readable: true; Writable: true


### RichEditorTextSpanResult

class RichEditorTextSpanResult
```cangjie
public class RichEditorTextSpanResult { public var spanPosition: RichEditorSpanPosition public var value: String public var textStyle: RichEditorTextStyleResult public var offsetInSpan: (Int32, Int32) public var symbolSpanStyle: RichEditorSymbolSpanStyleResult = RichEditorSymbolSpanStyleResult() public var paragraphStyle: RichEditorParagraphStyleResult = RichEditorParagraphStyleResult() public var previewText: String = "" public init( spanPosition: RichEditorSpanPosition, value: String, textStyle: RichEditorTextStyleResult, offsetInSpan: (Int32, Int32) ) public init( spanPosition: RichEditorSpanPosition, value: String, textStyle: RichEditorTextStyleResult, offsetInSpan: (Int32, Int32), symbolSpanStyle: RichEditorSymbolSpanStyleResult, paragraphStyle: RichEditorParagraphStyleResult, previewText: String ) }
```
功能： 后端返回的文本样式信息类型。

#### var offsetInSpan

```cangjie
public var offsetInSpan:(Int32, Int32)
```
功能： 表示文本Span内容里有效内容的起始和结束位置。
Type: (Int32, Int32); Readable: true; Writable: true

#### var paragraphStyle

```cangjie
public var paragraphStyle: RichEditorParagraphStyleResult = RichEditorParagraphStyleResult()
```
功能： 表示段落样式。
Type: RichEditorParagraphStyleResult; Readable: true; Writable: true

#### var previewText

```cangjie
public var previewText: String = ""
```
功能： 表示插入的预上屏文本内容。
Type: String; Readable: true; Writable: true

#### var spanPosition

```cangjie
public var spanPosition: RichEditorSpanPosition
```
功能： 表示Span位置。
Type: RichEditorSpanPosition; Readable: true; Writable: true

#### var symbolSpanStyle

```cangjie
public var symbolSpanStyle: RichEditorSymbolSpanStyleResult = RichEditorSymbolSpanStyleResult()
```
功能： 设置后端返回的SymbolSpan样式信息。
Type: RichEditorSymbolSpanStyleResult; Readable: true; Writable: true

#### var textStyle

```cangjie
public var textStyle: RichEditorTextStyleResult
```
功能： 表示文本Span样式信息。
Type: RichEditorTextStyleResult; Readable: true; Writable: true

#### var value

```cangjie
public var value: String
```
功能： 表示文本Span内容。
Type: String; Readable: true; Writable: true


### RichEditorTextStyleResult

class RichEditorTextStyleResult
```cangjie
public class RichEditorTextStyleResult { public var fontColor: String public var fontSize: Float64 public var fontStyle: FontStyle public var fontWeight: Int32 public var fontFamily: String public var decoration: TextDecorationResult public var textShadow: Array<ShadowOptionsResult> = [] public var lineHeight: Float64 = 0.0 public var letterSpacing: Float64 = 0.0 public var fontFeature: String = "" public init( fontColor: String, fontSize: Float64, fontStyle: FontStyle, fontWeight: Int32, fontFamily: String, decoration: TextDecorationResult ) public init( fontColor: String, fontSize: Float64, fontStyle: FontStyle, fontWeight: Int32, fontFamily: String, decoration: TextDecorationResult, textShadow: Array<ShadowOptionsResult>, lineHeight: Float64, letterSpacing: Float64, fontFeature: String ) }
```
功能： 后端返回的文本样式信息。

#### var decoration

```cangjie
public var decoration: TextDecorationResult
```
功能： 表示文本装饰线样式及其颜色。
Type: TextDecorationResult; Readable: true; Writable: true

#### var fontColor

```cangjie
public var fontColor: String
```
功能： 表示文本颜色。
Type: String; Readable: true; Writable: true

#### var fontFamily

```cangjie
public var fontFamily: String
```
功能： 表示字体列表。
Type: String; Readable: true; Writable: true

#### var fontSize

```cangjie
public var fontSize: Float64
```
功能： 表示字体大小。
Type: Float64; Readable: true; Writable: true

#### var fontStyle

```cangjie
public var fontStyle: FontStyle
```
功能： 表示字体样式。
Type: FontStyle; Readable: true; Writable: true

#### var fontWeight

```cangjie
public var fontWeight: Int32
```
功能： 表示字体粗细。
Type: Int32; Readable: true; Writable: true

#### let fontFeature

```cangjie
public var fontFeature: String = ""
```
功能： 表示文本字符间距。
Type: String; Readable: true; Writable: false

#### let letterSpacing

```cangjie
public var letterSpacing: Float64 = 0.0
```
功能： 表示文本字符间距。
Type: Float64; Readable: true; Writable: false

#### let lineHeight

```cangjie
public var lineHeight: Float64 = 0.0
```
功能： 表示文本行高。
Type: Float64; Readable: true; Writable: false

#### let textShadow

```cangjie
public var textShadow: Array<ShadowOptionsResult> = []
```
功能： 表示文字阴影效果。
Type: Array< ShadowOptionsResult >; Readable: true; Writable: false


### ShadowOptionsResult

class ShadowOptionsResult
```cangjie
public class ShadowOptionsResult { ShadowOptionsResult( public let radius: Float64, public let color: String, public let offsetX: Float64, public let offsetY: Float64 ) }
```
功能： 文字阴影效果。

#### let color

```cangjie
public let color: String
```
功能： 表示阴影的颜色。
Type: String; Readable: true; Writable: false

#### let offsetX

```cangjie
public let offsetX: Float64
```
功能： 表示阴影的X轴偏移量。
Type: Float64; Readable: true; Writable: false

#### let offsetY

```cangjie
public let offsetY: Float64
```
功能： 表示阴影的Y轴偏移量。
Type: Float64; Readable: true; Writable: false

#### let radius

```cangjie
public let radius: Float64
```
功能： 表示阴影模糊半径。
Type: Float64; Readable: true; Writable: false


### TextDecorationResult

class TextDecorationResult
```cangjie
public class TextDecorationResult { public TextDecorationResult( public var `type`: TextDecorationType, public var color: String ) }
```
功能： 后端返回的文本装饰信息。

#### var `type`

```cangjie
public var `type`: TextDecorationType
```
功能： 表示装饰线类型。
Type: TextDecorationType; Readable: true; Writable: true

#### var color

```cangjie
public var color: String
```
功能： 表示装饰颜色。
Type: String; Readable: true; Writable: true


### TextRange

class TextRange
```cangjie
public class TextRange { public TextRange( public var start: Int32, public var end: Int32 ) }
```
功能： 文本范围。

#### var start

```cangjie
public var start: Int32
```
功能： 表示起始索引。
Type: Int32; Readable: true; Writable: true

#### var end

```cangjie
public var end: Int32
```
功能： 表示结束索引。
Type: Int32; Readable: true; Writable: true


### RichEditorSymbolSpanStyleResult

class RichEditorSymbolSpanStyleResult
```cangjie
public class RichEditorSymbolSpanStyleResult { public var fontColor: String = "" public var fontSize: Float64 = 0.0 public var fontWeight: FontWeight = FontWeight.Normal public var renderingStrategy: SymbolRenderingStrategy = SymbolRenderingStrategy.SINGLE public var effectStrategy: SymbolEffectStrategy = SymbolEffectStrategy.NONE }
```
功能： 后端返回的SymbolSpan样式信息。

#### var fontColor

```cangjie
public var fontColor: String = ""
```
功能： 表示SymbolSpan组件颜色。
Type: String; Readable: true; Writable: true

#### var fontSize

```cangjie
public var fontSize: Float64 = 0.0
```
功能： 表示SymbolSpan组件大小。单位：fp。
Type: Float64; Readable: true; Writable: true

#### var fontWeight

```cangjie
public var fontWeight: FontWeight = FontWeight.Normal
```
功能： 表示SymbolSpan组件粗细。
Type: FontWeight; Readable: true; Writable: true

#### var renderingStrategy

```cangjie
public var renderingStrategy: SymbolRenderingStrategy = SymbolRenderingStrategy.SINGLE
```
功能： 表示SymbolSpan组件渲染策略。
Type: SymbolRenderingStrategy; Readable: true; Writable: true

#### var effectStrategy

```cangjie
public var effectStrategy: SymbolEffectStrategy = SymbolEffectStrategy.NONE
```
功能： 表示SymbolSpan组件动效策略。
Type: SymbolEffectStrategy; Readable: true; Writable: true



## Enums

### RichEditorSpanType

enum RichEditorSpanType
```cangjie
public enum RichEditorSpanType { | TEXT | IMAGE | MIXED }
```
功能： 表示Span类型信息。

#### TEXT

```cangjie
TEXT
```
功能： 表示Span为文字类型。

#### IMAGE

```cangjie
IMAGE
```
功能： 表示Span为图像类型。

#### MIXED

```cangjie
MIXED
```
功能： 表示Span为图文混合类型。


### RichEditorDeleteDirection

enum RichEditorDeleteDirection
```cangjie
public enum RichEditorDeleteDirection { | BACKWARD | FORWARD }
```
功能： 表示删除操作的方向。

#### BACKWARD

```cangjie
BACKWARD
```
功能： 表示向后删除。

#### FORWARD

```cangjie
FORWARD
```
功能： 表示向前删除。


### SpanType

enum SpanType
```cangjie
public enum SpanType { | TEXT | IMAGE }
```
功能： 表示Span类型信息的类型。

#### TEXT

```cangjie
TEXT
```
功能： 表示Span为文字类型。

#### IMAGE

```cangjie
IMAGE
```
功能： 表示Span为图像类型。



## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*
import kit.LocalizationKit.*

@Entry
@Component
class EntryView {
 let controller = RichEditorController()

 @State var position: Int64 = 0

 @Builder
 func builder(){
 Column {
 ForEach(["1", "2", "3", "4", "5", "6", "7", "8", "9", "*", "0", "#"], itemGeneratorFunc: {item: String, idx: Int64 =>
 Button(item)
 .width(40.vp)
 .height(40.vp)
 .fontColor(0x66000000)
 .fontSize(16.fp)
 })
 }
 .borderRadius(24.vp)
 .padding(top: 4.vp, bottom: 4.vp, left: 16.vp, right: 16.vp)
 .backgroundColor(Color.GREEN)
 .margin(right: 24.vp, bottom: 4.vp, top: 0.vp)
 .width(130.vp)
 }

 func build() {
 Column(30) {
 Row {
 Button("getCaretOffset")
 .onClick {
 evt =>
 AppLog.info(controller.getCaretOffset().toString())
 this.position = controller.getCaretOffset()
 }.width(400.px).height(150.px)

 Text("position: ${this.position.toString()}")
 }

 Row {
 Button("setCaretOffset 0")
 .onClick {
 evt =>
 controller.setCaretOffset(0)
 }.width(400.px).height(150.px)

 Text("position: ${this.position.toString()}")
 }

 Row {
 Button("addText Hello")
 .onClick {
 evt =>
 controller.addTextSpan(
 value: "Hello",
 options: RichEditorTextSpanOptions(
 style: RichEditorTextStyle(
 fontColor: Color(0XFF1298),
 fontSize: 20.fp,
 fontStyle: FontStyle.Italic,
 decoration: TextDecoration(
 `type`: TextDecorationType.Overline,
 color: Color(0X12FF98)
 ),
 )
 )
 )
 }.width(400.px).height(150.px)

 Button("addImage")
 .onClick {
 evt =>
 controller.addImageSpan(
 value: @r(app.media.startIcon),
 options: RichEditorImageSpanOptions(
 imageStyle: RichEditorImageSpanStyle(
 size: (24.vp, 24.vp)
 )
 )
 )
 }.width(400.px).height(150.px)
 }

 Row {
 Button("getSpans")
 .onClick {
 evt =>
 let array = controller.getSpans(start: 0, end: 10)
 for(i in 0..array.size) {
 match(array[i].spanType) {
 case TEXT =>
 let textResult = array[i].textResult.getOrThrow()
 AppLog.info("RichEditor textResult. value: ${textResult.value}")
 case IMAGE =>
 let imageResult = array[i].imageResult.getOrThrow()
 AppLog.info("RichEditor ImageResult. size: ${imageResult.size[0].toString()} - ${imageResult.size[1].toString()}")
 case _ => return
 }
 }
 }.width(400.px).height(150.px)
 }

 Row {
 Button("updateParagraphStyle")
 .onClick {
 evt =>
 let array = controller.updateParagraphStyle(
 start: 0,
 end: 100,
 style: RichEditorParagraphStyle(
 textAlign: TextAlign.Center,
 leadingMargin: 24.px
 )
 )
 }.width(400.px).height(150.px)
 }

 RichEditor(controller)
 .customKeyboard(builder: bind(builder, this))
 .bindSelectionMenu(
 spantype: RichEditorSpanType.TEXT,
 content: bind(builder, this),
 responseType: ResponseType.LongPress,
 options: SelectionMenuOptions(onAppear: { => AppLog.info("SelectionMenuOptions onAppear")}, onDisappear: { => AppLog.info("SelectionMenuOptions onDisappear")})
 )
 .copyOptions(CopyOptions.None)
 .onReady({ => AppLog.info("RichEditor onReady!!")})
 .onDeleteComplete({ => AppLog.info("RichEditor onDeleteComplete!!")})
 .onSelect({ value =>
 AppLog.info("RichEditor onSelect. ${value.selection[0]} ~ ${value.selection[1]}")
 })
 }
 }
}
```

