# SymbolSpan

作为 Text 组件的子组件，用于显示图标小符号的组件。

## Component Constructors

### public init(value: AppResource)

```cangjie
public init(value: AppResource)
```
功能： 创建SymbolSpan组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | SymbolSpan组件的资源名，如@r(sys.symbol.ohos_wifi)。 | value | - |


## Functions

### public func effectStrategy(value: SymbolEffectStrategy): This

```cangjie
public func effectStrategy(value: SymbolEffectStrategy): This
```
功能： 设置SymbolSpan动效策略。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| SymbolEffectStrategy | 是 | SymbolSpan动效策略。 初始值：SymbolEffectStrategy.NONE。 | value | - |

### public func fontColor(value: Array<Color>): This

```cangjie
public func fontColor(value: Array<Color>): This
```
功能： 设置SymbolSpan组件颜色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array< Color > | 是 | SymbolSpan组件颜色。 初始值：不同渲染策略下默认值不同。 | value | - |

### public func fontColor(value: Array<UInt32>): This

```cangjie
public func fontColor(value: Array<UInt32>): This
```
功能： 设置SymbolSpan组件颜色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array<UInt32> | 是 | SymbolSpan组件颜色。 初始值：不同渲染策略下默认值不同。 | value | - |

### public func fontColor(value: Array<AppResource>): This

```cangjie
public func fontColor(value: Array<AppResource>): This
```
功能： 设置SymbolSpan组件颜色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array< AppResource > | 是 | SymbolSpan组件颜色。 初始值：不同渲染策略下默认值不同。 | value | - |

### public func fontSize(value: Length): This

```cangjie
public func fontSize(value: Length): This
```
功能： 设置SymbolSpan组件大小。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | SymbolSpan组件大小。 初始值：16.fp。单位：fp。 | value | - |

### public func fontWeight(value: Int64): This

```cangjie
public func fontWeight(value: Int64): This
```
功能： 设置SymbolSpan组件粗细。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int64 | 是 | SymbolSpan组件粗细。Int64类型取值[100, 900]，取值间隔为100，取值越大，字体越粗。 | value | - |

### public func fontWeight(value: String): This

```cangjie
public func fontWeight(value: String): This
```
功能： 设置SymbolSpan组件粗细。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | SymbolSpan组件粗细。 string类型仅支持Int64类型取值的字符串形式，例如“400”，以及“bold”、“bolder”、“lighter”、“regular” 、“medium”分别对应FontWeight中相应的枚举值。 | value | - |

### public func fontWeight(value: FontWeight): This

```cangjie
public func fontWeight(value: FontWeight): This
```
功能： 设置SymbolSpan组件大小。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| FontWeight | 是 | SymbolSpan组件粗细。 FontWeight类型支持“bold”、“bolder”、“lighter”、“regular” 、“medium”等即对应FontWeight中相应的枚举值。 初始值：FontWeight.Normal。 | value | - |

### public func renderingStrategy(value: SymbolRenderingStrategy): This

```cangjie
public func renderingStrategy(value: SymbolRenderingStrategy): This
```
功能： 设置SymbolSpan渲染策略。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| SymbolRenderingStrategy | 是 | SymbolSpan渲染策略。 初始值：SymbolRenderingStrategy.SINGLE。 | value | - |


## Examples

```cangjie
package ohos_app_cangjie_entry

import kit.UIKit.*
import ohos.state_macro_manage.*
import kit.LocalizationKit.{AppResource, __GenerateResource__}

@Entry
@Component
class EntryView {
 func build() {
 Column() {
 Row() {
 //展示小图标不同样式
 Column() {
 Text("Light")
 Text() {
 SymbolSpan(@r(sys.symbol.ohos_trash))
 .fontWeight(FontWeight.Lighter)
 .fontSize(96)
 }
 }
 Column() {
 Text("Normal")
 Text() {
 SymbolSpan(@r(sys.symbol.ohos_trash))
 .fontWeight(FontWeight.Normal)
 .fontSize(96)
 }
 }
 Column() {
 Text("Bold")
 Text() {
 SymbolSpan(@r(sys.symbol.ohos_trash))
 .fontWeight(FontWeight.Bold)
 .fontSize(96)
 }
 }
 }
 Row() {
 //展示小图标不同渲染策略的效果
 Column() {
 Text("单色")
 Text() {
 SymbolSpan(@r(sys.symbol.ohos_folder_badge_plus))
 .fontSize(96)
 .renderingStrategy(SymbolRenderingStrategy.SINGLE)
 .fontColor([Color.BLACK, Color.GREEN, Color.WHITE])
 }
 }
 Column() {
 Text("多色")
 Text() {
 SymbolSpan(@r(sys.symbol.ohos_folder_badge_plus))
 .fontSize(96)
 .renderingStrategy(SymbolRenderingStrategy.MULTIPLE_COLOR)
 .fontColor([Color.BLACK, Color.GREEN, Color.WHITE])
 }
 }
 Column() {
 Text("分层")
 Text() {
 SymbolSpan(@r(sys.symbol.ohos_folder_badge_plus))
 .fontSize(96)
 .renderingStrategy(SymbolRenderingStrategy.MULTIPLE_OPACITY)
 .fontColor([Color.BLACK, Color.GREEN, Color.WHITE])
 }
 }
 }
 Row() {
 //展示小图标不同动效策略的效果
 Column() {
 Text("无动效")
 Text() {
 SymbolSpan(@r(sys.symbol.ohos_wifi))
 .fontSize(96)
 .effectStrategy(SymbolEffectStrategy.NONE)
 }
 }
 Column() {
 Text("整体缩放动效")
 Text() {
 SymbolSpan(@r(sys.symbol.ohos_wifi))
 .fontSize(96)
 .effectStrategy(SymbolEffectStrategy.SCALE)
 }
 }
 Column() {
 Text("层级动效")
 Text() {
 SymbolSpan(@r(sys.symbol.ohos_wifi))
 .fontSize(96)
 .effectStrategy(SymbolEffectStrategy.HIERARCHICAL)
 }
 }
 }
 }
 }
}
```

