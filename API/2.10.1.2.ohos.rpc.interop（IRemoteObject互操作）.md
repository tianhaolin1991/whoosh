# ohos.rpc.interop（IRemoteObject互操作）

本模块提供 RemoteObject 及 RemoteProxy 进行互操作的能力。

```cangjie
import kit.IPCKit.*
```
## Functions

### public func fromJSValue(context: JSContext, input: JSValue): IRemoteObject

```cangjie
public func fromJSValue(context: JSContext, input: JSValue): IRemoteObject
```
功能： 从 JSValue 转换为IRemoteObject类型。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| JSContext | 是 | ArkTS互操作上下文。 | context | - |
| JSValue | 是 | ArkTS统一类型。 | input | - |

**Returns:**

| type | description |
| --- | --- |
| IRemoteObject | 返回IRemoteObject类型实例。 |


## Interfaces

### SystemObjectInteropTypeToJS

interface SystemObjectInteropTypeToJS
```cangjie
public interface SystemObjectInteropTypeToJS { func toJSValue(context: JSContext): JSValue }
```
功能： 提供转换为ArkTS统一类型的接口。

#### func toJSValue(context: JSContext): JSValue

```cangjie
func toJSValue(context: JSContext): JSValue
```
功能： 转换为ArkTS统一类型。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| JSContext | 是 | ArkTS互操作上下文。 | context | - |

**Returns:**

| type | description |
| --- | --- |
| JSValue | 返回ArkTS统一类型。 |



## Classes

### RemoteObject

class RemoteObject
```cangjie
extend RemoteObject <: SystemObjectInteropTypeToJS {}
```
功能： 实现远程对象。服务提供者必须继承此类。


### RemoteProxy

class RemoteProxy
```cangjie
extend RemoteProxy <: SystemObjectInteropTypeToJS {}
```
功能： 实现IRemoteObject代理对象。



