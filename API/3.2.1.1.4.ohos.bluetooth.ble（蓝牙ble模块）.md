# ohos.bluetooth.ble（蓝牙ble模块）

ble模块提供了对蓝牙操作和管理的方法。

```cangjie
import kit.ConnectivityKit.*
```
## Functions

### public func createGattClientDevice(deviceId: String): GattClientDevice

```cangjie
public func createGattClientDevice(deviceId: String): GattClientDevice
```
功能： 创建一个可使用的GattClientDevice实例。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| String | 对端设备地址，例如："XX:XX:XX:XX:XX:XX"。 | deviceId | 是 |

**Returns:**

| type | description |
| --- | --- |
| GattClientDevice | client端类，使用client端方法之前需要创建该类的实例进行操作。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 401 | Invalid parameter. Possible causes: 1. Mandatory parameters are left unspecified. 2. Incorrect parameter types. 3. Parameter verification failed. |
| 801 | Capability not supported. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

try {
 let device: GattClientDevice = createGattClientDevice("XX:XX:XX:XX:XX:XX")
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```

### public func createGattServer(): GattServer

```cangjie
public func createGattServer(): GattServer
```
功能： 创建一个可使用的GattServer实例。

**Returns:**

| type | description |
| --- | --- |
| GattServer | 返回一个Gatt服务的实例。 |

**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

let gattServer: GattServer = createGattServer()
```

### public func disableAdvertising(advertisingDisableParams: AdvertisingDisableParams): Unit

```cangjie
public func disableAdvertising(advertisingDisableParams: AdvertisingDisableParams): Unit
```
功能： 临时停止BLE广播。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| AdvertisingDisableParams | 临时停止BLE广播的相关参数。 | advertisingDisableParams | 是 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 蓝牙服务子系统错误码 。

| code | description |
| --- | --- |
| 201 | Permission denied. |
| 401 | Invalid parameter. Possible causes: 1. Mandatory parameters are left unspecified. 2. Incorrect parameter types. |
| 801 | Capability not supported. |
| 2900001 | Service stopped. |
| 2900003 | Bluetooth disabled. |
| 2900099 | Operation failed. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

let advertisingSettings = AdvertiseSetting(32, 0, true)
let manufactureDataUnit = ManufactureData(
 4567u16,
 [1, 2, 3, 4]
)
let serviceDataUnit = ServiceData(
 "00001888-0000-1000-8000-00805f9b34fb",
 [5, 6, 7, 8]
)
let advertisingData = AdvertiseData(
 ["00001888-0000-1000-8000-00805f9b34fb"],
 [manufactureDataUnit],
 [serviceDataUnit],
 includeDeviceName: true
)
let advertisingResponse = AdvertiseData(
 ["00001888-0000-1000-8000-00805f9b34fb"],
 [manufactureDataUnit],
 [serviceDataUnit]
)
let advertisingParams = AdvertisingParams(
 advertisingSettings,
 advertisingData,
 advertisingResponse,
 duration: 300
)
var advHandle: UInt32 = 0xFF
try {
 advHandle = startAdvertising(advertisingParams)

 let advertisingDisableParams = AdvertisingDisableParams(advHandle)
 disableAdvertising(advertisingDisableParams)
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```

### public func enableAdvertising(advertisingEnableParams: AdvertisingEnableParams): Unit

```cangjie
public func enableAdvertising(advertisingEnableParams: AdvertisingEnableParams): Unit
```
功能： 临时启动BLE广播。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| AdvertisingEnableParams | 临时启动BLE广播的相关参数。 | advertisingEnableParams | 是 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 蓝牙服务子系统错误码 。

| code | description |
| --- | --- |
| 201 | Permission denied. |
| 401 | Invalid parameter. Possible causes: 1. Mandatory parameters are left unspecified. 2. Incorrect parameter types. |
| 801 | Capability not supported. |
| 2900001 | Service stopped. |
| 2900003 | Bluetooth disabled. |
| 2900099 | Operation failed. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

let advertisingSettings = AdvertiseSetting(32, 0, true)
let manufactureDataUnit = ManufactureData(
 4567u16,
 [1, 2, 3, 4]
)
let serviceDataUnit = ServiceData(
 "00001888-0000-1000-8000-00805f9b34fb",
 [5, 6, 7, 8]
)
let advertisingData = AdvertiseData(
 ["00001888-0000-1000-8000-00805f9b34fb"],
 [manufactureDataUnit],
 [serviceDataUnit],
 includeDeviceName: true
)
let advertisingResponse = AdvertiseData(
 ["00001888-0000-1000-8000-00805f9b34fb"],
 [manufactureDataUnit],
 [serviceDataUnit]
)
let advertisingParams = AdvertisingParams(
 advertisingSettings,
 advertisingData,
 advertisingResponse,
 duration: 300
)

var advHandle: UInt32 = 0xFF

try {
 advHandle = startAdvertising(advertisingParams)

 let advertisingEnableParams = AdvertisingEnableParams(advHandle, duration: 1000)
 enableAdvertising(advertisingEnableParams)
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```

### public func getConnectedBLEDevices(): Array<String>

```cangjie
public func getConnectedBLEDevices(): Array<String>
```
功能： 获取和当前设备连接的BLE设备。

**Returns:**

| type | description |
| --- | --- |
| Array<String> | 返回当前设备作为Server端时连接BLE设备地址集合。基于信息安全考虑，此处获取的设备地址为随机MAC地址。配对成功后，该地址不会变更；已配对设备取消配对后重新扫描或蓝牙服务下电时，该随机地址会变更。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 蓝牙服务子系统错误码 。

| code | description |
| --- | --- |
| 201 | Permission denied. |
| 801 | Capability not supported. |
| 2900001 | Service stopped. |
| 2900003 | Bluetooth disabled. |
| 2900099 | Operation failed. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

try {
 let result: Array<String> = getConnectedBLEDevices()
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```

### public func off(`type`: BluetoothBleCallbackType, callback!: ?CallbackObject = None): Unit

```cangjie
public func off(`type`: BluetoothBleCallbackType, callback!: ?CallbackObject = None): Unit
```
功能： 取消订阅BLE设备事件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| BluetoothBleCallbackType | 是 | 回调事件。 | `type` | - |
| ? CallbackObject | 否 | 命名参数。 表示取消订阅BLE事件。不填该参数则取消订阅该type对应的所有回调。 | callback | None |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 蓝牙服务子系统错误码 。

| code | description |
| --- | --- |
| 201 | Permission denied. |
| 401 | Invalid parameter. Possible causes: 1. Mandatory parameters are left unspecified. 2. Incorrect parameter types. 3. Parameter verification failed. |
| 801 | Capability not supported. |
| 2900099 | Operation failed. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

// 此处代码可添加在依赖项定义中
class BLEDeviceFindCallback <: Callback1Argument<Array<ScanResult>> {
 public func invoke(devices: Array<ScanResult>): Unit {
 for (device in devices) {
 Hilog.info(0, "Bluetooth", "device has find, deviceID is ${device.deviceId}, name is ${device.deviceName}")
 }
 }
}

let bleDeviceFindCallback = BLEDeviceFindCallback()
try {
 on(BLE_DEVICE_FIND, bleDeviceFindCallback)
 off(BLE_DEVICE_FIND, callback: bleDeviceFindCallback)
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```

### public func on(`type`: BluetoothBleCallbackType, callback: Callback1Argument<AdvertisingStateChangeInfo>): Unit

```cangjie
public func on(`type`: BluetoothBleCallbackType, callback: Callback1Argument<AdvertisingStateChangeInfo>): Unit
```
功能： 订阅BLE广播状态。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| BluetoothBleCallbackType | 填写 ADVERTISING_STATE_CHANGE，表示广播状态事件。 | `type` | 是 |
| Callback1Argument < AdvertisingStateChangeInfo > | 表示回调函数的入参，广播状态。回调函数由用户创建通过该接口注册。 | callback | 是 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 蓝牙服务子系统错误码 。

| code | description |
| --- | --- |
| 201 | Permission denied. |
| 401 | Invalid parameter. Possible causes: 1. Mandatory parameters are left unspecified. 2. Incorrect parameter types. 3. Parameter verification failed. |
| 801 | Capability not supported. |
| 2900099 | Operation failed. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

// 此处代码可添加在依赖项定义中
class AdvertisingStateChange <: Callback1Argument<AdvertisingStateChangeInfo> {
 public func invoke(info: AdvertisingStateChangeInfo): Unit {
 Hilog.info(0, "Bluetooth", "the advertising state is ${info.state}")
 }
}

let advertisingStateChange = AdvertisingStateChange()
try {
 on(ADVERTISING_STATE_CHANGE, advertisingStateChange)
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```

### public func on(`type`: BluetoothBleCallbackType, callback: Callback1Argument<Array<ScanResult>>): Unit

```cangjie
public func on(`type`: BluetoothBleCallbackType, callback: Callback1Argument<Array<ScanResult>>): Unit
```
功能： 订阅BLE设备发现上报事件。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| BluetoothBleCallbackType | 填写BLE_DEVICE_FIND，表示BLE设备发现事件。 | `type` | 是 |
| Callback1Argument <Array< ScanResult >> | 表示回调函数的入参，发现的设备集合。回调函数由用户创建通过该接口注册。 | callback | 是 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 蓝牙服务子系统错误码 。

| code | description |
| --- | --- |
| 201 | Permission denied. |
| 401 | Invalid parameter. Possible causes: 1. Mandatory parameters are left unspecified. 2. Incorrect parameter types. 3. Parameter verification failed. |
| 801 | Capability not supported. |
| 2900099 | Operation failed. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

// 此处代码可添加在依赖项定义中
class BLEDeviceFindCallback <: Callback1Argument<Array<ScanResult>> {
 public func invoke(devices: Array<ScanResult>): Unit {
 for (device in devices) {
 Hilog.info(0, "Bluetooth", "device has find, deviceID is ${device.deviceId}, name is ${device.deviceName}")
 }
 }
}

let bleDeviceFindCallback = BLEDeviceFindCallback()
try {
 on(BLE_DEVICE_FIND, bleDeviceFindCallback)
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```

### public func startAdvertising(setting: AdvertiseSetting, advData: AdvertiseData, advResponse!: ?AdvertiseData = None): Unit

```cangjie
public func startAdvertising(setting: AdvertiseSetting, advData: AdvertiseData, advResponse!: ?AdvertiseData = None): Unit
```
功能： 开始发送BLE广播。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AdvertiseSetting | 是 | BLE广播的相关参数。 | setting | - |
| AdvertiseData | 是 | BLE广播包内容。 | advData | - |
| ? AdvertiseData | 否 | 命名参数。 BLE回复扫描请求回复响应。 | advResponse | None |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 蓝牙服务子系统错误码 。

| code | description |
| --- | --- |
| 201 | Permission denied. |
| 401 | Invalid parameter. Possible causes: 1. Mandatory parameters are left unspecified. 2. Incorrect parameter types. 3. Parameter verification failed. |
| 801 | Capability not supported. |
| 2900001 | Service stopped. |
| 2900003 | Bluetooth disabled. |
| 2900099 | Operation failed. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

let advertisingSettings = AdvertiseSetting(32, 0, true)
let manufactureDataUnit = ManufactureData(
 4567u16,
 [1, 2, 3, 4]
)
let serviceDataUnit = ServiceData(
 "00001888-0000-1000-8000-00805f9b34fb",
 [5, 6, 7, 8]
)
let advertisingData = AdvertiseData(
 ["00001888-0000-1000-8000-00805f9b34fb"],
 [manufactureDataUnit],
 [serviceDataUnit],
 includeDeviceName: true
)
let advertisingResponse = AdvertiseData(
 ["00001888-0000-1000-8000-00805f9b34fb"],
 [manufactureDataUnit],
 [serviceDataUnit]
)
try {
 startAdvertising(advertisingSettings, advertisingData, advResponse: advertisingResponse)
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```

### public func startAdvertising(advertisingParams: AdvertisingParams): UInt32

```cangjie
public func startAdvertising(advertisingParams: AdvertisingParams): UInt32
```
功能： 开始发送BLE广播。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| AdvertisingParams | 启动BLE广播的相关参数。 | advertisingParams | 是 |

**Returns:**

| type | description |
| --- | --- |
| UInt32 | 广播ID标识。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 蓝牙服务子系统错误码 。

| code | description |
| --- | --- |
| 201 | Permission denied. |
| 401 | Invalid parameter. Possible causes: 1. Mandatory parameters are left unspecified. 2. Incorrect parameter types. 3. Parameter verification failed. |
| 801 | Capability not supported. |
| 2900001 | Service stopped. |
| 2900003 | Bluetooth disabled. |
| 2900099 | Operation failed. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

let advertisingSettings = AdvertiseSetting(32, 0, true)
let manufactureDataUnit = ManufactureData(
 4567u16,
 [1, 2, 3, 4]
)
let serviceDataUnit = ServiceData(
 "00001888-0000-1000-8000-00805f9b34fb",
 [5, 6, 7, 8]
)
let advertisingData = AdvertiseData(
 ["00001888-0000-1000-8000-00805f9b34fb"],
 [manufactureDataUnit],
 [serviceDataUnit],
 includeDeviceName: true
)
let advertisingResponse = AdvertiseData(
 ["00001888-0000-1000-8000-00805f9b34fb"],
 [manufactureDataUnit],
 [serviceDataUnit]
)
let advertisingParams = AdvertisingParams(
 advertisingSettings,
 advertisingData,
 advertisingResponse,
 duration: 300
)
try {
 let advHandle = startAdvertising(advertisingParams)
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```

### public func startBLEScan(filters: Array<ScanFilter>, options!: ?ScanOptions = None): Unit

```cangjie
public func startBLEScan(filters: Array<ScanFilter>, options!: ?ScanOptions = None): Unit
```
功能： 发起BLE扫描流程。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array< ScanFilter > | 是 | 表示扫描结果过滤策略集合，符合过滤条件的设备发现会保留。 | filters | - |
| ? ScanOptions | 否 | 命名参数。 表示扫描的参数配置，可选参数。 | options | None |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 蓝牙服务子系统错误码 。

| code | description |
| --- | --- |
| 201 | Permission denied. |
| 401 | Invalid parameter. Possible causes: 1. Mandatory parameters are left unspecified. 2. Incorrect parameter types. 3. Parameter verification failed. |
| 801 | Capability not supported. |
| 2900001 | Service stopped. |
| 2900003 | Bluetooth disabled. |
| 2900099 | Operation failed. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

// 此处代码可添加在依赖项定义中
class BLEDeviceFindCallback <: Callback1Argument<Array<ScanResult>> {
 public func invoke(devices: Array<ScanResult>): Unit {
 for (device in devices) {
 Hilog.info(0, "Bluetooth", "device has find, deviceID is ${device.deviceId}, name is ${device.deviceName}")
 }
 }
}

let bleDeviceFindCallback = BLEDeviceFindCallback()
try {
 on(BLE_DEVICE_FIND, bleDeviceFindCallback)
 var scanFilter = ScanFilter()
 scanFilter.serviceUuid = "00001888-0000-1000-8000-00805f9b34fb"
 let scanOptions = ScanOptions(0, SCAN_MODE_LOW_POWER, MATCH_MODE_AGGRESSIVE, PHY_LE_1M)
 startBLEScan([scanFilter], options: scanOptions)
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```

### public func stopAdvertising(): Unit

```cangjie
public func stopAdvertising(): Unit
```
功能： 停止发送BLE广播。

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 蓝牙服务子系统错误码 。

| code | description |
| --- | --- |
| 201 | Permission denied. |
| 801 | Capability not supported. |
| 2900001 | Service stopped. |
| 2900003 | Bluetooth disabled. |
| 2900099 | Operation failed. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

try {
 stopAdvertising()
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```

### public func stopAdvertising(advertisingId: UInt32): Unit

```cangjie
public func stopAdvertising(advertisingId: UInt32): Unit
```
功能： 停止发送BLE广播。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| UInt32 | 需要停止的广播ID标识。 | advertisingId | 是 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 蓝牙服务子系统错误码 。

| code | description |
| --- | --- |
| 201 | Permission denied. |
| 401 | Invalid parameter. Possible causes: 1. Mandatory parameters are left unspecified. 2. Incorrect parameter types. 3. Parameter verification failed. |
| 801 | Capability not supported. |
| 2900001 | Service stopped. |
| 2900003 | Bluetooth disabled. |
| 2900099 | Operation failed. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

let advertisingSettings = AdvertiseSetting(32, 0, true)
let manufactureDataUnit = ManufactureData(
 4567u16,
 [1, 2, 3, 4]
)
let serviceDataUnit = ServiceData(
 "00001888-0000-1000-8000-00805f9b34fb",
 [5, 6, 7, 8]
)
let advertisingData = AdvertiseData(
 ["00001888-0000-1000-8000-00805f9b34fb"],
 [manufactureDataUnit],
 [serviceDataUnit],
 includeDeviceName: true
)
let advertisingResponse = AdvertiseData(
 ["00001888-0000-1000-8000-00805f9b34fb"],
 [manufactureDataUnit],
 [serviceDataUnit]
)
let advertisingParams = AdvertisingParams(
 advertisingSettings,
 advertisingData,
 advertisingResponse,
 duration: 300
)
var advHandle: UInt32 = 0xFF
try {
 advHandle = startAdvertising(advertisingParams)
 stopAdvertising(advHandle)
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```

### public func stopBLEScan(): Unit

```cangjie
public func stopBLEScan(): Unit
```
功能： 停止BLE扫描流程。

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 蓝牙服务子系统错误码 。

| code | description |
| --- | --- |
| 201 | Permission denied. |
| 801 | Capability not supported. |
| 2900001 | Service stopped. |
| 2900003 | Bluetooth disabled. |
| 2900099 | Operation failed. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

try {
 stopBLEScan()
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```


## Classes

### AdvertiseData

class AdvertiseData
```cangjie
public class AdvertiseData { public AdvertiseData( public var serviceUuids: Array<String>, public var manufactureData: Array<ManufactureData>, public var serviceData: Array<ServiceData>, public var includeDeviceName!: Bool = false ) }
```
功能： 描述BLE广播数据包的内容，广播包数据长度为31个字节。

#### var includeDeviceName

```cangjie
public var includeDeviceName: Bool = false
```
功能： 表示是否携带设备名，可选参数。true表示携带，false或未设置此参数表示不携带。注意带上设备名时广播包长度不能超出31个字节。
Type: Bool; Readable: true; Writable: true

#### var manufactureData

```cangjie
public var manufactureData: Array<ManufactureData>
```
功能： 表示要广播的广播的制造商信息列表。
Type: Array< ManufactureData >; Readable: true; Writable: true

#### var serviceData

```cangjie
public var serviceData: Array<ServiceData>
```
功能： 表示要广播的服务数据列表。
Type: Array< ServiceData >; Readable: true; Writable: true

#### var serviceUuids

```cangjie
public var serviceUuids: Array<String>
```
功能： 表示要广播的服务列表。
Type: Array<String>; Readable: true; Writable: true


### AdvertiseSetting

class AdvertiseSetting
```cangjie
public class AdvertiseSetting { public var interval: UInt16 = BLE_ADV_DEFAULT_INTERVAL public var txPower: Int8 = BLE_ADV_TX_POWER_MEDIUM_VALUE public var connectable: Bool = true public init(interval: UInt16, txPower: Int8, connectable: Bool) }
```
功能： 描述蓝牙低功耗设备发送广播的参数。

#### var connectable

```cangjie
public var connectable: Bool = true
```
功能： 表示是否是可连接广播，默认值设置为true，表示可连接，false表示不可连接。
Type: Bool; Readable: true; Writable: true

#### var interval

```cangjie
public var interval: UInt16 = BLE_ADV_DEFAULT_INTERVAL
```
功能： 表示广播间隔，最小值设置160个slot表示100ms，最大值设置16384个slot，默认值设置为1600个slot表示1s。
Type: UInt16; Readable: true; Writable: true

#### var txPower

```cangjie
public var txPower: Int8 = BLE_ADV_TX_POWER_MEDIUM_VALUE
```
功能： 表示发送功率，最小值设置-127，最大值设置1，默认值设置-7，单位dbm。推荐值：高档（1），中档（-7），低档（-15）。
Type: Int8; Readable: true; Writable: true


### AdvertisingDisableParams

class AdvertisingDisableParams
```cangjie
public class AdvertisingDisableParams { public AdvertisingDisableParams( public var advertisingId: UInt32) }
```
功能： 描述临时停止广播设置的参数。

#### var advertisingId

```cangjie
public var advertisingId: UInt32
```
功能： 表示广播ID标识。
Type: UInt32; Readable: true; Writable: true


### AdvertisingEnableParams

class AdvertisingEnableParams
```cangjie
public class AdvertisingEnableParams { public AdvertisingEnableParams( public var advertisingId: UInt32, public var duration!: UInt16 = 0 ) }
```
功能： 描述临时启动广播设置的参数。

#### var advertisingId

```cangjie
public var advertisingId: UInt32
```
功能： 表示广播ID标识。
Type: UInt32; Readable: true; Writable: true

#### var duration

```cangjie
public var duration: UInt16 = 0
```
功能： 表示发送广播持续的时间。单位为10ms，有效范围为1(10ms)到65535(655350ms)，如果未指定此参数或者将其设置为0，则会连续发送广播。
Type: UInt16; Readable: true; Writable: true


### AdvertisingParams

class AdvertisingParams
```cangjie
public class AdvertisingParams { public AdvertisingParams( public var advertisingSettings: AdvertiseSetting, public var advertisingData: AdvertiseData, public var advertisingResponse: AdvertiseData, public var duration!: UInt16 = 0 ) }
```
功能： 描述首次启动广播设置的参数。

#### var advertisingData

```cangjie
public var advertisingData: AdvertiseData
```
功能： 表示广播的数据包内容。
Type: AdvertiseData; Readable: true; Writable: true

#### var advertisingResponse

```cangjie
public var advertisingResponse: AdvertiseData
```
功能： 表示回复扫描请求的响应内容。
Type: AdvertiseData; Readable: true; Writable: true

#### var advertisingSettings

```cangjie
public var advertisingSettings: AdvertiseSetting
```
功能： 表示发送广播的相关参数。
Type: AdvertiseSetting; Readable: true; Writable: true

#### var duration

```cangjie
public var duration: UInt16 = 0
```
功能： 表示发送广播持续的时间。单位为10ms，有效范围为1(10ms)到65535(655350ms)，如果未指定此参数或者将其设置为0，则会连续发送广播。
Type: UInt16; Readable: true; Writable: true


### AdvertisingStateChangeInfo

class AdvertisingStateChangeInfo
```cangjie
public class AdvertisingStateChangeInfo { public var advertisingId: Int32 public var state: AdvertisingState }
```
功能： 描述广播启动、停止等状态信息。

#### var advertisingId

```cangjie
public var advertisingId: Int32
```
功能： 表示广播ID标识。
Type: Int32; Readable: true; Writable: true

#### var state

```cangjie
public var state: AdvertisingState
```
功能： 表示广播状态。
Type: AdvertisingState; Readable: true; Writable: true


### BLECharacteristic

class BLECharacteristic
```cangjie
public class BLECharacteristic { public BLECharacteristic( public var serviceUuid: String, public var characteristicUuid: String, public var characteristicValue: Array<Byte>, public var descriptors: Array<BLEDescriptor>, public var properties: GattProperties ) }
```
功能： 描述characteristic的接口参数定义。

#### var characteristicUuid

```cangjie
public var characteristicUuid: String
```
功能： 特定特征（characteristic）的UUID，例如：00002a11-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: true

#### var characteristicValue

```cangjie
public var characteristicValue: Array<Byte>
```
功能： 特征对应的二进制值。
Type: Array<Byte>; Readable: true; Writable: true

#### var descriptors

```cangjie
public var descriptors: Array<BLEDescriptor>
```
功能： 特定特征的描述符列表。
Type: Array< BLEDescriptor >; Readable: true; Writable: true

#### var properties

```cangjie
public var properties: GattProperties
```
功能： 特定特征的属性描述。
Type: GattProperties; Readable: true; Writable: true

#### var serviceUuid

```cangjie
public var serviceUuid: String
```
功能： 特定服务（service）的UUID，例如：00001888-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: true


### BLEConnectionChangeState

class BLEConnectionChangeState
```cangjie
public class BLEConnectionChangeState { public BLEConnectionChangeState( public let deviceId: String, public var state: ProfileConnectionState ) }
```
功能： 描述Gatt profile连接状态。

#### var state

```cangjie
public var state: ProfileConnectionState
```
功能： 表示BLE连接状态的枚举。
Type: ProfileConnectionState; Readable: true; Writable: true

#### let deviceId

```cangjie
public let deviceId: String
```
功能： 表示远端设备地址，例如："XX:XX:XX:XX:XX:XX"。
Type: String; Readable: true; Writable: false


### BLEDescriptor

class BLEDescriptor
```cangjie
public class BLEDescriptor { public BLEDescriptor( public var serviceUuid: String, public var characteristicUuid: String, public var descriptorUuid: String, public var descriptorValue: Array<Byte> ) }
```
功能： 描述descriptor的接口参数定义。

#### var characteristicUuid

```cangjie
public var characteristicUuid: String
```
功能： 特定特征（characteristic）的UUID，例如：00002a11-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: true

#### var descriptorUuid

```cangjie
public var descriptorUuid: String
```
功能： 描述符（descriptor）的UUID，例如：00002902-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: true

#### var descriptorValue

```cangjie
public var descriptorValue: Array<Byte>
```
功能： 描述符对应的二进制值。
Type: Array<Byte>; Readable: true; Writable: true

#### var serviceUuid

```cangjie
public var serviceUuid: String
```
功能： 特定服务（service）的UUID，例如：00001888-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: true


### CharacteristicReadRequest

class CharacteristicReadRequest
```cangjie
public class CharacteristicReadRequest { public CharacteristicReadRequest( public let deviceId: String, public let transId: Int32, public let offset: Int32, public let characteristicUuid: String, public let serviceUuid: String ) }
```
功能： 描述server端订阅后收到的特征值读请求事件参数类。

#### let characteristicUuid

```cangjie
public let characteristicUuid: String
```
功能： 特定特征（characteristic）的UUID，例如：00002a11-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: false

#### let deviceId

```cangjie
public let deviceId: String
```
功能： 表示发送特征值读请求的远端设备地址，例如："XX:XX:XX:XX:XX:XX"。
Type: String; Readable: true; Writable: false

#### let offset

```cangjie
public let offset: Int32
```
功能： 表示读特征值数据的起始位置。例如：k表示从第k个字节开始读，server端回复响应时需填写相同的offset。
Type: Int32; Readable: true; Writable: false

#### let serviceUuid

```cangjie
public let serviceUuid: String
```
功能： 特定服务（service）的UUID，例如：00001888-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: false

#### let transId

```cangjie
public let transId: Int32
```
功能： 表示写请求的传输ID，server端回复响应时需填写相同的传输ID。
Type: Int32; Readable: true; Writable: false


### CharacteristicWriteRequest

class CharacteristicWriteRequest
```cangjie
public class CharacteristicWriteRequest { public CharacteristicWriteRequest( public let deviceId: String, public let transId: Int32, public let offset: Int32, public let isPrepared: Bool, public let needRsp: Bool, public let value: Array<Byte>, public let characteristicUuid: String, public let serviceUuid: String ) }
```
功能： 描述server端订阅后收到的特征值写请求事件参数类。

#### let characteristicUuid

```cangjie
public let characteristicUuid: String
```
功能： 特定特征（characteristic）的UUID，例如：00002a11-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: false

#### let deviceId

```cangjie
public let deviceId: String
```
功能： 表示扫描到的设备地址，例如："XX:XX:XX:XX:XX:XX"。
Type: String; Readable: true; Writable: false

#### let isPrepared

```cangjie
public let isPrepared: Bool
```
功能： 表示写请求是否立即执行。true表示立即执行。
Type: Bool; Readable: true; Writable: false

#### let needRsp

```cangjie
public let needRsp: Bool
```
功能： 表示是否要给client端回复响应。
Type: Bool; Readable: true; Writable: false

#### let offset

```cangjie
public let offset: Int32
```
功能： 表示写描述符数据的起始位置。例如：k表示从第k个字节开始写，server端回复响应时需填写相同的offset。
Type: Int32; Readable: true; Writable: false

#### let serviceUuid

```cangjie
public let serviceUuid: String
```
功能： 特定服务（service）的UUID，例如：00001888-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: false

#### let transId

```cangjie
public let transId: Int32
```
功能： 表示写请求的传输ID，server端回复响应时需填写相同的传输ID。
Type: Int32; Readable: true; Writable: false

#### let value

```cangjie
public let value: Array<Byte>
```
功能： 表示写入的描述符二进制数据。
Type: Array<Byte>; Readable: true; Writable: false


### DescriptorReadRequest

class DescriptorReadRequest
```cangjie
public class DescriptorReadRequest { public DescriptorReadRequest( public let deviceId: String, public let transId: Int32, public let offset: Int32, public let descriptorUuid: String, public let characteristicUuid: String, public let serviceUuid: String ) }
```
功能： 描述server端订阅后收到的描述符读请求事件参数类。

#### let characteristicUuid

```cangjie
public let characteristicUuid: String
```
功能： 特定特征（characteristic）的UUID，例如：00002a11-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: false

#### let descriptorUuid

```cangjie
public let descriptorUuid: String
```
功能： 表示描述符（descriptor）的UUID，例如：00002902-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: false

#### let deviceId

```cangjie
public let deviceId: String
```
功能： 表示发送描述符读请求的远端设备地址，例如："XX:XX:XX:XX:XX:XX"。
Type: String; Readable: true; Writable: false

#### let offset

```cangjie
public let offset: Int32
```
功能： 表示读描述符数据的起始位置。例如：k表示从第k个字节开始读，server端回复响应时需填写相同的offset。
Type: Int32; Readable: true; Writable: false

#### let serviceUuid

```cangjie
public let serviceUuid: String
```
功能： 特定服务（service）的UUID，例如：00001888-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: false

#### let transId

```cangjie
public let transId: Int32
```
功能： 表示读请求的传输ID，server端回复响应时需填写相同的传输ID。
Type: Int32; Readable: true; Writable: false


### DescriptorWriteRequest

class DescriptorWriteRequest
```cangjie
public class DescriptorWriteRequest { public DescriptorWriteRequest( public let deviceId: String, public let transId: Int32, public let offset: Int32, public let isPrepared: Bool, public let needRsp: Bool, public let value: Array<Byte>, public let descriptorUuid: String, public let characteristicUuid: String, public let serviceUuid: String ) }
```
功能： 描述server端订阅后收到的描述符写请求事件参数类。

#### let characteristicUuid

```cangjie
public let characteristicUuid: String
```
功能： 特定特征（characteristic）的UUID，例如：00002a11-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: false

#### let descriptorUuid

```cangjie
public let descriptorUuid: String
```
功能： 表示描述符（descriptor）的UUID，例如：00002902-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: false

#### let deviceId

```cangjie
public let deviceId: String
```
功能： 表示发送描述符写请求的远端设备地址，例如："XX:XX:XX:XX:XX:XX"。
Type: String; Readable: true; Writable: false

#### let isPrepared

```cangjie
public let isPrepared: Bool
```
功能： 表示写请求是否立即执行。
Type: Bool; Readable: true; Writable: false

#### let needRsp

```cangjie
public let needRsp: Bool
```
功能： 表示是否要给client端回复响应。
Type: Bool; Readable: true; Writable: false

#### let offset

```cangjie
public let offset: Int32
```
功能： 表示写描述符数据的起始位置。例如：k表示从第k个字节开始写，server端回复响应时需填写相同的offset。
Type: Int32; Readable: true; Writable: false

#### let serviceUuid

```cangjie
public let serviceUuid: String
```
功能： 特定服务（service）的UUID，例如：00001888-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: false

#### let transId

```cangjie
public let transId: Int32
```
功能： 表示写请求的传输ID，server端回复响应时需填写相同的传输ID。
Type: Int32; Readable: true; Writable: false

#### let value

```cangjie
public let value: Array<Byte>
```
功能： 表示写入的描述符二进制数据。
Type: Array<Byte>; Readable: true; Writable: false


### GattClientDevice

class GattClientDevice
```cangjie
public class GattClientDevice {}
```
功能： client端类。使用client端方法之前需要创建该类的实例进行操作，通过 createGattClientDevice(String) 方法构造此实例。


### GattProperties

class GattProperties
```cangjie
public class GattProperties { public GattProperties( let write!: Bool = true, let writeNoResponse!: Bool = true, let read!: Bool = true, let notify!: Bool = false, let indicate!: Bool = false ) }
```
功能： 描述gatt characteristic的属性。


### GattServer

class GattServer
```cangjie
public class GattServer {}
```
功能： server端类。使用server端方法之前需要创建该类的实例进行操作，通过 createGattServer() 方法构造此实例。


### GattService

class GattService
```cangjie
public class GattService { public GattService( public var serviceUuid: String, public var isPrimary: Bool, public var characteristics: Array<BLECharacteristic>, public var includeServices: Array<GattService> ) }
```
功能： 描述service的接口参数定义。

#### var characteristics

```cangjie
public var characteristics: Array<BLECharacteristic>
```
功能： 当前服务包含的特征列表。
Type: Array< BLECharacteristic >; Readable: true; Writable: true

#### var includeServices

```cangjie
public var includeServices: Array<GattService>
```
功能： 当前服务依赖的其它服务。
Type: Array< GattService >; Readable: true; Writable: true

#### var isPrimary

```cangjie
public var isPrimary: Bool
```
功能： 特如果是主服务设置为true，否则设置为false。
Type: Bool; Readable: true; Writable: true

#### var serviceUuid

```cangjie
public var serviceUuid: String
```
功能： 特定服务（service）的UUID，例如：00001888-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: true


### ManufactureData

class ManufactureData
```cangjie
public class ManufactureData { public ManufactureData( public var manufactureId: UInt16, public var manufactureValue: Array<Byte> ) }
```
功能： 描述BLE广播数据包的内容。

#### var manufactureId

```cangjie
public var manufactureId: UInt16
```
功能： 表示制造商的ID，由蓝牙SIG分配。
Type: UInt16; Readable: true; Writable: true

#### var manufactureValue

```cangjie
public var manufactureValue: Array<Byte>
```
功能： 表示制造商发送的制造商数据。
Type: Array<Byte>; Readable: true; Writable: true


### NotifyCharacteristic

class NotifyCharacteristic
```cangjie
public class NotifyCharacteristic { public NotifyCharacteristic( public var serviceUuid: String, public var characteristicUuid: String, public var characteristicValue: Array<Byte>, public var confirm: Bool ) }
```
功能： 描述server端特征值变化时发送的特征通知参数定义。

#### var characteristicUuid

```cangjie
public var characteristicUuid: String
```
功能： 特定特征（characteristic）的UUID，例如：00002a11-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: true

#### var characteristicValue

```cangjie
public var characteristicValue: Array<Byte>
```
功能： 特征对应的二进制值。
Type: Array<Byte>; Readable: true; Writable: true

#### var confirm

```cangjie
public var confirm: Bool
```
功能： 如果是indication，对端需要回复确认，则设置为true；如果是notification，对端不需要回复确认，则设置为false。
Type: Bool; Readable: true; Writable: true

#### var serviceUuid

```cangjie
public var serviceUuid: String
```
功能： 特定服务（service）的UUID，例如：00001888-0000-1000-8000-00805f9b34fb。
Type: String; Readable: true; Writable: true


### ScanFilter

class ScanFilter
```cangjie
public class ScanFilter { public var deviceId: ?String = None public var name: ?String = None public var serviceUuid: ?String = None public var serviceUuidMask: ?String = None public var serviceSolicitationUuid: ?String = None public var serviceSolicitationUuidMask: ?String = None public var serviceData: ?Array<Byte>= None public var serviceDataMask: ?Array<Byte>= None public var manufactureId: UInt16 = 0 public var manufactureData: ?Array<Byte>= None public var manufactureDataMask: ?Array<Byte>= None public init() }
```
功能： 扫描过滤参数。

#### var deviceId

```cangjie
public var deviceId: ?String = None
```
功能： 表示过滤的BLE设备地址，例如："XX:XX:XX:XX:XX:XX"。
Type: ?String; Readable: true; Writable: true

#### var manufactureData

```cangjie
public var manufactureData: ?Array<Byte>= None
```
功能： 表示过滤包含该制造商相关数据的设备，例如：[0x1F,0x2F,0x3F]。
Type: ?Array<Byte>; Readable: true; Writable: true

#### var manufactureDataMask

```cangjie
public var manufactureDataMask: ?Array<Byte>= None
```
功能： 表示过滤包含该制造商相关数据掩码的设备，例如：[0xFF,0xFF,0xFF]。
Type: ?Array<Byte>; Readable: true; Writable: true

#### var manufactureId

```cangjie
public var manufactureId: UInt16 = 0
```
功能： 表示过滤包含该制造商ID的设备，例如：0x0006。
Type: UInt16; Readable: true; Writable: true

#### var name

```cangjie
public var name: ?String = None
```
功能： 表示过滤的BLE设备名。
Type: ?String; Readable: true; Writable: true

#### var serviceData

```cangjie
public var serviceData: ?Array<Byte>= None
```
功能： 表示过滤包含该服务相关数据的设备，例如：[0x90,0x00,0xF1,0xF2]。
Type: ?Array<Byte>; Readable: true; Writable: true

#### var serviceDataMask

```cangjie
public var serviceDataMask: ?Array<Byte>= None
```
功能： 表示过滤包含该服务相关数据掩码的设备，例如：[0xFF,0xFF,0xFF,0xFF]。
Type: ?Array<Byte>; Readable: true; Writable: true

#### var serviceSolicitationUuid

```cangjie
public var serviceSolicitationUuid: ?String = None
```
功能： 表示过滤包含该UUID服务请求的设备，例如：00001888-0000-1000-8000-00805F9B34FB。
Type: ?String; Readable: true; Writable: true

#### var serviceSolicitationUuidMask

```cangjie
public var serviceSolicitationUuidMask: ?String = None
```
功能： 表示过滤包含该UUID服务请求掩码的设备，例如：FFFFFFFF-FFFF-FFFF-FFFF-FFFFFFFFFFFF。
Type: ?String; Readable: true; Writable: true

#### var serviceUuid

```cangjie
public var serviceUuid: ?String = None
```
功能： 表示过滤包含该UUID服务的设备，例如：00001888-0000-1000-8000-00805f9b34fb。
Type: ?String; Readable: true; Writable: true

#### var serviceUuidMask

```cangjie
public var serviceUuidMask: ?String = None
```
功能： 表示过滤包含该UUID服务掩码的设备，例如：FFFFFFFF-FFFF-FFFF-FFFF-FFFFFFFFFFFF。
Type: ?String; Readable: true; Writable: true


### ScanOptions

class ScanOptions
```cangjie
public class ScanOptions { public var interval: Int32 = 0 public var dutyMode: ScanDuty = SCAN_MODE_LOW_POWER public var matchMode: MatchMode = MATCH_MODE_AGGRESSIVE public var phyType: PhyType = PHY_LE_1M public init(interval: Int32, dutyMode: ScanDuty, matchMode: MatchMode, phyType: PhyType) }
```
功能： 扫描的配置参数。

#### var dutyMode

```cangjie
public var dutyMode: ScanDuty = SCAN_MODE_LOW_POWER
```
功能： 表示扫描模式，默认值为SCAN_MODE_LOW_POWER。
Type: ScanDuty; Readable: true; Writable: true

#### var interval

```cangjie
public var interval: Int32 = 0
```
功能： 表示扫描结果上报延迟时间，默认值为0。
Type: Int32; Readable: true; Writable: true

#### var matchMode

```cangjie
public var matchMode: MatchMode = MATCH_MODE_AGGRESSIVE
```
功能： 表示硬件的过滤匹配模式，默认值为MATCH_MODE_AGGRESSIVE。
Type: MatchMode; Readable: true; Writable: true

#### var phyType

```cangjie
public var phyType: PhyType = PHY_LE_1M
```
功能： 表示扫描中使用的PHY类型。
Type: PhyType; Readable: true; Writable: true


### ScanResult

class ScanResult
```cangjie
public class ScanResult {}
```
功能： 扫描结果上报数据。

#### let connectable

```cangjie
public let connectable: Bool
```
功能： 表示扫描到的设备是否可连接。true表示可连接，false表示不可连接。
Type: Bool; Readable: true; Writable: false

#### let data

```cangjie
public let data: Array<Byte>
```
功能： 表示扫描到的设备发送的广播包。
Type: Array<Byte>; Readable: true; Writable: false

#### let deviceId

```cangjie
public let deviceId: String
```
功能： 表示扫描到的设备地址，例如："XX:XX:XX:XX:XX:XX"。基于信息安全考虑，此处获取的设备地址为随机MAC地址。配对成功后，该地址不会变更；已配对设备取消配对后重新扫描或蓝牙服务下电时，该随机地址会变更。
Type: String; Readable: true; Writable: false

#### let deviceName

```cangjie
public let deviceName: String
```
功能： 表示扫描到的设备名称。
Type: String; Readable: true; Writable: false

#### let rssi

```cangjie
public let rssi: Int32
```
功能： 表示扫描到的设备的rssi值。
Type: Int32; Readable: true; Writable: false


### ServerResponse

class ServerResponse
```cangjie
public class ServerResponse { public ServerResponse( public let deviceId: String, public let transId: Int32, public let status: Int32, public let offset: Int32, public let value: Array<Byte> ) }
```
功能： 描述server端回复client端读/写请求的响应参数类。

#### let deviceId

```cangjie
public let deviceId: String
```
功能： 表示远端设备地址，例如："XX:XX:XX:XX:XX:XX"。
Type: String; Readable: true; Writable: false

#### let offset

```cangjie
public let offset: Int32
```
功能： 表示请求的读/写起始位置，与订阅的读/写请求事件携带的offset保持一致。
Type: Int32; Readable: true; Writable: false

#### let status

```cangjie
public let status: Int32
```
功能： 表示响应的状态，设置为0即可，表示正常。
Type: Int32; Readable: true; Writable: false

#### let transId

```cangjie
public let transId: Int32
```
功能： 表示请求的传输ID，与订阅的读/写请求事件携带的ID保持一致。
Type: Int32; Readable: true; Writable: false

#### let value

```cangjie
public let value: Array<Byte>
```
功能： 表示回复响应的二进制数据。
Type: Array<Byte>; Readable: true; Writable: false


### ServiceData

class ServiceData
```cangjie
public class ServiceData { public ServiceData( public var serviceUuid: String, public var serviceValue: Array<Byte> ) }
```
功能： 描述广播包中服务数据内容。

#### var serviceUuid

```cangjie
public var serviceUuid: String
```
功能： 表示服务的UUID。
Type: String; Readable: true; Writable: true

#### var serviceValue

```cangjie
public var serviceValue: Array<Byte>
```
功能： 表示服务数据。
Type: Array<Byte>; Readable: true; Writable: true



## Enums

### AdvertisingState

enum AdvertisingState
```cangjie
public enum AdvertisingState <: Equatable<AdvertisingState> & ToString { | STARTED | ENABLED | DISABLED | STOPPED | ... }
```
功能： 广播状态。

#### DISABLED

```cangjie
DISABLED
```
功能： 表示临时停止广播后的状态。

#### ENABLED

```cangjie
ENABLED
```
功能： 表示临时启动广播后的状态。

#### STARTED

```cangjie
STARTED
```
功能： 表示首次启动广播后的状态。

#### STOPPED

```cangjie
STOPPED
```
功能： 表示完全停止广播后的状态。


### BluetoothBleCallbackType

enum BluetoothBleCallbackType
```cangjie
public enum BluetoothBleCallbackType <: Equatable<BluetoothBleCallbackType> & Hashable & ToString { | ADVERTISING_STATE_CHANGE | BLE_DEVICE_FIND | ... }
```
功能： 广播扫描订阅事件类型。

#### ADVERTISING_STATE_CHANGE

```cangjie
ADVERTISING_STATE_CHANGE
```
功能： 表示广播状态事件类型。

#### BLE_DEVICE_FIND

```cangjie
BLE_DEVICE_FIND
```
功能： 表示BLE设备发现事件类型。


### BluetoothBleGattClientDeviceCallbackType

enum BluetoothBleGattClientDeviceCallbackType
```cangjie
public enum BluetoothBleGattClientDeviceCallbackType <: Equatable<BluetoothBleGattClientDeviceCallbackType> & Hashable & ToString { | BLE_CHARACTERISTIC_CHANGE | BLE_CONNECTION_STATE_CHANGE | BLE_MTU_CHANGE | ... }
```
功能： 客户端 on/off 事件的类型。

#### BLE_CHARACTERISTIC_CHANGE

```cangjie
BLE_CHARACTERISTIC_CHANGE
```
功能： 表示特征值变化事件类型。

#### BLE_CONNECTION_STATE_CHANGE

```cangjie
BLE_CONNECTION_STATE_CHANGE
```
功能： 表示连接状态变化事件类型。

#### BLE_MTU_CHANGE

```cangjie
BLE_MTU_CHANGE
```
功能： 表示MTU状态变化事件类型。


### BluetoothBleGattServerCallbackType

enum BluetoothBleGattServerCallbackType
```cangjie
public enum BluetoothBleGattServerCallbackType <: Equatable<BluetoothBleGattServerCallbackType> & Hashable & ToString { | CHARACTERISTIC_READ | CHARACTERISTIC_WRITE | DESCRIPTOR_READ | DESCRIPTOR_WRITE | CONNECTION_STATE_CHANGE | BLE_MTU_CHANGE | ... }
```
功能： 服务端 on/off 事件的类型。

#### BLE_MTU_CHANGE

```cangjie
BLE_MTU_CHANGE
```
功能： 表示MTU状态变化事件类型。

#### CHARACTERISTIC_READ

```cangjie
CHARACTERISTIC_READ
```
功能： 表示特征值读请求事件类型。

#### CHARACTERISTIC_WRITE

```cangjie
CHARACTERISTIC_WRITE
```
功能： 表示特征值写请求事件类型。

#### CONNECTION_STATE_CHANGE

```cangjie
CONNECTION_STATE_CHANGE
```
功能： 表示BLE连接状态变化事件类型。

#### DESCRIPTOR_READ

```cangjie
DESCRIPTOR_READ
```
功能： 表示描述符读请求事件类型。

#### DESCRIPTOR_WRITE

```cangjie
DESCRIPTOR_WRITE
```
功能： 表示描述符写请求事件类型。


### GattWriteType

enum GattWriteType
```cangjie
public enum GattWriteType <: Equatable<GattWriteType> & ToString { | WRITE | WRITE_NO_RESPONSE | ... }
```
功能： 表示gatt写入类型。

#### WRITE

```cangjie
WRITE
```
功能： 表示写入特征值，需要对端设备的回复。

#### WRITE_NO_RESPONSE

```cangjie
WRITE_NO_RESPONSE
```
功能： 表示写入特征值，不需要对端设备的回复。


### MatchMode

enum MatchMode
```cangjie
public enum MatchMode <: Equatable<MatchMode> & ToString { | MATCH_MODE_AGGRESSIVE | MATCH_MODE_STICKY | ... }
```
功能： 硬件过滤匹配模式。

#### MATCH_MODE_AGGRESSIVE

```cangjie
MATCH_MODE_AGGRESSIVE
```
功能： 表示硬件上报扫描结果门限较低，比如扫描到的功率较低或者一段时间扫描到的次数较少也触发上报，默认值。

#### MATCH_MODE_STICKY

```cangjie
MATCH_MODE_STICKY
```
功能： 表示硬件上报扫描结果门限较高，更高的功率门限以及扫描到多次才会上报。


### PhyType

enum PhyType
```cangjie
public enum PhyType <: Equatable<PhyType> & ToString { | PHY_LE_1M | PHY_LE_ALL_SUPPORTED | ... }
```
功能： 广播状态。

#### PHY_LE_1M

```cangjie
PHY_LE_1M
```
功能： 表示扫描中使用1M PHY。

#### PHY_LE_ALL_SUPPORTED

```cangjie
PHY_LE_ALL_SUPPORTED
```
功能： 表示扫描中使用蓝牙协议支持的PHY模式。


### ScanDuty

enum ScanDuty
```cangjie
public enum ScanDuty <: Equatable<ScanDuty> & ToString { | SCAN_MODE_LOW_POWER | SCAN_MODE_BALANCED | SCAN_MODE_LOW_LATENCY | ... }
```
功能： 扫描模式。

#### SCAN_MODE_BALANCED

```cangjie
SCAN_MODE_BALANCED
```
功能： 表示均衡模式。

#### SCAN_MODE_LOW_LATENCY

```cangjie
SCAN_MODE_LOW_LATENCY
```
功能： 表示低延迟模式。

#### SCAN_MODE_LOW_POWER

```cangjie
SCAN_MODE_LOW_POWER
```
功能： 表示低功耗模式，默认值。



