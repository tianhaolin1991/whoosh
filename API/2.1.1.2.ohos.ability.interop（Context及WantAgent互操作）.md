# ohos.ability.interop（Context及WantAgent互操作）

本模块提供 Context 及其相关子类和 WantAgent 进行互操作的能力。

```cangjie
import kit.AbilityKit.*
```
## Functions

### public func createAbilityContextFromJSValue(context: JSContext, input: JSValue): UIAbilityContext

```cangjie
public func createAbilityContextFromJSValue(context: JSContext, input: JSValue): UIAbilityContext
```
功能： 从JSValue转换为AbilityContext类型。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| JSContext | ArkTS互操作上下文。 | context | 是 |
| JSValue | ArkTS统一类型。 | input | 是 |

**Returns:**

| type | description |
| --- | --- |
| UIAbilityContext | 返回AbilityContext类型实例。 |

### public func createAbilityStageContextFromJSValue(context: JSContext, input: JSValue): AbilityStageContext

```cangjie
public func createAbilityStageContextFromJSValue(context: JSContext, input: JSValue): AbilityStageContext
```
功能： 从 JSValue 转换为 AbilityStageContext 类型。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| JSContext | ArkTS互操作上下文。 | context | 是 |
| JSValue | ArkTS统一类型。 | input | 是 |

**Returns:**

| type | description |
| --- | --- |
| AbilityStageContext | 返回AbilityStageContext类型实例。 |

### public func createApplicationContextFromJSValue(context: JSContext, input: JSValue): ApplicationContext

```cangjie
public func createApplicationContextFromJSValue(context: JSContext, input: JSValue): ApplicationContext
```
功能： 从 JSValue 转换为 ApplicationContext 类型。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| JSContext | ArkTS互操作上下文。 | context | 是 |
| JSValue | ArkTS统一类型。 | input | 是 |

**Returns:**

| type | description |
| --- | --- |
| ApplicationContext | 返回 ApplicationContext 类型实例。 |

### public func createContextFromJSValue(context: JSContext, input: JSValue): Context

```cangjie
public func createContextFromJSValue(context: JSContext, input: JSValue): Context
```
功能： 从 JSValue 转换为 Context 类型。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| JSContext | ArkTS互操作上下文。 | context | 是 |
| JSValue | ArkTS统一类型。 | input | 是 |

**Returns:**

| type | description |
| --- | --- |
| Context | 返回Context类型实例。 |

### public func createPhotoEditorExtensionContextFromJSValue(context: JSContext, input: JSValue): PhotoEditorExtensionContext

```cangjie
public func createPhotoEditorExtensionContextFromJSValue(context: JSContext, input: JSValue): PhotoEditorExtensionContext
```
功能： 从 JSValue 转换为 PhotoEditorExtensionContext 类型。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| JSContext | ArkTS互操作上下文。 | context | 是 |
| JSValue | ArkTS统一类型。 | input | 是 |

**Returns:**

| type | description |
| --- | --- |
| PhotoEditorExtensionContext | 返回PhotoEditorExtensionContext类型实例。 |

### public func createUIExtensionContextFromJSValue(context: JSContext, input: JSValue): UIExtensionContext

```cangjie
public func createUIExtensionContextFromJSValue(context: JSContext, input: JSValue): UIExtensionContext
```
功能： 从 JSValue 转换为 UIExtensionContext 类型。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| JSContext | ArkTS互操作上下文。 | context | 是 |
| JSValue | ArkTS统一类型。 | input | 是 |

**Returns:**

| type | description |
| --- | --- |
| UIExtensionContext | 返回UIExtensionContext类型实例。 |


## Interfaces

### JSSystemObjectInteropType

interface JSSystemObjectInteropType
```cangjie
public interface JSSystemObjectInteropType<T> { static func fromJSValue(context: JSContext, input: JSValue): T func toJSValue(context: JSContext): JSValue }
```
功能： JS系统对象专用的拓展接口，以实现与JSValue的互转。

#### func toJSValue(context: JSContext): JSValue

```cangjie
func toJSValue(context: JSContext): JSValue
```
功能： 将仓颉对象转换成 JSValue 。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| JSContext | ArkTS互操作上下文。 | context | 是 |

**Returns:**

| type | description |
| --- | --- |
| JSValue | ArkTS统一类型。 |


### SystemObjectInteropTypeToJS

interface SystemObjectInteropTypeToJS
```cangjie
public interface SystemObjectInteropTypeToJS { func toJSValue(context: JSContext): JSValue }
```
功能： 系统对象专用的拓展接口，以实现与 JSValue 的互转。

#### func toJSValue(context: JSContext): JSValue

```cangjie
func toJSValue(context: JSContext): JSValue
```
功能： 将仓颉对象转换成 JSValue 。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| JSContext | ArkTS互操作上下文。 | context | 是 |

**Returns:**

| type | description |
| --- | --- |
| JSValue | ArkTS统一类型。 |



## Classes

### Context

class Context
```cangjie
extend Context <: SystemObjectInteropTypeToJS {}
```
功能： 拓展 Context 类，可以和ArkTs互操作。


### WantAgent

class WantAgent
```cangjie
extend WantAgent <: JSSystemObjectInteropType<WantAgent> {}
```
功能： 拓展 WantAgent 类，可以和ArkTs互操作。



