# ohos.preferences（用户首选项）

用户首选项为应用提供Key-Value键值型的数据处理能力，支持应用持久化轻量级数据，并对其修改和查询。

```cangjie
import kit.ArkData.*
```
## Classes

### Preferences

class Preferences
```cangjie
public class Preferences {}
```
功能： 首选项类，提供获取和修改存储数据的接口。



## Structs

### PreferencesOptions

struct PreferencesOptions
```cangjie
public struct PreferencesOptions { public let name: String public let dataGroupId: String public init(name: String) public init(name: String, dataGroupId: String) }
```
功能： Preferences实例配置选项。



## Enums

### PreferencesValueType

enum PreferencesValueType
```cangjie
public enum PreferencesValueType { | integer(Int64) | double(Float64) | string(String) | bool(Bool) | boolArray(Array<Bool>) | doubleArray(Array<Float64>) | stringArray(Array<String>) | ... }
```
功能： 用于表示允许的数据字段的枚举类型。

#### bool(Bool)

```cangjie
bool(Bool)
```
功能： 表示值类型为布尔值。

#### boolArray(Array<Bool>)

```cangjie
boolArray(Array<Bool>)
```
功能： 表示值类型为布尔类型的数组。

#### double(Float64)

```cangjie
double(Float64)
```
功能： 表示值类型为Float64浮点数。

#### doubleArray(Array<Float64>)

```cangjie
doubleArray(Array<Float64>)
```
功能： 表示值类型为Float64类型的数组。

#### integer(Int64)

```cangjie
integer(Int64)
```
功能： 表示值类型为Int64数字。

#### string(String)

```cangjie
string(String)
```
功能： 表示值类型为字符串。

#### stringArray(Array<String>)

```cangjie
stringArray(Array<String>)
```
功能： 表示值类型为字符串类型的数组。



