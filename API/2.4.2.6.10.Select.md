# Select

提供下拉选择菜单，可以让用户在多个选项之间选择。

## Component Constructors

### public init(values: Array<SelectOption>)

```cangjie
public init(values: Array<SelectOption>)
```
功能： 构造一个下拉选择菜单组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array< SelectOption > | 是 | 设置下拉选项。 | values | - |


## Functions

### public func arrowPosition(value: ArrowPosition): This

```cangjie
public func arrowPosition(value: ArrowPosition): This
```
功能： 设置下拉菜单项的文本与箭头之间的对齐方式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ArrowPosition | 是 | 下拉菜单项的文本与箭头之间的对齐方式。 初始值：ArrowPosition.END。 | value | - |

### public func controlSize(value: ControlSize): This

```cangjie
public func controlSize(value: ControlSize): This
```
功能： 设置Select组件的尺寸。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ControlSize | 是 | Select组件的尺寸。 初始值：ControlSize.NORMAL。 | value | - |

### public func divider(options!: Option<DividerOptions> = Option.None): This

```cangjie
public func divider(options!: Option<DividerOptions> = Option.None): This
```
功能： 设置分割线样式，不设置该属性则按初始值展示分割线。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Option< DividerOptions > | 否 | 命名参数。 1.设置为DividerOptions，则按DividerOptions参数的样式显示分割线。 初始值：strokeWidth:1.px，color:0x33182431 2.设置为Option.None时，不显示分割线。 3.strokeWidth设置过宽时，会覆盖文字。分割线会从每一个Item底部开始，同时向上向下画分割线。 4.startMargin和endMargin的默认值与不设置divider属性时的分割线样式保持一致。startMargin和endMargin的和与optionWidth的值相等时，不显示分割线。 startMargin和endMargin的和超过optionWidth的值时，按照初始样式显示分割线。 | options | Option.None |

### public func font( style!: FontStyle = FontStyle.Normal, weight!: FontWeight = FontWeight.Medium, size!: Length = 16.vp, family!: String = "sans-serif" ): This

```cangjie
public func font( style!: FontStyle = FontStyle.Normal, weight!: FontWeight = FontWeight.Medium, size!: Length = 16.vp, family!: String = "sans-serif" ): This
```
功能： 设置下拉按钮本身的文本样式。当size为0的时候，文本不显示，当size为负值的时候，文本的size按照初始值显示。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| FontStyle | 否 | 命名参数。 用于指定字体样式。 | style | FontStyle.Normal |
| FontWeight | 否 | 命名参数。 用于指定字体的粗细。 | weight | FontWeight.Medium |
| Length | 否 | 命名参数。 指定字号和行高，不支持百分比设置。 | size | 16.vp |
| String | 否 | 命名参数。 指定字体系列。 | family | "sans-serif" |

### public func fontColor(value: ResourceColor): This

```cangjie
public func fontColor(value: ResourceColor): This
```
功能： 根据指定的Color，设置下拉按钮本身的文本颜色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 下拉按钮本身的文本颜色。 初始值：@r(sys.color.ohos_id_color_text_primary)混合@r(sys.color.ohos_id_alpha_content_primary)的透明度。 | value | - |

### public func menuAlign(alignType: MenuAlignType, offset!: MenuOffset): This

```cangjie
public func menuAlign(alignType: MenuAlignType, offset!: MenuOffset): This
```
功能： 设置下拉按钮与下拉菜单间的对齐方式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| MenuAlignType | 是 | 对齐方式类型。 初始值：MenuAlignType.START。 | alignType | - |
| MenuOffset | 是 | 命名参数。 按照对齐类型对齐后，下拉菜单相对下拉按钮的偏移量。 初始值：MenuOffset(0, 0)。 | offset | - |

### public func menuBackgroundBlurStyle(value: BlurStyle): This

```cangjie
public func menuBackgroundBlurStyle(value: BlurStyle): This
```
功能： 设置下拉菜单的背景模糊材质。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| BlurStyle | 是 | 下拉菜单的背景模糊材质。 初始值：BlurStyle.COMPONENT_ULTRA_THICK。 | value | - |

### public func menuBackgroundColor(value: ResourceColor): This

```cangjie
public func menuBackgroundColor(value: ResourceColor): This
```
功能： 根据指定的Color，设置下拉菜单的背景色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 下拉菜单的背景色。 初始值：Color.TRANSPARENT。 | value | - |

### public func optionBgColor(value: ResourceColor): This

```cangjie
public func optionBgColor(value: ResourceColor): This
```
功能： 根据指定的Color，设置下拉菜单项的背景色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 下拉菜单项的背景色。 初始值：Color.TRANSPARENT。 | value | - |

### public func optionFont( style!: FontStyle = FontStyle.Normal, weight!: FontWeight = FontWeight.Medium, size!: Length = 16.vp, family!: String = "sans-serif" ): This

```cangjie
public func optionFont( style!: FontStyle = FontStyle.Normal, weight!: FontWeight = FontWeight.Medium, size!: Length = 16.vp, family!: String = "sans-serif" ): This
```
功能： 设置下拉菜单项的文本样式。当size为0的时候，文本不显示，当size为负值的时候，文本的size按照初始值显示。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| FontStyle | 否 | 命名参数。 用于指定字体样式。 | style | FontStyle.Normal |
| FontWeight | 否 | 命名参数。 用于指定字体的粗细。 | weight | FontWeight.Medium |
| Length | 否 | 命名参数。 指定字号和行高，不支持百分比设置。 | size | 16.vp |
| String | 否 | 命名参数。 指定字体系列。 | family | "sans-serif" |

### public func optionFontColor(value: ResourceColor): This

```cangjie
public func optionFontColor(value: ResourceColor): This
```
功能： 根据指定的Color，设置下拉菜单项的文本颜色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 下拉菜单项的文本颜色。 初始值：@r(sys.color.ohos_id_color_text_primary)。 | value | - |

### public func optionHeight(value: Length): This

```cangjie
public func optionHeight(value: Length): This
```
功能： 根据指定的Length类型值，设置下拉菜单显示的最大高度。下拉菜单的初始最大高度是屏幕可用高度的80%，设置的菜单最大高度不能超过初始最大高度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 下拉菜单显示的最大高度。 | value | - |

### public func optionWidth(value: OptionWidthMode ): This

```cangjie
public func optionWidth(value: OptionWidthMode ): This
```
功能： 设置下拉菜单项的宽度。OptionWidthMode类型为枚举类型，OptionWidthMode决定下拉菜单是否继承下拉按钮宽度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| OptionWidthMode | 是 | 下拉菜单项的宽度。 | value | - |

### public func optionWidth(value: Length): This

```cangjie
public func optionWidth(value: Length): This
```
功能： 根据指定的Length类型值，设置下拉菜单项的宽度，不支持设置百分比。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 下拉菜单项的宽度。 | value | - |

### public func selected(value: Int32): This

```cangjie
public func selected(value: Int32): This
```
功能： 设置下拉菜单初始选项的索引，第一项的索引为0。当不设置selected属性或设置异常值时，初始选择值为-1，菜单项不选中。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 下拉菜单初始选项的索引。 | value | - |

### public func selectedOptionBgColor(value: ResourceColor): This

```cangjie
public func selectedOptionBgColor(value: ResourceColor): This
```
功能： 根据指定的Color，设置下拉菜单选中项的背景色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 下拉菜单选中项的背景色。 初始值：@r(sys.color.ohos_id_color_component_activated)混合@r(sys.color.ohos_id_alpha_highlight_bg)的透明度。 | value | - |

### public func selectedOptionFont( style!: FontStyle = FontStyle.Normal, weight!: FontWeight = FontWeight.Medium, size!: Length = 16.vp, family!: String = "sans-serif" ): This

```cangjie
public func selectedOptionFont( style!: FontStyle = FontStyle.Normal, weight!: FontWeight = FontWeight.Medium, size!: Length = 16.vp, family!: String = "sans-serif" ): This
```
功能： 设置下拉菜单选中项的文本样式。当size为0的时候，文本不显示，当size为负值的时候，文本的size按照初始值显示。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| FontStyle | 否 | 命名参数。 用于指定字体样式。 | style | FontStyle.Normal |
| FontWeight | 否 | 命名参数。 用于指定字体的粗细。 | weight | FontWeight.Medium |
| Length | 否 | 命名参数。 文本尺寸。不支持百分比设置。 | size | 16.vp |
| String | 否 | 命名参数。 指定字体列表。 | family | "sans-serif" |

### public func selectedOptionFontColor(value: ResourceColor): This

```cangjie
public func selectedOptionFontColor(value: ResourceColor): This
```
功能： 根据指定的Color，设置下拉菜单选中项的文本颜色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 下拉菜单选中项的文本颜色。 初始值：@r(sys.color.ohos_id_color_text_primary_activated) | value | - |

### public func space(value: Length): This

```cangjie
public func space(value: Length): This
```
功能： 根据指定的Length类型值，设置下拉菜单项的文本与箭头之间的间距。不支持设置百分比。设置为小于等于8的值，取初始值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 下拉菜单项的文本与箭头之间的间距。 初始值：8 | value | - |

### public func value(content: String): This

```cangjie
public func value(content: String): This
```
功能： 设置下拉按钮本身的文本内容。当菜单选中时默认会替换为菜单项文本内容。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 下拉按钮本身的文本内容。文本长度大于列宽时，文本被截断。 | content | - |

### public func value(content: AppResource): This

```cangjie
public func value(content: AppResource): This
```
功能： 设置下拉按钮本身的文本内容。当菜单选中时默认会替换为菜单项文本内容。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 下拉按钮本身的文本内容。文本长度大于列宽时，文本被截断。 | content | - |

### public func onSelect(callback: (Int32, String) -> Unit): This

```cangjie
public func onSelect(callback: (Int32, String) -> Unit): This
```
功能： 下拉菜单选中某一项时触发该回调。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| (Int32,String)->Unit | 是 | 选中项的索引和值。 | callback | - |


## Classes

### SelectOption

class SelectOption
```cangjie
public class SelectOption { public var value: String public var icon: String public init(value: String, icon!: String) public init(value: String, icon!: AppResource) public init(value: AppResource, icon!: String) public init(value: AppResource, icon!: AppResource) }
```
功能： 设置下拉菜单组件参数的对象。

#### var icon

```cangjie
public var icon: String
```
功能： 下拉选项图标。
Type: String; Readable: true; Writable: true

#### var value

```cangjie
public var value: String
```
功能： 下拉选项内容。
Type: String; Readable: true; Writable: true


### DividerOptions

class DividerOptions
```cangjie
public class DividerOptions { public let strokeWidth: Length public let startMargin: Length public let endMargin: Length public let color: ResourceColor public init(strokeWidth!: Length, startMargin!: Length, endMargin!: Length, color!: ResourceColor) }
```
功能： 设置分割线组件参数。

#### let color

```cangjie
public let color: ResourceColor
```
功能： 分割线的颜色。
Type: ResourceColor; Readable: true; Writable: false

#### let endMargin

```cangjie
public let endMargin: Length
```
功能： 分割线与TextPicker侧边结束端的距离（默认单位vp），也可指定单位为px，不支持百分比设置。
Type: Length; Readable: true; Writable: false

#### let startMargin

```cangjie
public let startMargin: Length
```
功能： 分割线与TextPicker侧边起始端的距离（默认单位vp），也可指定单位为px，不支持百分比设置。
Type: Length; Readable: true; Writable: false

#### let strokeWidth

```cangjie
public let strokeWidth: Length
```
功能： 分割线的线宽（默认单位vp），也可指定单位为px，不支持百分比设置。
Type: Length; Readable: true; Writable: false



## Enums

### MenuAlignType

enum MenuAlignType
```cangjie
public enum MenuAlignType { | START | CENTER | END }
```
功能： 对齐方式类型。

#### START

```cangjie
START
```
功能： 按照语言方向起始端对齐。

#### CENTER

```cangjie
CENTER
```
功能： 居中对齐。

#### END

```cangjie
END
```
功能： 按照语言方向末端对齐。



## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*
import kit.LocalizationKit.*
@Entry
@Component
class EntryView {
 @State var text: String = "TTTTT"
 @State var index: Int32 = 2
 @State var space: Int64 = 8

 @State var values1: Array<SelectOption> = [
 SelectOption("aaa", icon: @r(app.media.startIcon)),
 SelectOption("bbb", icon: @r(app.media.startIcon)),
 SelectOption("ccc", icon: @r(app.media.startIcon)),
 SelectOption("ddd", icon: @r(app.media.startIcon))]

 @State var arrow: ArrowPosition = ArrowPosition.END

 func build() {
 Column {
 Select(this.values1)
 .selected(1)
 .value(this.text)
 .font(size: 16.vp, weight: FontWeight.W500)
 .fontColor(0x182431)
 .selectedOptionFont(size: 16.vp, weight: FontWeight.W400)
 .space(this.space)
 .arrowPosition(this.arrow)
 .menuAlign(MenuAlignType.START, offset: MenuOffset(0, 0))
 .optionWidth(200)
 .optionHeight(300)
 .onSelect({ index: Int32, text: String =>
 AppLog.info(" ================== Select ====================: ${index}")
 AppLog.info(" ================== text ====================: ${text}")
 this.index = index;
 this.text = text;
 })
 }.width(100.percent)
 }
}
```
```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 func build() {
 Row {
 Column {
 Select(
 [
 SelectOption("选项1", icon: ""),
 SelectOption("选项2", icon: ""),
 SelectOption("选项3", icon: ""),
 SelectOption("选项4", icon: "")
 ]
 )
 .value("请选择")
 .divider(
 options: DividerOptions(strokeWidth: 5, startMargin: 15, endMargin: 1,
 color: Color(0XFFD700)))
 }.width(100.percent)
 }.height(100.percent)
 }
}
```
```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 func build() {
 Row {
 Column {
 Select(
 [
 SelectOption("选项1", icon: ""),
 SelectOption("选项2", icon: ""),
 SelectOption("选项3", icon: ""),
 SelectOption("选项4", icon: "")
 ]
 ).value("请选择").divider(options: Option.None)
 }.width(100.percent)
 }.height(100.percent)
 }
}
```

