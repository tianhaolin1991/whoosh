# Navigation

Navigation组件是路由导航的根视图容器，一般作为Page页面的根容器使用，其内部默认包含了标题栏、内容区和工具栏，其中内容区默认首页显示导航内容（Navigation的子组件）或非首页显示（ NavDestination 的子组件），首页和非首页通过路由进行切换。

## Component Constructors

### public init()

```cangjie
public init()
```
功能： 构造一个不包含子组件的Navigation容器。

### public init(child: () -> Unit)

```cangjie
public init(child: () -> Unit)
```
功能： 构造一个包含子组件的Navigation容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ()->Unit | 是 | Navigation容器的子组件。 | child | - |

### public init(pathInfos: NavPathStack)

```cangjie
public init(pathInfos: NavPathStack)
```
功能： 构造一个不包含子组件的Navigation容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| NavPathStack | 是 | 绑定到Navigation组件的路由栈。 | pathInfos | - |

### public init(pathInfos: NavPathStack, child: () -> Unit)

```cangjie
public init(pathInfos: NavPathStack, child: () -> Unit)
```
功能： 构造一个包含子组件的Navigation容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| NavPathStack | 是 | 绑定到Navigation组件的路由栈。 | pathInfos | - |
| ()->Unit | 是 | 一个Lambda表达式，用于接收Navigation容器的子组件。 | child | - |


## Functions

### public func navDestination(builder: (String) -> Unit): This

```cangjie
public func navDestination(builder: (String) -> Unit): This
```
功能： 创建NavDestination组件。使用builder函数，基于name构造NavDestination组件。builder下只能有一个根节点。builder中允许在NavDestination组件外包含一层自定义组件，但自定义组件不允许设置属性和事件，否则仅显示空白。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| (String)->Unit | 是 | 创建NavDestination组件。参数：NavDestination页面名称 | builder | - |


## Classes

### NavPathInfo

class NavPathInfo
```cangjie
public class NavPathInfo { public var name: String public var param: String public init(name: String, param: String) }
```
功能： 路由页面信息。

#### var name

```cangjie
public var name: String
```
功能： 表示NavDestination页面名称。
Type: String; Readable: true; Writable: true

#### var param

```cangjie
public var param: String
```
功能： 表示NavDestination页面详细参数。
Type: String; Readable: true; Writable: true


### NavPathStack

class NavPathStack
```cangjie
public class NavPathStack { public init() }
```
功能： Navigation路由栈。



## Enums

### NavigationMode

enum NavigationMode
```cangjie
public enum NavigationMode { | Stack | Split | Auto }
```
功能： 导航栏显示模式。

#### Auto

```cangjie
Auto
```
功能： 窗口宽度>=600.vp时，采用Split模式显示；窗口宽度<600.vp时，采用Stack模式显示，600.vp等于minNavBarWidth(240.vp) + minContentWidth (360.vp)。

#### Split

```cangjie
Split
```
功能： 导航栏与内容区分两栏显示。

#### Stack

```cangjie
Stack
```
功能： 导航栏与内容区独立显示，相当于两个页面。



## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Builder
func pageMap(name: String) {
 if (name == "pageOne") {
 PageOne()
 } else {
 PageTwo()
 }
}

@Entry
@Component
class EntryView {
 var pageInfos: NavPathStack = NavPathStack()
 public func build() {
 Navigation(pageInfos) {
 Button("Navigation page")
 .onClick {
 pageInfos.pushPath(NavPathInfo("pageTwo", "pageOne test"))
 }
 }
 .navDestination(bind<String>(pageMap, this))
 }
}

@Component
class PageOne {
 var pageInfos: NavPathStack = NavPathStack()

 public func build() {
 NavDestination() {
 Text("Page One")
 .onClick { evt =>
 pageInfos.pushPath(NavPathInfo("pageTwo", "pageTwo test"))
 }
 }
 .onReady { context =>
 pageInfos = context.pathStack
 }
 .onBackPressed {
 pageInfos.pop()
 true
 }
 }
}

@Component
class PageTwo {
 var pageInfos: NavPathStack = NavPathStack()

 public func build() {
 NavDestination() {
 Text("Page Two")
 .onClick { evt =>
 pageInfos.pushPath(NavPathInfo("pageOne", "pageOne test"))
 }
 }
 .onReady { context =>
 pageInfos = context.pathStack
 }
 .onBackPressed {
 pageInfos.pop()
 true
 }
 }
}
```

