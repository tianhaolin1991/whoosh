# ohos.ability（Ability）

程序框架服务提供了应用程序开发和运行的应用模型，是系统为开发者提供的应用程序所需能力的抽象提炼，它提供了应用程序必备的组件和运行机制。有了应用模型，开发者可以基于一套统一的模型进行应用开发，使应用开发更简单、高效。

```cangjie
import kit.AbilityKit.*
```
## Functions

### public func cancel(agent: WantAgent): Unit

```cangjie
public func cancel(agent: WantAgent): Unit
```
功能： 取消WantAgent实例。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| WantAgent | WantAgent对象。 | agent | 是 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 元能力子系统错误码 。

| code | description |
| --- | --- |
| 401 | Parameter error. |
| 16000007 | Service busy. There are concurrent tasks. Try again later. |
| 16000151 | Invalid wantagent object. |


**Example:**

```cangjie
// index.cj

import kit.AbilityKit.*

let wantAgentInfo = WantAgentInfo(
 wants: [Want(bundleName: "com.example.myapplication", abilityName: "EntryAbility")],
 actionType: START_ABILITIES, requestCode: 0, actionFlags: [UPDATE_PRESENT_FLAG])
let wantAgent = getWantAgent(wantAgentInfo)
let uid = cancel(wantAgent)
```

### public func createModuleContext(context: Context, moduleName: String): Context

```cangjie
public func createModuleContext(context: Context, moduleName: String): Context
```
功能： 根据入参context创建相应模块的 Context 。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| Context | 表示应用上下文。 | context | 是 |
| String | 表示应用模块名。 | moduleName | 是 |

**Returns:**

| type | description |
| --- | --- |
| Context | 返回创建的Context。 |

**Example:**

```cangjie
// ability_stage.cj

import ohos.base.*
import kit.AbilityKit.*

class MyAbilityStage <: AbilityStage {
 public override func onCreate(): Unit {
 AppLog.info("MyAbilityStage onCreated.")
 let hapInfo = this.context.currentHapModuleInfo
 try {
 let ctx = createModuleContext(this.context, hapInfo.name)
 } catch (e: BusinessException) {
 AppLog.info("MyAbilityStage create moduleContext failed")
 }
 AppLog.info("MyAbilityStage create ctx success")
 }
}
```

### public func enableAppRecovery(restart!: RestartFlag = ALWAYS_RESTART, saveOccasion!: SaveOccasionFlag = SAVE_WHEN_ERROR, saveMode!: SaveModeFlag = SAVE_WITH_FILE): Unit

```cangjie
public func enableAppRecovery(restart!: RestartFlag = ALWAYS_RESTART, saveOccasion!: SaveOccasionFlag = SAVE_WHEN_ERROR, saveMode!: SaveModeFlag = SAVE_WITH_FILE): Unit
```
功能： 使能应用恢复功能，参数按顺序填入。该接口调用后，应用从启动器启动时第一个Ability支持恢复。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| RestartFlag | 否 | 命名参数。 发生对应故障时是否重启。 | restart | ALWAYS_RESTART |
| SaveOccasionFlag | 否 | 命名参数。 状态保存时机。 | saveOccasion | SAVE_WHEN_ERROR |
| SaveModeFlag | 否 | 命名参数。 状态保存方式。 | saveMode | SAVE_WITH_FILE |

**Example:**

```cangjie
// ability_stage.cj

import ohos.base.*
import kit.AbilityKit.*

class MyAbilityStage <: AbilityStage {
 public override func onCreate(): Unit {
 AppLog.info("MyAbilityStage onCreated.")
 enableAppRecovery()
 }
}
```

### public func equal(agent: WantAgent, otherAgent: WantAgent): Bool

```cangjie
public func equal(agent: WantAgent, otherAgent: WantAgent): Bool
```
功能： 判断两个WantAgent实例是否相等，以此来判断是否是来自同一应用的相同操作。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| WantAgent | WantAgent对象。 | agent | 是 |
| WantAgent | WantAgent对象。 | otherAgent | 是 |

**Returns:**

| type | description |
| --- | --- |
| Bool | 返回true表示两个WantAgent实例相等，false表示两个WantAgent实例不相等。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 401 | Parameter error. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.AbilityKit.*

let wantAgentInfo1 = WantAgentInfo(
 wants: [Want(deviceId: "deviceId", bundleName: "com.example.myapplication",
 abilityName: "EntryAbility")], actionType: START_ABILITIES, requestCode: 0,
 actionFlags: [UPDATE_PRESENT_FLAG])
let wantAgent1 = getWantAgent(wantAgentInfo1)
let wantAgentInfo2 = WantAgentInfo(
 wants: [Want(deviceId: "deviceId", bundleName: "com.example.myapplication",
 abilityName: "EntryAbility")], actionType: START_ABILITIES, requestCode: 0,
 actionFlags: [UPDATE_PRESENT_FLAG])
let wantAgent2 = getWantAgent(wantAgentInfo2)
let isEqual = equal(wantAgent1, wantAgent2)
AppLog.info("isEqual is ${isEqual}")
```

### public func getAppMemorySize(): Int32

```cangjie
public func getAppMemorySize(): Int32
```
功能： 获取当前应用程序可以使用的内存的值。

**Returns:**

| type | description |
| --- | --- |
| Int32 | 获取当前应用程序可以使用的内存的值，可根据此值进行错误处理或其他自定义处理，单位是M。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 元能力子系统错误码 。

| code | description |
| --- | --- |
| 16000050 | Internal error. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.AbilityKit.*

let value = getAppMemorySize()
AppLog.info("getAppMemorySize = ${value}")
```

### public func getBundleName(agent: WantAgent): String

```cangjie
public func getBundleName(agent: WantAgent): String
```
功能： 获取WantAgent实例的包名。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| WantAgent | WantAgent对象。 | agent | 是 |

**Returns:**

| type | description |
| --- | --- |
| String | 获取WantAgent实例的包名。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 元能力子系统错误码 。

| code | description |
| --- | --- |
| 401 | Parameter error. |
| 16000007 | Service busy. There are concurrent tasks. Try again later. |
| 16000151 | Invalid wantagent object. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.AbilityKit.*

let wantAgentInfo = WantAgentInfo(
 wants: [Want(bundleName: "com.example.myapplication", abilityName: "EntryAbility")],
 actionType: START_ABILITIES, requestCode: 0, actionFlags: [UPDATE_PRESENT_FLAG])
let wantAgent = getWantAgent(wantAgentInfo)
let boundleName = getBundleName(wantAgent)
AppLog.info("boundleName is ${boundleName}")
```

### public func getNapiValue(env: CPointer<Unit>, context: StageContext): CPointer<Unit>

```cangjie
public func getNapiValue(env: CPointer<Unit>, context: StageContext): CPointer<Unit>
```
功能： 根据上下文环境获取napi_value。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| CPointer<Unit> | 环境指针。 | env | 是 |
| StageContext | 上下文信息。 | context | 是 |

**Returns:**

| type | description |
| --- | --- |
| CPointer<Unit> | 返回napi_value信息。 |

### public func getOperationType(agent: WantAgent): OperationType

```cangjie
public func getOperationType(agent: WantAgent): OperationType
```
功能： 获取一个WantAgent的 OperationType 信息。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| WantAgent | WantAgent对象。 | agent | 是 |

**Returns:**

| type | description |
| --- | --- |
| OperationType | 返回获取OperationType的结果。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 元能力子系统错误码 。

| code | description |
| --- | --- |
| 401 | Parameter error. |
| 16000007 | Service busy. There are concurrent tasks. Try again later. |
| 16000015 | Service timeout. |
| 16000151 | Invalid wantagent object. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.AbilityKit.*

let wantAgentInfo = WantAgentInfo(
 wants: [Want(deviceId: "deviceId", bundleName: "com.example.myapplication", abilityName: "EntryAbility")],
 actionType: OperationType.START_ABILITY, requestCode: 0, actionFlags: [UPDATE_PRESENT_FLAG])
let wantAgent = getWantAgent(wantAgentInfo)
let opType = getOperationType(wantAgent)
AppLog.info("OperationType is ${opType}")
```

### public func getRunningProcessInformation(): Array<ProcessInformation>

```cangjie
public func getRunningProcessInformation(): Array<ProcessInformation>
```
功能： 获取当前运行进程的有关信息。

**Returns:**

| type | description |
| --- | --- |
| Array< ProcessInformation > | 返回有关运行进程的信息，可进行错误处理或其他自定义处理。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 元能力子系统错误码 。

| code | description |
| --- | --- |
| 16000050 | Internal error. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.AbilityKit.*

let pInfos = getRunningProcessInformation()
AppLog.info("pInfoNum = ${pInfos.size}")
for (pInfo in pInfos) {
 AppLog.info("pid = ${pInfo.pid}, uid=${pInfo.uid}, procName=${pInfo.processName}, state=${pInfo.state}, appCloneIndex=${pInfo.appCloneIndex}")
 AppLog.info("bundleNameNum = ${pInfo.bundleNames.size}")
 for (name in pInfo.bundleNames) {
 AppLog.info("bundleName = ${name}")
 }
}
```

### public func getStageContext(abilityContext: UIAbilityContext): StageContext

```cangjie
public func getStageContext(abilityContext: UIAbilityContext): StageContext
```
功能： 将UIAbility上下文转换成StageContext类型，StageContext是CPointer<Unit> 类型的别名。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| UIAbilityContext | 仓颉上下文信息。 | abilityContext | 是 |

**Returns:**

| type | description |
| --- | --- |
| StageContext | StageContext是CPointer<Unit> 类型的别名。 |

### public func getUid(agent: WantAgent): Int32

```cangjie
public func getUid(agent: WantAgent): Int32
```
功能： 获取WantAgent实例的用户ID。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| WantAgent | WantAgent对象。 | agent | 是 |

**Returns:**

| type | description |
| --- | --- |
| Int32 | 获取WantAgent实例的用户ID。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 元能力子系统错误码 。

| code | description |
| --- | --- |
| 401 | Parameter error. |
| 16000007 | Service busy. There are concurrent tasks. Try again later. |
| 16000151 | Invalid wantagent object. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.AbilityKit.*

let wantAgentInfo = WantAgentInfo(
 wants: [Want(bundleName: "com.example.myapplication", abilityName: "EntryAbility")],
 actionType: START_ABILITIES, requestCode: 0, actionFlags: [UPDATE_PRESENT_FLAG])
let wantAgent = getWantAgent(wantAgentInfo)
let uid = getUid(wantAgent)
AppLog.info("uid is ${uid}")
```

### public func getWantAgent(info: WantAgentInfo): WantAgent

```cangjie
public func getWantAgent(info: WantAgentInfo): WantAgent
```
功能： 创建WantAgent。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| WantAgentInfo | WantAgent信息。 | info | 是 |

**Returns:**

| type | description |
| --- | --- |
| WantAgent | 创建的WantAgent。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 元能力子系统错误码 。

| code | description |
| --- | --- |
| 401 | Parameter error. |
| 16000007 | Service busy. There are concurrent tasks. Try again later. |
| 16000151 | Invalid wantagent object. |


**Example:**

```cangjie
// index.cj

import kit.AbilityKit.*

let wantAgentInfo = WantAgentInfo(
 wants: [Want(bundleName: "com.example.myapplication", abilityName: "EntryAbility")],
 actionType: START_ABILITIES, requestCode: 0, actionFlags: [UPDATE_PRESENT_FLAG])
let wantAgent = getWantAgent(wantAgentInfo)
```

### public func isRamConstrainedDevice(): Bool

```cangjie
public func isRamConstrainedDevice(): Bool
```
功能： 查询是否为ram受限设备。

**Returns:**

| type | description |
| --- | --- |
| Bool | true：当前设备为ram受限设备，false：当前设备为非ram受限设备。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 元能力子系统错误码 。

| code | description |
| --- | --- |
| 16000050 | Internal error. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.AbilityKit.*

let value = isRamConstrainedDevice()
AppLog.info("isRamConstrainedDevice = ${value}")
```

### public func isRunningInStabilityTest(): Bool

```cangjie
public func isRunningInStabilityTest(): Bool
```
功能： 查询当前是否处于稳定性测试场景。

**Returns:**

| type | description |
| --- | --- |
| Bool | true: 处于稳定性测试场景，false：处于非稳定性测试场景。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 元能力子系统错误码 。

| code | description |
| --- | --- |
| 16000050 | Internal error. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.AbilityKit.*

let value = isRunningInStabilityTest()
AppLog.info("isRunningInStabilityTest = ${value}")
```

### public func restartApp(): Unit

```cangjie
public func restartApp(): Unit
```
功能： 重启当前进程，并拉起应用启动时第一个Ability，如果该Ability存在已经保存的状态，这些状态数据会在Ability的OnCreate生命周期回调的want参数中作为wantParam属性传入。

**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.AbilityKit.*

AppLog.info("restartApp")
restartApp()
```

### public func saveAppState(context!: ?UIAbilityContext = None): Bool

```cangjie
public func saveAppState(context!: ?UIAbilityContext = None): Bool
```
功能： 保存当前App状态，可以配合 ErrorManager 相关接口使用。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ? UIAbilityContext | 否 | 命名参数。 需要保存状态的Ability所对应的context。 | context | None |

**Returns:**

| type | description |
| --- | --- |
| Bool | true：保存成功，false：保存失败。 |

**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.AbilityKit.*

AppLog.info("saveAppState")
saveAppState()
```

### public func setRestartWant(want: Want): Unit

```cangjie
public func setRestartWant(want: Want): Unit
```
功能： 设置下次恢复主动拉起场景下的Ability。该Ability必须为当前包下的Ability。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Want | 是 | 通过设置Want中"bundleName"和"abilityName"字段来指定恢复重启的Ability。 | want | - |

**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.AbilityKit.*

let want = Want(bundleName: "com.example.apprecoverytest", abilityName: "EntryAbility")
setRestartWant(want)
```

### public func trigger(agent: WantAgent, triggerInfo: TriggerInfo, callback!: (CompleteData) -> Unit = {_: CompleteData => ()}): Unit

```cangjie
public func trigger(agent: WantAgent, triggerInfo: TriggerInfo, callback!: (CompleteData) -> Unit = {_: CompleteData => ()}): Unit
```
功能： 主动激发WantAgent实例（callback形式）。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| WantAgent | 是 | WantAgent对象。 | agent | - |
| TriggerInfo | 是 | TriggerInfo对象。 | triggerInfo | - |
| ( CompleteData )->Unit | 否 | 命名参数。 主动激发WantAgent实例的回调方法。 | callback | { _: CompleteData =>() } |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 401 | Parameter error. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.AbilityKit.*

let wantAgentInfo = WantAgentInfo(
 wants: [Want(deviceId: "deviceId", bundleName: "com.example.myapplication",
 abilityName: "EntryAbility")], actionType: START_ABILITIES,
 requestCode: 0, actionFlags: [UPDATE_PRESENT_FLAG])
let wantAgent = getWantAgent(wantAgentInfo)
let triggerInfo = TriggerInfo(code: 2)
let callback = {
 value: CompleteData =>
 AppLog.info("wantAgentTrigger: ${value.want.deviceId}")
 AppLog.info("wantAgentTrigger: ${value.want.bundleName}")
 AppLog.info("wantAgentTrigger: ${value.want.parameters}")
 AppLog.info("wantAgentTrigger: ${value.finalCode}")
 AppLog.info("wantAgentTrigger: ${value.finalData}")
}
trigger(wantAgent, triggerInfo, callback: callback)
```


## Classes

### AbilityConfiguration

class AbilityConfiguration
```cangjie
public class AbilityConfiguration { public AbilityConfiguration ( public var language: String, public var colorMode: ConfigurationColorMode, public var direction: ConfigurationDirection, public var screenDensity: ConfigurationScreenDensity, public var displayId: Int32, public var hasPointerDevice: Bool, public var fontSizeScale: Float64, public var fontWeightScale: Float64, public var mcc: String, public var mnc: String ) }
```
功能： 定义环境变化信息的类。

#### var colorMode

```cangjie
public var colorMode: ConfigurationColorMode
```
功能： 表示深浅色模式，默认为浅色。取值范围：
Type: ConfigurationColorMode; Readable: true; Writable: true

#### var direction

```cangjie
public var direction: ConfigurationDirection
```
功能： 表示屏幕方向，取值范围：
Type: ConfigurationDirection; Readable: true; Writable: true

#### var displayId

```cangjie
public var displayId: Int32
```
功能： 表示应用所在的物理屏幕ID。
Type: Int32; Readable: true; Writable: true

#### var fontSizeScale

```cangjie
public var fontSizeScale: Float64
```
功能： 字体大小缩放比例，取值为非负数，默认值为1。
Type: Float64; Readable: true; Writable: true

#### var fontWeightScale

```cangjie
public var fontWeightScale: Float64
```
功能： 字体粗细缩放比例，取值为非负数，默认值为1。
Type: Float64; Readable: true; Writable: true

#### var hasPointerDevice

```cangjie
public var hasPointerDevice: Bool
```
功能： 指示指针类型设备是否已连接，如键鼠、触控板等。
Type: Bool; Readable: true; Writable: true

#### var language

```cangjie
public var language: String
```
功能： 表示应用程序的当前语言，例如“zh"。
Type: String; Readable: true; Writable: true

#### var mcc

```cangjie
public var mcc: String
```
功能： 移动设备国家代码。
Type: String; Readable: true; Writable: true

#### var mnc

```cangjie
public var mnc: String
```
功能： 移动设备网络代码。
Type: String; Readable: true; Writable: true

#### var screenDensity

```cangjie
public var screenDensity: ConfigurationScreenDensity
```
功能： 表示屏幕像素密度，取值范围：
Type: ConfigurationScreenDensity; Readable: true; Writable: true


### AbilityLifecycleCallback

class AbilityLifecycleCallback
```cangjie
public class AbilityLifecycleCallback { public AbilityLifecycleCallback( public let onAbilityCreate!: (UIAbility) -> Unit, public let onWindowStageCreate!: (UIAbility, WindowStage) -> Unit, public let onWindowStageActive!: (UIAbility, WindowStage) -> Unit, public let onWindowStageInactive!: (UIAbility, WindowStage) -> Unit, public let onWindowStageDestroy!: (UIAbility, WindowStage) -> Unit, public let onAbilityDestroy!: (UIAbility) -> Unit, public let onAbilityForeground!: (UIAbility) -> Unit, public let onAbilityBackground!: (UIAbility) -> Unit, public let onAbilityContinue!: (UIAbility) -> Unit, public let onAbilityWillCreate!: ?(UIAbility) -> Unit = None, public let onWindowStageWillCreate!: ?(UIAbility, WindowStage) -> Unit = None, public let onWindowStageWillDestroy!: ?(UIAbility, WindowStage) -> Unit = None, public let onAbilityWillForeground!: ?(UIAbility) -> Unit = None, public let onAbilityWillDestroy!: ?(UIAbility) -> Unit = None, public let onAbilityWillBackground!: ?(UIAbility) -> Unit = None, public let onWillNewWant!: ?(UIAbility) -> Unit = None, public let onNewWant!: ?(UIAbility) -> Unit = None, public let onAbilityWillContinue!: ?(UIAbility) -> Unit = None, public let onWindowStageWillRestore!: ?(UIAbility, WindowStage) -> Unit = None, public let onWindowStageRestore!: ?(UIAbility, WindowStage) -> Unit = None, public let onAbilityWillSaveState!: ?(UIAbility) -> Unit = None, public let onAbilitySaveState!: ?(UIAbility) -> Unit = None ) }
```
功能： AbilityLifecycleCallback类提供应用上下文的生命周期发生变化时触发相应回调的能力。

#### let onAbilityBackground

```cangjie
public let onAbilityBackground: (UIAbility) -> Unit
```
功能： 注册监听应用上下文的生命周期后，在ability的状态从前台转到后台时触发回调。
Type: ( UIAbility )->Unit; Readable: true; Writable: false

#### let onAbilityContinue

```cangjie
public let onAbilityContinue: (UIAbility) -> Unit
```
功能： 注册监听应用上下文的生命周期后，在ability迁移时触发回调。
Type: ( UIAbility )->Unit; Readable: true; Writable: false

#### let onAbilityCreate

```cangjie
public let onAbilityCreate: (UIAbility) -> Unit
```
功能： 注册监听应用上下文的生命周期后，在ability创建时触发回调。
Type: ( UIAbility )->Unit; Readable: true; Writable: false

#### let onAbilityDestroy

```cangjie
public let onAbilityDestroy: (UIAbility) -> Unit
```
功能： 注册监听应用上下文的生命周期后，在ability销毁时触发回调。
Type: ( UIAbility )->Unit; Readable: true; Writable: false

#### let onAbilityForeground

```cangjie
public let onAbilityForeground: (UIAbility) -> Unit
```
功能： 注册监听应用上下文的生命周期后，在ability的状态从后台转到前台时触发回调。
Type: ( UIAbility )->Unit; Readable: true; Writable: false

#### let onAbilitySaveState

```cangjie
public let onAbilitySaveState: ?(UIAbility) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的onSaveState触发后回调。
Type: ?( UIAbility )->Unit; Readable: true; Writable: false

#### let onAbilityWillBackground

```cangjie
public let onAbilityWillBackground: ?(UIAbility) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的 onBackground 触发前回调。
Type: ?( UIAbility )->Unit; Readable: true; Writable: false

#### let onAbilityWillContinue

```cangjie
public let onAbilityWillContinue: ?(UIAbility) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的onContinue触发前回调。
Type: ?( UIAbility )->Unit; Readable: true; Writable: false

#### let onAbilityWillCreate

```cangjie
public let onAbilityWillCreate: ?(UIAbility) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的 onCreate 触发前回调。
Type: ?( UIAbility )->Unit; Readable: true; Writable: false

#### let onAbilityWillDestroy

```cangjie
public let onAbilityWillDestroy: ?(UIAbility) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的 onDestroy 触发前回调。
Type: ?( UIAbility )->Unit; Readable: true; Writable: false

#### let onAbilityWillForeground

```cangjie
public let onAbilityWillForeground: ?(UIAbility) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的 onForeground 触发前回调。
Type: ?( UIAbility )->Unit; Readable: true; Writable: false

#### let onAbilityWillSaveState

```cangjie
public let onAbilityWillSaveState: ?(UIAbility) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的onSaveState触发前回调。
Type: ?( UIAbility )->Unit; Readable: true; Writable: false

#### let onNewWant

```cangjie
public let onNewWant: ?(UIAbility) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的 onNewWant 触发后回调。
Type: ?( UIAbility )->Unit; Readable: true; Writable: false

#### let onWillNewWant

```cangjie
public let onWillNewWant: ?(UIAbility) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的 onNewWant 触发前回调。
Type: ?( UIAbility )->Unit; Readable: true; Writable: false

#### let onWindowStageActive

```cangjie
public let onWindowStageActive: (UIAbility, WindowStage) -> Unit
```
功能： 注册监听应用上下文的生命周期后，在windowStage获焦时触发回调。
Type: ( UIAbility , WindowStage )->Unit; Readable: true; Writable: false

#### let onWindowStageCreate

```cangjie
public let onWindowStageCreate: (UIAbility, WindowStage) -> Unit
```
功能： 注册监听应用上下文的生命周期后，在windowStage创建时触发回调。
Type: ( UIAbility , WindowStage )->Unit; Readable: true; Writable: false

#### let onWindowStageDestroy

```cangjie
public let onWindowStageDestroy: (UIAbility, WindowStage) -> Unit
```
功能： 注册监听应用上下文的生命周期后，在windowStage销毁时触发回调。
Type: ( UIAbility , WindowStage )->Unit; Readable: true; Writable: false

#### let onWindowStageInactive

```cangjie
public let onWindowStageInactive: (UIAbility, WindowStage) -> Unit
```
功能： 注册监听应用上下文的生命周期后，在windowStage失焦时触发回调。
Type: ( UIAbility , WindowStage )->Unit; Readable: true; Writable: false

#### let onWindowStageRestore

```cangjie
public let onWindowStageRestore: ?(UIAbility, WindowStage) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的onWindowStageRestore触发后回调。
Type: ?( UIAbility , WindowStage )->Unit; Readable: true; Writable: false

#### let onWindowStageWillCreate

```cangjie
public let onWindowStageWillCreate: ?(UIAbility, WindowStage) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的 onWindowStageCreate 触发前回调。
Type: ?( UIAbility , WindowStage )->Unit; Readable: true; Writable: false

#### let onWindowStageWillDestroy

```cangjie
public let onWindowStageWillDestroy: ?(UIAbility, WindowStage) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的 onWindowStageDestroy 触发前回调。
Type: ?( UIAbility , WindowStage )->Unit; Readable: true; Writable: false

#### let onWindowStageWillRestore

```cangjie
public let onWindowStageWillRestore: ?(UIAbility, WindowStage) -> Unit = None
```
功能： 注册监听应用上下文的生命周期后，在Ability的onWindowStageRestore触发前回调。
Type: ?( UIAbility , WindowStage )->Unit; Readable: true; Writable: false


### AbilityStage

class AbilityStage
```cangjie
public open class AbilityStage {}
```
功能： AbilityStage类提供在HAP加载的时候，通知开发者，可以在此进行该HAP的初始化（如资源预加载，线程创建等）能力。

#### prop context

```cangjie
public prop context: AbilityStageContext
```
类型： AbilityStageContext
Type: AbilityStageContext; Readable: true; Writable: false


### AbilityStageContext

class AbilityStageContext
```cangjie
public class AbilityStageContext <: Context { public var hapModuleInfo: Option<CurrentHapInfo> = None }
```
功能： AbilityStageContext 提供允许访问特定于 abilityStage 的资源的能力，包括获取 AbilityStage 对应的 hapModuleInfo 对象、环境变化对象。

#### prop config

```cangjie
public prop config: AbilityConfiguration
```
功能： 环境变化对象。
Type: AbilityConfiguration; Readable: true; Writable: false

#### prop currentHapModuleInfo

```cangjie
public prop currentHapModuleInfo: HapModuleInfo
```
功能： AbilityStage 对应的 hapModuleInfo 对象。
Type: HapModuleInfo; Readable: true; Writable: false

#### var hapModuleInfo

```cangjie
public var hapModuleInfo: Option<CurrentHapInfo> = None
```
功能： Hap模块信息。
Type: Option< CurrentHapInfo >; Readable: true; Writable: true


### AbilityStartCallback

class AbilityStartCallback
```cangjie
public class AbilityStartCallback { public AbilityStartCallback( public let onError: (Int32, String, String) -> Unit, public let onResult!: ?(AbilityResult) -> Unit = None ) }
```
功能： 定义拉起UIExtensionAbility执行结果的回调。

#### let onError

```cangjie
public let onError: (Int32, String, String) -> Unit
```
功能： 拉起UIExtensionAbility执行失败的回调。
Type: (Int32, String, String)->Unit; Readable: true; Writable: false

#### let onResult

```cangjie
public let onResult: ?(AbilityResult) -> Unit = None
```
功能： 拉起UIExtensionAbility终止时的回调。
Type: ?( AbilityResult )->Unit; Readable: true; Writable: false


### ActionExtensionAbility

class ActionExtensionAbility
```cangjie
public open class ActionExtensionAbility <: UIExtensionAbility {}
```
功能： ActionExtensionAbility是为开发者提供的自定义操作业务模板，继承自 UIExtensionAbility 。ActionExtensionAbility主要用于查看宿主应用中的内容以及对其进行对应处理。


### ApplicationContext

class ApplicationContext
```cangjie
public class ApplicationContext <: Context {}
```
功能： 提供应用级别的的上下文的能力。


### ApplicationStateChangeCallback

class ApplicationStateChangeCallback
```cangjie
public class ApplicationStateChangeCallback { public ApplicationStateChangeCallback( public let onApplicationForeground!: () -> Unit, public let onApplicationBackground!: () -> Unit ) }
```
功能： ApplicationStateChangeCallback类提供应用上下文提供应用上下文 ApplicationContext 对当前应用前后台变化监听回调的能力。

#### let onApplicationBackground

```cangjie
public let onApplicationBackground:() -> Unit
```
功能： 注册当前应用前后台变化的监听后，在当前应用从前台切换到后台时触发回调。
Type: ()->Unit; Readable: true; Writable: false

#### let onApplicationForeground

```cangjie
public let onApplicationForeground:() -> Unit
```
功能： 注册当前应用前后台变化的监听后，在当前应用从后台切换到前台时触发回调。
Type: ()->Unit; Readable: true; Writable: false


### AtomicServiceOptions

class AtomicServiceOptions
```cangjie
public class AtomicServiceOptions <: StartOptions { public var flags: Int32 = 0 public var parameters: String = "" public init() }
```
功能： 作为 openAtomicService() 的入参，用于携带参数。

#### var flags

```cangjie
public var flags: Int32
```
功能： 系统处理该次启动的方式。 例如通过 wantConstant.Flags.FLAG_INSTALL_ON_DEMAND 表示使用免安装能力。
Type: Int32; Readable: true; Writable: true

#### var parameters

```cangjie
public var parameters: String
```
功能： 表示额外参数描述。具体描述参考 Want 中 parameters 字段描述。
Type: String; Readable: true; Writable: true


### BaseAbility

class BaseAbility
```cangjie
sealed abstract class BaseAbility {}
```
功能： UIAbility 和 ExtensionAbility 的基类，提供系统配置更新回调和系统内存调整回调。不支持开发者直接继承该基类。


### Callee

class Callee
```cangjie
public class Callee {}
```
功能： 通用组件服务端注册和解除客户端caller通知送信的callback接口。


### CalleeCallback

class CalleeCallback
```cangjie
public class CalleeCallback <: Callback1ArgumentWithReturn<MessageSequence, Parcelable> { public CalleeCallback(let callback: (MessageSequence) -> Parcelable) }
```
功能： 通用组件服务端注册消息通知的回调函数类型。


### Caller

class Caller
```cangjie
public class Caller {}
```
功能： 通用组件Caller通信客户端调用接口, 用来向通用组件服务端发送约定数据。


### CompleteData

class CompleteData
```cangjie
public class CompleteData { public CompleteData( public let info!: WantAgent, public let want!: Want, public let finalCode!: Int32, public let finalData!: String, public let extraInfo!: String ) }
```
功能： 表示主动激发WantAgent返回的数据。

#### let extraInfo

```cangjie
public let extraInfo: String
```
功能： 额外数据。Json字符串。
Type: String; Readable: true; Writable: false

#### let finalCode

```cangjie
public let finalCode: Int32
```
功能： 触发wantAgent的请求代码。
Type: Int32; Readable: true; Writable: false

#### let finalData

```cangjie
public let finalData: String
```
功能： 公共事件收集的最终数据。
Type: String; Readable: true; Writable: false

#### let info

```cangjie
public let info: WantAgent
```
功能： 触发的wantAgent。
Type: WantAgent; Readable: true; Writable: false

#### let want

```cangjie
public let want: Want
```
功能： 存在的被触发的want。
Type: Want; Readable: true; Writable: false


### ConnectOptions

class ConnectOptions
```cangjie
public class ConnectOptions { public var onConnect: (ElementName, IRemoteObject) -> Unit public var onDisconnect: (ElementName) -> Unit public var onFailed: (Int32) -> Unit public init( onDisconnect: (ElementName) -> Unit, onFailed: (Int32) -> Unit ) public init( onConnect: (ElementName, IRemoteObject) -> Unit, onDisconnect: (ElementName) -> Unit, onFailed: (Int32) -> Unit ) }
```
功能： 在连接指定的后台服务时作为入参用于接收连接过程中的状态变化。

#### var onConnect

```cangjie
public var onConnect: (ElementName, IRemoteObject) -> Unit
```
功能： 建立连接时的回调函数。
Type: ( ElementName , IRemoteObject ) -> Unit; Readable: true; Writable: true

#### var onDisconnect

```cangjie
public var onDisconnect: (ElementName) -> Unit
```
功能： 断开连接时的回调函数。
Type: ( ElementName ) -> Unit; Readable: true; Writable: true

#### var onFailed

```cangjie
public var onFailed: (Int32) -> Unit
```
功能： 连接失败时的回调函数。
Type: (Int32)->Unit; Readable: true; Writable: true


### Context

class Context
```cangjie
public open class Context { public let eventhub = EventHub() }
```
功能： 提供ability或application的上下文的能力。

#### prop applicationInfo

```cangjie
public prop applicationInfo: ApplicationInfo
```
功能： 当前应用程序的信息。
Type: ApplicationInfo; Readable: true; Writable: false

#### prop area

```cangjie
public prop area: AreaMode
```
功能： 文件分区信息。
Type: AreaMode; Readable: true; Writable: false

#### prop bundleCodeDir

```cangjie
public prop bundleCodeDir: String
```
功能： 安装包目录。不能拼接路径访问资源文件，请使用 资源管理接口 访问资源。
Type: String; Readable: true; Writable: false

#### prop cacheDir

```cangjie
public prop cacheDir: String
```
功能： 缓存目录。
Type: String; Readable: true; Writable: false

#### prop cloudFileDir

```cangjie
public prop cloudFileDir: String
```
功能： 云文件目录。
Type: String; Readable: true; Writable: false

#### prop databaseDir

```cangjie
public prop databaseDir: String
```
功能： 数据库目录。
Type: String; Readable: true; Writable: false

#### prop distributedFilesDir

```cangjie
public prop distributedFilesDir: String
```
功能： 分布式文件目录。
Type: String; Readable: true; Writable: false

#### prop filesDirectory

```cangjie
public prop filesDirectory: String
```
功能： 文件目录。
Type: String; Readable: true; Writable: false

#### prop preferencesDir

```cangjie
public prop preferencesDir: String
```
功能： preferences目录。
Type: String; Readable: true; Writable: false

#### prop resourceDir

```cangjie
public prop resourceDir: String
```
功能： 资源目录。
Type: String; Readable: true; Writable: false

#### prop resourceManager

```cangjie
public prop resourceManager: ResourceManager
```
功能： 资源管理对象。
Type: ResourceManager; Readable: true; Writable: false

#### prop tempDir

```cangjie
public prop tempDir: String
```
功能： 临时目录。
Type: String; Readable: true; Writable: false

#### let eventhub

```cangjie
public let eventhub = EventHub()
```
功能： 事件中心，提供订阅、取消订阅、触发事件对象。
Type: EventHub; Readable: true; Writable: false


### CurrentHapInfo

class CurrentHapInfo
```cangjie
public class CurrentHapInfo {}
```
功能： 表示当前Hap的信息。

#### prop description

```cangjie
public prop description: String
```
功能： 模块描述信息。
Type: String; Readable: true; Writable: false

#### prop descriptionId

```cangjie
public prop descriptionId: Int32
```
功能： 描述信息的资源Id。
Type: Int32; Readable: true; Writable: false

#### prop hashValue

```cangjie
public prop hashValue: String
```
功能： 模块的Hash值。
Type: String; Readable: true; Writable: false

#### prop icon

```cangjie
public prop icon: String
```
功能： 模块图标。
Type: String; Readable: true; Writable: false

#### prop iconId

```cangjie
public prop iconId: Int32
```
功能： 模块图标的资源Id。
Type: Int32; Readable: true; Writable: false

#### prop installationFree

```cangjie
public prop installationFree: Bool
```
功能： 是否支持免安装。
Type: Bool; Readable: true; Writable: false

#### prop label

```cangjie
public prop label: String
```
功能： 模块标签。
Type: String; Readable: true; Writable: false

#### prop labelId

```cangjie
public prop labelId: Int32
```
功能： 模块标签的资源id值。
Type: Int32; Readable: true; Writable: false

#### prop mainElementName

```cangjie
public prop mainElementName: String
```
功能： 入口ability信息。
Type: String; Readable: true; Writable: false

#### prop name

```cangjie
public prop name: String
```
功能： 模块名称。
Type: String; Readable: true; Writable: false


### DataUriUtils

class DataUriUtils
```cangjie
public class DataUriUtils {}
```
功能： 提供uri处理方法的类。


### DialogRequestResult

class DialogRequestResult
```cangjie
public class DialogRequestResult {}
```
功能： Diaglog请求结果对象，在调用requestDialogService时返回此对象表明此次申请的结果。

#### var resultCode

```cangjie
public var resultCode: ResultCode
```
功能： 此次请求的结果码。
Type: ResultCode; Readable: true; Writable: true

#### var want

```cangjie
public var want:?Want
```
功能： 请求的want信息。
Type: ? Want; Readable: true; Writable: true


### ElementName

class ElementName
```cangjie
public class ElementName { public init(deviceId: String, bundleName: String, abilityName: String, moduleName: String) public init(deviceId: String, bundleName: String, abilityName: String) }
```
功能： ElementName信息。

#### prop abilityName

```cangjie
public prop abilityName: String
```
功能： Ability名称。
Type: String; Readable: true; Writable: false

#### prop bundleName

```cangjie
public prop bundleName: String
```
功能： 应用Bundle名称。
Type: String; Readable: true; Writable: false

#### prop deviceId

```cangjie
public prop deviceId: String
```
功能： 设备ID。
Type: String; Readable: true; Writable: false

#### prop moduleName

```cangjie
public prop moduleName: String
```
功能： Ability所属的HAP的模块名称。
Type: String; Readable: true; Writable: false


### EmbeddableUIAbility

class EmbeddableUIAbility
```cangjie
public class EmbeddableUIAbility <: UIAbility {}
```
功能： EmbeddableUIAbility 是为原子化服务提供可以嵌入式启动的Ability，同时具备跳转启动和嵌入式启动两种启动方式。


### EmbeddableUIAbilityContext

class EmbeddableUIAbilityContext
```cangjie
public class EmbeddableUIAbilityContext <: UIAbilityContext {}
```
功能： 提供EmbeddableUIAbility的相关配置信息以及操作EmbeddableUIAbility和ServiceExtensionAbility的方法，如启动EmbeddableUIAbility，停止当前EmbeddableUIAbilityContext所属的EmbeddableUIAbility，启动、停止、连接、断开连接ServiceExtensionAbility等。


### EmbeddedUIExtensionAbility

class EmbeddedUIExtensionAbility
```cangjie
public open class EmbeddedUIExtensionAbility <: UIExtensionAbility {}
```
功能： EmbeddedUIExtensionAbility为开发者提供了跨进程界面嵌入的能力，继承自 UIExtensionAbility 。目前EmbeddedUIExtensionAbility只能被同应用的UIAbility拉起，并且仅允许在拥有多进程权限的场景下使用。


### EnvironmentCallback

class EnvironmentCallback
```cangjie
public class EnvironmentCallback { public EnvironmentCallback( public let onConfigurationUpdated!: (AbilityConfiguration) -> Unit, public let onMemoryLevel!: (MemoryLevel) -> Unit ) }
```
功能： EnvironmentCallback类提供应用上下文 ApplicationContext 对系统环境变化监听回调的能力。

#### let onConfigurationUpdated

```cangjie
public let onConfigurationUpdated:(AbilityConfiguration) -> Unit
```
功能： 注册系统环境变化的监听后，在系统环境变化时触发回调。
Type: ( AbilityConfiguration )->Unit; Readable: true; Writable: false

#### let onMemoryLevel

```cangjie
public let onMemoryLevel:(MemoryLevel) -> Unit
```
功能： 注册系统环境变化的监听后，在系统内存变化时触发回调。
Type: ( MemoryLevel )->Unit; Readable: true; Writable: false


### ErrorManager

class ErrorManager
```cangjie
public class ErrorManager {}
```
功能： 提供注册和注销错误观察器方法的类。


### ExtensionAbility

class ExtensionAbility
```cangjie
sealed abstract class ExtensionAbility <: BaseAbility {}
```
功能： ExtensionAbility是特定场景扩展能力的基类，继承自 BaseAbility ，未新增属性和方法。不支持开发者直接继承该基类。


### ExtensionContext

class ExtensionContext
```cangjie
public open class ExtensionContext <: Context {}
```
功能： 提供访问特定Extension的资源的能力。

#### prop config

```cangjie
public prop config: AbilityConfiguration
```
功能： 所属Module的配置信息。
Type: AbilityConfiguration; Readable: true; Writable: false

#### prop currentHapModuleInfo

```cangjie
public prop currentHapModuleInfo: HapModuleInfo
```
功能： 所属Hap包的信息。
Type: HapModuleInfo; Readable: true; Writable: false

#### prop extensionAbilityInfo

```cangjie
public prop extensionAbilityInfo: ExtensionAbilityInfo
```
功能： 所属Extension的信息。
Type: ExtensionAbilityInfo; Readable: true; Writable: false


### LaunchParam

class LaunchParam
```cangjie
public class LaunchParam { public var launchReason: LaunchReason = LaunchReason.UNKNOWN public var lastExitReason: LastExitReason = LastExitReason.NORMAL public var lastExitMessage: String = "" public init(launchReason!: LaunchReason = LaunchReason.UNKNOWN, lastExitReason!: LastExitReason = LastExitReason.NORMAL) public init(lastExitMessage: String, launchReason!: LaunchReason = LaunchReason.UNKNOWN, lastExitReason!: LastExitReason = LastExitReason.NORMAL) }
```
功能： 启动参数。Ability启动时由系统自动传入，开发者无需修改。

#### var lastExitMessage

```cangjie
public var lastExitMessage: String
```
功能： 表示最后退出详细原因。
Type: String; Readable: true; Writable: true

#### var lastExitReason

```cangjie
public var lastExitReason: LastExitReason = LastExitReason.NORMAL
```
功能： 枚举类型，表示最后退出原因。
Type: LastExitReason; Readable: true; Writable: true

#### var launchReason

```cangjie
public var launchReason: LaunchReason = LaunchReason.UNKNOWN
```
功能： 枚举类型，表示启动原因。
Type: LaunchReason; Readable: true; Writable: true


### LoopObserver

class LoopObserver
```cangjie
public class LoopObserver { public LoopObserver(let onLoopTimeOut!: ?(Int64) -> Unit = None) }
```
功能： 定义异常监听，可以作为 ErrorManager.on 的入参监听当前应用主线程事件处理事件。


### OnReleaseCallback

class OnReleaseCallback
```cangjie
public class OnReleaseCallback <: Callback1Argument<String> { public OnReleaseCallback(let callback: (String) -> Unit) }
```
功能： 注册通用组件服务端Stub（桩）断开监听通知的回调函数类型。


### OnRemoteStateChangeCallback

class OnRemoteStateChangeCallback
```cangjie
public class OnRemoteStateChangeCallback <: Callback1Argument<String> { public OnRemoteStateChangeCallback(let callback: (String) -> Unit) }
```
功能： 注册协同场景下跨设备组件状态变化监听通知的回调函数类型。


### OpenLinkOptions

class OpenLinkOptions
```cangjie
public class OpenLinkOptions { public OpenLinkOptions( public let appLinkingOnly!: Bool = false, public let parameters!: String = "" ) }
```
功能： 用于标识是否仅打开AppLinking和传递键值对可选参数。

#### let appLinkingOnly

```cangjie
public let appLinkingOnly: Bool = false
```
功能： 表示是否必须以AppLinking的方式启动Ability：
Type: Bool; Readable: true; Writable: false

#### let parameters

```cangjie
public let parameters: String = ""
```
功能： 表示WantParams参数。具体使用规则请参考 Want 中的parameters属性。
Type: String; Readable: true; Writable: false


### PermissionRequestResult

class PermissionRequestResult
```cangjie
public class PermissionRequestResult { public var permissions: Array<String> public var authResults: Array<Int32> public var dialogShownResults: ?Array<Bool> = None public init( permissions: Array<String>, authResults: Array<Int32> ) }
```
功能： 权限请求结果对象，在调用 requestPermissionsFromUser 申请权限时返回此对象表明此次权限申请的结果。

#### var authResults

```cangjie
public var authResults: Array<Int32>
```
功能： 相应请求权限的结果。
Type: Array<Int32>; Readable: true; Writable: true

#### var dialogShownResults

```cangjie
public var dialogShownResults: ?Array<Bool>
```
功能： 此权限申请是否有弹窗：
Type: ?Array<Bool>; Readable: true; Writable: true

#### var permissions

```cangjie
public var permissions: Array<String>
```
功能： 用户传入的权限。
Type: Array<String>; Readable: true; Writable: true


### PhotoEditorExtensionAbility

class PhotoEditorExtensionAbility
```cangjie
public open class PhotoEditorExtensionAbility <: ExtensionAbility {}
```
功能： PhotoEditorExtensionAbility继承自 ExtensionAbility ，开发者可通过PhotoEditorExtensionAbility实现图片编辑扩展页面。

#### prop context

```cangjie
public prop context: PhotoEditorExtensionContext
```
功能： PhotoEditorExtensionAbility的上下文。
Type: PhotoEditorExtensionContext; Readable: true; Writable: false


### PhotoEditorExtensionContext

class PhotoEditorExtensionContext
```cangjie
public class PhotoEditorExtensionContext <: ExtensionContext {}
```
功能： PhotoEditorExtensionContext是 PhotoEditorExtensionAbility 的上下文，继承自 ExtensionContext ，提供PhotoEditorExtensionAbility的相关配置信息以及保存图片接口。


### ProcessInformation

class ProcessInformation
```cangjie
public class ProcessInformation { public ProcessInformation( public let pid: Int32, public let uid: Int32, public let processName: String, public let bundleNames: Array<String>, public let state: ProcessState, public let bundleType: BundleType, public let appCloneIndex: Int32 ) }
```
功能： 提供对进程运行信息进行查询的能力。

#### let appCloneIndex

```cangjie
public let appCloneIndex: Int32
```
功能： 应用分身索引。
Type: Int32; Readable: true; Writable: false

#### let bundleNames

```cangjie
public let bundleNames: Array<String>
```
功能： 进程中所有运行的Bundle名称。
Type: Array<String>; Readable: true; Writable: false

#### let bundleType

```cangjie
public let bundleType: BundleType
```
功能： 进程中所有运行的Bundle类型。
Type: BundleType; Readable: true; Writable: false

#### let pid

```cangjie
public let pid: Int32
```
功能： 进程ID。
Type: Int32; Readable: true; Writable: false

#### let processName

```cangjie
public let processName: String
```
功能： 进程名称。
Type: String; Readable: true; Writable: false

#### let state

```cangjie
public let state: ProcessState
```
功能： 当前进程运行状态。
Type: ProcessState; Readable: true; Writable: false

#### let uid

```cangjie
public let uid: Int32
```
功能： 用户ID。
Type: Int32; Readable: true; Writable: false


### ShareExtensionAbility

class ShareExtensionAbility
```cangjie
public open class ShareExtensionAbility <: UIExtensionAbility {}
```
功能： ShareExtensionAbility是为开发者提供分享操作业务模板，继承自 UIExtensionAbility 。ShareExtension为人们提供了一种通过应用程序、社交媒体帐户和其他服务共享当前上下文信息的便捷方式。


### StartOptions

class StartOptions
```cangjie
public open class StartOptions { public var windowMode: WindowMode = WINDOW_MODE_UNDEFINED public var displayId: Int32 = 0 public var withAnimation: Bool = true public var windowLeft: Int32 = 0 public var windowTop: Int32 = 0 public var windowWidth: Int32 = 0 public var windowHeight: Int32 = 0 public var processMode: ?ProcessMode = None public var startupVisibility: ?StartupVisibility = None public init( windowMode!: WindowMode = WINDOW_MODE_UNDEFINED, displayId!: Int32 = 0 ) }
```
功能： 用于指定目标Ability的窗口模式。

#### var windowMode

```cangjie
public var windowMode: WindowMode = WINDOW_MODE_UNDEFINED
```
功能： 启动Ability时的窗口模式，详见 WindowMode 。
Type: WindowMode; Readable: true; Writable: true

#### var displayId

```cangjie
public var displayId: Int32 = 0
```
功能： 屏幕ID模式。默认是0，表示当前屏幕。
Type: Int32; Readable: true; Writable: true

#### var processMode

```cangjie
public var processMode:?ProcessMode = None
```
功能： 进程模式。
Type: ? ProcessMode; Readable: true; Writable: true

#### var startupVisibility

```cangjie
public var startupVisibility:?StartupVisibility = None
```
功能： Ability启动后的可见性。
Type: ? StartupVisibility; Readable: true; Writable: true

#### var windowHeight

```cangjie
public var windowHeight: Int32 = 0
```
功能： 窗口的高度。
Type: Int32; Readable: true; Writable: true

#### var windowLeft

```cangjie
public var windowLeft: Int32 = 0
```
功能： 窗口左边的位置。
Type: Int32; Readable: true; Writable: true

#### var windowTop

```cangjie
public var windowTop: Int32 = 0
```
功能： 窗口顶部的位置。
Type: Int32; Readable: true; Writable: true

#### var windowWidth

```cangjie
public var windowWidth: Int32 = 0
```
功能： 窗口的宽度。
Type: Int32; Readable: true; Writable: true

#### let withAnimation

```cangjie
public let withAnimation: Bool = true
```
功能： Ability是否具有动画效果。
Type: Bool; Readable: true; Writable: false


### TriggerInfo

class TriggerInfo
```cangjie
public class TriggerInfo { public TriggerInfo( public let code!: Int32, public let want!: ?Want = None, public let permission!: String = "", public let extraInfos!: ?String = None ) }
```
功能： 作为 trigger 的入参定义触发WantAgent所需要的信息。

#### let code

```cangjie
public let code: Int32
```
功能： 提供给目标wantAgent的自定义结果码。
Type: Int32; Readable: true; Writable: false

#### let extraInfos

```cangjie
public let extraInfos:?String = None
```
功能： 额外数据。
Type: ?String; Readable: true; Writable: false

#### let permission

```cangjie
public let permission: String = ""
```
功能： 权限定义。
Type: String; Readable: true; Writable: false

#### let want

```cangjie
public let want:?Want = None
```
功能： 对象间信息传递的载体，可以用于应用组件间的信息传递。
Type: ? Want; Readable: true; Writable: false


### UIAbility

class UIAbility
```cangjie
public open class UIAbility <: BaseAbility {}
```
功能： UIAbility是包含UI界面的应用组件，继承自BaseAbility，提供组件创建、销毁、前后台切换等生命周期回调，同时也具备组件协同的能力。组件协同主要提供如下常用功能：

#### prop callee

```cangjie
public prop callee: Callee
```
功能： UIAbility内部对象，通用组件服务端注册和解除客户端caller通知送信的callback接口。
Type: Callee; Readable: true; Writable: false

#### prop context

```cangjie
public prop context: UIAbilityContext
```
功能： 上下文。
Type: UIAbilityContext; Readable: true; Writable: false

#### prop lastRequestWant

```cangjie
public prop lastRequestWant: Want
```
功能： UIAbility最后请求时的参数。
Type: Want; Readable: true; Writable: false

#### prop launchWant

```cangjie
public prop launchWant: Want
```
功能： UIAbility启动时的参数。
Type: Want; Readable: true; Writable: false


### UIAbilityContext

class UIAbilityContext
```cangjie
public open class UIAbilityContext <: Context {}
```
功能： 提供允许访问特定UIAbility的资源的能力。

#### prop abilityInfo

```cangjie
public prop abilityInfo: AbilityInfo
```
功能： UIAbility 的相关信息。
Type: AbilityInfo; Readable: true; Writable: false

#### prop config

```cangjie
public prop config: AbilityConfiguration
```
功能： 与 UIAbility 相关的配置信息，如语言、颜色模式等。
Type: AbilityConfiguration; Readable: true; Writable: false

#### prop currentHapModuleInfo

```cangjie
public prop currentHapModuleInfo: HapModuleInfo
```
功能： 当前HAP的信息。
Type: HapModuleInfo; Readable: true; Writable: false

#### prop windowStage

```cangjie
public prop windowStage: ?WindowStage
```
功能： 当前WindowStage对象。仅支持在主线程调用。
Type: ?WindowStage; Readable: true; Writable: false

#### prop filesDir

```cangjie
public prop filesDir: String
```
功能： 文件目录。
Type: String; Readable: true; Writable: false


### UIExtensionAbility

class UIExtensionAbility
```cangjie
public open class UIExtensionAbility <: ExtensionAbility {}
```
功能： UIExtensionAbility是特定场景下带界面扩展能力的基类，继承自 ExtensionAbility ，新增带界面扩展能力相关的属性和方法。不支持开发者直接继承该基类。

#### prop context

```cangjie
public prop context: UIExtensionContext
```
功能： 获取UIExtensionAbility的上下文。
Type: UIExtensionContext; Readable: true; Writable: false


### UIExtensionContentSession

class UIExtensionContentSession
```cangjie
public class UIExtensionContentSession {}
```
功能： UIExtensionContentSession提供界面加载，结果通知等方法。


### UIExtensionContext

class UIExtensionContext
```cangjie
public class UIExtensionContext <: ExtensionContext {}
```
功能： UIExtensionContext是 UIExtensionAbility 的上下文环境，继承自 ExtensionContext ，提供UIExtensionAbility的相关配置信息以及操作Ability的方法，如启动Ability等。


### Want

class Want
```cangjie
public class Want { public init( deviceId!: String = "", bundleName!: String = "", abilityName!: String = "", moduleName!: String = "", flags!: UInt32 = 0, uri!: String = "", action!: String = "", entities!: Array<String> = [], `type`!: String = "", parameters!: String = "" ) }
```
功能： 创建相应的 Want。

#### prop type

```cangjie
public prop `type`: String
```
功能： MIME type类型描述，打开文件的类型，主要用于文管打开文件。比如："text/xml" 、 "image/*"等，MIME定义参考： Media Types 。
Type: String; Readable: true; Writable: false

#### prop abilityName

```cangjie
public prop abilityName: String
```
功能： 待启动Ability名称。
Type: String; Readable: true; Writable: false

#### prop action

```cangjie
public prop action: String
```
功能： 要执行的通用操作（如：查看、分享、应用详情）。在隐式Want中，您可以定义该字段，配合uri或parameters来表示对数据要执行的操作。
Type: String; Readable: true; Writable: false

#### prop bundleName

```cangjie
public prop bundleName: String
```
功能： 待启动Ability所在的应用Bundle名称。
Type: String; Readable: true; Writable: false

#### prop deviceId

```cangjie
public prop deviceId: String
```
功能： 运行指定Ability的设备ID。
Type: String; Readable: true; Writable: false

#### prop entities

```cangjie
public prop entities: Array<String>
```
功能： 目标Ability额外的类别信息（如：浏览器、视频播放器），在隐式Want中是对action字段的补充。在隐式Want中，您可以定义该字段，来过滤匹配Ability类型。
Type: Array<String>; Readable: true; Writable: false

#### prop flags

```cangjie
public prop flags: UInt32
```
功能： 处理Want的方式，具体参考： Flags 。
Type: UInt32; Readable: true; Writable: false

#### prop moduleName

```cangjie
public prop moduleName: String
```
功能： 待启动的Ability所属的模块名称。
Type: String; Readable: true; Writable: false

#### prop parameters

```cangjie
public prop parameters: String
```
功能： 开发者自行决定传入的json字符串形式。
Type: String; Readable: true; Writable: false

#### prop uri

```cangjie
public prop uri: String
```
功能： Uri描述。如果在Want中指定了Uri，则Want将匹配指定的Uri信息，包括scheme, schemeSpecificPart, authority和path信息。
Type: String; Readable: true; Writable: false


### WantAgent

class WantAgent
```cangjie
public class WantAgent {}
```
功能： WantAgent类。通过 getWantAgent 函数获取。


### WantAgentInfo

class WantAgentInfo
```cangjie
public class WantAgentInfo { public WantAgentInfo( public let wants!: Array<Want>, public let actionType!: OperationType = UNKNOWN_TYPE, public let requestCode!: Int32, public let actionFlags!: Array<WantAgentFlags> = Array<WantAgentFlags>(), public let extraInfos!: String = "" ) }
```
功能： 定义触发WantAgent所需要的信息，可以作为 getWantAgent 的入参创建指定的WantAgent对象。

#### let actionFlags

```cangjie
public let actionFlags: Array<WantAgentFlags>= Array<WantAgentFlags>()
```
功能： 动作执行属性。
Type: Array< WantAgentFlags >; Readable: true; Writable: false

#### let actionType

```cangjie
public let actionType: OperationType = UNKNOWN_TYPE
```
功能： 动作类型。
Type: OperationType; Readable: true; Writable: false

#### let extraInfos

```cangjie
public let extraInfos: String = ""
```
功能： 额外数据。由开发者自行决定传入的json字符串。
Type: String; Readable: true; Writable: false

#### let requestCode

```cangjie
public let requestCode: Int32
```
功能： 使用者定义的一个私有值。
Type: Int32; Readable: true; Writable: false

#### let wants

```cangjie
public let wants: Array<Want>
```
功能： 将被执行的动作列表。wants数组为预留能力，当前只支持一个want。传入多个时只取wants数组的第一个成员。
Type: Array< Want >; Readable: true; Writable: false



## Structs

### AbilityResult

struct AbilityResult
```cangjie
public struct AbilityResult { public AbilityResult( public let resultCode: Int32, public let want: Want ) }
```
功能： 定义Ability拉起、销毁之后返回的结果码和数据。 可以通过startAbilityForResult获取对端Ability销毁后返回的AbilityResult对象，被startAbilityForResult拉起的Ability对象可以通过terminateSelfWithResult返回AbilityResult对象。


### ErrorObject

struct ErrorObject
```cangjie
public struct ErrorObject { public let name: String public let message: String public let stack: Option<String> public init(name: String, message: String, stack!: Option<String> = None) }
```
功能： 异常事件信息。


### ErrorObserver

struct ErrorObserver
```cangjie
public struct ErrorObserver { public let onUnhandledException:(String) -> Unit public let onException: Option <(ErrorObject) -> Unit> public init(onUnhandledException : (String)->Unit, onException !: Option<(ErrorObject)->Unit> = None) }
```
功能： 定义异常监听，可以作为ErrorManager.on的入参监听当前应用发生的异常。



## Enums

### ApplicationContextType

enum ApplicationContextType
```cangjie
public enum ApplicationContextType <: Equatable<ApplicationContextType> { | ENVIRONMENT | ABILITY_LIFE_CYCLE | APPLICATION_STATE_CHANGE | ... }
```
功能： 用于描述注册回调所支持的数据类型。

#### ABILITY_LIFE_CYCLE

```cangjie
ABILITY_LIFE_CYCLE
```
功能： 监听应用内生命周期。

#### APPLICATION_STATE_CHANGE

```cangjie
APPLICATION_STATE_CHANGE
```
功能： 监听当前应用前后台变化。

#### ENVIRONMENT

```cangjie
ENVIRONMENT
```
功能： 监听系统环境变化。


### AreaMode

enum AreaMode
```cangjie
public enum AreaMode <: Equatable<AreaMode> & ToString { | EL1 | EL2 | EL3 | EL4 | EL5 | ... }
```
功能： 数据加密等级。

#### EL1

```cangjie
EL1
```
功能： 设备级加密区，设备开机后可访问的数据区。

#### EL2

```cangjie
EL2
```
功能： 用户级加密区，设备开机，首次输入密码后才能够访问的数据区。

#### EL3

```cangjie
EL3
```
功能： 用户级加密区，不同场景的文件权限如下：

#### EL4

```cangjie
EL4
```
功能： 用户级加密区，不同场景的文件权限如下：

#### EL5

```cangjie
EL5
```
功能： 应用级加密区，不同场景的文件权限如下：


### CallerType

enum CallerType
```cangjie
public enum CallerType <: Equatable<CallerType> { | RELEASE | ... }
```
功能： releaseCall事件类型。

#### RELEASE

```cangjie
RELEASE
```
功能： release类型的releaseCall事件。


### ConfigurationColorMode

enum ConfigurationColorMode
```cangjie
public enum ConfigurationColorMode <: Equatable<ConfigurationColorMode> & ToString { | COLOR_MODE_NOT_SET | COLOR_MODE_DARK | COLOR_MODE_LIGHT | ... }
```
功能： 表示颜色模式。

#### COLOR_MODE_DARK

```cangjie
COLOR_MODE_DARK
```
功能： 深色模式。

#### COLOR_MODE_LIGHT

```cangjie
COLOR_MODE_LIGHT
```
功能： 浅色模式。

#### COLOR_MODE_NOT_SET

```cangjie
COLOR_MODE_NOT_SET
```
功能： 未设置颜色模式。


### ConfigurationDirection

enum ConfigurationDirection
```cangjie
public enum ConfigurationDirection <: Equatable<ConfigurationDirection> & ToString { | DIRECTION_NOT_SET | DIRECTION_VERTICAL | DIRECTION_HORIZONTAL | ... }
```
功能： 表示屏幕方向。

#### DIRECTION_HORIZONTAL

```cangjie
DIRECTION_HORIZONTAL
```
功能： 水平方向。

#### DIRECTION_NOT_SET

```cangjie
DIRECTION_NOT_SET
```
功能： 未设置方向。

#### DIRECTION_VERTICAL

```cangjie
DIRECTION_VERTICAL
```
功能： 垂直方向。


### ConfigurationScreenDensity

enum ConfigurationScreenDensity
```cangjie
public enum ConfigurationScreenDensity <: Equatable<ConfigurationScreenDensity> & ToString { | SCREEN_DENSITY_NOT_SET | SCREEN_DENSITY_SDPI | SCREEN_DENSITY_MDPI | SCREEN_DENSITY_LDPI | SCREEN_DENSITY_XLDPI | SCREEN_DENSITY_XXLDPI | SCREEN_DENSITY_XXXLDPI | ... }
```
功能： 表示屏幕像素。

#### SCREEN_DENSITY_LDPI

```cangjie
SCREEN_DENSITY_LDPI
```
功能： 屏幕像素密度为'LDPI'。

#### SCREEN_DENSITY_MDPI

```cangjie
SCREEN_DENSITY_MDPI
```
功能： 屏幕像素密度为'MDPI'。

#### SCREEN_DENSITY_NOT_SET

```cangjie
SCREEN_DENSITY_NOT_SET
```
功能： 未设置屏幕像素密度。

#### SCREEN_DENSITY_SDPI

```cangjie
SCREEN_DENSITY_SDPI
```
功能： 屏幕像素密度为'SDPI'。

#### SCREEN_DENSITY_XLDPI

```cangjie
SCREEN_DENSITY_XLDPI
```
功能： 屏幕像素密度为'XLDPI'。

#### SCREEN_DENSITY_XXLDPI

```cangjie
SCREEN_DENSITY_XXLDPI
```
功能： 屏幕像素密度为'XXLDPI'。

#### SCREEN_DENSITY_XXXLDPI

```cangjie
SCREEN_DENSITY_XXXLDPI
```
功能： 屏幕像素密度为'XXXLDPI'。


### ContinueState

enum ContinueState
```cangjie
public enum ContinueState <: Equatable<ContinueState> & ToString { | ACTIVE | INACTIVE | ... }
```
功能： 流转状态枚举值。用于表示当前应用任务流转的状态。可配合AbilityContext的 setMissionContinueState 方法进行设置。

#### ACTIVE

```cangjie
ACTIVE
```
功能： 指示当前应用任务流转处于激活状态。

#### INACTIVE

```cangjie
INACTIVE
```
功能： 指示当前应用任务流转处于未激活状态。


### Flags

enum Flags
```cangjie
public enum Flags <: Equatable<Flags> & ToString { | FLAG_AUTH_READ_URI_PERMISSION | FLAG_AUTH_WRITE_URI_PERMISSION | FLAG_AUTH_PERSISTABLE_URI_PERMISSION | FLAG_INSTALL_ON_DEMAND | FLAG_START_WITHOUT_TIPS | ... }
```
功能： Flags说明。用于表示处理 Want 的方式。

#### FLAG_AUTH_PERSISTABLE_URI_PERMISSION

```cangjie
FLAG_AUTH_PERSISTABLE_URI_PERMISSION
```
功能： 指示该URI可被接收方持久化。该字段仅在平板类设备上生效。值为：0x00000040。

#### FLAG_AUTH_READ_URI_PERMISSION

```cangjie
FLAG_AUTH_READ_URI_PERMISSION
```
功能： 指示对URI执行读取操作的授权。值为：0x00000001。

#### FLAG_AUTH_WRITE_URI_PERMISSION

```cangjie
FLAG_AUTH_WRITE_URI_PERMISSION
```
功能： 指示对URI执行写入操作的授权。值为：0x00000002。

#### FLAG_INSTALL_ON_DEMAND

```cangjie
FLAG_INSTALL_ON_DEMAND
```
功能： 如果未安装指定的功能，请安装该功能。值为：0x00000800。

#### FLAG_START_WITHOUT_TIPS

```cangjie
FLAG_START_WITHOUT_TIPS
```
功能： 如果隐式启动能力不能匹配任何应用程序，则不会弹出提示对话框。值为：0x40000000。


### LastExitReason

enum LastExitReason
```cangjie
public enum LastExitReason { | UNKNOWN | ABILITY_NOT_RESPONDING | NORMAL | CPP_CRASH | CJ_ERROR | APP_FREEZE | PERFORMANCE_CONTROL | RESOURCE_CONTROL | UPGRADE | ... }
```
功能： Ability上次退出原因，该类型为枚举，可配合Ability的 onCreate(want, launchParam) 方法根据launchParam.lastExitReason的不同类型执行相应操作。

#### ABILITY_NOT_RESPONDING

```cangjie
ABILITY_NOT_RESPONDING
```
功能： ability未响应。

#### APP_FREEZE

```cangjie
APP_FREEZE
```
功能： 由于watchdog检测出应用Freeze故障，导致应用程序退出。

#### CJ_ERROR

```cangjie
CJ_ERROR
```
功能： 当应用存在CJ语法错误并未被开发者捕获时，触发CJ_ERROR故障，导致应用程序退出。

#### CPP_CRASH

```cangjie
CPP_CRASH
```
功能： 本机异常信号，导致应用程序退出。

#### NORMAL

```cangjie
NORMAL
```
功能： 用户主动关闭，应用程序正常退出。

#### PERFORMANCE_CONTROL

```cangjie
PERFORMANCE_CONTROL
```
功能： 由于系统性能问题（如设备内存不足），导致应用程序退出。

#### RESOURCE_CONTROL

```cangjie
RESOURCE_CONTROL
```
功能： 系统资源使用不当，导致应用程序退出。具体错误原因可以通过 LaunchParam.lastExitMessage 获取，可能原因如下：

#### UNKNOWN

```cangjie
UNKNOWN
```
功能： 未知原因。

#### UPGRADE

```cangjie
UPGRADE
```
功能： 应用程序因升级而退出。


### LaunchReason

enum LaunchReason
```cangjie
public enum LaunchReason { | UNKNOWN | START_ABILITY | CALL | CONTINUATION | APP_RECOVERY | SHARE | AUTO_STARTUP | INSIGHT_INTENT | PREPARE_CONTINUATION | ... }
```
功能： Ability初次启动原因，该类型为枚举，可配合Ability的 onCreate(want, launchParam) 方法根据launchParam.launchReason的不同类型执行相应操作。

#### APP_RECOVERY

```cangjie
APP_RECOVERY
```
功能： 设置应用恢复后，应用故障时自动恢复启动Ability。

#### AUTO_STARTUP

```cangjie
AUTO_STARTUP
```
功能： 通过设置开机自启动来启动Ability。

#### CALL

```cangjie
CALL
```
功能： 通过startAbilityByCall接口启动Ability。

#### CONTINUATION

```cangjie
CONTINUATION
```
功能： 跨端迁移启动Ability。

#### INSIGHT_INTENT

```cangjie
INSIGHT_INTENT
```
功能： 通过洞察意图来启动Ability。

#### PREPARE_CONTINUATION

```cangjie
PREPARE_CONTINUATION
```
功能： 跨端迁移提前启动Ability。

#### SHARE

```cangjie
SHARE
```
功能： 通过原子化服务分享启动Ability。

#### START_ABILITY

```cangjie
START_ABILITY
```
功能： 通过 startAbility 接口启动Ability。

#### UNKNOWN

```cangjie
UNKNOWN
```
功能： 未知原因。


### MemoryLevel

enum MemoryLevel
```cangjie
public enum MemoryLevel <: Equatable<MemoryLevel> & ToString { | MEMORY_LEVEL_MODERATE | MEMORY_LEVEL_LOW | MEMORY_LEVEL_CRITICAL | ... }
```
功能： 内存级别，该类型为枚举。

#### MEMORY_LEVEL_CRITICAL

```cangjie
MEMORY_LEVEL_CRITICAL
```
功能： 内存占用高。

#### MEMORY_LEVEL_LOW

```cangjie
MEMORY_LEVEL_LOW
```
功能： 内存占用低。

#### MEMORY_LEVEL_MODERATE

```cangjie
MEMORY_LEVEL_MODERATE
```
功能： 内存占用适中。


### OnContinueResult

enum OnContinueResult
```cangjie
public enum OnContinueResult { | AGREE | REJECT | MISMATCH | ... }
```
功能： Ability迁移结果，可配合UIAbility的 onContinue 方法进完成相应的返回。

#### AGREE

```cangjie
AGREE
```
功能： 表示同意。

#### MISMATCH

```cangjie
MISMATCH
```
功能： 表示版本不匹配：迁移发起端应用可以在 onContinue 中获取到迁移目标端应用的版本号，进行协商后，如果版本不匹配导致无法迁移，可以返回该错误。

#### REJECT

```cangjie
REJECT
```
功能： 表示拒绝：如应用在 onContinue 中异常会导致迁移以后数据恢复时显示异常，则可以建议REJECT。


### OnSaveResult

enum OnSaveResult
```cangjie
public enum OnSaveResult <: Equatable<OnSaveResult> & ToString { | ALL_AGREE | CONTINUATION_REJECT | CONTINUATION_MISMATCH | RECOVERY_AGREE | RECOVERY_REJECT | ALL_REJECT | ... }
```
功能： 保存应用数据的结果，该类型为枚举，可配合Ability的 onSaveState(reason, wantParam) 方法完成相应的返回。

#### ALL_AGREE

```cangjie
ALL_AGREE
```
功能： 总是同意保存状态。

#### ALL_REJECT

```cangjie
ALL_REJECT
```
功能： 总是拒绝保存状态。

#### CONTINUATION_MISMATCH

```cangjie
CONTINUATION_MISMATCH
```
功能： 迁移不匹配。

#### CONTINUATION_REJECT

```cangjie
CONTINUATION_REJECT
```
功能： 拒绝迁移保存状态。

#### RECOVERY_AGREE

```cangjie
RECOVERY_AGREE
```
功能： 同意恢复保存状态。

#### RECOVERY_REJECT

```cangjie
RECOVERY_REJECT
```
功能： 拒绝恢复保存状态。


### OperationType

enum OperationType
```cangjie
public enum OperationType <: Equatable<OperationType> & ToString { | UNKNOWN_TYPE | START_ABILITY | START_ABILITIES | START_SERVICE | SEND_COMMON_EVENT | ... }
```
功能： 表示操作 WantAgent 类型。

#### SEND_COMMON_EVENT

```cangjie
SEND_COMMON_EVENT
```
功能： 发送一个公共事件。

#### START_ABILITIES

```cangjie
START_ABILITIES
```
功能： 开启多个有页面的Ability。

#### START_ABILITY

```cangjie
START_ABILITY
```
功能： 开启一个有页面的Ability。

#### START_SERVICE

```cangjie
START_SERVICE
```
功能： 开启一个无页面的Ability（仅在FA模型下生效）。

#### UNKNOWN_TYPE

```cangjie
UNKNOWN_TYPE
```
功能： 不识别的类型。


### Params

enum Params
```cangjie
public enum Params <: Equatable<Params> & ToString { | ABILITY_BACK_TO_OTHER_MISSION_STACK | ABILITY_RECOVERY_RESTART | CONTENT_TITLE_KEY | SHARE_ABSTRACT_KEY | SHARE_URL_KEY | SUPPORT_CONTINUE_PAGE_STACK_KEY | SUPPORT_CONTINUE_SOURCE_EXIT_KEY | CALLER_REQUEST_CODE | APP_CLONE_INDEX_KEY | PARAMS_STREAM | SHOW_MODE_KEY | SUB_PACKAGE_NAME | BUILD_FUNCTION | PAGE_SOURCE_FILE | ROUTER_NAME | PAGE_PATH | ... }
```
功能： Want 的Params操作。

#### ABILITY_BACK_TO_OTHER_MISSION_STACK

```cangjie
ABILITY_BACK_TO_OTHER_MISSION_STACK
```
功能： 表示是否支持跨任务链返回。值为：ability.params.backToOtherMissionStack。

#### ABILITY_RECOVERY_RESTART

```cangjie
ABILITY_RECOVERY_RESTART
```
功能： 指示当前Ability是否发生了故障恢复重启。值为：ohos.ability.params.abilityRecoveryRestart。

#### APP_CLONE_INDEX_KEY

```cangjie
APP_CLONE_INDEX_KEY
```
功能： 指示分身应用索引。值为：ohos.extra.param.key.appCloneIndex。

#### BUILD_FUNCTION

```cangjie
BUILD_FUNCTION
```
功能： 指示构建函数。值为：ohos.param.atomicservice.buildFunction。

#### CALLER_REQUEST_CODE

```cangjie
CALLER_REQUEST_CODE
```
功能： 当调用 startAbilityForResult 拉起目标方 UIAbility 时, 需要目标方返回结果。为了确保目标方能够将结果准确返回到调用方，系统会自动生成唯一的requestCode，以标识本次调用。值为：ohos.extra.param.key.callerRequestCode。

#### CONTENT_TITLE_KEY

```cangjie
CONTENT_TITLE_KEY
```
功能： 指示元服务支持分享标题的参数的操作。值为：ohos.extra.param.key.contentTitle。

#### PAGE_PATH

```cangjie
PAGE_PATH
```
功能： 指示页面路径。值为：ohos.param.atomicservice.pagePath。

#### PAGE_SOURCE_FILE

```cangjie
PAGE_SOURCE_FILE
```
功能： 指示页面源文件。值为：ohos.param.atomicservice.pageSourceFile。

#### PARAMS_STREAM

```cangjie
PARAMS_STREAM
```
功能： 指示携带的文件URI要授权给目标方。对应的value必须是String类型的文件URI数组。值为：ability.params.stream。

#### ROUTER_NAME

```cangjie
ROUTER_NAME
```
功能： 指示路由名。值为：ohos.param.atomicservice.routerName。

#### SHARE_ABSTRACT_KEY

```cangjie
SHARE_ABSTRACT_KEY
```
功能： 指示元服务支持分享内容的参数的操作。值为：ohos.extra.param.key.shareAbstract。

#### SHARE_URL_KEY

```cangjie
SHARE_URL_KEY
```
功能： 指示元服务支持分享链接的参数的操作。值为：ohos.extra.param.key.shareUrl。

#### SHOW_MODE_KEY

```cangjie
SHOW_MODE_KEY
```
功能： 指示展示模式，值为枚举类型 ShowMode 。值为：ohos.extra.param.key.showMode。

#### SUB_PACKAGE_NAME

```cangjie
SUB_PACKAGE_NAME
```
功能： 指示子包名。值为：ohos.param.atomicservice.subpackageName。

#### SUPPORT_CONTINUE_PAGE_STACK_KEY

```cangjie
SUPPORT_CONTINUE_PAGE_STACK_KEY
```
功能： 指示在跨端迁移过程中是否迁移页面栈信息，默认值为true，自动迁移页面栈信息。值为：ohos.extra.param.key.supportContinuePageStack。

#### SUPPORT_CONTINUE_SOURCE_EXIT_KEY

```cangjie
SUPPORT_CONTINUE_SOURCE_EXIT_KEY
```
功能： 指示跨端迁移源端应用是否退出，默认值为true，源端应用自动退出。值为：ohos.extra.param.key.supportContinueSourceExit。


### ProcessMode

enum ProcessMode
```cangjie
public enum ProcessMode <: Equatable<ProcessMode> & ToString { | NEW_PROCESS_ATTACH_TO_PARENT | NEW_PROCESS_ATTACH_TO_STATUS_BAR_ITEM | ATTACH_TO_STATUS_BAR_ITEM | ... }
```
功能： 进程模式。该功能仅在平板类设备上生效。 ProcessMode作为StartOptions的一个属性，仅在 UIAbilityContext.startAbility 中生效，用来指定目标Ability的进程模式。

#### ATTACH_TO_STATUS_BAR_ITEM

```cangjie
ATTACH_TO_STATUS_BAR_ITEM
```
功能： 启动Ability，并绑定该Ability所在进程到状态栏图标上。

#### NEW_PROCESS_ATTACH_TO_PARENT

```cangjie
NEW_PROCESS_ATTACH_TO_PARENT
```
功能： 创建一个新进程，并在该进程上启动Ability。该进程会跟随父进程退出。

#### NEW_PROCESS_ATTACH_TO_STATUS_BAR_ITEM

```cangjie
NEW_PROCESS_ATTACH_TO_STATUS_BAR_ITEM
```
功能： 创建一个新进程，在该进程上启动Ability，并绑定该进程到状态栏图标上。


### ProcessState

enum ProcessState
```cangjie
public enum ProcessState <: Equatable<ProcessState> & ToString { | STATE_CREATE | STATE_FOREGROUND | STATE_ACTIVE | STATE_BACKGROUND | STATE_DESTROY | ... }
```
功能： 表示进程状态。

#### STATE_ACTIVE

```cangjie
STATE_ACTIVE
```
功能： 当进程在获焦的时候处于的状态。

#### STATE_BACKGROUND

```cangjie
STATE_BACKGROUND
```
功能： 当进程处于后台不可见时处于的状态。

#### STATE_CREATE

```cangjie
STATE_CREATE
```
功能： 当进程在创建中的时候处于的状态。

#### STATE_DESTROY

```cangjie
STATE_DESTROY
```
功能： 当进程在销毁的时候处于的状态。

#### STATE_FOREGROUND

```cangjie
STATE_FOREGROUND
```
功能： 当进程切换到前台的时候处于的状态。


### RestartFlag

enum RestartFlag
```cangjie
public enum RestartFlag <: Equatable<RestartFlag> & ToString { | ALWAYS_RESTART | RESTART_WHEN_JS_CRASH | RESTART_WHEN_APP_FREEZE | NO_RESTART | ... }
```
功能： 应用重启标志， enableAppRecovery 接口重启选项参数。

#### ALWAYS_RESTART

```cangjie
ALWAYS_RESTART
```
功能： 总是重启应用。

#### NO_RESTART

```cangjie
NO_RESTART
```
功能： 总是不重启应用。

#### RESTART_WHEN_APP_FREEZE

```cangjie
RESTART_WHEN_APP_FREEZE
```
功能： 发生APP_FREEZE时重启应用。

#### RESTART_WHEN_JS_CRASH

```cangjie
RESTART_WHEN_JS_CRASH
```
功能： 发生JS_CRASH时重启应用。


### ResultCode

enum ResultCode
```cangjie
public enum ResultCode { | RESULT_OK | RESULT_CANCEL | ... }
```
功能： 模态弹框请求结果码。

#### RESULT_CANCEL

```cangjie
RESULT_CANCEL
```
功能： 表示失败。

#### RESULT_OK

```cangjie
RESULT_OK
```
功能： 表示成功。


### SaveModeFlag

enum SaveModeFlag
```cangjie
public enum SaveModeFlag <: Equatable<SaveModeFlag> & ToString { | SAVE_WITH_FILE | SAVE_WITH_SHARED_MEMORY | ... }
```
功能： 状态保存标志， enableAppRecovery 接口状态保存方式的参数。

#### SAVE_WITH_FILE

```cangjie
SAVE_WITH_FILE
```
功能： 每次状态保存都会写入到本地文件缓存。

#### SAVE_WITH_SHARED_MEMORY

```cangjie
SAVE_WITH_SHARED_MEMORY
```
功能： 状态先保存在内存中，应用故障退出时写入到本地文件缓存。


### SaveOccasionFlag

enum SaveOccasionFlag
```cangjie
public enum SaveOccasionFlag <: Equatable<SaveOccasionFlag> & ToString { | SAVE_WHEN_ERROR | SAVE_WHEN_BACKGROUND | ... }
```
功能： 保存条件标志， enableAppRecovery 接口状态保存时的选项参数。

#### SAVE_WHEN_BACKGROUND

```cangjie
SAVE_WHEN_BACKGROUND
```
功能： 当应用切入后台时保存。

#### SAVE_WHEN_ERROR

```cangjie
SAVE_WHEN_ERROR
```
功能： 当发生应用故障时保存。


### ShowMode

enum ShowMode
```cangjie
public enum ShowMode <: Equatable<ShowMode> & ToString { | WINDOW | EMBEDDED_FULL | ... }
```
功能： ShowMode说明。用于表示拉起原子化服务的展示模式。

#### EMBEDDED_FULL

```cangjie
EMBEDDED_FULL
```
功能： 指示嵌入式全屏拉起模式。值为：1。

#### WINDOW

```cangjie
WINDOW
```
功能： 指示独立窗口拉起模式。值为：0。


### StartupVisibility

enum StartupVisibility
```cangjie
public enum StartupVisibility <: Equatable<StartupVisibility> & ToString { | STARTUP_HIDE | STARTUP_SHOW | ... }
```
功能： Ability启动后的可见性。该功能仅在平板类设备上生效。 StartupVisibility作为 StartOptions 的一个属性，仅在 UIAbilityContext.startAbility 中生效，用来指定目标Ability启动后的可见性。

#### STARTUP_HIDE

```cangjie
STARTUP_HIDE
```
功能： 目标Ability启动后，进入隐藏状态。不会调用Ability的onForeground生命周期。

#### STARTUP_SHOW

```cangjie
STARTUP_SHOW
```
功能： 目标Ability启动后，正常显示。


### StateType

enum StateType
```cangjie
public enum StateType <: Equatable<StateType> & ToString { | CONTINUATION | APP_RECOVERY | ... }
```
功能： 保存应用数据场景原因，该类型为枚举，可配合Ability的 onSaveState(reason, wantParam) 方法根据reason的不同类型执行相应操作。

#### APP_RECOVERY

```cangjie
APP_RECOVERY
```
功能： 应用恢复保存状态。

#### CONTINUATION

```cangjie
CONTINUATION
```
功能： 迁移保存状态。


### WantAgentFlags

enum WantAgentFlags
```cangjie
public enum WantAgentFlags <: Equatable<WantAgentFlags> & ToString { | ONE_TIME_FLAG | NO_BUILD_FLAG | CANCEL_PRESENT_FLAG | UPDATE_PRESENT_FLAG | CONSTANT_FLAG | REPLACE_ELEMENT | REPLACE_ACTION | REPLACE_URI | REPLACE_ENTITIES | REPLACE_BUNDLE | ... }
```
功能： 表示使用 WantAgent 类型。

#### CANCEL_PRESENT_FLAG

```cangjie
CANCEL_PRESENT_FLAG
```
功能： 在生成一个新的WantAgent对象前取消已存在的一个WantAgent对象。

#### CONSTANT_FLAG

```cangjie
CONSTANT_FLAG
```
功能： WantAgent是不可变的。

#### NO_BUILD_FLAG

```cangjie
NO_BUILD_FLAG
```
功能： 如果描述WantAgent对象不存在，则不创建它，直接返回null。

#### ONE_TIME_FLAG

```cangjie
ONE_TIME_FLAG
```
功能： WantAgent仅能使用一次。

#### REPLACE_ACTION

```cangjie
REPLACE_ACTION
```
功能： 当前Want中的action属性可被WantAgent.trigger()中Want的action属性取代。当前版本暂不支持。

#### REPLACE_BUNDLE

```cangjie
REPLACE_BUNDLE
```
功能： 当前Want中的bundleName属性可被WantAgent.trigger()中Want的bundleName属性取代。当前版本暂不支持。

#### REPLACE_ELEMENT

```cangjie
REPLACE_ELEMENT
```
功能： 当前Want中的element属性可被WantAgent.trigger()中Want的element属性取代。当前版本暂不支持。

#### REPLACE_ENTITIES

```cangjie
REPLACE_ENTITIES
```
功能： 当前Want中的entities属性可被WantAgent.trigger()中Want的entities属性取代。当前版本暂不支持。

#### REPLACE_URI

```cangjie
REPLACE_URI
```
功能： 当前Want中的uri属性可被WantAgent.trigger()中Want的uri属性取代。当前版本暂不支持。

#### UPDATE_PRESENT_FLAG

```cangjie
UPDATE_PRESENT_FLAG
```
功能： 使用新的WantAgent的额外数据替换已存在的WantAgent中的额外数据。


### WindowMode

enum WindowMode
```cangjie
public enum WindowMode { | WINDOW_MODE_UNDEFINED | WINDOW_MODE_FULLSCREEN | WINDOW_MODE_SPLIT_PRIMARY | WINDOW_MODE_SPLIT_SECONDARY | ... }
```
功能： 启动Ability时的窗口模式，类型为枚举。可配合 startAbility 使用，指定启动Ability的窗口模式。

#### WINDOW_MODE_FULLSCREEN

```cangjie
WINDOW_MODE_FULLSCREEN
```
功能： 全屏模式。

#### WINDOW_MODE_SPLIT_PRIMARY

```cangjie
WINDOW_MODE_SPLIT_PRIMARY
```
功能： 屏幕如果是水平方向表示左分屏，屏幕如果是竖直方向表示上分屏。

#### WINDOW_MODE_SPLIT_SECONDARY

```cangjie
WINDOW_MODE_SPLIT_SECONDARY
```
功能： 屏幕如果是水平方向表示右分屏，屏幕如果是竖直方向表示下分屏。

#### WINDOW_MODE_UNDEFINED

```cangjie
WINDOW_MODE_UNDEFINED
```
功能： 未定义窗口模式。



## Types

### Ability

type Ability
```cangjie
public type Ability = UIAbility
```
功能： UIAbility的别名，已废弃。


### AbilityContext

type AbilityContext
```cangjie
public type AbilityContext = UIAbilityContext
```
功能： UIAbilityContext的别名，已废弃。



