# Menu

以垂直列表形式显示的菜单。

## Component Constructors

### public init(child: () -> Unit)

```cangjie
public init(child: () -> Unit)
```
功能： 创建一个存在子组件的菜单。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ()->Unit | 是 | 声明容器内的子组件。 | child | - |


## Functions

### public func font( size!: Length = 16.vp, weight!: FontWeight = FontWeight.Normal, family!: String = "HarmonyOS Sans", style!: FontStyle = FontStyle.Normal ): This

```cangjie
public func font( size!: Length = 16.vp, weight!: FontWeight = FontWeight.Normal, family!: String = "HarmonyOS Sans", style!: FontStyle = FontStyle.Normal ): This
```
功能： 设置Menu中所有文本的尺寸。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 否 | 命名参数。 设置文本尺寸，Length为Int64、Float64类型时，使用fp单位。不支持百分比设置。 | size | 16.vp |
| FontWeight | 否 | 命名参数。 设置文本的字体粗细。 | weight | FontWeight.Normal |
| String | 否 | 命名参数。 设置文本的字体列表。使用多个字体，使用','进行分割，优先级按顺序生效。例如：'Arial, HarmonyOS Sans'。当前支持'HarmonyOS Sans'字体和 注册自定义字体 。 | family | "HarmonyOS Sans" |
| FontStyle | 否 | 命名参数。 设置文本的字体样式。 | style | FontStyle.Normal |

### public func font( size!: Length = 16.vp, weight!: FontWeight = FontWeight.Normal, family!: AppResource, style!: FontStyle = FontStyle.Normal ): This

```cangjie
public func font( size!: Length = 16.vp, weight!: FontWeight = FontWeight.Normal, family!: AppResource, style!: FontStyle = FontStyle.Normal ): This
```
功能： 设置Menu中所有文本的尺寸。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 否 | 命名参数。 设置文本尺寸，Length为Int64、Float64类型时，使用fp单位。不支持百分比设置。 | size | 16.vp |
| FontWeight | 否 | 命名参数。 设置文本的字体粗细。 | weight | FontWeight.Normal |
| AppResource | 是 | 命名参数。 设置文本的字体列表。使用多个字体，使用','进行分割，优先级按顺序生效。例如：'Arial, HarmonyOS Sans'。 初始值：'HarmonyOS Sans'。 当前支持'HarmonyOS Sans'字体和 注册自定义字体 。 | family | - |
| FontStyle | 否 | 命名参数。 设置文本的字体样式。 | style | FontStyle.Normal |

### public func fontColor(color: ResourceColor): This

```cangjie
public func fontColor(color: ResourceColor): This
```
功能： 统一设置Menu中所有文本的颜色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | Menu中所有文本的颜色。 初始值：0xE5000000 | color | - |

### public func menuItemDivider(options: ?DividerStyleOptions): This

```cangjie
public func menuItemDivider(options: ?DividerStyleOptions): This
```
功能： 设置menuItem分割线样式，不设置该属性则不展示分割线。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ? DividerStyleOptions | 是 | 设置menuItem分割线样式。 -strokeWidth：分割线的线宽。 -color：分割线的颜色。 -startMargin：分割线与menuItem侧边起端的距离。 -endMargin：分割线与menuItem侧边结束端的距离。 | options | - |

### public func menuItemGroupDivider(options: ?DividerStyleOptions): This

```cangjie
public func menuItemGroupDivider(options: ?DividerStyleOptions): This
```
功能： 设置menuItemGroup上下分割线的样式，不设置该属性则默认展示分割线。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ? DividerStyleOptions | 是 | 设置menuItemGroup顶部和底部分割线样式。 -strokeWidth：分割线的线宽, 初始值：1.px。 -color：分割线的颜色, 初始值： 0x33000000。 -startMargin：分割线与menuItemGroup侧边起端的距离, 初始值：16。 -endMargin：分割线与menuItemGroup侧边结束端的距离, 初始值：16。 | options | - |

### public func radius(value: Length): This

```cangjie
public func radius(value: Length): This
```
功能： 设置Menu边框圆角半径。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | Menu边框圆角半径。 初始值：20.vp。 | value | - |

### public func radius(value: BorderRadiuses): This

```cangjie
public func radius(value: BorderRadiuses): This
```
功能： 设置Menu边框圆角半径。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| BorderRadiuses | 是 | Menu边框圆角半径。 | value | - |

### public func subMenuExpandingMode(mode: SubMenuExpandingMode): This

```cangjie
public func subMenuExpandingMode(mode: SubMenuExpandingMode): This
```
功能： 设置Menu子菜单展开样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| SubMenuExpandingMode | 是 | Menu子菜单展开样式。 初始值：SubMenuExpandingMode.SIDE_EXPAND | mode | - |


## Enums

### SubMenuExpandingMode

enum SubMenuExpandingMode
```cangjie
public enum SubMenuExpandingMode { | SIDE_EXPAND | EMBEDDED_EXPAND | STACK_EXPAND }
```
功能： Menu子菜单展开样式枚举。

#### EMBEDDED_EXPAND

```cangjie
EMBEDDED_EXPAND
```
功能： 直接展开样式, 子菜单嵌于主菜单内展开。

#### SIDE_EXPAND

```cangjie
SIDE_EXPAND
```
功能： 默认展开样式, 子菜单位于同一平面侧边展开。

#### STACK_EXPAND

```cangjie
STACK_EXPAND
```
功能： 堆叠样式, 子菜单浮于主菜单上方展开。



## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*
import kit.LocalizationKit.*

@Entry
@Component
class EntryView {
 @State
 var select: Bool = true
 let iconStr: AppResource = @r(app.media.startIcon)
 var iconStr2: AppResource = @r(app.media.right)

 @Builder
 func SubMenu() {
 Menu() {
 MenuItem(startIcon: "", content: "复制", endIcon: "", labelInfo: "Ctrl+C")
 MenuItem(startIcon: "", content: "粘贴", endIcon: "", labelInfo: "Ctrl+V")
 }
 }

 @Builder
 func MyMenu() {
 Menu() {
 MenuItem(startIcon: @r(app.media.startIcon), content: @r(app.string.contentName),
 endIcon: @r(app.media.blank), labelInfo: @r(app.string.emptyName))
 MenuItem(startIcon: @r(app.media.startIcon), content: @r(app.string.contentName),
 endIcon: @r(app.media.blank), labelInfo: @r(app.string.emptyName)).enabled(false)
 MenuItem(
 startIcon: this.iconStr,
 content: @r(app.string.contentName),
 endIcon: this.iconStr,
 labelInfo: @r(app.string.emptyName),
 builder: {=> bind(this.SubMenu, this)()}
 )
 MenuItemGroup(header: "小标题", footer: "") {
 =>
 MenuItem(
 startIcon: this.iconStr,
 content: @r(app.string.contentName),
 endIcon: @r(app.string.emptyName),
 labelInfo: @r(app.string.emptyName),
 builder: {=> bind(this.SubMenu, this)()}
 )
 MenuItem(
 startIcon: @r(app.media.startIcon),
 content: @r(app.string.contentName),
 endIcon: @r(app.media.right),
 labelInfo: @r(app.string.emptyName),
 builder: {=> bind(this.SubMenu, this)()}
 )
 MenuItem(
 startIcon: "",
 content: "菜单选项",
 endIcon: "",
 labelInfo: "",
 ).selectIcon(true).selected(
 select).onChange({
 selected => iconStr2 = @r(app.media.foreground)
 })
 }

 }
 }

 func build() {
 Row() {
 Column() {
 Text("click to show menu")
 .fontSize(50)
 .fontWeight(FontWeight.Bold)
 }.bindMenu(builder: this.MyMenu).width(50.percent)
 }.height(100.percent)
 }
}
```

