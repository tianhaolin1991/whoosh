# 边框设置

设置组件边框样式。

## Functions

### public func border(width!: Length, color!: ResourceColor = Color.BLACK, radius!: Length = 0.vp, style!: BorderStyle = BorderStyle.Solid): This

```cangjie
public func border(width!: Length, color!: ResourceColor = Color.BLACK, radius!: Length = 0.vp, style!: BorderStyle = BorderStyle.Solid): This
```
功能： 设置边框样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 命名参数。 边框宽度。 | width | - |
| ResourceColor | 否 | 命名参数。 边框颜色。 | color | Color.BLACK |
| Length | 否 | 命名参数。 边框圆角半径。 | radius | 0.vp |
| BorderStyle | 否 | 命名参数。 边框样式。 | style | BorderStyle.Solid |

### public func borderStyle(style: BorderStyle): This

```cangjie
public func borderStyle(style: BorderStyle): This
```
功能： 设置元素的边框线条样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| BorderStyle | 是 | 元素的边框样式。 初始值：BorderStyle.Solid。 | style | - |

### public func borderWidth(width: Length): This

```cangjie
public func borderWidth(width: Length): This
```
功能： 设置边框的宽度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 设置元素的边框宽度，不支持百分比。 | width | - |

### public func borderWidth(edgeWidths: EdgeWidths): This

```cangjie
public func borderWidth(edgeWidths: EdgeWidths): This
```
功能： 设置边框的宽度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| EdgeWidths | 是 | 设置元素的边框宽度，不支持百分比。 | edgeWidths | - |

### public func borderColor(color: ResourceColor): This

```cangjie
public func borderColor(color: ResourceColor): This
```
功能： 设置边框的颜色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 元素的边框颜色。 初始值：Color.BLACK。 | color | - |

### public open func borderRadius(radius: Length): This

```cangjie
public open func borderRadius(radius: Length): This
```
功能： 设置边框的圆角。圆角大小受组件尺寸限制，最大值为组件宽或高的一半。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 元素的边框圆角半径，支持百分比，百分比依据组件宽度。设置圆角后，可搭配 clip 属性进行裁剪，避免子组件超出组件自身。 | radius | - |

### public func borderRadius(topLeft!: Length = 0.vp, topRight!: Length = 0.vp, bottomLeft!: Length = 0.vp, bottomRight!: Length = 0.vp): This

```cangjie
public func borderRadius(topLeft!: Length = 0.vp, topRight!: Length = 0.vp, bottomLeft!: Length = 0.vp, bottomRight!: Length = 0.vp): This
```
功能： 设置边框的圆角。圆角大小受组件尺寸限制，最大值为组件宽或高的一半。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 否 | 命名参数。 左上角圆角半径。 | topLeft | 0.vp |
| Length | 否 | 命名参数。 右上角圆角半径。 | topRight | 0.vp |
| Length | 否 | 命名参数。 左下角圆角半径。 | bottomLeft | 0.vp |
| Length | 否 | 命名参数。 右下角圆角半径。 | bottomRight | 0.vp |


## Classes

### LocalizedEdgeWidths

class LocalizedEdgeWidths
```cangjie
public class LocalizedEdgeWidths { public var bottom: Option<Length> public var end: Option<Length> public var start: Option<Length> public var top: Option<Length> public init( bottom!: Option<Length> = Option.None, end!: Option<Length> = Option.None, start!: Option<Length> = Option.None, top!: Option<Length> = Option.None ) }
```
功能： 边框宽度类型，用于描述组件边框不同方向的宽度。引入该对象时，至少传入一个参数。

#### var bottom

```cangjie
public var bottom: Option<Length>
```
功能： 下侧边框宽度。
Type: Option< Length >; Readable: true; Writable: true

#### var end

```cangjie
public var end: Option<Length>
```
功能： 右侧边框宽度。从右至左显示语言模式下为左侧边框宽度。
Type: Option< Length >; Readable: true; Writable: true

#### var start

```cangjie
public var start: Option<Length>
```
功能： 左侧边框宽度。从右至左显示语言模式下为右侧边框宽度。
Type: Option< Length >; Readable: true; Writable: true

#### var top

```cangjie
public var top: Option<Length>
```
功能： 上侧边框宽度。
Type: Option< Length >; Readable: true; Writable: true


### EdgeColors

class EdgeColors
```cangjie
public class EdgeColors { public var bottom: Option<ResourceColor> public var left: Option<ResourceColor> public var top: Option<ResourceColor> public var right: Option<ResourceColor> public init( bottom!: Option<ResourceColor> = Option.None, left!: Option<ResourceColor> = Option.None, top!: Option<ResourceColor> = Option.None, right!: Option<ResourceColor> = Option.None ) }
```
功能： 边框颜色类型，用于描述组件边框不同方向的颜色。

#### var bottom

```cangjie
public var bottom: Option<ResourceColor>
```
功能： 下侧边框颜色。
Type: Option< ResourceColor >; Readable: true; Writable: true

#### var left

```cangjie
public var left: Option<ResourceColor>
```
功能： 左侧边框颜色。
Type: Option< ResourceColor >; Readable: true; Writable: true

#### var right

```cangjie
public var right: Option<ResourceColor>
```
功能： 右侧边框颜色。
Type: Option< ResourceColor >; Readable: true; Writable: true

#### var top

```cangjie
public var top: Option<ResourceColor>
```
功能： 上侧边框颜色。
Type: Option< ResourceColor >; Readable: true; Writable: true


### LocalizedEdgeColors

class LocalizedEdgeColors
```cangjie
public class LocalizedEdgeColors { public var bottom: Option<ResourceColor> public var end: Option<ResourceColor> public var start: Option<ResourceColor> public var top: Option<ResourceColor> public init( bottom!: Option<ResourceColor> = Option.None, end!: Option<ResourceColor> = Option.None, start!: Option<ResourceColor> = Option.None， top!: Option<ResourceColor> = Option.None ) }
```
功能： 边框颜色，用于描述组件边框四条边的颜色。

#### var bottom

```cangjie
public var bottom: Option<ResourceColor>
```
功能： 下侧边框颜色。
Type: Option< ResourceColor >; Readable: true; Writable: true

#### var end

```cangjie
public var end: Option<ResourceColor>
```
功能： 右侧边框颜色。从右至左显示语言模式下为左侧边框颜色。
Type: Option< ResourceColor >; Readable: true; Writable: true

#### var start

```cangjie
public var start: Option<ResourceColor>
```
功能： 左侧边框颜色。从右至左显示语言模式下为右侧边框颜色。
Type: Option< ResourceColor >; Readable: true; Writable: true

#### var top

```cangjie
public var top: Option<ResourceColor>
```
功能： 上侧边框颜色。
Type: Option< ResourceColor >; Readable: true; Writable: true


### LocalizedBorderRadiuses

class LocalizedBorderRadiuses
```cangjie
public class LocalizedBorderRadiuses { public var bottomEnd: Option<Length> public var bottomStart: Option<Length> public var topEnd: Option<Length> public var topStart: Option<Length> public init( bottomEnd!: Option<Length> = Option.None, bottomStart!: Option<Length> = Option.None, topEnd!: Option<Length> = Option.None, topStart!: Option<Length> = Option.None ) }
```
功能： 圆角类型，用于描述组件边框圆角半径。

#### var bottomEnd

```cangjie
public var bottomEnd: Option<Length>
```
功能： 右下角圆角半径。从右至左显示语言模式下为左下角圆角半径。
Type: Option< Length >; Readable: true; Writable: true

#### var bottomStart

```cangjie
public var bottomStart: Option<Length>
```
功能： 左下角圆角半径。从右至左显示语言模式下为右下角圆角半径。
Type: Option< Length >; Readable: true; Writable: true

#### var topEnd

```cangjie
public var topEnd: Option<Length>
```
功能： 右上角圆角半径。从右至左显示语言模式下为左上角圆角半径。
Type: Option< Length >; Readable: true; Writable: true

#### var topStart

```cangjie
public var topStart: Option<Length>
```
功能： 左上角圆角半径。从右至左显示语言模式下为右上角圆角半径。
Type: Option< Length >; Readable: true; Writable: true


### EdgeWidths

class EdgeWidths
```cangjie
public class EdgeWidths { public var top: Length public var right: Length public var bottom: Length public var left: Length public init(top!: Length = 0.vp, right!: Length = 0.vp, bottom!: Length = 0.vp, left!: Length = 0.vp) }
```
功能： 设置弹窗背板的边框宽度。引入该对象时，至少传入一个参数。

#### var top

```cangjie
public var top: Length
```
功能： 上侧边框宽度。
Type: Length; Readable: true; Writable: true

#### var right

```cangjie
public var right: Length
```
功能： 右侧边框宽度。
Type: Length; Readable: true; Writable: true

#### var left

```cangjie
public var left: Length
```
功能： 左侧边框宽度。
Type: Length; Readable: true; Writable: true

#### var bottom

```cangjie
public var bottom: Length
```
功能： 下侧边框宽度。
Type: Length; Readable: true; Writable: true



## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 func build() {
 Column() {
 Flex(FlexParams(justifyContent: FlexAlign.SpaceAround, alignItems: ItemAlign.Center)) {
 Text("dashed")
 .borderStyle(BorderStyle.Dashed).borderWidth(5).borderColor(0xAFEEEE).borderRadius(10)
 .width(120).height(120).textAlign(TextAlign.Center).fontSize(16)

 Text("dotted")
 .border(width: 5.px, color: Color(0x317AF7), radius: 10.px, style: BorderStyle.Dotted)
 .width(120).height(120).textAlign(TextAlign.Center).fontSize(16)
 }.width(100.percent).height(150)
 }
 }
}
```

