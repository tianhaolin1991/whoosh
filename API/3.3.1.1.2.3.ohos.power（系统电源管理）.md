# ohos.power（系统电源管理）

该模块主要提供重启、关机、查询屏幕状态等接口。

```cangjie
import kit.BasicServicesKit.*
```
## Functions

### public func getPowerMode(): DevicePowerMode

```cangjie
public func getPowerMode(): DevicePowerMode
```
功能： 获取当前设备的电源模式。

**Returns:**

| type | description |
| --- | --- |
| DevicePowerMode | 电源模式。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 系统电源管理错误码 。

| code | description |
| --- | --- |
| 4900101 | Failed to connect to the service. |


### public func isActive(): Bool

```cangjie
public func isActive(): Bool
```
功能： 检测当前设备是否处于活动状态。有屏的设备为亮屏状态，无屏的设备为非休眠状态。

**Returns:**

| type | description |
| --- | --- |
| Bool | 处于活动状态返回true，否则返回false。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 系统电源管理错误码 。

| code | description |
| --- | --- |
| 4900101 | Failed to connect to the service. |


**Example:**

```cangjie
// index.cj

import kit.BasicServicesKit.*
import ohos.base.*

try {
 let ret = isActive()
 AppLog.info("test_power_isActive ret is :${ret}")
} catch (e: Exception) {
 AppLog.error("test_power_isActive ret is :${e.message.toString()}")
}
```

### public func isStandby(): Bool

```cangjie
public func isStandby(): Bool
```
功能： 检测当前设备是否进入待机低功耗续航模式。

**Returns:**

| type | description |
| --- | --- |
| Bool | 进入待机模式返回true，否则返回false。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 系统电源管理错误码 。

| code | description |
| --- | --- |
| 4900101 | Failed to connect to the service. |



## Enums

### DevicePowerMode

enum DevicePowerMode
```cangjie
public enum DevicePowerMode { | MODE_NORMAL | MODE_POWER_SAVE | MODE_PERFORMANCE | MODE_EXTREME_POWER_SAVE | ... }
```
功能： 表示电源模式的枚举值。

#### MODE_EXTREME_POWER_SAVE

```cangjie
MODE_EXTREME_POWER_SAVE
```
功能： 表示超级省电模式。

#### MODE_NORMAL

```cangjie
MODE_NORMAL
```
功能： 表示标准模式，默认值。

#### MODE_PERFORMANCE

```cangjie
MODE_PERFORMANCE
```
功能： 表示性能模式。

#### MODE_POWER_SAVE

```cangjie
MODE_POWER_SAVE
```
功能： 表示省电模式。



