# Web

提供具有网页显示能力的Web组件， @ohos.web.webview 提供web控制能力。

## Component Constructors

### public init( src!: String = "", controller!: WebviewController = WebviewController() )

```cangjie
public init( src!: String = "", controller!: WebviewController = WebviewController() )
```
功能： 创建一个Web组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 否 | 命名参数。 src不能通过状态变量（例如：@State）动态更改地址。 | src | "" |
| WebviewController | 否 | 命名参数。 设置Web控制器。 | controller | WebviewController() |

### public init( src!: AppResource, controller!: WebviewController = WebviewController() )

```cangjie
public init( src!: AppResource, controller!: WebviewController = WebviewController() )
```
功能： 创建一个Web组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 命名参数。 网页资源地址。如果访问本地资源文件，请使用@rawfile或者resource协议。如果加载应用包外沙箱路径的本地资源文件，请使用file://沙箱文件路径。 | src | - |
| WebviewController | 否 | 命名参数。 设置Web控制器。 | controller | WebviewController() |


## Functions

### public func allowWindowOpenMethod(flag: Bool): This

```cangjie
public func allowWindowOpenMethod(flag: Bool): This
```
功能： 设置网页是否可以通过JavaScript自动打开新窗口。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 网页是否可以通过JavaScript自动打开窗口。true表示网页可以通过JavaScript自动打开窗口，false表示网页不可以通过JavaScript自动打开窗口。初始值与系统参数关联，当系统参数persist.web.allowWindowOpenMethod.enabled为true时，初始值为true，否则为false。 | flag | - |

### public func blockNetwork(block: Bool): This

```cangjie
public func blockNetwork(block: Bool): This
```
功能： 设置Web组件是否阻止从网络加载资源。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | Web组件是否阻止从网络加载资源。true表示设置Web组件阻止从网络加载资源，false表示设置Web组件不阻止从网络加载资源。 初始值：false。 | block | - |

### public func cacheMode(cacheMode: CacheMode): This

```cangjie
public func cacheMode(cacheMode: CacheMode): This
```
功能： 设置缓存模式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| CacheMode | 是 | 缓存模式。 初始值：CacheMode.Default。 | cacheMode | - |

### public func copyOptions(value: CopyOptions): This

```cangjie
public func copyOptions(value: CopyOptions): This
```
功能： 设置剪贴板复制范围选项。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| CopyOptions | 是 | 剪贴板复制范围选项。 初始值：CopyOptions.LocalDevice。 | value | - |

### public func darkMode(mode: WebDarkMode): This

```cangjie
public func darkMode(mode: WebDarkMode): This
```
功能： 设置Web深色模式，默认关闭。当深色模式开启时，Web将启用媒体查询prefers-color-scheme中网页所定义的深色样式，若网页未定义深色样式，则保持原状。如需开启强制深色模式，建议配合 forceDarkAccess 使用。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| WebDarkMode | 是 | Web的深色模式为关闭、开启或跟随系统。 初始值：WebDarkMode.Off。 | mode | - |

### public func databaseAccess(databaseAccess: Bool): This

```cangjie
public func databaseAccess(databaseAccess: Bool): This
```
功能： 设置是否开启数据库存储API权限，默认不开启。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否开启数据库存储API权限。true表示设置开启数据库存储API权限，false表示设置不开启数据库存储API权限。 初始值：false。 | databaseAccess | - |

### public func defaultFixedFontSize(size: Int32): This

```cangjie
public func defaultFixedFontSize(size: Int32): This
```
功能： 设置网页的默认等宽字体大小。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 设置网页的默认等宽字体大小，单位px。输入值的范围为-2^31到2^31-1，实际渲染时超过72的值按照72进行渲染，低于1的值按照1进行渲染。 初始值：13。 | size | - |

### public func defaultFontSize(size: Int32): This

```cangjie
public func defaultFontSize(size: Int32): This
```
功能： 设置网页的默认字体大小。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 网页的默认字体大小，单位px。输入值的范围为-2^31到2^31-1，实际渲染时超过72的值按照72进行渲染，低于1的值按照1进行渲染。 初始值：16。 | size | - |

### public func defaultTextEncodingFormat(textEncodingFormat: String): This

```cangjie
public func defaultTextEncodingFormat(textEncodingFormat: String): This
```
功能： 设置网页的默认字符编码。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 默认字符编码。 初始值："UTF-8"。 | textEncodingFormat | - |

### public func domStorageAccess(domStorageAccess: Bool): This

```cangjie
public func domStorageAccess(domStorageAccess: Bool): This
```
功能： 设置是否开启文档对象模型存储接口（DOM Storage API）权限。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否开启文档对象模型存储接口（DOM Storage API）权限。true表示开启，false表示未开启。 初始值：false。 | domStorageAccess | - |

### public func editMenuOptions(onCreateMenu: (Array<TextMenuItem>) -> Array<TextMenuItem>, onMenuItemClick: (TextMenuItem, Int32, Int32) -> Bool): This

```cangjie
public func editMenuOptions(onCreateMenu: (Array<TextMenuItem>) -> Array<TextMenuItem>, onMenuItemClick: (TextMenuItem, Int32, Int32) -> Bool): This
```
功能： Web组件自定义文本选择菜单。用户可以通过该属性设置自定义的文本菜单。 该函数在菜单选项被点击后触发，并根据返回值决定是否执行系统默认的回调。返回true不执行系统回调，返回false继续执行系统回调。本接口在与 selectionMenuOptions 同时使用时，会使selectionMenuOptions不生效。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| (Array< TextMenuItem >)->Array< TextMenuItem > | 是 | 可以修改、增加、删除菜单选项，如果希望不显示文本菜单，需要返回空数组。 | onCreateMenu | - |
| ( TextMenuItem ,Int32,Int32)->Bool | 是 | 可以自定义菜单选项的回调函数。该函数在菜单选项被点击后触发，并根据返回值决定是否执行系统默认的回调。返回true不执行系统回调，返回false继续执行系统回调。 | onMenuItemClick | - |

### public func enableNativeEmbedMode(mode: Bool): This

```cangjie
public func enableNativeEmbedMode(mode: Bool): This
```
功能： 设置是否开启同层渲染功能，默认不开启。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否开启同层渲染功能。true表示开启同层渲染功能，false表示不开启同层渲染功能。 初始值：false。 | mode | - |

### public func enableNativeMediaPlayer(enable!: Bool = false, shouldOverlay!: Bool = false): This

```cangjie
public func enableNativeMediaPlayer(enable!: Bool = false, shouldOverlay!: Bool = false): This
```
功能： 开启应用接管网页媒体播放功能。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 否 | 命名参数。 是否开启该功能。true：开启，false：关闭。 初始值：false。 | enable | false |
| Bool | 否 | 命名参数。 该功能开启后， 应用接管网页视频的播放器画面是否覆盖网页内容。true：开启，false：关闭。 初始值：false。 | shouldOverlay | false |

### public func fileAccess(fileAccess: Bool): This

```cangjie
public func fileAccess(fileAccess: Bool): This
```
功能： 设置是否开启应用中文件系统的访问，默认启用。rawfile路径的文件不受该属性影响而限制访问。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否开启应用中文件系统的访问，默认启用。 | fileAccess | - |

### public func forceDarkAccess(access: Bool): This

```cangjie
public func forceDarkAccess(access: Bool): This
```
功能： 设置网页是否开启强制深色模式。默认关闭。该属性仅在darkMode开启深色模式时生效。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 设置网页是否开启强制深色模式。true：开启，false：关闭。 | access | - |

### public func geolocationAccess(geolocationAccess: Bool): This

```cangjie
public func geolocationAccess(geolocationAccess: Bool): This
```
功能： 设置是否开启获取地理位置权限，默认开启。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 设置是否开启获取地理位置权限。 | geolocationAccess | - |

### public func horizontalScrollBarAccess(horizontalScrollBar: Bool): This

```cangjie
public func horizontalScrollBarAccess(horizontalScrollBar: Bool): This
```
功能： 设置是否显示横向滚动条，包括系统默认滚动条和用户自定义滚动条。默认显示。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否显示横向滚动条。true表示设置显示横向滚动条，false表示设置不显示横向滚动条。 初始值：true。 | horizontalScrollBar | - |

### public func imageAccess(imageAccess: Bool): This

```cangjie
public func imageAccess(imageAccess: Bool): This
```
功能： 设置是否允许自动加载图片资源，默认允许。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否允许自动加载图片资源。 | imageAccess | - |

### public func initialScale(percent: Float32): This

```cangjie
public func initialScale(percent: Float32): This
```
功能： 设置整体页面的缩放百分比，默认为100。

### public func javaScriptAccess(javaScriptAccess: Bool): This

```cangjie
public func javaScriptAccess(javaScriptAccess: Bool): This
```
功能： 设置是否允许执行JavaScript脚本，默认允许执行。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否允许执行JavaScript脚本。true表示允许执行JavaScript脚本，false表示不允许执行JavaScript脚本。 初始值：true。 | javaScriptAccess | - |

### public func javaScriptOnDocumentEnd(scripts: Array<ScriptItem>): This

```cangjie
public func javaScriptOnDocumentEnd(scripts: Array<ScriptItem>): This
```
功能： 将JavaScript脚本注入到Web组件中，当指定页面或者文档加载完成时，该脚本将在其来源与scriptRules匹配的任何页面中执行。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array< ScriptItem > | 是 | 需要注入的ScriptItem数组。 | scripts | - |

### public func javaScriptOnDocumentStart(scripts: Array<ScriptItem>): This

```cangjie
public func javaScriptOnDocumentStart(scripts: Array<ScriptItem>): This
```
功能： 注入JavaScript对象到window对象中，并在window对象中调用该对象的方法。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array< ScriptItem > | 是 | 需要注入的ScriptItem数组。 | scripts | - |

### public func javaScriptProxy(funcList!: Array<(String)->String>, name!: String, methodList!: Array<String>, controller!: WebviewController): This

```cangjie
public func javaScriptProxy(funcList!: Array<(String)->String>, name!: String, methodList!: Array<String>, controller!: WebviewController): This
```
功能： 注入JavaScript对象到window对象中，并在window对象中调用该对象的方法。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array<(String)->String> | 是 | 命名参数。 参与注册的应用侧JavaScript对象的同步方法。 | funcList | - |
| String | 是 | 命名参数。 注册对象的名称，与window中调用的对象名一致。 | name | - |
| Array<String> | 是 | 命名参数。 参与注册的应用侧JavaScript对象的异步方法。 | methodList | - |
| WebviewController | 是 | 命名参数。 设置Web控制器 | controller | - |

### public func keyboardAvoidMode(mode: WebKeyboardAvoidMode): This

```cangjie
public func keyboardAvoidMode(mode: WebKeyboardAvoidMode): This
```
功能： Web组件自定义软件键盘避让模式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| WebKeyboardAvoidMode | 是 | Web软键盘避让模式。默认是WebKeyboardAvoidMode.RESIZE_CONTENT避让行为。嵌套滚动场景下不推荐使用web软键盘避让，包括RESIZE_VISUAL与RESIZE_CONTENT。 | mode | - |

### public func layoutMode(mode: WebLayoutMode): This

```cangjie
public func layoutMode(mode: WebLayoutMode): This
```
功能： 设置Web布局模式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| WebLayoutMode | 是 | web布局模式，跟随系统或自适应布局。 初始值：WebLayoutMode.NONE。 | mode | - |

### public func mediaOptions(resumeInterval!: Int32 = 0, audioExclusive!: Bool = true): This

```cangjie
public func mediaOptions(resumeInterval!: Int32 = 0, audioExclusive!: Bool = true): This
```
功能： Web媒体策略的配置。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 否 | 命名参数。 被暂停的Web音频能够自动续播的有效期，单位：秒。有效期范围0~60秒，如若超过60秒，按照60s处理，由于近似值原因，该有效期可能存在一秒内的误差。 | resumeInterval | 0 |
| Bool | 否 | 命名参数。 应用内多个Web实例的音频是否独占。true表示独占，false表示不独占。 | audioExclusive | true |

### public func mediaPlayGestureAccess(access: Bool): This

```cangjie
public func mediaPlayGestureAccess(access: Bool): This
```
功能： 设置有声视频播放是否需要用户手动点击，静音视频播放不受该接口管控，默认需要。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 有声视频播放是否需要用户手动点击。true表示设置有声视频播放需要用户手动点击，false表示设置有声视频播放不需要用户手动点击。 初始值：true。 | access | - |

### public func metaViewport(enabled: Bool): This

```cangjie
public func metaViewport(enabled: Bool): This
```
功能： 设置meta标签的viewport属性是否可用。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否支持meta标签的viewport属性。true表示支持meta标签的viewport属性，false表示不支持meta标签的viewport属性。 初始值：true。 | enabled | - |

### public func minFontSize(size: Int32): This

```cangjie
public func minFontSize(size: Int32): This
```
功能： 设置网页字体大小最小值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 网页字体大小最小值，单位px。输入值的范围为-2^31到2^31-1，实际渲染时超过72的值按照72进行渲染，低于1的值按照1进行渲染。 初始值：8。 | size | - |

### public func minLogicalFontSize(size: Int32): This

```cangjie
public func minLogicalFontSize(size: Int32): This
```
功能： 设置网页逻辑字体大小最小值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 网页逻辑字体大小最小值，单位px。输入值的范围为-2^31到2^31-1，实际渲染时超过72的值按照72进行渲染，低于1的值按照1进行渲染。 初始值：8 | size | - |

### public func mixedMode(mixedMode: MixMode): This

```cangjie
public func mixedMode(mixedMode: MixMode): This
```
功能： 设置是否允许加载超文本传输协议（HTTP）和超文本传输安全协议（HTTPS）混合内容，默认不允许加载HTTP和HTTPS混合内容。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| MixMode | 是 | 混合内容。 初始值：MixedMode.None，表示不允许安全来源（secure origin）加载不安全来源（insecure origin）的内容。 | mixedMode | - |

### public func multiWindowAccess(multiWindow: Bool): This

```cangjie
public func multiWindowAccess(multiWindow: Bool): This
```
功能： 设置是否开启多窗口权限，默认不开启。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否开启多窗口权限。true表示设置开启多窗口权限，false表示设置不开启多窗口权限。 初始值：false。 | multiWindow | - |

### public func nestedScroll( scrollForward !: NestedScrollMode = NestedScrollMode.SELF_FIRST, scrollBackward !: NestedScrollMode = NestedScrollMode.SELF_FIRST ): This

```cangjie
public func nestedScroll( scrollForward !: NestedScrollMode = NestedScrollMode.SELF_FIRST, scrollBackward !: NestedScrollMode = NestedScrollMode.SELF_FIRST ): This
```
功能： 设置向前向后两个方向上的嵌套滚动模式，实现与父组件的滚动联动。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| NestedScrollMode | 否 | 命名参数。 向前滚动方向。 | scrollForward | NestedScrollMode.SELF_FIRST |
| NestedScrollMode | 否 | 命名参数。 向后滚动方向。 | scrollBackward | NestedScrollMode.SELF_FIRST |

### public func overScrollMode(mode: OverScrollMode): This

```cangjie
public func overScrollMode(mode: OverScrollMode): This
```
功能： 设置Web过滚动模式，默认关闭。当过滚动模式开启时，当用户在Web根页面上滑动到边缘时，Web会通过弹性动画弹回界面，根页面上的内部页面不会触发回弹。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| OverScrollMode | 是 | Web的过滚动模式为关闭或开启。 初始值：OverScrollMode.NEVER。 | mode | - |

### public func overviewModeAccess(overviewModeAccess: Bool): This

```cangjie
public func overviewModeAccess(overviewModeAccess: Bool): This
```
功能： 设置是否使用概览模式加载网页，默认使用该方式。当前仅支持移动设备。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 设置是否使用概览模式加载网页。true表示设置使用概览模式加载网页，false表示设置不使用概览模式加载网页。 初始值：true。 | overviewModeAccess | - |

### public func pinchSmooth(isEnabled: Bool): This

```cangjie
public func pinchSmooth(isEnabled: Bool): This
```
功能： 设置网页是否开启捏合流畅模式，默认不开启。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 网页是否开启捏合流畅模式。true表示设置网页开启捏合流畅模式，false表示设置网页不开启捏合流畅模式。 初始值：false。 | isEnabled | - |

### public func registerNativeEmbedRule(tag: String, `type`: String): This

```cangjie
public func registerNativeEmbedRule(tag: String, `type`: String): This
```
功能： 注册使用同层渲染的HTML标签名和类型。标签名仅支持使用object和embed。标签类型只能使用ASCII可显示字符。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 标签名。 | tag | - |
| String | 是 | 标签类型,内核使用前缀匹配此参数。 | type | - |

### public func selectionMenuOptions(expandedMenuOptions: Array<ExpandedMenuItemOptions>): This

```cangjie
public func selectionMenuOptions(expandedMenuOptions: Array<ExpandedMenuItemOptions>): This
```
功能： Web组件自定义菜单扩展项接口，允许用户设置扩展项的文本内容、图标、回调方法。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array< ExpandedMenuItemOptions > | 是 | 扩展菜单选项。 菜单项数量，及菜单的content大小、startIcon图标尺寸，与ArkUI Menu组件保持一致。 | expandedMenuOptions | - |

### public func textAutosizing(textAutosizing: Bool): This

```cangjie
public func textAutosizing(textAutosizing: Bool): This
```
功能： 设置使能文本自动调整大小。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 文本自动调整大小。true表示文本自动调整大小，false表示文本不自动调整大小。 初始值：true。 | textAutosizing | - |

### public func textZoomRatio(textZoomRatio: Int32): This

```cangjie
public func textZoomRatio(textZoomRatio: Int32): This
```
功能： 设置页面的文本缩放百分比，默认为100。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 页面的文本缩放百分比。取值为整数，范围为(0, 2147483647]。 初始值：100。 | textZoomRatio | - |

### public func verticalScrollBarAccess(verticalScrollBar: Bool): This

```cangjie
public func verticalScrollBarAccess(verticalScrollBar: Bool): This
```
功能： 设置是否显示纵向滚动条，包括系统默认滚动条和用户自定义滚动条。默认显示。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否显示纵向滚动条。true表示设置显示纵向滚动条，false表示设置不显示纵向滚动条。 初始值：true。 | verticalScrollBar | - |

### public func webCursiveFont(family: String): This

```cangjie
public func webCursiveFont(family: String): This
```
功能： 设置网页的cursive font字体库。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 网页的cursive font字体库。 初始值：cursive。 | family | - |

### public func webFantasyFont(family: String): This

```cangjie
public func webFantasyFont(family: String): This
```
功能： 设置网页的fantasy font字体库。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 网页的fantasy font字体库。 初始值：fantasy。 | family | - |

### public func webFixedFont(family: String): This

```cangjie
public func webFixedFont(family: String): This
```
功能： 设置网页的fixed font字体库。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 网页的fixed font字体库。 初始值：monospace。 | family | - |

### public func webSansSerifFont(family: String): This

```cangjie
public func webSansSerifFont(family: String): This
```
功能： 设置网页的sans serif font字体库。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 网页的sans serif font字体库。 初始值：sans-serif。 | family | - |

### public func webSerifFont(family: String): This

```cangjie
public func webSerifFont(family: String): This
```
功能： 设置网页的serif font字体库。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 网页的serif font字体库。 初始值：serif。 | family | - |

### public func webStandardFont(family: String): This

```cangjie
public func webStandardFont(family: String): This
```
功能： 设置网页的standard font字体库。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 网页的standard font字体库。 初始值：sans serif。 | family | - |

### public func zoomAccess(zoomAccess: Bool): This

```cangjie
public func zoomAccess(zoomAccess: Bool): This
```
功能： 设置是否支持手势进行缩放，默认允许执行缩放。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否支持手势进行缩放。true表示设置支持手势进行缩放，false表示设置不支持手势进行缩放。 初始值：true。 | zoomAccess | - |

### public func onAdsBlocked(callback: (AdsBlockedDetails) -> Unit): This

```cangjie
public func onAdsBlocked(callback: (AdsBlockedDetails) -> Unit): This
```
功能： 一个页面发生广告过滤后，通过此回调接口通知过滤的详细信息。由于页面可能随时发生变化并不断产生网络请求，为了减少通知频次、降低对页面加载过程的影响，仅在页面加载完成时进行首次通知，此后发生的过滤将间隔1秒钟上报，无广告过滤则无通知。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( AdsBlockedDetails )->Unit | 是 | 回调函数，onAdsBlocked的回调。 | callback | - |

### public func onAlert(callback: (OnAlertEvent) -> Bool): This

```cangjie
public func onAlert(callback: (OnAlertEvent) -> Bool): This
```
功能： 网页触发alert()告警弹窗时触发回调。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( OnAlertEvent )->Bool | 是 | 网页触发alert()告警弹窗时触发返回值boolean。当回调返回true时，应用可以调用自定义弹窗能力（包括确认和取消），并且需要根据用户的确认或取消操作调用WebResult通知Web组件最终是否离开当前页面。当回调返回false时，函数中绘制的自定义弹窗无效。 | callback | - |

### public func onBeforeUnload(callback: (OnBeforeUnloadEvent) -> Bool): This

```cangjie
public func onBeforeUnload(callback: (OnBeforeUnloadEvent) -> Bool): This
```
功能： 刷新或关闭场景下，在即将离开当前页面时触发此回调。刷新或关闭当前页面应先通过点击等方式获取焦点，才会触发此回调。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( OnBeforeUnloadEvent )->Bool | 是 | 刷新或关闭场景下，在即将离开当前页面时触发。返回值boolean。当回调返回true时，应用可以调用自定义弹窗能力（包括确认和取消），并且需要根据用户的确认或取消操作调用WebResult通知Web组件最终是否离开当前页面。当回调返回false时，函数中绘制的自定义弹窗无效。 | callback | - |

### public func onConfirm(callback: (OnConfirmEvent) -> Bool): This

```cangjie
public func onConfirm(callback: (OnConfirmEvent) -> Bool): This
```
功能： 网页调用confirm()告警时触发此回调。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( OnConfirmEvent )->Bool | 是 | 网页调用confirm()告警时触发 返回值boolean。当回调返回true时，应用可以调用自定义弹窗能力（包括确认和取消），并且需要根据用户的确认或取消操作调用WebResult通知Web组件最终是否离开当前页面。当回调返回false时，函数中绘制的自定义弹窗无效。 | callback | - |

### public func onConsole(callback: (OnConsoleEvent) -> Bool): This

```cangjie
public func onConsole(callback: (OnConsoleEvent) -> Bool): This
```
功能： 通知宿主应用JavaScript console消息。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( OnConsoleEvent )->Bool | 是 | 网页收到JavaScript控制台消息时触发。返回值boolean。当返回true时，该条消息将不会再打印至控制台，反之仍会打印至控制台。 | callback | - |

### public func onControllerAttached(callback: () -> Unit): This

```cangjie
public func onControllerAttached(callback: () -> Unit): This
```
功能： 当Controller成功绑定到Web组件时触发该回调，并且该Controller必须为WebviewController，且禁止在该事件回调前调用Web组件相关的接口，否则会抛出异常。因该回调调用时网页还未加载，无法在回调中使用有关操作网页的接口，例如 zoomIn 、 zoomOut 等，可以使用 loadUrl 、 getWebId 等操作网页不相关的接口。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ()->Unit | 是 | 回调函数，当Controller成功绑定到Web组件时触发该回调。 | callback | - |

### public func onDownloadStart(callback: (OnDownloadStartEvent) -> Unit): This

```cangjie
public func onDownloadStart(callback: (OnDownloadStartEvent) -> Unit): This
```
功能： 通知主应用开始下载一个文件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( OnDownloadStartEvent )->Unit | 是 | 开始下载时触发。 | callback | - |

### public func onErrorReceive(callback: (OnErrorReceiveEvent) -> Unit): This

```cangjie
public func onErrorReceive(callback: (OnErrorReceiveEvent) -> Unit): This
```
功能： 网页加载遇到错误时触发该回调。主资源与子资源出错都会回调该接口，可以通过request.isMainFrame来判断是否是主资源报错。出于性能考虑，建议此回调中尽量执行简单逻辑。在无网络的情况下，触发此回调。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( OnErrorReceiveEvent )->Unit | 是 | 回调函数，网页收到 Web 资源加载错误时触发。 | callback | - |

### public func onLoadIntercept(callback: (WebResourceRequest) -> Bool): This

```cangjie
public func onLoadIntercept(callback: (WebResourceRequest) -> Bool): This
```
功能： 当Web组件加载url之前触发该回调，用于判断是否阻止此次访问。默认允许加载。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( WebResourceRequest )->Bool | 是 | 回调函数，截获资源加载时触发的回调。 返回值boolean。返回true表示阻止此次加载，否则允许此次加载。 | callback | - |

### public func onPageBegin(callback: (OnPageEvent) -> Unit): This

```cangjie
public func onPageBegin(callback: (OnPageEvent) -> Unit): This
```
功能： 网页开始加载时触发该回调，且只在主frame触发，iframe或者frameset的内容加载时不会触发此回调。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( OnPageEvent )->Unit | 是 | 回调函数，网页加载开始时触发回调。 | callback | - |

### public func onPageEnd(callback: (OnPageEvent) -> Unit): This

```cangjie
public func onPageEnd(callback: (OnPageEvent) -> Unit): This
```
功能： 网页加载完成时触发该回调，且只在主frame触发。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( OnPageEvent )->Unit | 是 | 网页加载结束时触发。 | callback | - |

### public func onPermissionRequest(callback: (OnPermissionRequestEvent) -> Unit): This

```cangjie
public func onPermissionRequest(callback: (OnPermissionRequestEvent) -> Unit): This
```
功能： 通知收到获取权限请求。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( OnPermissionRequestEvent )->Unit | 是 | 回调函数，通知收到获取权限请求触发。 | callback | - |

**Example:**

```cangjie
// main_ability.cj
internal import kit.UIKit.*
internal import kit.AbilityKit.*

var globalAbilityContext = Option<AbilityContext>.None
class MainAbility <: Ability {
 public init() {
 super()
 registerSelf()
 }

 public override func onCreate(want: Want, launchParam: LaunchParam): Unit {
 AppLog.info("MainAbility OnCreated.${want.abilityName}")
 match (launchParam.launchReason) {
 case LaunchReason.START_ABILITY => AppLog.info("START_ABILITY")
 case _ => ()
 }
 globalAbilityContext = Option<AbilityContext>.Some(this.context)
 }

 public override func onWindowStageCreate(windowStage: WindowStage): Unit {
 AppLog.info("MainAbility onWindowStageCreate.")
 windowStage.loadContent("EntryView")
 }
}

//index.cj
package ohos_app_cangjie_entry

import ohos.state_macro_manage.*
import kit.LocalizationKit.*
import kit.ArkWeb.*

let webController = WebviewController()
func getContext(): AbilityContext {
 match (globalAbilityContext) {
 case Some(context) =>
 AppLog.info("get globalAbilityContext successfully")
 context
 case None =>
 AppLog.error("get globalAbilityContext failed")
 throw Exception("get globalAbilityContext failed")
 }
}

@Entry
@Component
class EntryView {
 public func aboutToAppear() {
 WebviewController.setWebDebuggingAccess(true)
 var resultCallback = {
 errorCode: Option<AsyncError>, data: Option<AccessCtrlPermissionRequestResult> => match (errorCode) {
 case Some(e) => AppLog.info("permissionResultCallBack request error: errcode is ${e.code}")
 case _ => match (data) {
 case Some(value) => for (i in (0..value.permissions.size)) {
 AppLog.info("permissionResultCallBack: ${value.permissions[i]} - ${value.authResults[i]}")
 }
 case _ => AppLog.info("permissionResultCallBack request error: data is null")
 }
 }
 }
 let abilityAccessCtrl = AbilityAccessCtrl.createAtManager()
 let stageContext = getStageContext(getContext())
 abilityAccessCtrl.requestPermissionsFromUser(stageContext, ["ohos.permission.CAMERA", "ohos.permission.MICROPHONE"], resultCallback)
 }
 func build() {
 Column(40) {
 Web(src: @rawfile("index.html"), controller: webController)
 .onControllerAttached({
 => AppLog.info("controller attachec")
 })
 .enableNativeMediaPlayer(enable: true, shouldOverlay: true)
 .onPermissionRequest {
 event => AlertDialog.show(
 AlertDialogParamWithButtons(
 "text",
 title: 'title',
 primaryButton: AlertDialogButtonOptions(
 value: 'deny',
 action: {
 => event.request.deny();
 }
 ),
 secondaryButton: AlertDialogButtonOptions(
 value: 'onConfirm',
 action: {
 => event.request.grant(event.request.getAccessibleResource());
 }
 ),
 cancel: {
 => event.request.deny();
 }
 )
 )
 }
 }.width(100.percent)
 }
}
```

### public func onPrompt(callback: (OnPromptEvent) -> Bool): This

```cangjie
public func onPrompt(callback: (OnPromptEvent) -> Bool): This
```
功能： 网页调用prompt()告警时触发此回调。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ( OnPromptEvent )->Bool | 是 | 回调函数，网页调用prompt()告警时触发。返回值bool。当回调返回true时，应用可以调用自定义弹窗能力（包括确认和取消），并且需要根据用户的确认或取消操作调用WebResult通知Web组件最终是否离开当前页面。当回调返回false时，函数中绘制的自定义弹窗无效。 | callback | - |

### public func onlineImageAccess(onlineImageAccess: Bool): This

```cangjie
public func onlineImageAccess(onlineImageAccess: Bool): This
```
功能： 设置是否允许从网络加载图片资源（通过HTTP和HTTPS访问的资源），默认允许访问。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否允许从网络加载图片资源。true表示设置允许从网络加载图片资源，false表示设置不允许从网络加载图片资源。 初始值：true。 | onlineImageAccess | - |


## Classes

### ConsoleMessage

class ConsoleMessage
```cangjie
public class ConsoleMessage {}
```
功能： Web组件获取控制台信息对象。示例代码参考 onConsole 事件。


### PermissionRequest

class PermissionRequest
```cangjie
public class PermissionRequest {}
```
功能： Web组件返回授权或拒绝权限功能的对象。示例代码参考 onPermissionRequest 事件。


### WebResourceError

class WebResourceError
```cangjie
public class WebResourceError {}
```
功能： Web组件资源管理错误信息对象。示例代码参考 onErrorReceive 事件。


### WebResourceResponse

class WebResourceResponse
```cangjie
public class WebResourceResponse {}
```
功能： Web组件资源响应对象。示例代码参考 onHttpErrorReceive 事件。


### WebResult

class WebResult
```cangjie
public class WebResult { public WebResult( public let result: Int64 ) }
```
功能： 描述Web组件返回的弹窗确认或弹窗取消信息的参数结构。

#### let result

```cangjie
public let result: Int64
```
功能： Web组件返回的弹窗确认或弹窗取消信息。
Type: Int64; Readable: true; Writable: false


### AdsBlockedDetails

class AdsBlockedDetails
```cangjie
public class AdsBlockedDetails { public AdsBlockedDetails( public let adsBlocked: Array<String> ) }
```
功能： 描述发生广告拦截时，广告资源信息的参数结构。

#### let adsBlocked

```cangjie
public let adsBlocked: Array<String>
```
功能： 被过滤的资源的url或dompath标识，被过滤的多个对象url相同则可能出现重复元素。
Type: Array<String>; Readable: true; Writable: false

#### let url

```cangjie
public let url: String
```
功能： 发生广告过滤的页面url。
Type: String; Readable: true; Writable: false


### ExpandedMenuItemOptions

class ExpandedMenuItemOptions
```cangjie
public class ExpandedMenuItemOptions { public var content: String public var startIcon:?String = None public var action:(String) -> Unit public init(content!: String, startIcon!: ?String = None, action!: (String) -> Unit) public init(content!: AppResource, startIcon!: ?AppResource = None, action!: (String) -> Unit) }
```
功能： 描述自定义菜单扩展项的参数结构。

#### var action

```cangjie
public var action:(String) -> Unit
```
功能： 选中的文本信息。
Type: (String)->Unit; Readable: true; Writable: true

#### var content

```cangjie
public var content: String
```
功能： 显示内容。
Type: String; Readable: true; Writable: true

#### var startIcon

```cangjie
public var startIcon:?String = None
```
功能： 显示图标。
Type: ?String; Readable: true; Writable: true


### Header

class Header
```cangjie
public class Header {}
```
功能： 描述Web组件返回的请求/响应头对象。

#### var headerKey

```cangjie
public var headerKey: String
```
功能： 请求/响应头的key。
Type: String; Readable: true; Writable: true

#### var headerValue

```cangjie
public var headerValue: String
```
功能： 请求/响应头的Value。
Type: String; Readable: true; Writable: true


### OnAlertEvent

class OnAlertEvent
```cangjie
public class OnAlertEvent { public OnAlertEvent( public let url: String, public let message: String, public let result: WebResult ) }
```
功能： 描述定义网页触发alert()告警弹窗的参数结构。

#### let message

```cangjie
public let message: String
```
功能： 弹窗中显示的信息。
Type: String; Readable: true; Writable: false

#### let result

```cangjie
public let result: WebResult
```
功能： 通知Web组件用户操作行为。
Type: WebResult; Readable: true; Writable: false

#### let url

```cangjie
public let url: String
```
功能： 当前显示弹窗所在网页的URL。
Type: String; Readable: true; Writable: false


### OnBeforeUnloadEvent

class OnBeforeUnloadEvent
```cangjie
public class OnBeforeUnloadEvent { public OnBeforeUnloadEvent( public let url: String, public let message: String, public let result: WebResult ) }
```
功能： 描述定义刷新或关闭场景下，在即将离开当前页面的参数结构。

#### let message

```cangjie
public let message: String
```
功能： 弹窗中显示的信息。
Type: String; Readable: true; Writable: false

#### let result

```cangjie
public let result: WebResult
```
功能： 通知Web组件用户操作行为。
Type: WebResult; Readable: true; Writable: false

#### let url

```cangjie
public let url: String
```
功能： 当前显示弹窗所在网页的URL。
Type: String; Readable: true; Writable: false


### OnConfirmEvent

class OnConfirmEvent
```cangjie
public class OnConfirmEvent { public OnConfirmEvent( public let url: String, public let message: String, public let result: WebResult ) }
```
功能： 描述定义网页调用confirm()告警的参数结构。

#### let message

```cangjie
public let message: String
```
功能： 弹窗中显示的信息。
Type: String; Readable: true; Writable: false

#### let result

```cangjie
public let result: WebResult
```
功能： 通知Web组件用户操作行为。
Type: WebResult; Readable: true; Writable: false

#### let url

```cangjie
public let url: String
```
功能： 当前显示弹窗所在网页的URL。
Type: String; Readable: true; Writable: false


### OnConsoleEvent

class OnConsoleEvent
```cangjie
public class OnConsoleEvent { public OnConsoleEvent( public let message: ConsoleMessage ) }
```
功能： 描述通知宿主应用JavaScript console消息参数结构。

#### let message

```cangjie
public let message: ConsoleMessage
```
功能： 触发的控制台信息。
Type: ConsoleMessage; Readable: true; Writable: false


### OnDownloadStartEvent

class OnDownloadStartEvent
```cangjie
public class OnDownloadStartEvent { public OnDownloadStartEvent( public let url: String, public let userAgent: String, public let contentDisposition: String, public let mimetype: String, public let contentLength: Int64 ) }
```
功能： 定义通知主应用开始下载一个文件。

#### let contentDisposition

```cangjie
public let contentDisposition: String
```
功能： 服务器返回的 Content-Disposition响应头，可能为空。
Type: String; Readable: true; Writable: false

#### let contentLength

```cangjie
public let contentLength: Int64
```
功能： 服务器返回文件的长度。
Type: Int64; Readable: true; Writable: false

#### let mimetype

```cangjie
public let mimetype: String
```
功能： 服务器返回内容媒体类型（MIME）信息。
Type: String; Readable: true; Writable: false

#### let url

```cangjie
public let url: String
```
功能： 文件下载的URL。
Type: String; Readable: true; Writable: false

#### let userAgent

```cangjie
public let userAgent: String
```
功能： 用于下载的用户代理。
Type: String; Readable: true; Writable: false


### OnErrorReceiveEvent

class OnErrorReceiveEvent
```cangjie
public class OnErrorReceiveEvent { public OnErrorReceiveEvent( public let request: WebResourceRequest, public let error: WebResourceError ) }
```
功能： 描述网页加载遇到错误时触发该回调的参数结构。

#### let error

```cangjie
public let error: WebResourceError
```
功能： 网页加载资源错误的封装信息 。
Type: WebResourceError; Readable: true; Writable: false

#### let request

```cangjie
public let request: WebResourceRequest
```
功能： 网页请求的封装信息。
Type: WebResourceRequest; Readable: true; Writable: false


### OnHttpErrorReceiveEvent

class OnHttpErrorReceiveEvent
```cangjie
public class OnHttpErrorReceiveEvent { public OnHttpErrorReceiveEvent( public let request: WebResourceRequest, public let response: WebResourceResponse ) }
```
功能： 描述网页收到加载资源加载HTTP错误时触发。

#### let request

```cangjie
public let request: WebResourceRequest
```
功能： 网页请求的封装信息。
Type: WebResourceRequest; Readable: true; Writable: false

#### let response

```cangjie
public let response: WebResourceResponse
```
功能： 资源响应的封装信息。
Type: WebResourceResponse; Readable: true; Writable: false


### OnPageEvent

class OnPageEvent
```cangjie
public class OnPageEvent { public OnPageEvent( public var url: String ) }
```
功能： 描述Web组件加载时的回调信息参数结构。

#### var url

```cangjie
public var url: String
```
功能： 当前加载页面的URL。
Type: String; Readable: true; Writable: true


### OnPermissionRequestEvent

class OnPermissionRequestEvent
```cangjie
public class OnPermissionRequestEvent { public OnPermissionRequestEvent( public let request: PermissionRequest ) }
```
功能： 描述通知收到获取权限请求的参数结构。

#### let request

```cangjie
public let request: PermissionRequest
```
功能： 通知Web组件用户操作行为。
Type: PermissionRequest; Readable: true; Writable: false


### OnPromptEvent

class OnPromptEvent
```cangjie
public class OnPromptEvent { public OnPromptEvent( public let url: String, public let message: String, public let value: String, public let result: WebResult ) }
```
功能： 描述网页调用prompt()告警时触发此回调参数结构。

#### let message

```cangjie
public let message: String
```
功能： 弹窗中显示的信息。
Type: String; Readable: true; Writable: false

#### let result

```cangjie
public let result: WebResult
```
功能： 通知Web组件用户操作行为。
Type: WebResult; Readable: true; Writable: false

#### let url

```cangjie
public let url: String
```
功能： 当前显示弹窗所在网页的URL。
Type: String; Readable: true; Writable: false

#### let value

```cangjie
public let value: String
```
功能： 提示对话框的信息。
Type: String; Readable: true; Writable: false


### ScriptItem

class ScriptItem
```cangjie
public class ScriptItem { public ScriptItem( public var script: String, public var scriptRules: Array<String> ) }
```
功能： 描述通过 javaScriptOnDocumentStart 属性注入到Web组件的ScriptItem对象的参数结构。

#### var script

```cangjie
public var script: String
```
功能： 需要注入、执行的JavaScript脚本。
Type: String; Readable: true; Writable: true

#### var scriptRules

```cangjie
public var scriptRules: Array<String>
```
功能： 一组允许来源的匹配规则。
Type: Array<String>; Readable: true; Writable: true


### WebEvent

class WebEvent
```cangjie
public class WebEvent { public WebEvent( public var url: String, public var message: String, public var value: String, public var result: WebResult ) }
```
功能： 描述Web组件弹窗时的回调信息的参数结构。

#### var message

```cangjie
public var message: String
```
功能： 弹窗中显示的信息。
Type: String; Readable: true; Writable: true

#### var result

```cangjie
public var result: WebResult
```
功能： 页面返回的信息。
Type: WebResult; Readable: true; Writable: true

#### var url

```cangjie
public var url: String
```
功能： 当前弹窗所在页面的URL。
Type: String; Readable: true; Writable: true

#### var value

```cangjie
public var value: String
```
功能： 提示对话框的信息。
Type: String; Readable: true; Writable: true


### WebResourceRequest

class WebResourceRequest
```cangjie
public class WebResourceRequest {}
```
功能： 描述web组件获取资源请求对象。



## Enums

### CacheMode

enum CacheMode
```cangjie
public enum CacheMode { | Default | None | Online | Only }
```
功能： 设置缓存模式。

#### Default

```cangjie
Default
```
功能： 优先使用未过期cache加载资源，无效或无cache时从网络获取。

#### None

```cangjie
None
```
功能： 优先使用cache(含过期)加载资源，无cache时从网络获取。

#### Online

```cangjie
Online
```
功能： 强制从网络获取最新资源，不使用任何cache。

#### Only

```cangjie
Only
```
功能： 仅使用本地cache加载资源。


### MessageLevel

enum MessageLevel
```cangjie
public enum MessageLevel { | Debug | Error | Info | Log | Warn }
```
功能： ConsoleMessage的信息级别。

#### Debug

```cangjie
Debug
```
功能： 调试级别。

#### Error

```cangjie
Error
```
功能： 错误级别。

#### Info

```cangjie
Info
```
功能： 消极级别。

#### Log

```cangjie
Log
```
功能： 日志级别。

#### Warn

```cangjie
Warn
```
功能： 警告级别。


### OverScrollMode

enum OverScrollMode
```cangjie
public enum OverScrollMode { | NEVER | ALWAYS }
```
功能： 设置Web过滚动模式，默认关闭。当过滚动模式开启时，当用户在Web根页面上滑动到边缘时，Web会通过弹性动画弹回界面，根页面上的内部页面不会触发回弹。

#### ALWAYS

```cangjie
ALWAYS
```
功能： 设置Web的过滚动模式为开启。

#### NEVER

```cangjie
NEVER
```
功能： 设置Web的过滚动模式为关闭。


### WebKeyboardAvoidMode

enum WebKeyboardAvoidMode
```cangjie
public enum WebKeyboardAvoidMode { | RESIZE_VISUAL | RESIZE_CONTENT | OVERLAYS_CONTENT }
```
功能： 软键盘避让的模式。

#### OVERLAYS_CONTENT

```cangjie
OVERLAYS_CONTENT
```
功能： 不调整任何视口大小，不会触发软键盘避让。

#### RESIZE_CONTENT

```cangjie
RESIZE_CONTENT
```
功能： 软键盘避让时，同时调整可视视口和布局视口的大小。

#### RESIZE_VISUAL

```cangjie
RESIZE_VISUAL
```
功能： 软键盘避让时，仅调整可视视口大小，不调整布局视口大小。


### WebLayoutMode

enum WebLayoutMode
```cangjie
public enum WebLayoutMode { | NONE | FIT_CONTENT }
```
功能： 设置Web布局模式。

#### FIT_CONTENT

```cangjie
FIT_CONTENT
```
功能： Web布局跟随系统。

#### NONE

```cangjie
NONE
```
功能： Web基于页面大小的自适应网页布局。


### MixMode

enum MixMode
```cangjie
public enum MixMode { | All | Compatible | None public func getValue(): Int32 { match(this) { case All => 0 case None => 1 case Compatible => 2 } } }
```
功能： 设置是否允许加载超文本传输协议（HTTP）和超文本传输安全协议（HTTPS）混合内容，默认不允许加载HTTP和HTTPS混合内容。

#### All

```cangjie
All
```
功能： 允许加载HTTP和HTTPS混合内容。所有不安全的内容都可以被加载。

#### Compatible

```cangjie
All
```
功能： 混合内容兼容性模式，部分不安全的内容可能被加载。

#### None

```cangjie
All
```
功能： 不允许加载HTTP和HTTPS混合内容。



## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*
import ohos.webview.*

@Entry
@Component
class EntryView {
 let webController = WebviewController()
 @State var url: String = "www.example.com"
 func build() {
 Column(10) {
 Button("refresh")
 .onClick {
 evt =>
 AppLog.info("refresh")
 webController.refresh()
 }.width(400.px).height(150.px)
 Button("loadUrl")
 .onClick {
 evt =>
 AppLog.info("loadUrl")
 webController.loadUrl(this.url)

 }.width(400.px).height(150.px)
 Web(src: "www.example.com", controller: webController)
 .onPageBegin({evt =>
 AppLog.info("page begin url: ${evt.url}")
 })
 .onPageEnd({evt =>
 AppLog.info("page end url: ${evt.url}")
 })
 }
 }
}
```

