# CanvasPattern

一个Object对象，使用createPattern方法创建，通过指定图像和重复方式创建图片填充的模板。

```cangjie
import kit.UIKit.*
```
## Classes

### CanvasPattern

class CanvasPattern
```cangjie
public class CanvasPattern { public init() }
```
功能： 一个Object对象，使用createPattern方法创建，通过指定图像和重复方式创建图片填充的模板。



