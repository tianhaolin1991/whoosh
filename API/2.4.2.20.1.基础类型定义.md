# 基础类型定义

功能： Float64、Int64、AppResource 均实现了 Length 接口类型。

```cangjie
import kit.UIKit.*
```
## Interfaces

### Length

interface Length
```cangjie
public interface Length
```
功能： Float64、Int64、AppResource 均实现了 Length 接口类型。


### ResourceColor

interface ResourceColor
```cangjie
public interface ResourceColor
```
功能： Color、UInt32、Int64、AppResource 均实现了 ResourceColor 接口类型。



## Classes

### Color

class Color
```cangjie
public class Color <: ResourceColor { public static let BLACK: Color = Color(0xff000000) public static let BLUE: Color = Color(0xff0000ff) public static let BROWN: Color = Color(0xffa52a2a) public static let GRAY: Color = Color(0xff808080) public static let GREY: Color = Color(0xff808080) public static let GREEN: Color = Color(0xff008000) public static let ORANGE: Color = Color(0xffffa500) public static let PINK: Color = Color(0xffffc0cb) public static let RED: Color = Color(0xffff0000) public static let WHITE: Color = Color(0xffffffff) public static let YELLOW: Color = Color(0xffffff00) public static let TRANSPARENT: Color = Color(0, 0, 0, alpha: 0.0) public static let FOREGROUND: Color = Color(0x00000001) public init(red: UInt8, green: UInt8, blue: UInt8, alpha!: Float32 = 1.0) public init(value: UInt32) }
```
功能： 颜色类型。


### Fonts

class Fonts
```cangjie
public class Fonts { public var size: Length public var weight: FontWeight public var family: String public var style: FontStyle public init(size!: Length = 16.fp, weight!: FontWeight = FontWeight.Normal, family!: String = "HarmonyOS Sans", style!: FontStyle = FontStyle.Normal) public init(size!: Length = 16.fp, weight!: FontWeight = FontWeight.Normal, family!: AppResource, style!: FontStyle = FontStyle.Normal) }
```
功能： 文本样式。

#### var family

```cangjie
public var family: String
```
功能： 设置文本的字体列表。
Type: String; Readable: true; Writable: true

#### var size

```cangjie
public var size: Length
```
功能： 设置文本尺寸，使用fp单位。
Type: Length; Readable: true; Writable: true

#### var style

```cangjie
public var style: FontStyle
```
功能： 设置文本的字体样式。
Type: FontStyle; Readable: true; Writable: true

#### var weight

```cangjie
public var weight: FontWeight
```
功能： 设置文本的字体粗细。
Type: FontWeight; Readable: true; Writable: true


### Rectangle

class Rectangle
```cangjie
public class Rectangle { public var x: Length public var y: Length public var width: Length public var height: Length public init(x!: Length = 0.vp, y!: Length = 0.vp, width!: Length = 100.percent, height!: Length = 100.percent) }
```
功能： 定义区域位置类型。

#### var height

```cangjie
public var height: Length
```
功能： 触摸热区的高度。
Type: Length; Readable: true; Writable: true

#### var width

```cangjie
public var width: Length
```
功能： 触摸热区的宽度。
Type: Length; Readable: true; Writable: true

#### var x

```cangjie
public var x: Length
```
功能： 触摸点相对于组件左上角的x轴坐标。
Type: Length; Readable: true; Writable: true

#### var y

```cangjie
public var y: Length
```
功能： 触摸点相对于组件左上角的y轴坐标。
Type: Length; Readable: true; Writable: true


### RectResult

class RectResult
```cangjie
public class RectResult { public var x: Float64 public var y: Float64 public var width: Float64 public var height: Float64 public init( x: Float64, y: Float64, width: Float64, height: Float64 ) }
```
功能： 位置和尺寸类型，用于描述组建的位置和高度。通过scroller.getItemRect获取。

#### var height

```cangjie
public var height: Float64
```
功能： 内容高度大小。
Type: Float64; Readable: true; Writable: true

#### var width

```cangjie
public var width: Float64
```
功能： 内容宽度大小。
Type: Float64; Readable: true; Writable: true

#### var x

```cangjie
public var x: Float64
```
功能： 水平方向横坐标。
Readable: true; Writable: true

#### var y

```cangjie
public var y: Float64
```
功能： 竖直方向纵坐标。
Type: Float64; Readable: true; Writable: true


### AnimateParam

class AnimateParam
```cangjie
public class AnimateParam { public var duration: Option<Int32> public var tempo: Option<Float32> public var curve: Option<Curve> public var delay: Option<Int32> public var iterations: Option<Int32> public var playMode: Option<PlayMode> public var onFinish: Option <() -> Unit> public var finishCallbackType: Option<FinishCallbackType> public var expectedFrameRateRange: Option<ExpectedFrameRateRange> public init( duration!: Option<Int32> = 1000, tempo!: Option<Float32> = 1.0, curve!: Option<Curve> = Curve.EaseInOut, delay!: Option<Int32> = 0, iterations!: Option<Int32> = 1, playMode!: Option<PlayMode> = PlayMode.Normal, onFinish!: Option<() -> Unit> = Option.None, finishCallbackType!: Option<FinishCallbackType> = FinishCallbackType.REMOVED, expectedFrameRateRange!: Option<ExpectedFrameRateRange> = Option.None ) }
```
功能： 设置动画效果相关参数。

#### var curve

```cangjie
public var curve: Option<Curve>
```
功能： 动画曲线。
Type: Option< Curve >; Readable: true; Writable: true

#### var delay

```cangjie
public var delay: Option<Int32>
```
功能： 动画延迟播放时间，单位为ms(毫秒)，默认不延时播放。
Type: Option<Int32>; Readable: true; Writable: true

#### var duration

```cangjie
public var duration: Option<Int32>
```
功能： 动画持续时间，单位为毫秒。设置小于0的值时按0处理。
Type: Option<Int32>; Readable: true; Writable: true

#### var expectedFrameRateRange

```cangjie
public var expectedFrameRateRange: Option<ExpectedFrameRateRange>
```
功能： 设置动画的期望帧率。
Type: Option< ExpectedFrameRateRange >; Readable: true; Writable: true

#### var finishCallbackType

```cangjie
public var finishCallbackType: Option<FinishCallbackType>
```
功能： 在动画中定义onFinish回调的类型。
Type: Option< FinishCallbackType >; Readable: true; Writable: true

#### var iterations

```cangjie
public var iterations: Option<Int32>
```
功能： 动画播放次数。默认播放一次，设置为-1时表示无限次播放。设置为0时表示无动画效果。
Type: Option<Int32>; Readable: true; Writable: true

#### var onFinish

```cangjie
public var onFinish: Option <() -> Unit>
```
功能： 动画播放完成回调。
Type: Option<()->Unit>; Readable: true; Writable: true

#### var playMode

```cangjie
public var playMode: Option<PlayMode>
```
功能： 动画播放模式，默认播放完成后重头开始播放。
Type: Option< PlayMode >; Readable: true; Writable: true

#### var tempo

```cangjie
public var tempo: Option<Float32>
```
功能： 动画播放速度，值越大动画播放越快，值越小播放越慢，为0时无动画效果。
Type: Option<Float32>; Readable: true; Writable: true


### Area

class Area
```cangjie
public class Area { public Area( public var width: Float64, public var height: Float64, public var position: Position, public var globalPosition: Position ) }
```
功能： 当前目标区域。

#### var globalPosition

```cangjie
public var globalPosition: Position
```
功能： 定义目标元素左上角与屏幕左上角的位置关系。
Type: Position; Readable: true; Writable: true

#### var height

```cangjie
public var height: Float64
```
功能： 定义目标元素的宽度。
Type: Float64; Readable: true; Writable: true

#### var position

```cangjie
public var position: Position
```
功能： 定义目标元素左上角与父元素左上角的相对位置。
Type: Position; Readable: true; Writable: true

#### var width

```cangjie
public var width: Float64
```
功能： 定义目标元素的宽度。
Type: Float64; Readable: true; Writable: true


### BorderRadiuses

class BorderRadiuses
```cangjie
public class BorderRadiuses { public var topLeft: Length public var topRight: Length public var bottomLeft: Length public var bottomRight: Length public init(topLeft!: Length = 0.vp, topRight!: Length = 0.vp, bottomLeft!: Length = 0.vp, bottomRight!: Length = 0.vp) }
```
功能： 圆角类型，用于描述组件边框圆角半径。

#### var bottomLeft

```cangjie
public var bottomLeft: Length
```
功能： 组件左下角圆角半径。
Type: Length; Readable: true; Writable: true

#### var bottomRight

```cangjie
public var bottomRight: Length
```
功能： 组件右下角圆角半径。
Type: Length; Readable: true; Writable: true

#### var topLeft

```cangjie
public var topLeft: Length
```
功能： 组件左上角圆角半径。
Type: Length; Readable: true; Writable: true

#### var topRight

```cangjie
public var topRight: Length
```
功能： 组件右上角圆角半径。
Type: Length; Readable: true; Writable: true


### DividerStyleOptions

class DividerStyleOptions
```cangjie
public class DividerStyleOptions { public let strokeWidth: Length public let color: ResourceColor public let startMargin: Length public let endMargin: Length public init(strokeWidth: Length,color: ResourceColor,startMargin: Length,endMargin: Length) }
```
功能： 分割线样式属性集合，用于描述分割线相关信息。

#### let color

```cangjie
public let color: ResourceColor
```
功能： 分割线的颜色。
Type: ResourceColor; Readable: true; Writable: false

#### let endMargin

```cangjie
public let endMargin: Length
```
功能： 分割线与菜单侧边结束端的距离。
Type: Length; Readable: true; Writable: false

#### let startMargin

```cangjie
public let startMargin: Length
```
功能： 分割线与菜单侧边起始端的距离。
Type: Length; Readable: true; Writable: false

#### let strokeWidth

```cangjie
public let strokeWidth: Length
```
功能： 分割线的线宽。
Type: Length; Readable: true; Writable: false


### EdgeColor

class EdgeColor
```cangjie
public class EdgeColor { public var top: Color public var right: Color public var bottom: Color public var left: Color public init(top!: Color = Color.BLACK, right!: Color = Color.BLACK, bottom!: Color = Color.BLACK, left!: Color = Color.BLACK) }
```
功能： 边框颜色，用于描述组件边框四条边的颜色。

#### var top

```cangjie
public var top: Color
```
功能： 组件上边框颜色。
Type: Color; Readable: true; Writable: true

#### var right

```cangjie
public var right: Color
```
功能： 组件右边框颜色。
Type: Color; Readable: true; Writable: true

#### var bottom

```cangjie
public var bottom: Color
```
功能： 组件下边框颜色。
Type: Color; Readable: true; Writable: true

#### var left

```cangjie
public var left: Color
```
功能： 组件左边框颜色。
Type: Color; Readable: true; Writable: true


### EdgeStyles

class EdgeStyles
```cangjie
public class EdgeStyles { public var top: BorderStyle public var right: BorderStyle public var bottom: BorderStyle public var left: BorderStyle public init(top!: BorderStyle = BorderStyle.Solid, right!: BorderStyle = BorderStyle.Solid, bottom!: BorderStyle = BorderStyle.Solid, left!: BorderStyle = BorderStyle.Solid) }
```
功能： 边框样式，用于描述组件边框四条边的样式。

#### var bottom

```cangjie
public var bottom: BorderStyle
```
功能： 设置组件下边框样式。
Type: BorderStyle; Readable: true; Writable: true

#### var left

```cangjie
public var left: BorderStyle
```
功能： 设置组件左边框样式。
Type: BorderStyle; Readable: true; Writable: true

#### var right

```cangjie
public var right: BorderStyle
```
功能： 设置组件右边框样式。
Type: BorderStyle; Readable: true; Writable: true

#### var top

```cangjie
public var top: BorderStyle
```
功能： 设置组件上边框样式。
Type: BorderStyle; Readable: true; Writable: true


### LocalizedPadding

class LocalizedPadding
```cangjie
public class LocalizedPadding { public let top: Length public let end: Length public let bottom: Length public let start: Length public LocalizedPadding( top!: Length = 17.0.vp, end!: Length = 8.0.vp, bottom!: Length = 18.0.vp, start!: Length = 8.0.vp ) }
```
功能： 内边距类型，用于描述组件不同方向的内边距。

#### let bottom

```cangjie
public let bottom: Length
```
功能： 下内边距，组件内元素距组件底部的尺寸。
Type: Length; Readable: true; Writable: false

#### let end

```cangjie
public let end: Length
```
功能： 右内边距，组件内元素距组件右边界的尺寸。
Type: Length; Readable: true; Writable: false

#### let start

```cangjie
public let start: Length
```
功能： 左内边距，组件内元素距组件左边界的尺寸。
Type: Length; Readable: true; Writable: false

#### let top

```cangjie
public let top: Length
```
功能： 上内边距，组件内元素距组件顶部的尺寸。
Type: Length; Readable: true; Writable: false


### Margin

class Margin
```cangjie
public class Margin { public init(top!: Length = 0.vp, right!: Length = 0.vp, bottom!: Length = 0.vp, left!: Length = 0.vp) }
```
功能： 外边距类型，用于描述组件不同方向的外边距。


### MenuOffset

class MenuOffset
```cangjie
public class MenuOffset { public var dx: Length public var dy: Length public init(dx: Length, dy: Length) }
```
功能： 位置坐标偏移量。

#### var dx

```cangjie
public var dx: Length
```
功能： 水平方向偏移量。
Type: Length; Readable: true; Writable: true

#### var dy

```cangjie
public var dy: Length
```
功能： 竖直方向偏移量。
Type: Length


### Offset

class Offset
```cangjie
public class Offset { public var dx: Length public var dy: Length public init(dx: Length, dy: Length) }
```
功能： 相对布局完成位置坐标偏移量。

#### var dx

```cangjie
public var dx: Length
```
功能： 水平方向偏移量。
Type: Length; Readable: true; Writable: true

#### var dy

```cangjie
public var dy: Length
```
功能： 竖直方向偏移量。
Type: Length; Readable: true; Writable: true


### Padding

class Padding
```cangjie
public class Padding { public let top: Length public let right: Length public let bottom: Length public let left: Length public Padding( top!: Length = 17.0.vp, right!: Length = 8.0.vp, bottom!: Length = 18.0.vp, left!: Length = 8.0.vp ) }
```
功能： 内边距类型，用于描述组件不同方向的内边距。

#### let bottom

```cangjie
public let bottom: Length
```
功能： 下内边距，组件内元素距组件底部的尺寸。
Type: Length; Readable: true; Writable: false

#### let left

```cangjie
public let left: Length
```
功能： 左内边距，组件内元素距组件左边界的尺寸。
Type: Length; Readable: true; Writable: false

#### let right

```cangjie
public let right: Length
```
功能： 右内边距，组件内元素距组件右边界的尺寸。
Type: Length; Readable: true; Writable: false

#### let top

```cangjie
public let top: Length
```
功能： 上内边距，组件内元素距组件顶部的尺寸。
Type: Length; Readable: true; Writable: false


### Position

class Position
```cangjie
public class Position { public Position( public var x: Float64, public var y: Float64 ) }
```
功能： 位置信息。

#### var x

```cangjie
public var x: Float64
```
功能： 定义x轴坐标。
Type: Float64; Readable: true; Writable: true

#### var y

```cangjie
public var y: Float64
```
功能： 定义y轴坐标。
Type: Float64; Readable: true; Writable: true



## Structs

### CJEdge

struct CJEdge
```cangjie
public struct CJEdge { public init(topLength: Length, rightLength: Length, bottomLength: Length, leftLength: Length) }
```
功能： 边框长度。



## Enums

### AccessibilityHoverType

enum AccessibilityHoverType
```cangjie
public enum AccessibilityHoverType { | HOVER_ENTER | HOVER_MOVE | HOVER_EXIT | HOVER_CANCEL }
```
功能： 无障碍悬浮类型类型。

#### HOVER_CANCEL

```cangjie
HOVER_CANCEL
```
功能： 打断取消当前触发的事件。

#### HOVER_ENTER

```cangjie
HOVER_ENTER
```
功能： 手指按下时触发。

#### HOVER_EXIT

```cangjie
HOVER_EXIT
```
功能： 抬手触发。

#### HOVER_MOVE

```cangjie
HOVER_MOVE
```
功能： 触摸移动时触发。


### AdaptiveColor

enum AdaptiveColor
```cangjie
public enum AdaptiveColor { | DEFAULT | AVERAGE }
```
功能： 取色模式。

#### AVERAGE

```cangjie
AVERAGE
```
功能： 使用取色模糊。将取色区域的颜色平均值作为蒙版颜色。

#### DEFAULT

```cangjie
DEFAULT
```
功能： 不使用取色模糊。使用默认的颜色作为蒙版颜色。


### Alignment

enum Alignment
```cangjie
public enum Alignment { | TopStart | Top | TopEnd | Start | Center | End | BottomStart | Bottom | BottomEnd }
```
功能： 对齐方式。

#### Bottom

```cangjie
Bottom
```
功能： 底部横向居中。

#### BottomEnd

```cangjie
BottomEnd
```
功能： 底部尾端。

#### BottomStart

```cangjie
BottomStart
```
功能： 底部起始端。

#### Center

```cangjie
Center
```
功能： 横向和纵向居中。

#### End

```cangjie
End
```
功能： 尾端纵向居中。

#### Start

```cangjie
Start
```
功能： 起始端纵向居中。

#### Top

```cangjie
Top
```
功能： 顶部横向居中。

#### TopEnd

```cangjie
TopEnd
```
功能： 顶部尾端。

#### TopStart

```cangjie
TopStart
```
功能： 顶部起始端。


### AnimationStatus

enum AnimationStatus
```cangjie
public enum AnimationStatus { | Initial | Running | Paused | Stopped }
```
功能： 动画播放状态。

#### Initial

```cangjie
Initial
```
功能： 动画初始状态。

#### Paused

```cangjie
Paused
```
功能： 动画处于暂停状态。

#### Running

```cangjie
Running
```
功能： 动画处于播放状态。

#### Stopped

```cangjie
Stopped
```
功能： 动画处于停止状态。


### AppRotation

enum AppRotation
```cangjie
public enum AppRotation { | ROTATION_0 | ROTATION_90 | ROTATION_180 | ROTATION_270 }
```
功能： 应用方向旋转角度。

#### ROTATION_0

```cangjie
ROTATION_0
```
功能： 应用方向为0度。

#### ROTATION_90

```cangjie
ROTATION_90
```
功能： 应用方向为90度。

#### ROTATION_180

```cangjie
ROTATION_180
```
功能： 应用方向为180度。

#### ROTATION_270

```cangjie
ROTATION_270
```
功能： 应用方向为270度。


### ArrowPointPosition

enum ArrowPointPosition
```cangjie
public enum ArrowPointPosition { | START | CENTER | END }
```
功能： 箭头指向位置。

#### CENTER

```cangjie
CENTER
```
功能： 位于父组件居中位置。

#### END

```cangjie
END
```
功能： 水平方向：位于父组件最右侧，垂直方向：位于父组件最下侧。

#### START

```cangjie
START
```
功能： 水平方向：位于父组件最左侧，垂直方向：位于父组件最上侧。


### Axis

enum Axis
```cangjie
public enum Axis { | Vertical | Horizontal }
```
功能： 轴方向。

#### Horizontal

```cangjie
Horizontal
```
功能： 方向为横向。

#### Vertical

```cangjie
Vertical
```
功能： 方向为纵向。


### BarrierDirection

enum BarrierDirection
```cangjie
public enum BarrierDirection { | LEFT | RIGHT | TOP | BOTTOM }
```
功能： 定义屏障线的方向。

#### BOTTOM

```cangjie
BOTTOM
```
功能： 屏障在其所有referencedId的最下方。

#### LEFT

```cangjie
LEFT
```
功能： 屏障在其所有referencedId的最左侧。

#### RIGHT

```cangjie
RIGHT
```
功能： 屏障在其所有referencedId的最右侧。

#### TOP

```cangjie
TOP
```
功能： 屏障在其所有referencedId的最上方。


### BarState

enum BarState
```cangjie
public enum BarState { | Off | Auto | On }
```
功能： 滚动条的显示模式。

#### Auto

```cangjie
Auto
```
功能： 按需显示(触摸时显示，2s后消失)。

#### Off

```cangjie
Off
```
功能： 不显示。

#### On

```cangjie
On
```
功能： 常驻显示。


### BlendApplyType

enum BlendApplyType
```cangjie
public enum BlendApplyType { | FAST | OFFSCREEN }
```
功能： 指示如何将指定的混合模式应用于视图的内容。

#### FAST

```cangjie
FAST
```
功能： 在目标图像上按顺序混合视图的内容。

#### OFFSCREEN

```cangjie
OFFSCREEN
```
功能： 将此组件和子组件内容绘制到离屏画布上，然后整体进行混合。


### BlendMode

enum BlendMode
```cangjie
public enum BlendMode { | NONE | CLEAR | SRC | DST | SRC_OVER | DST_OVER | SRC_IN | DST_IN | SRC_OUT | DST_OUT | SRC_ATOP | DST_ATOP | XOR | PLUS | MODULATE | SCREEN | OVERLAY | DARKEN | LIGHTEN | COLOR_DODGE | COLOR_BURN | HARD_LIGHT | SOFT_LIGHT | DIFFERENCE | EXCLUSION | MULTIPLY | HUE | SATURATION | COLOR | LUMINOSITY }
```
功能： 混合模式。

#### CLEAR

```cangjie
CLEAR
```
功能： 将源像素覆盖的目标像素清除为完全透明。

#### COLOR

```cangjie
COLOR
```
功能： 保留源像素的饱和度和色调，但会使用目标像素的亮度来替换源像素的亮度。

#### COLOR_BURN

```cangjie
COLOR_BURN
```
功能： 使目标像素变得更暗来反映源像素。

#### COLOR_DODGE

```cangjie
COLOR_DODGE
```
功能： 使目标像素变得更亮来反映源像素。

#### DARKEN

```cangjie
DARKEN
```
功能： rc = s + d - max(s da, d sa), ra = kSrcOver，当两个颜色重叠时，较暗的颜色会覆盖较亮的颜色。

#### DIFFERENCE

```cangjie
DIFFERENCE
```
功能： rc = s + d - 2 (min(s da, d * sa)), ra = kSrcOver，对比源像素和目标像素，亮度更高的像素减去亮度更低的像素，产生高对比度的效果。

#### DST

```cangjie
DST
```
功能： r = d，只显示目标像素。

#### DST_ATOP

```cangjie
DST_ATOP
```
功能： r = d sa + s (1 - da)，在源像素和目标像素重叠的地方绘制目标像素，在源像素和目标像素不重叠的地方绘制源像素。

#### DST_IN

```cangjie
DST_IN
```
功能： r = d * sa，只显示目标像素中与源像素重叠的部分。

#### DST_OUT

```cangjie
DST_OUT
```
功能： r = d * (1 - sa)，只显示目标像素中与源像素不重叠的部分。

#### DST_OVER

```cangjie
DST_OVER
```
功能： r = d + (1 - da) * s，将目标像素按照透明度进行混合，覆盖在源像素上。

#### EXCLUSION

```cangjie
EXCLUSION
```
功能： rc = s + d - two(s * d), ra = kSrcOver，对比源像素和目标像素，亮度更高的像素减去亮度更低的像素，产生柔和的效果。

#### HARD_LIGHT

```cangjie
HARD_LIGHT
```
功能： 根据源像素的值来决定目标像素变得更亮或者更暗。根据源像素来决定使用MULTIPLY混合模式还是SCREEN混合模式。

#### HUE

```cangjie
HUE
```
功能： 保留源图像的亮度和饱和度，但会使用目标图像的色调来替换源图像的色调。

#### LIGHTEN

```cangjie
LIGHTEN
```
功能： rc = s + d - min(s da, d sa), ra = kSrcOver，将源图像和目标图像中的像素进行比较，选取两者中较亮的像素作为最终的混合结果。

#### LUMINOSITY

```cangjie
LUMINOSITY
```
功能： 保留目标像素的色调和饱和度，但会用源像素的亮度替换目标像素的亮度。

#### MODULATE

```cangjie
MODULATE
```
功能： r = s * d，将源像素与目标像素进行乘法运算，并将结果作为新的像素值。

#### MULTIPLY

```cangjie
MULTIPLY
```
功能： r = s (1 - da) + d (1 - sa) + s * d，将源图像与目标图像进行乘法混合，得到一张新的图像。

#### NONE

```cangjie
NONE
```
功能： 将上层图像直接覆盖到下层图像上，不进行任何混合操作。

#### OVERLAY

```cangjie
OVERLAY
```
功能： 根据目标像素来决定使用MULTIPLY混合模式还是SCREEN混合模式。

#### PLUS

```cangjie
PLUS
```
功能： r = min(s + d, 1)，将源像素值与目标像素值相加，并将结果作为新的像素值。

#### SATURATION

```cangjie
SATURATION
```
功能： 保留目标像素的亮度和色调，但会使用源像素的饱和度来替换目标像素的饱和度。

#### SCREEN

```cangjie
SCREEN
```
功能： r = s + d - s * d，将两个图像的像素值相加，然后减去它们的乘积来实现混合。

#### SOFT_LIGHT

```cangjie
SOFT_LIGHT
```
功能： 根据源像素来决定使用LIGHTEN混合模式还是DARKEN混合模式。

#### SRC

```cangjie
SRC
```
功能： r = s，只显示源像素。

#### SRC_ATOP

```cangjie
SRC_ATOP
```
功能： r = s da + d (1 - sa)，在源像素和目标像素重叠的地方绘制源像素，在源像素和目标像素不重叠的地方绘制目标像素。

#### SRC_IN

```cangjie
SRC_IN
```
功能： r = s * da，只显示源像素中与目标像素重叠的部分。

#### SRC_OUT

```cangjie
SRC_OUT
```
功能： r = s * (1 - da)，只显示源像素中与目标像素不重叠的部分。

#### SRC_OVER

```cangjie
SRC_OVER
```
功能： r = s + (1 - sa) * d，将源像素按照透明度进行混合，覆盖在目标像素上。

#### XOR

```cangjie
XOR
```
功能： r = s (1 - da) + d (1 - sa)，只显示源像素与目标像素不重叠的部分。


### BlurStyleActivePolicy

enum BlurStyleActivePolicy
```cangjie
public enum BlurStyleActivePolicy { | FOLLOWS_WINDOW_ACTIVE_STATE | ALWAYS_ACTIVE | ALWAYS_INACTIVE }
```
功能： 模糊效果激活策略。

#### ALWAYS_ACTIVE

```cangjie
ALWAYS_ACTIVE
```
功能： 一直有模糊效果。

#### ALWAYS_INACTIVE

```cangjie
ALWAYS_INACTIVE
```
功能： 一直无模糊效果。

#### FOLLOWS_WINDOW_ACTIVE_STATE

```cangjie
FOLLOWS_WINDOW_ACTIVE_STATE
```
功能： 模糊效果跟随窗口焦点状态变化，非焦点不模糊，焦点模糊。


### BorderStyle

enum BorderStyle
```cangjie
public enum BorderStyle { | Solid | Dashed | Dotted }
```
功能： 边框样式，用于描述组件边框四条边的样式。

#### Dashed

```cangjie
Dashed
```
功能： 显示为一系列短的方形虚线。

#### Dotted

```cangjie
Dotted
```
功能： 显示为一系列圆点，圆点半径为borderWidth的一半。

#### Solid

```cangjie
Solid
```
功能： 显示为一条实线。


### ButtonRole

enum ButtonRole
```cangjie
public enum ButtonRole { | NORMAL | ERROR }
```
功能： 按键类型。

#### ERROR

```cangjie
ERROR
```
功能： 警示按钮。

#### NORMAL

```cangjie
NORMAL
```
功能： 正常按钮。


### ButtonStyleMode

enum ButtonStyleMode
```cangjie
public enum ButtonStyleMode { | NORMAL | EMPHASIZED | TEXTUAL }
```
功能： 按钮的样式和重要程度类型。

#### EMPHASIZED

```cangjie
EMPHASIZED
```
功能： 强调按钮（用于强调当前操作）。

#### NORMAL

```cangjie
NORMAL
```
功能： 普通按钮（一般界面操作）。

#### TEXTUAL

```cangjie
TEXTUAL
```
功能： 文本按钮（纯文本，无背景颜色）。


### ButtonType

enum ButtonType
```cangjie
public enum ButtonType { | Normal | Capsule | Circle | ROUNDED_RECTANGLE }
```
功能： 按键形状类型。

#### Capsule

```cangjie
Capsule
```
功能： 胶囊型按钮（圆角默认为高度的一半）。

#### Circle

```cangjie
Circle
```
功能： 圆形按钮。

#### Normal

```cangjie
Normal
```
功能： 普通按钮（默认不带圆角）。

#### ROUNDED_RECTANGLE

```cangjie
ROUNDED_RECTANGLE
```
功能： 圆角矩形按钮。


### CanvasDirection

enum CanvasDirection
```cangjie
public enum CanvasDirection { | inherit | ltr | rtl }
```
功能： 设置绘制文字时使用的文字方向。

#### inherit

```cangjie
inherit
```
功能： 继承canvas组件通用属性已设定的文本方向。

#### ltr

```cangjie
ltr
```
功能： 从左往右。

#### rtl

```cangjie
rtl
```
功能： 从右往左。


### CanvasFillRule

enum CanvasFillRule
```cangjie
public enum CanvasFillRule { | evenodd | nonzero }
```
功能： 指定要填充对象的规则。

#### evenodd

```cangjie
evenodd
```
功能： 奇偶规则。

#### nonzero

```cangjie
nonzero
```
功能： 非零规则。


### CheckBoxShape

enum CheckBoxShape
```cangjie
public enum CheckBoxShape { | CIRCLE | ROUNDED_SQUARE }
```
功能： 多选框形状类型。

#### CIRCLE

```cangjie
CIRCLE
```
功能： 圆形形状。

#### ROUNDED_SQUARE

```cangjie
ROUNDED_SQUARE
```
功能： 圆角方形形状。


### ClickEffectLevel

enum ClickEffectLevel
```cangjie
public enum ClickEffectLevel { | LIGHT | MIDDLE | HEAVY }
```
功能： 点击回弹动效设置。

#### HEAVY

```cangjie
HEAVY
```
功能： 大面积（厚重）。弹簧动效， 刚性：350，阻尼：35，初始速度：0.5。

#### LIGHT

```cangjie
LIGHT
```
功能： 小面积（轻盈）。弹簧动效， 刚性：410，阻尼：38，初始速度：1。

#### MIDDLE

```cangjie
MIDDLE
```
功能： 中面积（稳定）。弹簧动效， 刚性：350，阻尼：35，初始速度：0.5。


### ColoringStrategy

enum ColoringStrategy
```cangjie
public enum ColoringStrategy { | INVERT | AVERAGE | PRIMARY }
```
功能： 智能取色枚举类型。

#### AVERAGE

```cangjie
AVERAGE
```
功能： 设置控件背景阴影色为控件背景阴影区域的平均色。仅支持在入参类型为ShadowOptions的shadow中设置该枚举。

#### INVERT

```cangjie
INVERT
```
功能： 设置前景色为控件背景色的反色。仅支持在foregroundColor中设置该枚举。

#### PRIMARY

```cangjie
PRIMARY
```
功能： 设置控件背景阴影色为控件背景阴影区域的主色。仅支持在入参类型为ShadowOptions的shadow中设置该枚举。


### CompositeOperation

enum CompositeOperation
```cangjie
public enum CompositeOperation { | SourceOver | SourceAtop | SourceIn | SourceOut | DestinationOver | DestinationAtop | DestinationIn | DestinationOut | Lighter | Copy | Xor }
```
功能： 设置合成操作的方式。

#### copy

```cangjie
copy
```
功能： 显示新绘制内容而忽略现有绘制内容。

#### destination-atop

```cangjie
destination-atop
```
功能： 在新绘制内容顶部显示现有绘制内容。

#### destination-in

```cangjie
destination-in
```
功能： 在新绘制内容中显示现有绘制内容。

#### destination-out

```cangjie
destination-out
```
功能： 在新绘制内容外显示现有绘制内容。

#### destination-over

```cangjie
destination-over
```
功能： 在新绘制内容上方显示现有绘制内容。

#### lighter

```cangjie
lighter
```
功能： 显示新绘制内容和现有绘制内容。

#### source-atop

```cangjie
source-atop
```
功能： 在现有绘制内容顶部显示新绘制内容。

#### source-in

```cangjie
source-in
```
功能： 在现有绘制内容中显示新绘制内容。

#### source-out

```cangjie
source-out
```
功能： 在现有绘制内容之外显示新绘制内容。

#### source-over

```cangjie
source-over
```
功能： 在现有绘制内容上显示新绘制内容，属于默认值。

#### xor

```cangjie
xor
```
功能： 使用异或操作对新绘制内容与现有绘制内容进行融合。


### ContentType

enum ContentType
```cangjie
public enum ContentType { | USER_NAME | PASSWORD | NEW_PASSWORD | FULL_STREET_ADDRESS | HOUSE_NUMBER | DISTRICT_ADDRESS | CITY_ADDRESS | PROVINCE_ADDRESS | COUNTRY_ADDRESS | PERSON_FULL_NAME | PERSON_LAST_NAME | PERSON_FIRST_NAME | PHONE_NUMBER | PHONE_COUNTRY_CODE | FULL_PHONE_NUMBER | EMAIL_ADDRESS | BANK_CARD_NUMBER | ID_CARD_NUMBER | NICKNAME | DETAIL_INFO_WITHOUT_STREET | FORMAT_ADDRESS }
```
功能： 自动填充类型。

#### HOUSE_NUMBER

```cangjie
HOUSE_NUMBER
```
功能： 【门牌号】在已启用情景化自动填充的情况下，支持门牌号的自动保存和自动填充。

#### ID_CARD_NUMBER

```cangjie
ID_CARD_NUMBER
```
功能： 【身份证号】在已启用情景化自动填充的情况下，支持身份证号的自动保存和自动填充。

#### NEW_PASSWORD

```cangjie
NEW_PASSWORD
```
功能： 【新密码】在已启用密码保险箱的情况下，支持自动生成新密码。

#### NICKNAME

```cangjie
NICKNAME
```
功能： 【昵称】在已启用情景化自动填充的情况下，支持昵称的自动保存和自动填充。

#### PASSWORD

```cangjie
PASSWORD
```
功能： 【密码】在已启用密码保险箱的情况下，支持密码的自动保存和自动填充。

#### PERSON_FIRST_NAME

```cangjie
PERSON_FIRST_NAME
```
功能： 【名字】在已启用情景化自动填充的情况下，支持名字的自动保存和自动填充。

#### PERSON_FULL_NAME

```cangjie
PERSON_FULL_NAME
```
功能： 【姓名】在已启用情景化自动填充的情况下，支持姓名的自动保存和自动填充。

#### PERSON_LAST_NAME

```cangjie
PERSON_LAST_NAME
```
功能： 【姓氏】在已启用情景化自动填充的情况下，支持姓氏的自动保存和自动填充。

#### PHONE_COUNTRY_CODE

```cangjie
PHONE_COUNTRY_CODE
```
功能： 【国家代码】在已启用情景化自动填充的情况下，支持国家代码的自动保存和自动填充。

#### PHONE_NUMBER

```cangjie
PHONE_NUMBER
```
功能： 【手机号码】在已启用情景化自动填充的情况下，支持手机号码的自动保存和自动填充。

#### PROVINCE_ADDRESS

```cangjie
PROVINCE_ADDRESS
```
功能： 【省】在已启用情景化自动填充的情况下，支持省的自动保存和自动填充。

#### USER_NAME

```cangjie
USER_NAME
```
功能： 【用户名】在已启用密码保险箱的情况下，支持用户名的自动保存和自动填充。

#### BANK_CARD_NUMBER

```cangjie
BANK_CARD_NUMBER
```
功能： 【银行卡号】在已启用情景化自动填充的情况下，支持银行卡号的自动保存和自动填充。

#### CITY_ADDRESS

```cangjie
CITY_ADDRESS
```
功能： 【市】在已启用情景化自动填充的情况下，支持市的自动保存和自动填充。

#### COUNTRY_ADDRESS

```cangjie
COUNTRY_ADDRESS
```
功能： 【国家】在已启用情景化自动填充的情况下，支持国家的自动保存和自动填充。

#### DETAIL_INFO_WITHOUT_STREET

```cangjie
DETAIL_INFO_WITHOUT_STREET
```
功能： 【无街道地址】在已启用情景化自动填充的情况下，支持无街道地址的自动保存和自动填充。

#### DISTRICT_ADDRESS

```cangjie
DISTRICT_ADDRESS
```
功能： 【区/县】在已启用情景化自动填充的情况下，支持区/县的自动保存和自动填充。

#### EMAIL_ADDRESS

```cangjie
EMAIL_ADDRESS
```
功能： 【邮箱地址】在已启用情景化自动填充的情况下，支持邮箱地址的自动保存和自动填充。

#### FORMAT_ADDRESS

```cangjie
FORMAT_ADDRESS
```
功能： 【标准地址】在已启用情景化自动填充的情况下，支持标准地址的自动保存和自动填充。

#### FULL_PHONE_NUMBER

```cangjie
FULL_PHONE_NUMBER
```
功能： 【包含国家代码的手机号码】在已启用情景化自动填充的情况下，支持包含国家代码的手机号码的自动保存和自动填充。

#### FULL_STREET_ADDRESS

```cangjie
FULL_STREET_ADDRESS
```
功能： 【详细地址】在已启用情景化自动填充的情况下，支持详细地址的自动保存和自动填充。


### ControlSize

enum ControlSize
```cangjie
public enum ControlSize { | SMALL | NORMAL }
```
功能： 控制尺寸大小。

#### NORMAL

```cangjie
NORMAL
```
功能： 正常尺寸。

#### SMALL

```cangjie
SMALL
```
功能： 小尺寸。


### CopyOptions

enum CopyOptions
```cangjie
public enum CopyOptions { | None | InApp | LocalDevice }
```
功能： 输入的文本复制模式。

#### InApp

```cangjie
InApp
```
功能： 支持应用内复制。

#### LocalDevice

```cangjie
LocalDevice
```
功能： 支持设备内复制。

#### None

```cangjie
None
```
功能： 不支持复制。


### Curve

enum Curve
```cangjie
public enum Curve { | Linear | Ease | EaseIn | EaseOut | EaseInOut | FastOutSlowIn | LinearOutSlowIn | FastOutLinearIn | ExtremeDeceleration | Sharp | Rhythm | Smooth | Friction }
```
功能： 插值曲线，动效请参考贝塞尔曲线。

#### Ease

```cangjie
Ease
```
功能： 表示动画以低速开始，然后加快，在结束前变慢，CubicBezier(0.25, 0.1, 0.25, 1.0)。

#### EaseIn

```cangjie
EaseIn
```
功能： 表示动画以低速开始，CubicBezier(0.42, 0.0, 1.0, 1.0)。

#### EaseInOut

```cangjie
EaseInOut
```
功能： 表示动画以低速开始和结束，CubicBezier(0.42, 0.0, 0.58, 1.0)。

#### EaseOut

```cangjie
EaseOut
```
功能： 表示动画以低速结束，CubicBezier(0.0, 0.0, 0.58, 1.0)。

#### ExtremeDeceleration

```cangjie
ExtremeDeceleration
```
功能： 急缓曲线，cubic-bezier(0.0, 0.0, 0.0, 1.0)。

#### FastOutLinearIn

```cangjie
FastOutLinearIn
```
功能： 加速曲线，cubic-bezier(0.4, 0.0, 1.0, 1.0)。

#### FastOutSlowIn

```cangjie
FastOutSlowIn
```
功能： 标准曲线，cubic-bezier(0.4, 0.0, 0.2, 1.0)。

#### Friction

```cangjie
Friction
```
功能： 阻尼曲线，CubicBezier(0.2, 0.0, 0.2, 1.0)。

#### Linear

```cangjie
Linear
```
功能： 表示动画从头到尾的速度都是相同的。

#### LinearOutSlowIn

```cangjie
LinearOutSlowIn
```
功能： 标准曲线，cubic-bezier(0.4, 0.0, 0.2, 1.0)。

#### Rhythm

```cangjie
Rhythm
```
功能： 节奏曲线，cubic-bezier(0.7, 0.0, 0.2, 1.0)。

#### Sharp

```cangjie
Sharp
```
功能： 锐利曲线，cubic-bezier(0.33, 0.0, 0.67, 1.0)。

#### Smooth

```cangjie
Smooth
```
功能： 平滑曲线，cubic-bezier(0.4, 0.0, 0.4, 1.0)。


### DialogAlignment

enum DialogAlignment
```cangjie
public enum DialogAlignment { | Top | Center | Bottom | Default | TopStart | TopEnd | CenterStart | CenterEnd | BottomStart | BottomEnd }
```
功能： 弹窗在竖直方向上的对齐方式。

#### Bottom

```cangjie
Bottom
```
功能： 垂直底部对齐。

#### BottomEnd

```cangjie
BottomEnd
```
功能： 右下对齐。

#### BottomStart

```cangjie
BottomStart
```
功能： 左下对齐。

#### Center

```cangjie
Center
```
功能： 垂直居中对齐。

#### CenterEnd

```cangjie
CenterEnd
```
功能： 右中对齐。

#### CenterStart

```cangjie
CenterStart
```
功能： 左中对齐。

#### Default

```cangjie
Default
```
功能： 默认对齐。

#### Top

```cangjie
Top
```
功能： 垂直顶部对齐。

#### TopEnd

```cangjie
TopEnd
```
功能： 右上对齐。

#### TopStart

```cangjie
TopStart
```
功能： 左上对齐。


### DialogButtonStyle

enum DialogButtonStyle
```cangjie
public enum DialogButtonStyle { | DEFAULT | HIGHLIGHT }
```
功能： 对话框按钮样式。

#### DEFAULT

```cangjie
DEFAULT
```
功能： 白底蓝字（深色主题：白底=黑底）。

#### HIGHLIGHT

```cangjie
HIGHLIGHT
```
功能： 蓝底白字。


### Direction

enum Direction
```cangjie
public enum Direction { | Ltr | Rtl | Auto }
```
功能： 元素布局方向。

#### Auto

```cangjie
Auto
```
功能： 使用系统默认布局方向。

#### Ltr

```cangjie
Ltr
```
功能： 元素从左到右布局。

#### Rtl

```cangjie
Rtl
```
功能： 元素从右到左布局。


### Edge

enum Edge
```cangjie
public enum Edge { | Top | Start | Bottom | End }
```
功能： 滚动到容器边缘。

#### Bottom

```cangjie
Bottom
```
功能： 竖直方向下边缘。

#### End

```cangjie
End
```
功能： 水平方向末尾位置。

#### Start

```cangjie
Start
```
功能： 水平方向起始位置。

#### Top

```cangjie
Top
```
功能： 竖直方向上边缘。


### EdgeEffect

enum EdgeEffect
```cangjie
public enum EdgeEffect { | Spring | Fade | None }
```
功能： 边缘滑动效果。

#### Fade

```cangjie
Fade
```
功能： 阴影效果，滑动到边缘后会有圆弧状的阴影。

#### None

```cangjie
None
```
功能： 滑动到边缘后无效果。

#### Spring

```cangjie
Spring
```
功能： 弹性物理动效，滑动到边缘后可以根据初始速度或通过触摸事件继续滑动一段距离，松手后回弹。


### EllipsisMode

enum EllipsisMode
```cangjie
public enum EllipsisMode { | START | CENTER | END }
```
功能： 省略方式。

#### CENTER

```cangjie
CENTER
```
功能： 省略行中内容。

#### END

```cangjie
END
```
功能： 省略行末内容。

#### START

```cangjie
START
```
功能： 省略行首内容。


### EmbeddedType

enum EmbeddedType
```cangjie
public enum EmbeddedType { | EMBEDDED_UI_EXTENSION }
```
功能： 用于指定EmbeddedComponent可拉起的提供方类型。

#### EMBEDDED_UI_EXTENSION

```cangjie
EMBEDDED_UI_EXTENSION
```
功能： 表示当前拉起的提供方类型为EmbeddedUIExtensionAbility。


### FillMode

enum FillMode
```cangjie
public enum FillMode { | None | Forwards | Backwards | Both }
```
功能： 当前播放方向下，动画开始前和结束后的状态。

#### Backwards

```cangjie
Backwards
```
功能： 动画将在应用于目标时立即应用第一个关键帧中定义的值，并在delay期间保留此值。第一个关键帧取决于playMode，playMode为Normal或Alternate时为from的状态，playMode为Reverse或AlternateReverse时为to的状态。

#### Both

```cangjie
Both
```
功能： 动画将遵循Forwards和Backwards的规则，从而在两个方向上扩展动画属性。

#### Forwards

```cangjie
Forwards
```
功能： 目标将保留动画执行期间最后一个关键帧的状态。

#### None

```cangjie
None
```
功能： 动画未执行时不会将任何样式应用于目标，动画播放完成之后恢复初始默认状态。


### FinishCallbackType

enum FinishCallbackType
```cangjie
public enum FinishCallbackType { | REMOVED | LOGICALLY }
```
功能： 动画结束时的回调类型。

#### LOGICALLY

```cangjie
LOGICALLY
```
功能： 当动画在逻辑上处于下降状态，但可能仍处于其长尾状态时，将触发回调。

#### REMOVED

```cangjie
REMOVED
```
功能： 当整个动画结束并立即删除时，将触发回调。


### FlexAlign

enum FlexAlign
```cangjie
public enum FlexAlign { | Start | Center | End | SpaceBetween | SpaceAround | SpaceEvenly }
```
功能： Flex容器对齐方式。

#### Center

```cangjie
Center
```
功能： 元素在主轴方向中心对齐，第一个元素与行首的距离与最后一个元素与行尾距离相同。

#### End

```cangjie
End
```
功能： 元素在主轴方向尾部对齐，最后一个元素与行尾对齐，其他元素与后一个对齐。

#### SpaceAround

```cangjie
SpaceAround
```
功能： Flex主轴方向均匀分配弹性元素，相邻元素之间距离相同。第一个元素到行首的距离和最后一个元素到行尾的距离是相邻元素之间距离的一半。

#### SpaceBetween

```cangjie
SpaceBetween
```
功能： Flex主轴方向均匀分配弹性元素，相邻元素之间距离相同。第一个元素与行首对齐，最后一个元素与行尾对齐。

#### SpaceEvenly

```cangjie
SpaceEvenly
```
功能： Flex主轴方向元素等间距布局，相邻元素之间的间距、第一个元素与行首的间距、最后一个元素到行尾的间距都完全一样。

#### Start

```cangjie
Start
```
功能： 元素在主轴方向首端对齐，第一个元素与行首对齐，同时后续的元素与前一个对齐。


### FlexDirection

enum FlexDirection
```cangjie
public enum FlexDirection { | Row | Column | RowReverse | ColumnReverse }
```
功能： Flex布局容器方向。

#### Column

```cangjie
Column
```
功能： 主轴与列方向一致作为布局模式。

#### ColumnReverse

```cangjie
ColumnReverse
```
功能： 与Column相反方向进行布局。

#### Row

```cangjie
Row
```
功能： 主轴与行方向一致作为布局模式。

#### RowReverse

```cangjie
RowReverse
```
功能： 与Row方向相反方向进行布局。


### FlexWrap

enum FlexWrap
```cangjie
public enum FlexWrap { | NoWrap | Wrap | WrapReverse }
```
功能： Flex布局容器约束方式。

#### NoWrap

```cangjie
NoWrap
```
功能： Flex容器的元素单行/列布局，子元素尽可能约束在容器内。当子元素有最小尺寸约束等设置时，Flex容器不会对其强制弹性压缩。

#### Wrap

```cangjie
Wrap
```
功能： Flex容器的元素多行/列排布，子项允许超出容器。

#### WrapReverse

```cangjie
WrapReverse
```
功能： Flex容器的元素反向多行/列排布，子项允许超出容器。


### FoldStatus

enum FoldStatus
```cangjie
public enum FoldStatus { | FOLD_STATUS_UNKNOWN | FOLD_STATUS_EXPANDED | FOLD_STATUS_FOLDED | FOLD_STATUS_HALF_FOLDED }
```
功能： 表示折叠设备折叠状态。

#### FOLD_STATUS_EXPANDED

```cangjie
FOLD_STATUS_EXPANDED
```
功能： 表示设备当前折叠状态为完全展开。

#### FOLD_STATUS_FOLDED

```cangjie
FOLD_STATUS_FOLDED
```
功能： 表示设备当前折叠状态为折叠。

#### FOLD_STATUS_HALF_FOLDED

```cangjie
FOLD_STATUS_HALF_FOLDED
```
功能： 表示设备当前折叠状态为半折叠。半折叠指完全展开和折叠之间的状态。

#### FOLD_STATUS_UNKNOWN

```cangjie
FOLD_STATUS_UNKNOWN
```
功能： 表示设备当前折叠状态未知。


### FontStyle

enum FontStyle
```cangjie
public enum FontStyle { | Normal | Italic }
```
功能： 字体样式。

#### Italic

```cangjie
Italic
```
功能： 标准的字体样式。

#### Normal

```cangjie
Normal
```
功能： 标准的字体样式。


### FontWeight

enum FontWeight
```cangjie
public enum FontWeight { | Normal | Bold | Bolder | Lighter | Medium | Regular | W100 | W200 | W300 | W400 | W500 | W600 | W700 | W800 | W900 }
```
功能： 设置文本的字体粗细，设置过大可能会在不同字体下有截断。

#### Bold

```cangjie
Bold
```
功能： 字体较粗。

#### Bolder

```cangjie
Bolder
```
功能： 字体非常粗。

#### Lighter

```cangjie
Lighter
```
功能： 字体较细。

#### Medium

```cangjie
Medium
```
功能： 字体粗细适中。

#### Normal

```cangjie
Normal
```
功能： 字体粗细正常。

#### Regular

```cangjie
Regular
```
功能： 字体粗细稍粗。

#### W100

```cangjie
W100
```
功能： 100（最细）。

#### W200

```cangjie
W200
```
功能： 200。

#### W300

```cangjie
W300
```
功能： 300。

#### W400

```cangjie
W400
```
功能： 400（正常）。

#### W500

```cangjie
W500
```
功能： 500。

#### W600

```cangjie
W600
```
功能： 600。

#### W700

```cangjie
W700
```
功能： 700。

#### W800

```cangjie
W800
```
功能： 800。

#### W900

```cangjie
W900
```
功能： 900（最粗）。


### ForegroundBlurStyle

enum ForegroundBlurStyle
```cangjie
public enum ForegroundBlurStyle { | NONE | THIN | REGULAR | THICK | BACKGROUND_THIN | BACKGROUND_REGULAR | BACKGROUND_THICK | BACKGROUND_ULTRA_THICK | COMPONENT_ULTRA_THIN | COMPONENT_THIN | COMPONENT_REGULAR | COMPONENT_THICK | COMPONENT_ULTRA_THICK }
```
功能： 前景模糊样式。

#### BACKGROUND_REGULAR

```cangjie
BACKGROUND_REGULAR
```
功能： 中距景深模糊。

#### BACKGROUND_THICK

```cangjie
BACKGROUND_THICK
```
功能： 远距景深模糊。

#### BACKGROUND_THIN

```cangjie
BACKGROUND_THIN
```
功能： 近距景深模糊。

#### BACKGROUND_ULTRA_THICK

```cangjie
BACKGROUND_ULTRA_THICK
```
功能： 超远距景深模糊。

#### COMPONENT_REGULAR

```cangjie
COMPONENT_REGULAR
```
功能： 组件普通材质模糊。

#### COMPONENT_THICK

```cangjie
COMPONENT_THICK
```
功能： 组件厚材质模糊。

#### COMPONENT_THIN

```cangjie
COMPONENT_THIN
```
功能： 组件轻薄材质模糊。

#### COMPONENT_ULTRA_THICK

```cangjie
COMPONENT_ULTRA_THICK
```
功能： 组件超厚材质模糊。

#### COMPONENT_ULTRA_THIN

```cangjie
COMPONENT_ULTRA_THIN
```
功能： 组件超轻薄材质模糊。

#### NONE

```cangjie
NONE
```
功能： 关闭模糊。

#### REGULAR

```cangjie
REGULAR
```
功能： 普通厚度材质模糊。

#### THICK

```cangjie
THICK
```
功能： 厚材质模糊。


### FunctionKey

enum FunctionKey
```cangjie
public enum FunctionKey { | ESC | F1 | F2 | F3 | F4 | F5 | F6 | F7 | F8 | F9 | F10 | F11 | F12 | TAB | DPAD_UP | DPAD_DOWN | DPAD_LEFT | DPAD_RIGHT }
```
功能： 键盘功能键。

#### DPAD_DOWN

```cangjie
DPAD_DOWN
```
功能： 表示键盘上DPAD_DOWN功能键。

#### DPAD_LEFT

```cangjie
DPAD_LEFT
```
功能： 表示键盘上DPAD_LEFT功能键。

#### DPAD_RIGHT

```cangjie
DPAD_RIGHT
```
功能： 表示键盘上DPAD_RIGHT功能键。

#### DPAD_UP

```cangjie
DPAD_UP
```
功能： 表示键盘上DPAD_UP功能键。

#### ESC

```cangjie
ESC
```
功能： 表示键盘上ESC功能键。

#### F1

```cangjie
F1
```
功能： 表示键盘上F1功能键。

#### F10

```cangjie
F10
```
功能： 表示键盘上F10功能键。

#### F11

```cangjie
F11
```
功能： 表示键盘上F11功能键。

#### F12

```cangjie
F12
```
功能： 表示键盘上F12功能键。

#### F2

```cangjie
F2
```
功能： 表示键盘上F2功能键。

#### F3

```cangjie
F3
```
功能： 表示键盘上F3功能键。

#### F4

```cangjie
F4
```
功能： 表示键盘上F4功能键。

#### F5

```cangjie
F5
```
功能： 表示键盘上F5功能键。

#### F6

```cangjie
F6
```
功能： 表示键盘上F6功能键。

#### F7

```cangjie
F7
```
功能： 表示键盘上F7功能键。

#### F8

```cangjie
F8
```
功能： 表示键盘上F8功能键。

#### F9

```cangjie
F9
```
功能： 表示键盘上F9功能键。

#### TAB

```cangjie
TAB
```
功能： 表示键盘上TAB功能键。


### GradientDirection

enum GradientDirection
```cangjie
public enum GradientDirection { | Left | Top | Right | Bottom | LeftTop | LeftBottom | RightTop | RightBottom | None }
```
功能： 梯度方向。

#### Bottom

```cangjie
Bottom
```
功能： 从上向下。

#### Left

```cangjie
Left
```
功能： 从右向左。

#### LeftBottom

```cangjie
LeftBottom
```
功能： 左下。

#### LeftTop

```cangjie
LeftTop
```
功能： 左上。

#### None

```cangjie
None
```
功能： 无。

#### Right

```cangjie
Right
```
功能： 从左向右。

#### RightBottom

```cangjie
RightBottom
```
功能： 右下。

#### RightTop

```cangjie
RightTop
```
功能： 右上。

#### Top

```cangjie
Top
```
功能： 从下向上。


### GridItemStyle

enum GridItemStyle
```cangjie
public enum GridItemStyle { | NONE | PLAIN }
```
功能： 设置GridItem样式。

#### NONE

```cangjie
NONE
```
功能： 设置为GridItemStyle.NONE时不显示Hover（悬停）和Press（按压）态样式。

#### PLAIN

```cangjie
PLAIN
```
功能： 设置为GridItemStyle.PLAIN时，显示Hover（悬停）、Press（按压）态样式。


### HeightBreakpoint

enum HeightBreakpoint
```cangjie
public enum HeightBreakpoint { | HEIGHT_SM | HEIGHT_MD | HEIGHT_LG }
```
功能： 表示窗口不同高宽比阈值下对应的高度断点枚举值。

#### HEIGHT_LG

```cangjie
HEIGHT_LG
```
功能： 窗口高宽比大于等于1.2。

#### HEIGHT_MD

```cangjie
HEIGHT_MD
```
功能： 窗口高宽比大于等于0.8，且小于1.2。

#### HEIGHT_SM

```cangjie
HEIGHT_SM
```
功能： 窗口高宽比小于0.8。


### HitTestMode

enum HitTestMode
```cangjie
public enum HitTestMode { | Default | Block | Transparent | None }
```
功能： 触摸测试效果。

#### Block

```cangjie
Block
```
功能： 自身节点响应触摸事件的命中测试，但阻止被该节点屏蔽的子节点和其他节点的命中测试。

#### Default

```cangjie
Default
```
功能： 自身节点和子节点都响应触摸事件的命中测试，但会阻止被该节点屏蔽的其他节点的命中测试。

#### None

```cangjie
None
```
功能： 自身节点不会响应触摸事件的命中测试，但子节点会对触摸事件进行命中测试。

#### Transparent

```cangjie
Transparent
```
功能： 自身节点和子节点响应触摸事件的命中测试，并允许对被该节点屏蔽的其他节点进行命中测试。


### HorizontalAlign

enum HorizontalAlign
```cangjie
public enum HorizontalAlign { | Start | Center | End }
```
功能： 水平方向对齐方式。

#### Center

```cangjie
Center
```
功能： 居中对齐，默认对齐方式。

#### End

```cangjie
End
```
功能： 按照语言方向末端对齐。

#### Start

```cangjie
Start
```
功能： 按照语言方向起始端对齐。


### HoverEffect

enum HoverEffect
```cangjie
public enum HoverEffect { | Auto | Scale | Highlight | None }
```
功能： 设置当前组件悬停态下的悬浮效果。

#### Auto

```cangjie
Auto
```
功能： 使用组件的系统默认悬浮效果。

#### Highlight

```cangjie
Highlight
```
功能： 背景淡入淡出的强调效果。

#### None

```cangjie
None
```
功能： 不设置效果。

#### Scale

```cangjie
Scale
```
功能： 放大缩小效果。


### HoverModeAreaType

enum HoverModeAreaType
```cangjie
public enum HoverModeAreaType { | TOP_SCREEN | BOTTOM_SCREEN }
```
功能： 悬停态显示区域类型。

#### BOTTOM_SCREEN

```cangjie
BOTTOM_SCREEN
```
功能： 下半屏。

#### TOP_SCREEN

```cangjie
TOP_SCREEN
```
功能： 上半屏。


### ImageFit

enum ImageFit
```cangjie
public enum ImageFit { | Fill | Contain | Cover | Auto | None | ScaleDown | TOP_START | TOP | TOP_END | START | CENTER | END | BOTTOM_START | BOTTOM | BOTTOM_END }
```
功能： 图片的显示适配方式。

#### Auto

```cangjie
Auto
```
功能： 图像会根据其自身尺寸和组件的尺寸进行适当缩放，以在保持比例的同时填充视图。

#### BOTTOM

```cangjie
BOTTOM
```
功能： 图像显示在Image组件的底部横向居中，保持原有尺寸显示。

#### BOTTOM_END

```cangjie
BOTTOM_END
```
功能： 图像显示在Image组件的底部尾端，保持原有尺寸显示。

#### BOTTOM_START

```cangjie
BOTTOM_START
```
功能： 图像显示在Image组件的底部起始端，保持原有尺寸显示。

#### CENTER

```cangjie
CENTER
```
功能： 图像显示在Image组件的横向和纵向居中，保持原有尺寸显示。

#### Contain

```cangjie
Contain
```
功能： 保持宽高比进行缩小或者放大，使得图片完全显示在显示边界内。

#### Cover

```cangjie
Cover
```
功能： 保持宽高比进行缩小或者放大，使得图片两边都大于或等于显示边界。

#### END

```cangjie
END
```
功能： 图像显示在Image组件的尾端纵向居中，保持原有尺寸显示。

#### Fill

```cangjie
Fill
```
功能： 不保持宽高比进行放大缩小，使得图片充满显示边界。

#### None

```cangjie
None
```
功能： 保持原有尺寸显示。

#### ScaleDown

```cangjie
ScaleDown
```
功能： 保持宽高比显示，图片缩小或者保持不变。

#### START

```cangjie
START
```
功能： 图像显示在Image组件的起始端纵向居中，保持原有尺寸显示。

#### TOP

```cangjie
TOP
```
功能： 图像显示在Image组件的顶部横向居中，保持原有尺寸显示。

#### TOP_END

```cangjie
TOP_END
```
功能： 图像显示在Image组件的顶部尾端，保持原有尺寸显示。

#### TOP_START

```cangjie
TOP_START
```
功能： 图像显示在Image组件的顶部起始端，保持原有尺寸显示。


### ImageRepeat

enum ImageRepeat
```cangjie
public enum ImageRepeat { | NoRepeat | X | Y | XY }
```
功能： 图片重复方式。

#### NoRepeat

```cangjie
NoRepeat
```
功能： 不重复绘制图片。

#### X

```cangjie
X
```
功能： 只在水平轴上重复绘制图片。

#### XY

```cangjie
XY
```
功能： 在两个轴上重复绘制图片。

#### Y

```cangjie
Y
```
功能： 只在竖直轴上重复绘制图片。


### ImageSize

enum ImageSize
```cangjie
public enum ImageSize { | Contain | Cover | Auto | FILL }
```
功能： 图片尺寸显示设置。

#### Auto

```cangjie
Auto
```
功能： 默认值，保持原图的比例不变。

#### Contain

```cangjie
Contain
```
功能： 保持宽高比进行缩小或者放大，使得图片完全显示在显示边界内。

#### Cover

```cangjie
Cover
```
功能： 保持宽高比进行缩小或者放大，使得图片两边都大于或等于显示边界。

#### FILL

```cangjie
FILL
```
功能： 不保持宽高比进行放大缩小，使得图片充满显示边界。


### ImageSpanAlignment

enum ImageSpanAlignment
```cangjie
public enum ImageSpanAlignment { | TOP | CENTER | BOTTOM | BASELINE }
```
功能： 图片基于行高的对齐方式。

#### BASELINE

```cangjie
BASELINE
```
功能： 图片下边沿与文本BaseLine对齐。

#### BOTTOM

```cangjie
BOTTOM
```
功能： 图片下边沿与行下边沿对齐。

#### CENTER

```cangjie
CENTER
```
功能： 图片中间与行中间对齐。

#### TOP

```cangjie
TOP
```
功能： 图片上边沿与行上边沿对齐。


### ImageType

enum ImageType
```cangjie
public enum ImageType { | png | jpeg | webp }
```
功能： 指定图像格式。

#### jpeg

```cangjie
jpeg
```
功能： jpeg图片格式。

#### png

```cangjie
png
```
功能： png图片格式。

#### webp

```cangjie
webp
```
功能： webp图片格式。


### ItemAlign

enum ItemAlign
```cangjie
public enum ItemAlign { | Auto | Start | Center | End | Stretch | Baseline }
```
功能： 元素对齐方式。

#### Auto

```cangjie
Auto
```
功能： 使用Flex容器中默认配置。

#### Baseline

```cangjie
Baseline
```
功能： 元素在Flex容器中，交叉轴方向文本基线对齐。

#### Center

```cangjie
Center
```
功能： 元素在Flex容器中，交叉轴方向居中对齐。

#### End

```cangjie
End
```
功能： 元素在Flex容器中，交叉轴方向底部对齐。

#### Start

```cangjie
Start
```
功能： 元素在Flex容器中，交叉轴方向首部对齐。

#### Stretch

```cangjie
Stretch
```
功能： 元素在Flex容器中，交叉轴方向拉伸填充。容器为Flex且设置Wrap为FlexWrap.Wrap或FlexWrap.WrapReverse时，元素拉伸到与当前行/列交叉轴长度最长的元素尺寸。其余情况下，无论元素尺寸是否设置，均拉伸到容器尺寸。


### KeyboardAvoidMode

enum KeyboardAvoidMode
```cangjie
public enum KeyboardAvoidMode { | DEFAULT | NONE }
```
功能： 弹窗是否在拉起软键盘时进行自动避让。

#### DEFAULT

```cangjie
DEFAULT
```
功能： 默认避让软键盘并在到达极限高度之后进行高度压缩。

#### NONE

```cangjie
NONE
```
功能： 不避让软键盘。


### KeySource

enum KeySource
```cangjie
public enum KeySource { | Unknown | Keyboard }
```
功能： 输入设备类型。

#### Keyboard

```cangjie
Keyboard
```
功能： 输入设备类型为键盘。

#### Unknown

```cangjie
Unknown
```
功能： 输入设备类型未知。


### KeyType

enum KeyType
```cangjie
public enum KeyType { | Down | Up }
```
功能： 按键类型。

#### Down

```cangjie
Down
```
功能： 按键按下。

#### Up

```cangjie
Up
```
功能： 按键松开。


### LayoutSafeAreaEdge

enum LayoutSafeAreaEdge
```cangjie
public enum LayoutSafeAreaEdge { | TOP | BOTTOM }
```
功能： 扩展安全区域的方向。

#### BOTTOM

```cangjie
BOTTOM
```
功能： 下方区域。

#### TOP

```cangjie
TOP
```
功能： 上方区域。


### LayoutSafeAreaType

enum LayoutSafeAreaType
```cangjie
public enum LayoutSafeAreaType { SYSTEM }
```
功能： 扩展布局安全区域的枚举类型。

#### SYSTEM

```cangjie
SYSTEM
```
功能： 系统默认非安全区域，包括状态栏、导航栏。


### LengthMetricsUnit

enum LengthMetricsUnit
```cangjie
public enum LengthMetricsUnit { | DEFAULT | PX }
```
功能： 长度属性单位枚举。

#### DEFAULT

```cangjie
DEFAULT
```
功能： 长度类型，用于描述以默认的vp像素单位为单位的长度。

#### PX

```cangjie
PX
```
功能: 长度类型，用于描述以px像素单位为单位的长度。


### LengthType

enum LengthType
```cangjie
public enum LengthType { | px | vp | fp | percent | lpx }
```
功能： 长度类型。

#### fp

```cangjie
fp
```
功能： 字体像素单位。

#### lpx

```cangjie
lpx
```
功能： 逻辑像素单位。

#### percent

```cangjie
percent
```
功能： 百分比。

#### px

```cangjie
px
```
功能： 基本像素单位。

#### vp

```cangjie
vp
```
功能： 屏幕密度单位。


### LineBreakStrategy

enum LineBreakStrategy
```cangjie
public enum LineBreakStrategy { | GREEDY | HIGH_QUALITY | BALANCED }
```
功能： 文本的折行规则。

#### BALANCED

```cangjie
BALANCED
```
功能： 尽可能保证在不拆词的情况下，使一个段落中每一行的宽度相同。

#### GREEDY

```cangjie
GREEDY
```
功能： 使每一行尽量显示多的字符，直到这一行不能显示更多字符再进行折行。

#### HIGH_QUALITY

```cangjie
HIGH_QUALITY
```
功能： 在BALANCED的基础上，尽可能填满行，在最后一行的权重上比较低，可能会出现最后一行留白比较多。


### LineCapStyle

enum LineCapStyle
```cangjie
public enum LineCapStyle { | Butt | Round | Square }
```
功能： 线条样式。

#### Butt

```cangjie
Butt
```
功能： 线条两端为平行线，不额外扩展。

#### Round

```cangjie
Round
```
功能： 在线条两端延伸半个圆，直径等于线宽。

#### Square

```cangjie
Square
```
功能： 在线条两端延伸半个圆，直径等于线宽。


### LineJoinStyle

enum LineJoinStyle
```cangjie
public enum LineJoinStyle { | Miter | Round | Bevel }
```
功能： 路径段连接方式。

#### Bevel

```cangjie
Bevel
```
功能： 使用斜角连接路径段。

#### Miter

```cangjie
Dotted
```
功能： 使用尖角连接路径段。

#### Round

```cangjie
Round
```
功能： 使用圆角连接路径段。


### ListItemAlign

enum ListItemAlign
```cangjie
public enum ListItemAlign { | Start | Center | End }
```
功能： ListItem在List中，交叉轴方向的对齐方式。

#### Center

```cangjie
Center
```
功能： ListItem在List中，交叉轴方向居中对齐。

#### End

```cangjie
End
```
功能： ListItem在List中，交叉轴方向尾部对齐。

#### Start

```cangjie
Start
```
功能： ListItem在List中，交叉轴方向首部对齐。


### ListItemGroupArea

enum ListItemGroupArea
```cangjie
public enum ListItemGroupArea { | NONE | IN_LIST_ITEM_AREA | IN_HEADER_AREA | IN_FOOTER_AREA }
```
功能： 表示处于ListItemGroup的哪一个区域。

#### IN_FOOTER_AREA

```cangjie
IN_FOOTER_AREA
```
功能： 当前页面可视边处于footer位置。

#### IN_HEADER_AREA

```cangjie
IN_HEADER_AREA
```
功能： 当前页面可视边处于header位置。

#### IN_LIST_ITEM_AREA

```cangjie
IN_LIST_ITEM_AREA
```
功能： 当前页面可视边处于ListItem位置。

#### NONE

```cangjie
NONE
```
功能： 当前页面可视边处于none位置。例如，ListItemGroup中既没有header、footer，也没有ListItem。


### MarqueeUpdateStrategy

enum MarqueeUpdateStrategy
```cangjie
public enum MarqueeUpdateStrategy { | DEFAULT | PRESERVE_POSITION }
```
功能： 跑马灯效果更新策略。

#### DEFAULT

```cangjie
DEFAULT
```
功能： 跑马灯组件属性更新后， 从开始位置， 运行跑马灯效果。

#### PRESERVE_POSITION

```cangjie
PRESERVE_POSITION
```
功能： 跑马灯组件属性更新后， 保持当前位置， 运行跑马灯效果。


### MenuPolicy

enum MenuPolicy
```cangjie
public enum MenuPolicy { | Default | Hide | Show }
```
功能： 菜单弹出的策略。

#### DEFAULT

```cangjie
DEFAULT
```
功能： 按照底层默认逻辑决定是否弹出菜单。

#### HIDE

```cangjie
HIDE
```
功能： 始终不弹出菜单。

#### SHOW

```cangjie
SHOW
```
功能： 始终弹出菜单。


### MenuPreviewMode

enum MenuPreviewMode
```cangjie
public enum MenuPreviewMode { | NONE | IMAGE }
```
功能： 菜单的预览内容样式。

#### IMAGE

```cangjie
IMAGE
```
功能： 预览内容为触发长按悬浮菜单组件的截图。

#### NONE

```cangjie
NONE
```
功能： 不显示预览内容。


### ModalTransition

enum ModalTransition
```cangjie
public enum ModalTransition { | DEFAULT | NONE | ALPHA }
```
功能： 全屏模态切换动画。

#### ALPHA

```cangjie
ALPHA
```
功能： 全屏模态透明度渐变动画。

#### DEFAULT

```cangjie
DEFAULT
```
功能： 全屏模态上下切换动画。

#### NONE

```cangjie
NONE
```
功能： 全屏模态无转场动画。


### ModifierKey

enum ModifierKey
```cangjie
public enum ModifierKey { | CTRL | SHIFT | ALT }
```
功能： 输入法修饰键类型。

#### ALT

```cangjie
ALT
```
功能： 表示键盘上Alt键。

#### CTRL

```cangjie
CTRL
```
功能： 表示键盘上Ctrl键。

#### SHIFT

```cangjie
SHIFT
```
功能： 表示键盘上Shift键。


### MouseAction

enum MouseAction
```cangjie
public enum MouseAction { | Press | Release | Move | Hover }
```
功能： 鼠标动作。

#### Hover

```cangjie
Hover
```
功能： 鼠标悬浮。 说明： 该枚举值无效。

#### Move

```cangjie
Move
```
功能： 鼠标移动。

#### Press

```cangjie
Press
```
功能： 鼠标按键按下。

#### Release

```cangjie
Release
```
功能： 鼠标按键松开。


### MouseButton

enum MouseButton
```cangjie
public enum MouseButton { | None | Left | Right | Middle | Back | Forward }
```
功能： 鼠标按键。

#### Back

```cangjie
Back
```
功能： 鼠标左侧后退键。

#### Forward

```cangjie
Forward
```
功能： 鼠标左侧前进键。

#### Left

```cangjie
Left
```
功能： 鼠标左键。

#### Middle

```cangjie
Middle
```
功能： 鼠标中键。

#### None

```cangjie
None
```
功能： 无按键。

#### Right

```cangjie
Right
```
功能： 鼠标右键。


### NestedScrollMode

enum NestedScrollMode
```cangjie
public enum NestedScrollMode { | SELF_ONLY | SELF_FIRST | PARENT_FIRST | PARALLEL }
```
功能： 可滚动组件往末尾端滚动时的嵌套滚动选项。

#### PARALLEL

```cangjie
PARALLEL
```
功能： 自身和父组件同时滚动，自身和父组件都到达边缘以后，如果自身有边缘效果，则自身触发边缘效果，否则父组件触发边缘效果。

#### PARENT_FIRST

```cangjie
PARENT_FIRST
```
功能： 父组件先滚动，父组件滚动到边缘以后自身滚动。自身滚动到边缘后，如果有边缘效果，会触发自身的边缘效果，否则触发父组件的边缘效果。

#### SELF_FIRST

```cangjie
SELF_FIRST
```
功能： 自身先滚动，自身滚动到边缘以后父组件滚动。父组件滚动到边缘以后，如果父组件有边缘效果，则父组件触发边缘效果，否则子组件触发边缘效果。

#### SELF_ONLY

```cangjie
SELF_ONLY
```
功能： 只自身滚动，不与父组件联动。


### ObscuredReasons

enum ObscuredReasons
```cangjie
public enum ObscuredReasons { | PLACEHOLDER }
```
系统能力： SystemCapability.ArkUI.ArkUI.Full

#### PLACEHOLDER

```cangjie
PLACEHOLDER
```
功能： 显示的数据为通用占位符。


### OptionWidthMode

enum OptionWidthMode
```cangjie
public enum OptionWidthMode { | FIT_CONTENT | FIT_TRIGGER }
```
功能： 下拉菜单宽度设置。

#### FIT_CONTENT

```cangjie
FIT_CONTENT
```
功能： 设置该值时，下拉菜单宽度按默认2栅格显示。

#### FIT_TRIGGER

```cangjie
FIT_TRIGGER
```
功能： 设置下拉菜单继承下拉按钮宽度。


### OutlineStyle

enum OutlineStyle
```cangjie
public enum OutlineStyle { | SOLID | DASHED | DOTTED }
```
功能： 外描边样式。

#### DASHED

```cangjie
DASHED
```
功能： 显示为一系列短的方形虚线。

#### DOTTED

```cangjie
DOTTED
```
功能： 显示为一系列圆点，圆点半径为outlineWidth的一半。

#### SOLID

```cangjie
SOLID
```
功能： 显示为一条实线。


### PixelRoundCalcPolicy

enum PixelRoundCalcPolicy
```cangjie
public enum PixelRoundCalcPolicy { | NO_FORCE_ROUND | FORCE_CEIL | FORCE_FLOOR }
```
功能： 当前组件边界取整策略。

#### FORCE_CEIL

```cangjie
FORCE_CEIL
```
功能： 取上整计算。

#### FORCE_FLOOR

```cangjie
FORCE_FLOOR
```
功能： 取下整计算。

#### NO_FORCE_ROUND

```cangjie
NO_FORCE_ROUND
```
功能： 不取整计算。


### Placement

enum Placement
```cangjie
public enum Placement { | Left | Right | Top | Bottom | TopLeft | TopRight | BottomLeft | BottomRight | LeftTop | LeftBottom | RightTop | RightBottom | None }
```
功能： 气泡提示位置设置。

#### Bottom

```cangjie
Bottom
```
功能： 气泡提示位于组件下侧，与组件下侧中心对齐。

#### BottomLeft

```cangjie
BottomLeft
```
功能： 气泡提示位于组件下侧，与组件左侧边缘对齐。

#### BottomRight

```cangjie
BottomRight
```
功能： 气泡提示位于组件下侧，与组件右侧边缘对齐。

#### Left

```cangjie
Left
```
功能： 气泡提示位于组件左侧，与组件左侧中心对齐。

#### LeftBottom

```cangjie
LeftBottom
```
功能： 气泡提示位于组件左侧，与组件下侧边缘对齐。

#### LeftTop

```cangjie
LeftTop
```
功能： 气泡提示位于组件左侧，与组件上侧边缘对齐。

#### Right

```cangjie
Right
```
功能： 气泡提示位于组件右侧，与组件右侧中心对齐。

#### RightBottom

```cangjie
RightBottom
```
功能： 气泡提示位于组件右侧，与组件下侧边缘对齐。

#### RightTop

```cangjie
RightTop
```
功能： 气泡提示位于组件右侧，与组件上侧边缘对齐。

#### Top

```cangjie
Top
```
功能： 气泡提示位于组件上侧，与组件上侧中心对齐。

#### TopLeft

```cangjie
TopLeft
```
功能： 气泡提示位于组件上侧，与组件左侧边缘对齐。

#### TopRight

```cangjie
TopRight
```
功能： 气泡提示位于组件上侧，与组件右侧边缘对齐。


### PlayMode

enum PlayMode
```cangjie
public enum PlayMode { | Normal | Reverse | Alternate | AlternateReverse }
```
功能： 动画播放方向设置。

#### Alternate

```cangjie
Alternate
```
功能： 动画在奇数次（1、3、5...）正向播放，在偶数次（2、4、6...）反向播放。

#### AlternateReverse

```cangjie
AlternateReverse
```
功能： 动画在奇数次（1、3、5...）反向播放，在偶数次（2、4、6...）正向播放。

#### Normal

```cangjie
Normal
```
功能： 动画正向播放。

#### Reverse

```cangjie
Reverse
```
功能： 动画反向播放。


### ProgressType

enum ProgressType
```cangjie
public enum ProgressType { | Linear | Ring | Eclipse | ScaleRing | Capsule }
```
功能： Progress组件的样式类型。

#### Capsule

```cangjie
Capsule
```
功能： 胶囊样式，头尾两端圆弧处的进度展示效果与Eclipse相同；中段处的进度展示效果与Linear相同。高度大于宽度的时候自适应垂直显示。

#### Eclipse

```cangjie
Eclipse
```
功能： 圆形样式，显示类似月圆月缺的进度展示效果，从月牙逐渐变化至满月。

#### Linear

```cangjie
Linear
```
功能： 线性样式。

#### Ring

```cangjie
Ring
```
功能： 环形无刻度样式，环形圆环逐渐显示至完全填充效果。

#### ScaleRing

```cangjie
ScaleRing
```
功能： 环形有刻度样式，显示类似时钟刻度形式的进度展示效果。


### QualityType

enum QualityType
```cangjie
public enum QualityType { | Low | Medium | High }
```
功能： 设置图像平滑度。

#### high

```cangjie
high
```
功能： 高画质。

#### low

```cangjie
low
```
功能： 低画质。

#### medium

```cangjie
medium
```
功能： 中画质。


### RefreshStatus

enum RefreshStatus
```cangjie
public enum RefreshStatus { | Inactive | Drag | OverDrag | Refresh | Done }
```
功能： 下拉状态。

#### Inactive

```cangjie
Inactive
```
功能： 默认未下拉状态。

#### Drag

```cangjie
Drag
```
功能： 下拉中，下拉距离小于刷新距离。

#### OverDrag

```cangjie
OverDrag
```
功能： 下拉中，下拉距离超过刷新距离。

#### Refresh

```cangjie
Refresh
```
功能： 下拉结束，回弹至刷新距离，进入刷新状态。

#### Done

```cangjie
Done
```
功能： 刷新结束，返回初始状态（顶部）。


### RelateType

enum RelateType
```cangjie
public enum RelateType { | FILL | FIT }
```
系统能力： SystemCapability.ArkUI.ArkUI.Full

#### FILL

```cangjie
FILL
```
功能： 缩放当前子组件以填充满父组件。

#### FIT

```cangjie
FIT
```
功能： 缩放当前子组件以自适应父组件。


### Repetition

enum Repetition
```cangjie
public enum Repetition { | repeat | repeat_x | repeat_y | no_repeat | clamp | mirror }
```
功能： 设置图像重复的方式。

#### clamp

```cangjie
clamp
```
功能： 在原始边界外绘制时，超出部分使用边缘的颜色绘制。

#### mirror

```cangjie
mirror
```
功能： 沿x轴和y轴重复翻转绘制图像。

#### no_repeat

```cangjie
no_repeat
```
功能： 不重复绘制图像。

#### repeat

```cangjie
repeat
```
功能： 沿x轴和y轴重复绘制图像。

#### repeat_x

```cangjie
repeat_x
```
功能： 沿x轴重复绘制图像。

#### repeat_y

```cangjie
repeat_y
```
功能： 沿y轴重复绘制图像。


### ResourceType

enum ResourceType
```cangjie
public enum ResourceType { | ResColor | Float | String | Plural | Boolean | IntArray | Integer | Pattern | StrArray | Media | Rawfile }
```
功能： 写入资源的类型。

#### Boolean

```cangjie
Boolean
```
功能： Bool类型。

#### Float

```cangjie
Float
```
功能： Float类型。

#### IntArray

```cangjie
IntArray
```
功能： int数组类型。

#### Integer

```cangjie
Integer
```
功能： Integer类型。

#### Media

```cangjie
Media
```
功能： Media类型。

#### Pattern

```cangjie
Pattern
```
功能： Pattern类型。

#### Plural

```cangjie
Plural
```
功能： Plural类型。

#### Rawfile

```cangjie
Rawfile
```
功能： Rawfile类型。

#### ResColor

```cangjie
ResColor
```
功能： ResColor类型。

#### StrArray

```cangjie
StrArray
```
功能： StrArray类型。

#### String

```cangjie
String
```
功能： String类型。


### ResponseType

enum ResponseType
```cangjie
public enum ResponseType { | RightClick | LongPress }
```
功能： 响应类型。

#### LongPress

```cangjie
LongPress
```
功能： 通过长按触发菜单弹出。

#### RightClick

```cangjie
RightClick
```
功能： 通过鼠标右键触发菜单弹出。


### RichEditorResponseType

enum RichEditorResponseType
```cangjie
public enum RichEditorResponseType { | LONG_PRESS | RIGHT_CLICK | SELECT }
```
功能： 菜单的响应类型。

#### LONG_PRESS

```cangjie
LONG_PRESS
```
功能： 通过长按触发菜单弹出。

#### RIGHT_CLICK

```cangjie
RIGHT_CLICK
```
功能： 通过鼠标右键触发菜单弹出。

#### SELECT

```cangjie
SELECT
```
功能： 通过鼠标选中触发菜单弹出。


### ScrollBarDirection

enum ScrollBarDirection
```cangjie
public enum ScrollBarDirection { | Vertical | Horizontal }
```
功能： 设置滚动条的方向。

#### Vertical

```cangjie
Vertical
```
功能： 设置滚动条方向为纵向。

#### Horizontal

```cangjie
Horizontal
```
功能： 设置滚动条方向为横向。


### ScrollDirection

enum ScrollDirection
```cangjie
public enum ScrollDirection { | Vertical | Horizontal | None }
```
功能： 滚动方向枚举。

#### Horizontal

```cangjie
Horizontal
```
功能： 仅支持水平方向滚动。

#### None

```cangjie
None
```
功能： 不可滚动。

#### Vertical

```cangjie
Vertical
```
功能： 仅支持竖直方向滚动。


### ScrollSource

enum ScrollSource
```cangjie
public enum ScrollSource { | DRAG | FLING | EDGE_EFFECT | OTHER_USER_INPUT | SCROLL_BAR | SCROLL_BAR_FLING | SCROLLER | SCROLLER_ANIMATION }
```
功能： 滑动操作的来源。

#### DRAG

```cangjie
DRAG
```
功能： 拖拽事件。

#### EDGE_EFFECT

```cangjie
EDGE_EFFECT
```
功能： EdgeEffect.Spring的边缘滚动效果。

#### FLING

```cangjie
FLING
```
功能： 拖拽结束之后的惯性滑动。

#### OTHER_USER_INPUT

```cangjie
OTHER_USER_INPUT
```
功能： 除拖拽外的其他用户输入，如鼠标滚轮、键盘事件等。

#### SCROLL_BAR

```cangjie
SCROLL_BAR
```
功能： 滚动条的拖拽事件。

#### SCROLL_BAR_FLING

```cangjie
SCROLL_BAR_FLING
```
功能： 滚动条拖拽结束后的带速度的惯性滑动。

#### SCROLLER

```cangjie
SCROLLER
```
功能： Scroller的不带动效方法。

#### SCROLLER_ANIMATION

```cangjie
SCROLLER_ANIMATION
```
功能： Scroller的带动效方法。


### ScrollState

enum ScrollState
```cangjie
public enum ScrollState { | Idle | Scrolling | Fling }
```
功能： 设置当前滑动状态。

#### Fling

```cangjie
Fling
```
功能： 惯性滑动状态。

#### Idle

```cangjie
Idle
```
功能： 未滑动状态。

#### Scrolling

```cangjie
Scrolling
```
功能： 手指拖动状态。


### StickyStyle

enum StickyStyle
```cangjie
public enum StickyStyle { | None | Header | Footer | Both }
```
功能： 设置ListItemGroup中header和footer是否要吸顶或吸底。

#### None

```cangjie
None
```
功能： 设置ListItemGroup的headerh不吸顶，footer不吸底。

#### Header

```cangjie
Header
```
功能： 设置ListItemGroup的headerh吸顶。

#### Footer

```cangjie
Footer
```
功能： 设置ListItemGroup的footer吸底。

#### Both

```cangjie
Both
```
功能： 设置ListItemGroup的headerh吸顶，footer吸底。


### SwipeEdgeEffect

enum SwipeEdgeEffect
```cangjie
public enum SwipeEdgeEffect { | Spring | None }
```
功能： ListItem的滑动效果。

#### Spring

```cangjie
Spring
```
功能： ListItem划动距离超过划出组件大小后可以继续划动，松手后按照弹簧阻尼曲线回弹。

#### CARD

```cangjie
CARD
```
功能： ListItem划动距离不能超过划出组件大小。


### ShadowStyle

enum ShadowStyle
```cangjie
public enum ShadowStyle { | OUTER_DEFAULT_XS | OUTER_DEFAULT_SM | OUTER_DEFAULT_MD | OUTER_DEFAULT_LG | OUTER_FLOATING_SM | OUTER_FLOATING_MD }
```
功能： 阴影样式。

#### OUTER_DEFAULT_LG

```cangjie
OUTER_DEFAULT_LG
```
功能： 大阴影。

#### OUTER_DEFAULT_MD

```cangjie
OUTER_DEFAULT_MD
```
功能： 中阴影。

#### OUTER_DEFAULT_SM

```cangjie
OUTER_DEFAULT_SM
```
功能： 小阴影。

#### OUTER_DEFAULT_XS

```cangjie
OUTER_DEFAULT_XS
```
功能： 超小阴影。

#### OUTER_FLOATING_MD

```cangjie
OUTER_FLOATING_MD
```
功能： 浮动中阴影。

#### OUTER_FLOATING_SM

```cangjie
OUTER_FLOATING_SM
```
功能： 浮动小阴影。


### ShadowType

enum ShadowType
```cangjie
public enum ShadowType { | COLOR | BLUR }
```
功能： 阴影类型。

#### BLUR

```cangjie
BLUR
```
功能： 模糊。

#### COLOR

```cangjie
COLOR
```
功能： 颜色。


### ShapeType

enum ShapeType
```cangjie
public enum ShapeType { | Normal | Capsule | CircleType }
```
功能： 组件形状类型。

#### Capsule

```cangjie
Capsule
```
功能： 胶囊形状。

#### CircleType

```cangjie
CircleType
```
功能： 圆形形状。

#### Normal

```cangjie
Normal
```
功能： 正常形状。


### SharedTransitionEffectType

enum SharedTransitionEffectType
```cangjie
public enum SharedTransitionEffectType { | SharedEffectStatic | SharedEffectExchange }
```
功能： 共享元素转场动效类型。

#### SharedEffectExchange

```cangjie
SharedEffectExchange
```
功能： 将源页面元素移动到目标页面元素位置并适当缩放。

#### SharedEffectStatic

```cangjie
SharedEffectStatic
```
功能： 目标页面元素的位置保持不变，可以配置透明度动画。目前，只有为重定向到目标页面而配置的静态效果才会生效。


### SideBarContainerType

enum SideBarContainerType
```cangjie
public enum SideBarContainerType { | Embed | Overlay | AUTO }
```
功能： 容器内侧边栏样式枚举。

#### AUTO

```cangjie
AUTO
```
功能： 组件尺寸大于等于 minSideBarWidth + minContentWidth 时，采用Embed模式显示。

#### Embed

```cangjie
Embed
```
功能： 侧边栏嵌入到组件内，和内容区并列显示。

#### Overlay

```cangjie
Overlay
```
功能： 侧边栏浮在内容区上面。


### SideBarPosition

enum SideBarPosition
```cangjie
public enum SideBarPosition { | Start | End }
```
功能： 设置侧边栏显示位置。

#### End

```cangjie
End
```
功能： 侧边栏位于容器右侧。

#### Start

```cangjie
Start
```
功能： 侧边栏位于容器左侧。


### SourceTool

enum SourceTool
```cangjie
public enum SourceTool { | Unknown | Finger | Pen | Mouse | Touchpad | Joystick }
```
功能： 事件输入源。

#### Finger

```cangjie
Finger
```
功能： 手指输入。

#### Joystick

```cangjie
Joystick
```
功能： 手柄输入。

#### Mouse

```cangjie
Mouse
```
功能： 鼠标输入。

#### Pen

```cangjie
Pen
```
功能： 手写笔输入。

#### Touchpad

```cangjie
Touchpad
```
功能： 触控板输入。触控板单指输入被视为鼠标输入操作。

#### Unknown

```cangjie
Unknown
```
功能： 未知输入源。


### SourceType

enum SourceType
```cangjie
public enum SourceType { | Unknown | Mouse | TouchScreen }
```
功能： 事件输入设备。

#### Mouse

```cangjie
Mouse
```
功能： 鼠标。

#### TouchScreen

```cangjie
TouchScreen
```
功能： 触摸屏。

#### Unknown

```cangjie
Unknown
```
功能： 未知设备。


### SwiperNestedScrollMode

enum SwiperNestedScrollMode
```cangjie
public enum SwiperNestedScrollMode { | SELF_ONLY | SELF_FIRST }
```
功能： Swiper组件和父组件的嵌套滚动模式枚举。

#### SELF_FIRST

```cangjie
SELF_FIRST
```
功能： Swiper自身先滚动，自身滚动到边缘以后父组件滚动。父组件滚动到边缘以后，如果父组件有边缘效果，则父组件触发边缘效果，否则Swiper触发边缘效果。

#### SELF_ONLY

```cangjie
SELF_ONLY
```
功能： Swiper只自身滚动，不与父组件联动。


### TextAlign

enum TextAlign
```cangjie
public enum TextAlign { | Start | Center | End | JUSTIFY }
```
功能： 文本对齐方式。

#### Center

```cangjie
Center
```
功能： 水平居中对齐。

#### End

```cangjie
End
```
功能： 水平对齐尾部。

#### JUSTIFY

```cangjie
JUSTIFY
```
功能： 双端对齐。

#### Start

```cangjie
Start
```
功能： 水平对齐首部。


### TextAlignStyle

enum TextAlignStyle
```cangjie
public enum TextAlignStyle { | Left | Right | Center | Justify | Start | End }
```
功能： 文本对齐方式类型。

#### Center

```cangjie
Center
```
功能： 文本居中对齐。

#### End

```cangjie
End
```
功能： 文本对齐界线结束的地方。

#### Justify

```cangjie
Justify
```
功能： 文本两端对齐。

#### Left

```cangjie
Left
```
功能： 文本左对齐。

#### Right

```cangjie
Right
```
功能： 文本右对齐。

#### Start

```cangjie
Start
```
功能： 文本对齐界线开始的地方。


### TextBaseline

enum TextBaseline
```cangjie
public enum TextBaseline { | Alphabetic | Ideographic | Top | Bottom | Middle | Hanging }
```
功能： 设置文本绘制中的水平对齐方式。

#### Alphabetic

```cangjie
Alphabetic
```
功能： 文本基线是标准的字母基线。

#### Bottom

```cangjie
Bottom
```
功能： 文本基线在文本块的底部。 与ideographic基线的区别在于ideographic基线不需要考虑下行字母。

#### Hanging

```cangjie
Hanging
```
功能： 文本基线是悬挂基线。

#### Ideographict

```cangjie
Ideographict
```
功能： 文字基线是表意字基线；如果字符本身超出了alphabetic基线，那么ideograhpic基线位置在字符本身的底部。

#### Middle

```cangjie
Middle
```
功能： 文本基线在文本块的中间。

#### Top

```cangjie
Top
```
功能： 文本基线在文本块的顶部。


### TextCase

enum TextCase
```cangjie
public enum TextCase { | Normal | LowerCase | UpperCase }
```
功能： 文本大小写格式。

#### LowerCase

```cangjie
LowerCase
```
功能： 文本采用全小写。

#### Normal

```cangjie
Normal
```
功能： 保持文本原有大小写。

#### UpperCase

```cangjie
UpperCase
```
功能： 文本采用全大写。


### TextContentStyle

enum TextContentStyle
```cangjie
public enum TextContentStyle { | DEFAULT | INLINE }
```
功能： 文本框多态样式。

#### DEFAULT

```cangjie
DEFAULT
```
功能： 默认风格，光标宽1.5vp，光标高度与文本选中底板高度和字体大小相关。

#### INLINE

```cangjie
INLINE
```
功能： 内联输入风格。文本选中底板高度与输入框高度相同。 内联输入是在有明显的编辑态/非编辑态的区分场景下使用，例如：文件列表视图中的重命名。 不支持showError属性。 内联模式下，不支持拖入文本。


### TextDecorationStyle

enum TextDecorationStyle
```cangjie
public enum TextDecorationStyle { | SOLID | DOUBLE | DOTTED | DASHED | WAVY }
```
功能： 设置文本装饰线样式。

#### DASHED

```cangjie
DASHED
```
功能： 虚线。

#### DOTTED

```cangjie
DOTTED
```
功能： 点线。

#### DOUBLE

```cangjie
DOUBLE
```
功能： 双实线。

#### SOLID

```cangjie
SOLID
```
功能： 单实线（默认值）。

#### WAVY

```cangjie
WAVY
```
功能： 波浪线。


### TextDecorationType

enum TextDecorationType
```cangjie
public enum TextDecorationType { | None | Underline | Overline | LineThrough }
```
功能： 装饰线类型枚举。

#### LineThrough

```cangjie
LineThrough
```
功能： 穿过文本的修饰线。

#### None

```cangjie
None
```
功能： 不使用文本装饰线。

#### Overline

```cangjie
Overline
```
功能： 文字上划线修饰。

#### Underline

```cangjie
Underline
```
功能： 文字下划线修饰。


### TextHeightAdaptivePolicy

enum TextHeightAdaptivePolicy
```cangjie
public enum TextHeightAdaptivePolicy { | MAX_LINES_FIRST | MIN_FONT_SIZE_FIRST | LAYOUT_CONSTRAINT_FIRST }
```
功能： 设置文本高度自适应方式。

#### LAYOUT_CONSTRAINT_FIRST

```cangjie
LAYOUT_CONSTRAINT_FIRST
```
功能： 设置文本高度自适应方式为以布局约束（高度）优先。

#### MAX_LINES_FIRST

```cangjie
MAX_LINES_FIRST
```
功能： 设置文本高度自适应方式为以MaxLines优先。

#### MIN_FONT_SIZE_FIRST

```cangjie
MIN_FONT_SIZE_FIRST
```
功能： 设置文本高度自适应方式为以缩小字体优先。


### TextOverflow

enum TextOverflow
```cangjie
public enum TextOverflow { | Clip | Ellipsis | None | MARQUEE }
```
功能： 文本超长时的显示方式。

#### Clip

```cangjie
Clip
```
功能： 文本超长时按最大行截断显示。

#### Ellipsis

```cangjie
Ellipsis
```
功能： 文本超长时显示不下的文本用省略号代替。

#### MARQUEE

```cangjie
MARQUEE
```
功能： 文本超长时以跑马灯的方式展示。

#### None

```cangjie
None
```
功能： 文本超长时按最大行截断显示。


### TextSelectableMode

enum TextSelectableMode
```cangjie
public enum TextSelectableMode { | SELECTABLE_UNFOCUSABLE | SELECTABLE_FOCUSABLE | UNSELECTABLE }
```
功能： 文本是否支持可选择、可获焦。

#### SELECTABLE_FOCUSABLE

```cangjie
SELECTABLE_FOCUSABLE
```
功能： 文本可选择，可获焦并Touch后获得焦点。

#### SELECTABLE_UNFOCUSABLE

```cangjie
SELECTABLE_UNFOCUSABLE
```
功能： 文本可选择，但不可获焦，设置属性selection、bindSelectionMenu、copyOption不影响当前行为。

#### UNSELECTABLE

```cangjie
UNSELECTABLE
```
功能： 文本不可选择，不可获焦，设置属性selection、bindSelectionMenu、copyOption都不生效。


### ThemeColorMode

enum ThemeColorMode
```cangjie
public enum ThemeColorMode { | SYSTEM | LIGHT | DARK }
```
功能： 主题颜色模式。

#### DARK

```cangjie
DARK
```
功能： 固定使用深色模式。

#### LIGHT

```cangjie
LIGHT
```
功能： 固定使用浅色模式。

#### SYSTEM

```cangjie
COMPONENT_ULTRA_THICK
```
功能： 跟随系统深浅色模式。


### TitleHeight

enum TitleHeight
```cangjie
public enum TitleHeight { | MainOnly | MainWithSub }
```
功能： 设置标题栏高度。

#### MainOnly

```cangjie
MainOnly
```
功能： 只有主标题时标题栏的推荐高度（56vp）。

#### MainWithSub

```cangjie
MainWithSub
```
功能： 同时有主标题和副标题时标题栏的推荐高度（82vp）。


### TouchType

enum TouchType
```cangjie
public enum TouchType { | Down | Up | Move | Cancel | Unknown }
```
功能： 触摸触发方式。

#### Cancel

```cangjie
Cancel
```
功能： 触摸事件取消时触发。

#### Down

```cangjie
Down
```
功能： 手指按下时触发。

#### Move

```cangjie
Move
```
功能： 手指按压态在屏幕上移动时触发。

#### Up

```cangjie
Up
```
功能： 手指抬起时触发。


### TransitionType

enum TransitionType
```cangjie
public enum TransitionType { | All | Insert | Delete }
```
功能： 指定该转场样式生效的场景。

#### All

```cangjie
All
```
功能： 指定当前的Transition动效生效在组件的所有变化场景。

#### Delete

```cangjie
Delete
```
功能： 指定当前的Transition动效生效在组件的删除隐藏场景。

#### Insert

```cangjie
Insert
```
功能： 指定当前的Transition动效生效在组件的插入显示场景。


### VerticalAlign

enum VerticalAlign
```cangjie
public enum VerticalAlign { | Top | Center | Bottom }
```
功能： 垂直方向上对齐方式。

#### Bottom

```cangjie
Bottom
```
功能： 底部对齐。

#### Center

```cangjie
Center
```
功能： 居中对齐，默认对齐方式。

#### Top

```cangjie
Top
```
功能： 顶部对齐。


### Visibility

enum Visibility
```cangjie
public enum Visibility { | Visible | Hidden | None }
```
功能： 当前组件显示或隐藏。

#### Hidden

```cangjie
Hidden
```
功能： 隐藏，但参与布局进行占位。

#### None

```cangjie
None
```
功能： 隐藏，但不参与布局，不进行占位。

#### Visible

```cangjie
Visible
```
功能： 显示。


### WebDarkMode

enum WebDarkMode
public enum WebDarkMode { | Off | On | Auto }

#### Off

```cangjie
Off
```
功能： Web的深色模式为关闭。

#### On

```cangjie
On
```
功能： Web的深色模式为开启。

#### Auto

```cangjie
Auto
```
功能： Web的深色模式为跟随系统。


### Week

enum Week
```cangjie
public enum Week { | Mon | Tue | Wed | Thur | Fri | Sat | Sun }
```
功能： 星期日期信息。

#### Fri

```cangjie
Fri
```
功能： 星期五。

#### Mon

```cangjie
Mon
```
功能： 星期一。

#### Sat

```cangjie
Sat
```
功能： 星期六。

#### Sun

```cangjie
Sun
```
功能： 星期日。

#### Thur

```cangjie
Thur
```
功能： 星期四。

#### Tue

```cangjie
Tue
```
功能： 星期二。

#### Wed

```cangjie
Wed
```
功能： 星期三。


### WordBreak

enum WordBreak
```cangjie
public enum WordBreak { | Normal | BreakAll | BreakWord }
```
功能： 设置文本断行规则。

#### BreakAll

```cangjie
Normal
```
功能： 对于Non-CJK的文本，可在任意2个字符间断行。对于CJK与NORMAL效果一致。

#### BreakWord

```cangjie
BreakWord
```
功能： 与BREAKALL相同，对于Non-CJK的文本可在任意2个字符间断行，一行文本中有断行破发点（如空白符）时，优先按破发点换行，保障单词优先完整显示。若整一行文本均无断行破发点时，则在任意2个字符间断行。对于CJK与NORMAL效果一致。

#### Normal

```cangjie
Normal
```
功能： CJK(中文、日文、韩文)文本可以在任意2个字符间断行，而Non-CJK文本（如英文等）只能在空白符处断行。


### XComponentType

enum XComponentType
```cangjie
public enum XComponentType { | SURFACE | COMPONENT | TEXTURE | NODE }
```
功能： 定义XComponent的具体配置参数。

#### COMPONENT

```cangjie
COMPONENT
```
功能： XComponent将变成一个容器组件，并可在其中执行非UI逻辑以动态加载显示内容。

#### NODE

```cangjie
NODE
```
功能： 用于Native UI节点的占位容器，开发者通过Native API 开发的页面组件可展示在此容器区域内。

#### SURFACE

```cangjie
SURFACE
```
功能： 用于EGL/OpenGLES和媒体数据写入，开发者定制的绘制内容单独展示到屏幕上。背景色设置为黑色时会走显示子系统（DSS）。

#### TEXTURE

```cangjie
TEXTURE
```
功能： 用于EGL/OpenGLES和媒体数据写入，开发者定制的绘制内容会和XComponent组件的内容合成后展示到屏幕上。1、保持帧同步，保持在同一帧将图形处理器（GPU）纹理和ArkUI其他的绘制指令统一发给渲染服务(RenderService)。2、动效和原生组件统一。3、走图形处理器（GPU）合成，相比surface可能走显示子系统（DSS）功耗更高。



