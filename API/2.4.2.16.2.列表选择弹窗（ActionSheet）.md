# 列表选择弹窗（ActionSheet）

列表弹窗。

## Classes

### ActionSheet

class ActionSheet
```cangjie
public class ActionSheet {}
```
功能： 构造一个ActionSheet类型的对象。


### ActionSheetOptions

class ActionSheetOptions
```cangjie
public class ActionSheetOptions { public ActionSheetOptions( public var title: String, public var message: String, public var sheets: Array<SheetInfo>, public var subtitle!: Option<String> = Option.None, public var confirm!: Option<Confirm> = Option.None, public var autoCancel!: Option<Bool> = true, public var cancel!: Option<() -> Unit> = Option.None, public var alignment!: Option<DialogAlignment> = DialogAlignment.Bottom, public var offset!: Option<Offset> = Option.None, public var maskRect!: Option<Rectangle> = Rectangle(x: Length(0), y: Length(0), width: Length(100, unitType: LengthType.percent), height: Length(100, unitType: LengthType.percent)), public var showInSubWindow!: Option<Bool> = false, public var isModal!: Option<Bool> = true, public var backgroundColor!: Option<Color> = Color.TRANSPARENT, public var backgroundBlurStyle!: Option<BlurStyle> = BlurStyle.COMPONENT_ULTRA_THICK, public var onWillDismiss!: Option<( DismissDialogAction ) -> Unit> = None, public var cornerRadius!: Option<BorderRadiuses> = BorderRadiuses(topLeft: 32.vp, topRight: 32.vp, bottomLeft: 32.vp, bottomRight: 32.vp), public var borderWidth!: Option<BorderWidth> = BorderWidth(0.vp, 0.vp, 0.vp, 0.vp), public var borderColor!: Option<Color> = Color.BLACK, public var borderStyle!: Option<EdgeStyle> = EdgeStyle.SOILD, public var width!: Option<Length> = Option<Length>.None, public var height!: Option<Length> = Option<Length>.None, public var transition!: Option<TransitionEffect> = Option.None ){} }
```
功能： 列表选择弹窗的参数配置。

#### var alignment

```cangjie
public var alignment: Option<DialogAlignment>= DialogAlignment.Bottom
```
功能： 弹窗在竖直方向上的对齐方式。
Type: Option< DialogAlignment >; Readable: true; Writable: true

#### var autoCancel

```cangjie
public var autoCancel: Option<Bool>= true
```
功能： 点击遮障层时，是否关闭弹窗。
Type: Option<Bool>; Readable: true; Writable: true

#### var backgroundBlurStyle

```cangjie
public var backgroundBlurStyle: Option<BlurStyle>= BlurStyle.COMPONENT_ULTRA_THICK
```
功能： 弹窗背板模糊材质。
Type: Option< BlurStyle >; Readable: true; Writable: true

#### var backgroundColor

```cangjie
public var backgroundColor: Option<Color>= Color.TRANSPARENT
```
功能： 弹窗背板颜色。
Type: Option< Color >; Readable: true; Writable: true

#### var borderColor

```cangjie
public var borderColor: Option<Color>= Color.BLACK
```
功能： 设置弹窗背板的边框颜色。如果使用borderColor属性，需要和borderWidth属性一起使用。
Type: Option< Color >; Readable: true; Writable: true

#### var borderStyle

```cangjie
public var borderStyle: Option<EdgeStyle>= EdgeStyle.SOILD
```
功能： 设置弹窗背板的边框样式。如果使用borderStyle属性，需要和borderWidth属性一起使用。
Type: Option< EdgeStyle >; Readable: true; Writable: true

#### var borderWidth

```cangjie
public var borderWidth: Option<Length>= 0.vp
```
功能： 设置弹窗背板的边框宽度。
Type: Option< Length >; Readable: true; Writable: true

#### var cancel

```cangjie
public var cancel: Option <() -> Unit>= Option.None
```
功能： 点击遮障层关闭dialog时的回调。
Type: Option<()->Unit>; Readable: true; Writable: true

#### var confirm

```cangjie
public var confirm: Option<Confirm>= Option.None
```
功能： 确认Button的使能状态、默认焦点、按钮风格、文本内容和点击回调。在弹窗获焦且未进行tab键走焦时，该按钮默认响应Enter键，且多重弹窗可自动获焦连续响应。
Type: Option< Confirm >; Readable: true; Writable: true

#### var cornerRadius

```cangjie
public var cornerRadius: Option<BorderRadiuses>= BorderRadiuses(topLeft: 32.vp, topRight: 32.vp, bottomLeft: 32.vp, bottomRight: 32.vp)
```
功能： 设置背板的圆角半径。可分别设置4个圆角的半径。
Type: Option< BorderRadiuses >; Readable: true; Writable: true

#### var height

```cangjie
public var height: Option<Length>= Option<Length>.None
```
功能： 设置弹窗背板的高度。
Type: Option< Length >; Readable: true; Writable: true

#### var isModal

```cangjie
public var isModal: Option<Bool>= true
```
功能： 弹窗是否为模态窗口，模态窗口有蒙层，非模态窗口无蒙层。
Type: Option<Bool>; Readable: true; Writable: true

#### var maskRect

```cangjie
public var maskRect: Option<Rectangle>= Rectangle(x: 0, y: 0, width: 100.percent, height: 100.percent)
```
功能： 弹窗遮蔽层区域，在遮蔽层区域内的事件不透传，在遮蔽层区域外的事件透传。
Type: Option< Rectangle >; Readable: true; Writable: true

#### var message

```cangjie
public var message: String
```
功能： 弹窗内容。
Type: String; Readable: true; Writable: true

#### var offset

```cangjie
public var offset: Option<Offset>= Option.None
```
功能： 弹窗相对alignment所在位置的偏移量。
Type: Option< Offset >; Readable: true; Writable: true

#### var onWillDismiss

```cangjie
public var onWillDismiss: Option <(DismissDialogAction) -> Unit>= None
```
功能： 交互式关闭回调函数。
Type: Option<( DismissDialogAction ) -> Unit>; Readable: true; Writable: true

#### var sheets

```cangjie
public var sheets: Array<SheetInfo>
```
功能： 设置选项内容，每个选择项支持设置图片、文本和选中的回调。
Type: Array< SheetInfo >; Readable: true; Writable: true

#### var showInSubWindow

```cangjie
public var showInSubWindow: Option<Bool> = false
```
功能： 某弹框需要显示在主窗口之外时，是否在子窗口显示此弹窗。
Type: Option<Bool>; Readable: true; Writable: true

#### var subtitle

```cangjie
public var subtitle: Option<String> = Option.None
```
功能： 弹窗副标题。
Type: Option<String>; Readable: true; Writable: true

#### var title

```cangjie
public var title: String
```
功能： 弹窗标题。
Type: String; Readable: true; Writable: true

#### var transition

```cangjie
public var transition: Option<TransitionEffect> = Option.None
```
功能： 设置弹窗显示和退出的过渡效果。
Type: Option< TransitionEffect >; Readable: true; Writable: true

#### var width

```cangjie
public var width: Option<Length> = Option<Length>.None
```
功能： 设置弹窗背板的宽度。
Type: Option< Length >; Readable: true; Writable: true


### ActionSheetShadowOptions

class ActionSheetShadowOptions
```cangjie
public class ActionSheetShadowOptions { public ActionSheetShadowOptions( public var radius: Float64, public var shadowType!: ShadowType = ShadowType.COLOR, public var color!: Color = Color.BLACK, public var offsetX!: Float64 = 0.0, public var offsetY!: Float64 = 0.0, public var fill!: Bool = false ) }
```
功能： 列表选择弹窗阴影参数的配置。

#### var color

```cangjie
public var color: Color = Color.BLACK
```
功能： 阴影的颜色。
Type: Color; Readable: true; Writable: true

#### var fill

```cangjie
public var fill: Bool = false
```
功能： 阴影是否内部填充。
Type: Bool; Readable: true; Writable: true

#### var offsetX

```cangjie
public var offsetX: Float64 = 0.0
```
功能： 阴影的X轴偏移量。
Type: Float64; Readable: true; Writable: true

#### var offsetY

```cangjie
public var offsetY: Float64 = 0.0
```
功能： 阴影的Y轴偏移量。
Type: Float64; Readable: true; Writable: true

#### var radius

```cangjie
public var radius: Float64
```
功能： 阴影模糊半径。
Type: Float64; Readable: true; Writable: true

#### var shadowType

```cangjie
public var shadowType: ShadowType = ShadowType.COLOR
```
功能： 阴影类型。
Type: ShadowType; Readable: true; Writable: true


### Confirm

class Confirm
```cangjie
public class Confirm { public Confirm( public var value: String, public var action: () -> Unit, public var enabled!: Bool = true, public var defaultFocus!: Bool = false, public var style!: DialogButtonStyle = DialogButtonStyle.DEFAULT ) }
```
功能： 确认Button的使能状态、默认焦点、按钮风格、文本内容和点击回调。

#### var action

```cangjie
public var action:() -> Unit
```
功能： Button选中时的回调。
Type: ()->Unit; Readable: true; Writable: true

#### var defaultFocus

```cangjie
public var defaultFocus: Bool = false
```
功能： 设置Button是否是默认焦点。
Type: Bool; Readable: true; Writable: true

#### var enabled

```cangjie
public var enabled: Bool = true
```
功能： 点击Button是否响应。
Type: Bool; Readable: true; Writable: true

#### var style

```cangjie
public var style: DialogButtonStyle = DialogButtonStyle.DEFAULT
```
功能： 设置Button的风格样式。
Type: DialogButtonStyle; Readable: true; Writable: true

#### var value

```cangjie
public var value: String
```
功能： Button文本内容。
Type: String; Readable: true; Writable: true


### DismissDialogAction

class DismissDialogAction
```cangjie
public class DismissDialogAction { public DismissDialogAction( public let reason: DismissReason ){} }
```
功能： Dialog关闭的信息。

#### let reason

```cangjie
public let reason: DismissReason
```
功能： Dialog无法关闭原因。根据开发者需要选择不同操作下，Dialog是否需要关闭。
Type: DismissReason; Readable: true; Writable: false


### EdgeStyle

class EdgeStyle
```cangjie
public class EdgeStyle { public EdgeStyle( public var top: BorderStyle, public var right: BorderStyle, public var bottom: BorderStyle, public var left: BorderStyle ) {} }
```
功能： 边框样式，用于描述组件边框四条边的样式。

#### var bottom

```cangjie
public var bottom: BorderStyle
```
功能： 组件下边框样式。
Type: BorderStyle; Readable: true; Writable: true

#### var left

```cangjie
public var left: BorderStyle
```
功能： 组件左边框样式。
Type: BorderStyle; Readable: true; Writable: true

#### var right

```cangjie
public var right: BorderStyle
```
功能： 组件右边框样式。
Type: BorderStyle; Readable: true; Writable: true

#### var top

```cangjie
public var top: BorderStyle
```
功能： 组件上边框样式。
Type: BorderStyle; Readable: true; Writable: true


### SheetInfo

class SheetInfo
```cangjie
public class SheetInfo { public SheetInfo( public var title: String, public var action: () -> Unit, public var icon!: Option<AppResource> = Option.None ) {} }
```
功能： 设置选项内容，每个选择项支持设置图片、文本和选中的回调。

#### var action

```cangjie
public var action:() -> Unit
```
功能： 选项选中的回调。
Type: ()->Unit; Readable: true; Writable: true

#### var icon

```cangjie
public var icon: Option<AppResource> = Option.None
```
功能： 选项的图标，默认无图标显示。
Type: Option< AppResource >; Readable: true; Writable: true

#### var title

```cangjie
public var title: String
```
功能： 选项的文本内容。
Type: String; Readable: true; Writable: true



## Enums

### DismissReason

enum DismissReason
```cangjie
public enum DismissReason { | PRESS_BACK | TOUCH_OUTSIDE | CLOSE_BUTTON | SLIDE_DOWN }
```
功能： 弹窗关闭原因。

#### CLOSE_BUTTON

```cangjie
CLOSE_BUTTON
```
功能： 点击了关闭按钮。

#### PRESS_BACK

```cangjie
PRESS_BACK
```
功能： 点击三键back、左滑/右滑、键盘ESC。

#### SLIDE_DOWN

```cangjie
SLIDE_DOWN
```
功能： 下拉关闭。

#### TOUCH_OUTSIDE

```cangjie
TOUCH_OUTSIDE
```
功能： 点击遮障层时。



