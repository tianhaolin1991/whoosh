# ohos.multimodalInput.keyEvent（按键输入事件）

设备上报的按键事件，继承自 InputEvent 。

```cangjie
import kit.InputKit.*
```
## Classes

### Key

class Key
```cangjie
public class Key { public Key( public var code: KeyCode, public var pressedTime: Int64, public var deviceId: Int32 ) }
```
功能： 按键。

#### var code

```cangjie
public var code: KeyCode
```
功能： 按键码。
Type: KeyCode; Readable: true; Writable: true

#### var pressedTime

```cangjie
public var pressedTime: Int64
```
功能： 按键按下时间，单位为微秒（μs）。
Type: Int64; Readable: true; Writable: true

#### var deviceId

```cangjie
public var deviceId: Int32
```
功能： 按键所属设备id。
Type: Int32; Readable: true; Writable: true


### KeyEvent

class KeyEvent
```cangjie
public class KeyEvent <: InputEvent { public var action: Action public var key: Key public var unicodeChar: UInt32 public var keys: Array<Key> public var ctrlKey: Bool public var altKey: Bool public var shiftKey: Bool public var logoKey: Bool public var fnKey: Bool public var capsLock: Bool public var numLock: Bool public var scrollLock: Bool public init ( base: InputEvent, action: Action, key: Key, unicodeChar: UInt32, keys: Array<Key>, ctrlKey: Bool, altKey: Bool, shiftKey: Bool, logoKey: Bool, fnKey: Bool, capsLock: Bool, numLock: Bool, scrollLock: Bool ) }
```
功能： 按键事件。

#### var action

```cangjie
public var action: Action
```
功能： 按键动作。
Type: Action; Readable: true; Writable: true

#### var key

```cangjie
public var key: Key
```
功能： 当前上报的按键。
Type: Key; Readable: true; Writable: true

#### var unicodeChar

```cangjie
public var unicodeChar: UInt32
```
功能： 按键对应的uniCode字符。
Type: UInt32; Readable: true; Writable: true

#### var keys

```cangjie
public var keys: Array<Key>
```
功能： 当前处于按下状态的按键列表。
Type: Array< Key >; Readable: true; Writable: true

#### var ctrlKey

```cangjie
public var ctrlKey: Bool
```
功能： 当前ctrlKey是否处于按下状态。true表示处于按下状态，false表示处于抬起状态。
Type: Bool; Readable: true; Writable: true

#### var altKey

```cangjie
public var altKey: Bool
```
功能： 当前altKey是否处于按下状态。true表示处于按下状态，false表示处于抬起状态。
Type: Bool; Readable: true; Writable: true

#### var shiftKey

```cangjie
public var shiftKey: Bool
```
功能： 当前shiftKey是否处于按下状态。true表示处于按下状态，false表示处于抬起状态。
Type: Bool; Readable: true; Writable: true

#### var logoKey

```cangjie
public var logoKey: Bool
```
功能： 当前logoKey是否处于按下状态。true表示处于按下状态，false表示处于抬起状态。
Type: Bool; Readable: true; Writable: true

#### var fnKey

```cangjie
public var fnKey: Bool
```
功能： 当前fnKey是否处于按下状态。true表示处于按下状态，false表示处于抬起状态。
Type: Bool; Readable: true; Writable: true

#### var capsLock

```cangjie
public var capsLock: Bool
```
功能： 当前capsLock是否处于激活状态。true表示处于激活状态，false表示处于未激活状态。
Type: Bool; Readable: true; Writable: true

#### var numLock

```cangjie
public var numLock: Bool
```
功能： 当前numLock是否处于激活状态。true表示处于激活状态，false表示处于未激活状态。
Type: Bool; Readable: true; Writable: true

#### var scrollLock

```cangjie
public var scrollLock: Bool
```
功能： 当前scrollLock是否处于激活状态。true表示处于激活状态，false表示处于未激活状态。
Type: Bool; Readable: true; Writable: true



## Enums

### Action

enum Action
```cangjie
public enum Action <: Equatable<Action> & ToString { | CANCEL | DOWN | UP |... }
```
功能： 按键事件类型。

#### CANCEL

```cangjie
CANCEL
```
功能： 按键取消。

#### DOWN

```cangjie
DOWN
```
功能： 按键按下。

#### UP

```cangjie
UP
```
功能： 按键抬起。



