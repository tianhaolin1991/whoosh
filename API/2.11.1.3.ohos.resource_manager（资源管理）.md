# ohos.resource_manager（资源管理）

资源管理模块，根据当前configuration：语言、区域、横竖屏、Mcc（移动国家码）和Mnc（移动网络码）、Device capability（设备类型）、Density（分辨率）提供获取应用资源对象读取接口。

```cangjie
import kit.LocalizationKit.*
```
## Functions

### public func __GenerateResource__( bundleName: String, moudleType: String, moduleName: String, resId: Int32, resStr: String, params: Array<Any>, resType: Int32 ): AppResource

```cangjie
public func __GenerateResource__( bundleName: String, moudleType: String, moduleName: String, resId: Int32, resStr: String, params: Array<Any>, resType: Int32 ): AppResource
```
功能： 资源管理宏使用。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 应用的包名称。 | bundleName | - |
| String | 是 | 应用的模块类型。 | moudleType | - |
| String | 是 | 应用的模块名称。 | moduleName | - |
| Int32 | 是 | 资源id。 | resId | - |
| String | 是 | 资源名称。 | resStr | - |
| Array<Any> | 是 | 其他资源参数。 | params | - |
| Int32 | 是 | 资源的类型。 | resType | - |

**Returns:**

| type | description |
| --- | --- |
| AppResource | 资源类型。 |


## Classes

### AppResource

class AppResource
```cangjie
public class AppResource <: Length & ResourceColor { public AppResource( public let bundleName: String, public let moduleName: String, public let id: Int32, public let params!: ?Array<Any> = None, public let resType!: ?Int32 = None ) }
```
功能： 表示资源类型。

#### let bundleName

```cangjie
public let bundleName: String
```
功能： 应用的包名称。
Type: String; Readable: true; Writable: false

#### let id

```cangjie
public let id: Int32
```
功能： 资源id。
Type: Int32; Readable: true; Writable: false

#### let moduleName

```cangjie
public let moduleName: String
```
功能： 应用的模块名称。
Type: String; Readable: true; Writable: false

#### let params

```cangjie
public let params: ?Array<Any>=None
```
功能： 其他资源参数（可选）。
Type: ?Array<Any>; Readable: true; Writable: false

#### let resType

```cangjie
public let resType: ?Int32 = None
```
功能： 资源的类型（可选）。
Type: ?Int32; Readable: true; Writable: false


### Configuration

class Configuration
```cangjie
public class Configuration <: ToString { public Configuration( public let direction: Direction, public let locale: String ) }
```
功能： 表示当前设备的配置。

#### let direction

```cangjie
public let direction: Direction
```
功能： 当前设备屏幕方向。
Type: Direction; Readable: true; Writable: false

#### let locale

```cangjie
public let locale: String
```
功能： 当前系统语言。
Type: String; Readable: true; Writable: false


### DeviceCapability

class DeviceCapability
```cangjie
public class DeviceCapability <: ToString { public DeviceCapability( public let screenDensity: ScreenDensity, public let deviceType: DeviceType ) }
```
功能： 表示设备支持的能力。

#### let deviceType

```cangjie
public let deviceType: DeviceType
```
功能： 当前设备类型。
Type: DeviceType; Readable: true; Writable: false

#### let screenDensity

```cangjie
public let screenDensity: ScreenDensity
```
功能： 当前设备屏幕密度。
Type: ScreenDensity; Readable: true; Writable: false


### DrawableDescriptor

class DrawableDescriptor
```cangjie
public class DrawableDescriptor {}
```
功能： 表示 DrawableDescriptor 实例。


### RawFileDescriptor

class RawFileDescriptor
```cangjie
public class RawFileDescriptor { public RawFileDescriptor( public let fd: Int32, public let offset: Int64, public let length: Int64 ) }
```
功能： 表示rawfile的描述符信息。

#### let fd

```cangjie
public let fd: Int32
```
功能： rawfile所在hap的文件描述符。
Type: Int32; Readable: true; Writable: false

#### let length

```cangjie
public let length: Int64
```
功能： rawfile的文件长度。
Type: Int64; Readable: true; Writable: false

#### let offset

```cangjie
public let offset: Int64
```
功能： rawfile的起始偏移量。
Type: Int64; Readable: true; Writable: false


### ResourceManager

class ResourceManager
```cangjie
public class ResourceManager {}
```
功能： 提供访问应用资源的能力。



## Enums

### DeviceType

enum DeviceType
```cangjie
public enum DeviceType { | DEVICE_TYPE_PHONE | DEVICE_TYPE_TABLET | DEVICE_TYPE_CAR | DEVICE_TYPE_PC | DEVICE_TYPE_TV | DEVICE_TYPE_WEARABLE | DEVICE_TYPE_2IN1 | ... }
```
功能： 用于表示当前设备类型。

#### DEVICE_TYPE_2IN1

```cangjie
DEVICE_TYPE_2IN1
```
功能： 二合一设备。

#### DEVICE_TYPE_CAR

```cangjie
DEVICE_TYPE_CAR
```
功能： 汽车。

#### DEVICE_TYPE_PC

```cangjie
DEVICE_TYPE_PC
```
功能： 电脑。

#### DEVICE_TYPE_PHONE

```cangjie
DEVICE_TYPE_PHONE
```
功能： 手机。

#### DEVICE_TYPE_TABLET

```cangjie
DEVICE_TYPE_TABLET
```
功能： 平板。

#### DEVICE_TYPE_TV

```cangjie
DEVICE_TYPE_TV
```
功能： 电视。

#### DEVICE_TYPE_WEARABLE

```cangjie
DEVICE_TYPE_WEARABLE
```
功能： 穿戴。

#### static func parse(Int32)

```cangjie
public static func parse(val: Int32): DeviceType
```
功能： 根据设备类型值，构造设备类型实例。


### Direction

enum Direction
```cangjie
public enum Direction { | DIRECTION_VERTICAL | DIRECTION_HORIZONTAL | ... }
```
功能： 用于表示设备屏幕方向。

#### DIRECTION_HORIZONTAL

```cangjie
DIRECTION_HORIZONTAL
```
功能： 横屏。

#### DIRECTION_VERTICAL

```cangjie
DIRECTION_VERTICAL
```
功能： 竖屏。

#### static func parse(Int32)

```cangjie
public static func parse(val: Int32): Direction
```
功能： 根据设备屏幕方向值，构造设备屏幕方向实例。


### FormatArgs

enum FormatArgs
```cangjie
public enum FormatArgs { | INT(Int32) | FLOAT(Float32) | STRING(String) | ... }
```
功能： 表示字符串的格式化数据。

#### FLOAT(Float32)

```cangjie
FLOAT(Float32)
```
功能： 存储Float32类型值的格式化数据。

#### INT(Int32)

```cangjie
INT(Int32)
```
功能： 存储Int32类型值的格式化数据。

#### STRING(String)

```cangjie
STRING(String)
```
功能： 存储String类型值的格式化数据。


### Number

enum Number
```cangjie
public enum Number { | INT(Int32) | FLOAT(Float32) | ... }
```
功能： 表示从资源中获取到的数字类型。

#### FLOAT(Float32)

```cangjie
FLOAT(Float32)
```
功能： 存储Float32类型值的Number类型。

#### INT(Int32)

```cangjie
INT(Int32)
```
功能： 存储Int32类型值的Number类型。


### ScreenDensity

enum ScreenDensity
```cangjie
public enum ScreenDensity { | SCREEN_SDPI | SCREEN_MDPI | SCREEN_LDPI | SCREEN_XLDPI | SCREEN_XXLDPI | SCREEN_XXXLDPI | ... }
```
功能： 用于表示当前设备屏幕密度。

#### SCREEN_LDPI

```cangjie
SCREEN_LDPI
```
功能： 大规模的屏幕密度。

#### SCREEN_MDPI

```cangjie
SCREEN_MDPI
```
功能： 中规模的屏幕密度。

#### SCREEN_SDPI

```cangjie
SCREEN_SDPI
```
功能： 小规模的屏幕密度。

#### SCREEN_XLDPI

```cangjie
SCREEN_XLDPI
```
功能： 特大规模的屏幕密度。

#### SCREEN_XXLDPI

```cangjie
SCREEN_XXLDPI
```
功能： 超大规模的屏幕密度。

#### SCREEN_XXXLDPI

```cangjie
SCREEN_XXXLDPI
```
功能： 超特大规模的屏幕密度。

#### static func parse(Int32)

```cangjie
public static func parse(val: Int32): ScreenDensity
```
功能： 根据屏幕密度值，构造屏幕密度实例。



