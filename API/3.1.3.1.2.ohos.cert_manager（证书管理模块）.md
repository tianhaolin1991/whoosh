# ohos.cert_manager（证书管理模块）

证书管理主要提供系统级的证书管理能力，实现证书全生命周期（安装，存储，使用，销毁）的管理和安全使用。

```cangjie
import kit.DeviceCertificateKit.*
```
## Functions

### public func `init`(authUri: String, spec: CMSignatureSpec): CMHandle

```cangjie
public func `init`(authUri: String, spec: CMSignatureSpec): CMHandle
```
功能： 表示使用凭据进行签名、验签的初始化操作。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| String | 表示使用凭据的唯一标识符。 | authUri | 是 |
| CMSignatureSpec | 表示签名、验签的属性。 | spec | 是 |

**Returns:**

| type | description |
| --- | --- |
| CMHandle | 返回签名、验签的初始化操作结果。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 证书管理错误码 和 通用错误码 。

| code | description |
| --- | --- |
| 201 | Permission verification failed. |
| 401 | Parameter error. |
| 17500001 | Internal error. |
| 17500002 | The certificate does not exist. |
| 17500005 | Permission verification failed. |


**Example:**

```cangjie
// index.cj

import kit.DeviceCertificateKit.*

// 此处代码可添加在依赖项定义中
func cm_init(): CMHandle {
 //以下只是一个示例，需要根据具体业务来赋值
 let g_rsa2048P12CertInfo: Array<UInt8> = [0x30, 0x82, 0x01, 0x22, 0x30, 0x0d]
 let test_certInfo = g_rsa2048P12CertInfo
 let test_passwd = "123456"
 let test_alias = "test_install"
 var keyUri = installPrivateCertificate(g_rsa2048P12CertInfo, test_passwd, test_alias)

 let spec: CMSignatureSpec = CMSignatureSpec(CmKeyPurpose.CM_KEY_PURPOSE_SIGN, CmKeyPadding.CM_PADDING_PSS, CmKeyDigest.CM_DIGEST_SHA256)
 let handle1: CMHandle = `init`(keyUri, spec)
 uninstallPrivateCertificate(keyUri)
 return handle1
 }

cm_init()
```

### public func abort(handle: CMHandle): Unit

```cangjie
public func abort(handle: CMHandle): Unit
```
功能： 表示中止签名、验签的操作。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| CMHandle | 表示初始化操作返回的句柄。 | handle | 是 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 证书管理错误码 和 通用错误码 。

| code | description |
| --- | --- |
| 201 | Permission verification failed. |
| 401 | Parameter error. |
| 17500001 | Internal error. |


**Example:**

```cangjie
// index.cj

import kit.DeviceCertificateKit.*

//以下只是一个示例，需要根据具体业务来赋值
let g_rsa2048P12CertInfo: Array<UInt8> = [0x30, 0x82, 0x01, 0x22, 0x30, 0x0d]
let test_certInfo = g_rsa2048P12CertInfo
let test_passwd = "123456"
let test_alias = "test_install"
var keyUri = installPrivateCertificate(g_rsa2048P12CertInfo, test_passwd, test_alias)

let spec: CMSignatureSpec = CMSignatureSpec(CmKeyPurpose.CM_KEY_PURPOSE_SIGN, CmKeyPadding.CM_PADDING_PSS, CmKeyDigest.CM_DIGEST_SHA256)
let handle1: CMHandle = `init`(keyUri, spec)
let data: Array<UInt8> = [0x00, 0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x08]
update(handle1, data)
abort(handle1)
uninstallPrivateCertificate(keyUri)
```

### public func finish(handle: CMHandle, signature: Array<UInt8>): Unit

```cangjie
public func finish(handle: CMHandle, signature: Array<UInt8>): Unit
```
功能： 表示完成验签的操作。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| CMHandle | 表示初始化操作返回的句柄。 | handle | 是 |
| Array<UInt8> | 表示签名数据。 | signature | 是 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 证书管理错误码 和 通用错误码 。

| code | description |
| --- | --- |
| 201 | Permission verification failed. |
| 401 | Parameter error. |
| 17500001 | Internal error. |


**Example:**

```cangjie
// index.cj

import kit.DeviceCertificateKit.*

//以下只是一个示例，需要根据具体业务来赋值
let g_rsa2048P12CertInfo: Array<UInt8> = [0x30, 0x82, 0x01, 0x22, 0x30, 0x0d]
let signRes: Array<UInt8> = [0]
let test_certInfo = g_rsa2048P12CertInfo
let test_passwd = "123456"
let test_alias = "test_install"
var keyUri = installPrivateCertificate(g_rsa2048P12CertInfo, test_passwd, test_alias)

let spec: CMSignatureSpec = CMSignatureSpec(CmKeyPurpose.CM_KEY_PURPOSE_SIGN, CmKeyPadding.CM_PADDING_PSS, CmKeyDigest.CM_DIGEST_SHA256)
let handle1: CMHandle = `init`(keyUri, spec)
let data: Array<UInt8> = [0x00, 0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x08]
update(handle1, data)
finish(handle1, signRes)
uninstallPrivateCertificate(keyUri)
```

### public func finish(handle: CMHandle): Array<UInt8>

```cangjie
public func finish(handle: CMHandle): Array<UInt8>
```
功能： 表示完成签名的操作。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| CMHandle | 表示初始化操作返回的句柄。 | handle | 是 |

**Returns:**

| type | description |
| --- | --- |
| Array<UInt8> | 返回签名的结果。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 证书管理错误码 和 通用错误码 。

| code | description |
| --- | --- |
| 201 | Permission verification failed. |
| 401 | Parameter error. |
| 17500001 | Internal error. |


**Example:**

```cangjie
// index.cj

import kit.DeviceCertificateKit.*

// 此处代码可添加在依赖项定义中
func cm_finish(): Array<UInt8> {
 //以下只是一个示例，需要根据具体业务来赋值
 let g_rsa2048P12CertInfo: Array<UInt8> = [0x30, 0x82, 0x01, 0x22, 0x30, 0x0d]
 let test_certInfo = g_rsa2048P12CertInfo
 let test_passwd = "123456"
 let test_alias = "test_install"
 var keyUri = installPrivateCertificate(g_rsa2048P12CertInfo, test_passwd, test_alias)

 let spec: CMSignatureSpec = CMSignatureSpec(CmKeyPurpose.CM_KEY_PURPOSE_SIGN, CmKeyPadding.CM_PADDING_PSS, CmKeyDigest.CM_DIGEST_SHA256)
 let handle1: CMHandle = `init`(keyUri, spec)
 let data: Array<UInt8> = [0x00, 0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x08]
 update(handle1, data)
 let signRes = finish(handle1)
 uninstallPrivateCertificate(keyUri)
 return signRes
}

cm_finish()
```

### public func getAllUserTrustedCertificates(): Array<CertAbstract>

```cangjie
public func getAllUserTrustedCertificates(): Array<CertAbstract>
```
功能： 表示获取所有用户根CA证书列表。

**Returns:**

| type | description |
| --- | --- |
| Array< CertAbstract > | 返回获取用户根CA证书列表的结果。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 证书管理错误码 和 通用错误码 。

| code | description |
| --- | --- |
| 201 | Permission verification failed. |
| 17500001 | Internal error. |


**Example:**

```cangjie
// index.cj

import kit.DeviceCertificateKit.*

// 此处代码可添加在依赖项定义中
func cm_getAllUserTrustedCertificates(): Array<CertAbstract> {
 let certList = getAllUserTrustedCertificates()
 return certList
}

cm_getAllUserTrustedCertificates()
```

### public func getPrivateCertificate(keyUri: String): Credential

```cangjie
public func getPrivateCertificate(keyUri: String): Credential
```
功能： 表示获取私有凭据详情。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| String | 表示待获取凭据的唯一标识符。 | keyUri | 是 |

**Returns:**

| type | description |
| --- | --- |
| Credential | 返回获取私有凭据详细信息的结果。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 证书管理错误码 和 通用错误码 。

| code | description |
| --- | --- |
| 201 | Permission verification failed. |
| 401 | Parameter error. |
| 17500001 | Internal error. |
| 17500002 | The certificate does not exist. |


**Example:**

```cangjie
// index.cj

import kit.DeviceCertificateKit.*

// 此处代码可添加在依赖项定义中
func cm_getPrivateCertificate(): Credential {
 //以下只是一个示例，需要根据具体业务来赋值
 let g_rsa2048P12CertInfo: Array<UInt8> = [0x30, 0x82, 0x01, 0x22, 0x30, 0x0d]
 let test_certInfo = g_rsa2048P12CertInfo
 let test_passwd = "123456"
 let test_alias = "test_uninstall"
 let keyUri = installPrivateCertificate(g_rsa2048P12CertInfo, test_passwd, test_alias)
 let cred = getPrivateCertificate(keyUri)
 uninstallPrivateCertificate(keyUri)
 return cred
}

cm_getPrivateCertificate()
```

### public func getPublicCertificate(keyUri: String): Credential

```cangjie
public func getPublicCertificate(keyUri: String): Credential
```
功能： 表示获取用户公共凭据的详细信息。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| String | 表示用户公共凭据的唯一标识符。 | keyUri | 是 |

**Returns:**

| type | description |
| --- | --- |
| Credential | 返回获取用户公共凭据详细信息的结果。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 证书管理错误码 和 通用错误码 。

| code | description |
| --- | --- |
| 201 | Permission verification failed. |
| 401 | Parameter error. |
| 17500001 | Internal error. |
| 17500002 | The certificate does not exist. |
| 17500005 | The application is not authorized by the user. |


**Example:**

```cangjie
// index.cj

import kit.DeviceCertificateKit.*

// 此处代码可添加在依赖项定义中
func cm_getPublicCertificate(): Credential {
 //以下只是一个示例，需要根据具体业务来赋值
 let g_rsa2048P12CertInfo: Array<UInt8> = [0x30, 0x82, 0x01, 0x22, 0x30, 0x0d]
 let test_certInfo = g_rsa2048P12CertInfo
 let test_passwd = "123456"
 let test_alias = "test_uninstall"
 let keyUri = installPrivateCertificate(g_rsa2048P12CertInfo, test_passwd, test_alias)
 let cred = getPublicCertificate(keyUri)
 uninstallPrivateCertificate(keyUri)
 return cred
}

cm_getPublicCertificate()
```

### public func getUserTrustedCertificate(certUri: String): CertInfo

```cangjie
public func getUserTrustedCertificate(certUri: String): CertInfo
```
功能： 表示获取用户根CA证书的详细信息。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| String | 表示用户用户根CA证书的唯一标识符。 | certUri | 是 |

**Returns:**

| type | description |
| --- | --- |
| CertInfo | 返回获取用户根CA证书详细信息的结果。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 证书管理错误码 和 通用错误码 。

| code | description |
| --- | --- |
| 201 | Permission verification failed. |
| 401 | Parameter error. |
| 17500001 | Internal error. |
| 17500002 | The certificate does not exist. |


**Example:**

```cangjie
// index.cj

import kit.DeviceCertificateKit.*

// 此处代码可添加在依赖项定义中
func cm_getUserTrustedCertificate(): CertInfo {
 let certList = getAllUserTrustedCertificates()
 let info = getUserTrustedCertificate(certList[0].uri)
 return info
}

cm_getUserTrustedCertificate()
```

### public func installPrivateCertificate(keystore: Array<UInt8>, keystorePwd: String, certAlias: String): String

```cangjie
public func installPrivateCertificate(keystore: Array<UInt8>, keystorePwd: String, certAlias: String): String
```
功能： 表示安装私有凭据。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| Array<UInt8> | 表示带有密钥对和证书的密钥库文件。 | keystore | 是 |
| String | 表示密钥库文件的密码，长度限制32字节以内。 | keystorePwd | 是 |
| String | 表示用户输入的凭据别名，当前仅支持传入数字、字母或下划线，长度建议32字节以内。 | certAlias | 是 |

**Returns:**

| type | description |
| --- | --- |
| String | 返回安装私有凭据的结果。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 证书管理错误码 和 通用错误码 。

| code | description |
| --- | --- |
| 201 | Permission verification failed. |
| 401 | Parameter error. |
| 17500001 | Internal error. |
| 17500003 | The keystore is in an invalid format or the keystore password is incorrect. |
| 17500004 | The number of certificates or credentials reaches the maximum allowed. |


**Example:**

```cangjie
// index.cj

import kit.DeviceCertificateKit.*

// 此处代码可添加在依赖项定义中
func cm_installPrivateCertificate(): String {
 //以下只是一个示例，需要根据具体业务来赋值
 let g_rsa2048P12CertInfo: Array<UInt8> = [0x30, 0x82, 0x01, 0x22, 0x30, 0x0d]
 let test_certInfo = g_rsa2048P12CertInfo
 let test_passwd = "123456"
 let test_alias = "test_install"
 let keyUri = installPrivateCertificate(g_rsa2048P12CertInfo, test_passwd, test_alias)
 return keyUri
}

cm_installPrivateCertificate()
```

### public func isAuthorizedApp(keyUri: String): Bool

```cangjie
public func isAuthorizedApp(keyUri: String): Bool
```
功能： 表示当前应用是否由指定的用户凭据授权。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| String | 表示用户授权给应用使用的凭据的唯一标识符。 | keyUri | 是 |

**Returns:**

| type | description |
| --- | --- |
| Bool | 返回查询应用是否被授权的结果。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 证书管理错误码 和 通用错误码 。

| code | description |
| --- | --- |
| 201 | Permission verification failed. |
| 401 | Parameter error. |
| 17500001 | Internal error. |


**Example:**

```cangjie
// index.cj

import kit.DeviceCertificateKit.*

// 此处代码可添加在依赖项定义中
func asset_query(): Bool {
 let keyUri = "test_isAuthorizedApp"
 let retVal = isAuthorizedApp(keyUri)
 return retVal
}

asset_query()
```

### public func uninstallPrivateCertificate(keyUri: String): Unit

```cangjie
public func uninstallPrivateCertificate(keyUri: String): Unit
```
功能： 表示卸载指定的私有凭据。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| String | 表示待卸载凭据的唯一标识符。 | keyUri | 是 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 证书管理错误码 和 通用错误码 。

| code | description |
| --- | --- |
| 201 | Permission verification failed. |
| 401 | Parameter error. |
| 17500001 | Internal error. |
| 17500002 | The certificate does not exist. |


**Example:**

```cangjie
// index.cj

import kit.DeviceCertificateKit.*

// 此处代码可添加在依赖项定义中
func cm_uninstallPrivateCertificate(): Unit {
 //以下只是一个示例，需要根据具体业务来赋值
 let g_rsa2048P12CertInfo: Array<UInt8> = [0x30, 0x82, 0x01, 0x22, 0x30, 0x0d]
 let test_certInfo = g_rsa2048P12CertInfo
 let test_passwd = "123456"
 let test_alias = "test_uninstall"
 let keyUri = installPrivateCertificate(g_rsa2048P12CertInfo, test_passwd, test_alias)
 uninstallPrivateCertificate(keyUri)
}

cm_uninstallPrivateCertificate()
```

### public func update(handle: CMHandle, data: Array<UInt8>): Unit

```cangjie
public func update(handle: CMHandle, data: Array<UInt8>): Unit
```
功能： 表示签名、验签的数据更新操作。

**Parameters:**

| type | description | name | required |
| --- | --- | --- | --- |
| CMHandle | 表示初始化操作返回的句柄。 | handle | 是 |
| Array<UInt8> | 表示待签名、验签的数据。 | data | 是 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 证书管理错误码 和 通用错误码 。

| code | description |
| --- | --- |
| 201 | Permission verification failed. |
| 401 | Parameter error. |
| 17500001 | Internal error. |


**Example:**

```cangjie
// index.cj

import kit.DeviceCertificateKit.*

// 此处代码可添加在依赖项定义中
func cm_update(): Unit {
 //以下只是一个示例，需要根据具体业务来赋值
 let g_rsa2048P12CertInfo: Array<UInt8> = [0x30, 0x82, 0x01, 0x22, 0x30, 0x0d]
 let test_certInfo = g_rsa2048P12CertInfo
 let test_passwd = "123456"
 let test_alias = "test_install"
 var keyUri = installPrivateCertificate(g_rsa2048P12CertInfo, test_passwd, test_alias)

 let spec: CMSignatureSpec = CMSignatureSpec(CmKeyPurpose.CM_KEY_PURPOSE_SIGN, CmKeyPadding.CM_PADDING_PSS, CmKeyDigest.CM_DIGEST_SHA256)
 let handle1: CMHandle = `init`(keyUri, spec)
 let data: Array<UInt8> = [0x00, 0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x08]
 update(handle1, data)
 uninstallPrivateCertificate(keyUri)
}

cm_update()
```


## Classes

### CMHandle

class CMHandle
```cangjie
public class CMHandle {}
```
功能： 表示签名、验签的初始化操作句柄。


### CMSignatureSpec

class CMSignatureSpec
```cangjie
public class CMSignatureSpec { public CMSignatureSpec( public var purpose: CmKeyPurpose, public var padding: ?CmKeyPadding, public var digest: ?CmKeyDigest ) }
```
功能： 表示签名、验签操作使用的参数集合，包括密钥使用目的、填充方式和摘要算法。

#### var digest

```cangjie
public var digest: ?CmKeyDigest
```
功能： 表示摘要算法。
Type: ? CmKeyDigest; Readable: true; Writable: true

#### var padding

```cangjie
public var padding: ?CmKeyPadding
```
功能： 表示填充方式。
Type: ? CmKeyPadding; Readable: true; Writable: true

#### var purpose

```cangjie
public var purpose: CmKeyPurpose
```
功能： 表示密钥使用目的。
Type: CmKeyPurpose; Readable: true; Writable: true


### CertAbstract

class CertAbstract
```cangjie
public class CertAbstract {}
```
功能： 表示证书简要信息。

#### let certAlias

```cangjie
public let certAlias: String
```
功能： 表示证书的别名。
Type: String; Readable: true; Writable: false

#### let state

```cangjie
public let state: Bool
```
功能： 表示证书的状态，true为启用状态、false为禁用状态。
Type: Bool; Readable: true; Writable: false

#### let subjectName

```cangjie
public let subjectName: String
```
功能： 表示证书的使用者名称。
Type: String; Readable: true; Writable: false

#### let uri

```cangjie
public let uri: String
```
功能： 表示证书的唯一标识符。
Type: String; Readable: true; Writable: false


### CertInfo

class CertInfo
```cangjie
public class CertInfo {}
```
功能： 表示证书详细信息。

#### let cert

```cangjie
public let cert: Array<UInt8>
```
功能： 表示证书二进制数据。
Type: Array<UInt8>; Readable: true; Writable: false

#### let certAlias

```cangjie
public let certAlias: String
```
功能： 表示证书的别名。
Type: String; Readable: true; Writable: false

#### let fingerprintSha256

```cangjie
public let fingerprintSha256: String
```
功能： 表示证书的指纹值。
Type: String; Readable: true; Writable: false

#### let issuerName

```cangjie
public let issuerName: String
```
功能： 表示证书的颁发者名称。
Type: String; Readable: true; Writable: false

#### let notAfter

```cangjie
public let notAfter: String
```
功能： 表示证书有效期截止日期。
Type: String; Readable: true; Writable: false

#### let notBefore

```cangjie
public let notBefore: String
```
功能： 表示证书有效期起始日期。
Type: String; Readable: true; Writable: false

#### let serial

```cangjie
public let serial: String
```
功能： 表示证书的序列号。
Type: String; Readable: true; Writable: false

#### let state

```cangjie
public let state: Bool
```
功能： 表示证书的状态，true为启用状态、false为禁用状态。
Type: Bool; Readable: true; Writable: false

#### let subjectName

```cangjie
public let subjectName: String
```
功能： 表示证书的使用者名称。
Type: String; Readable: true; Writable: false

#### let uri

```cangjie
public let uri: String
```
功能： 表示证书的唯一标识符。
Type: String; Readable: true; Writable: false


### Credential

class Credential
```cangjie
public class Credential {}
```
功能： 表示凭据详细信息。

#### let alias

```cangjie
public let alias: String
```
功能： 表示凭据的别名。
Type: String; Readable: true; Writable: false

#### let certNum

```cangjie
public let certNum: Int32
```
功能： 表示凭据中包含的证书个数。
Type: Int32; Readable: true; Writable: false

#### let credentialData

```cangjie
public let credentialData: Array<UInt8>
```
功能： 表示凭据二进制数据。
Type: Array<UInt8>; Readable: true; Writable: false

#### let credentialType

```cangjie
public let credentialType: String
```
功能： 表示凭据的类型。
Type: String; Readable: true; Writable: false

#### let keyNum

```cangjie
public let keyNum: Int32
```
功能： 表示凭据中包含的密钥个数。
Type: Int32; Readable: true; Writable: false

#### let keyUri

```cangjie
public let keyUri: String
```
功能： 表示凭据的唯一标识符。
Type: String; Readable: true; Writable: false


### CredentialAbstract

class CredentialAbstract
```cangjie
public class CredentialAbstract {}
```
功能： 表示凭据的简要信息。

#### let alias

```cangjie
public let alias: String
```
功能： 表示凭据的别名。
Type: String; Readable: true; Writable: false

#### let credentialType

```cangjie
public let credentialType: String
```
功能： 表示凭据的类型。
Type: String; Readable: true; Writable: false

#### let keyUri

```cangjie
public let keyUri: String
```
功能： 表示凭据的唯一标识符。
Type: String; Readable: true; Writable: false


### ErrorCode

class ErrorCode
```cangjie
public class ErrorCode { public static const HAS_NO_PERMISSION: Int32 = 201 public static const PARAM_ERROR: Int32 = 401 public static const INNER_FAILURE: Int32 = 17500001 public static const NOT_FOUND: Int32 = 17500002 public static const INVALID_CERT_FORMAT: Int32 = 17500003 public static const MAX_CERT_COUNT_REACHED: Int32 = 17500004 public static const NO_AUTHORIZATION: Int32 = 17500005 public static const NOT_SYSTEM_APP: Int32 = 202 public static const ALIAS_LENGTH_REACHED_LIMIT: Int32 = 17500006 public static const DEVICE_ENTER_ADVSECMODE: Int32 = 17500007 public static const PASSWORD_IS_ERROR: Int32 = 17500008 }
```
功能： 表示调用证书管理相关API的错误码。



## Enums

### CmKeyDigest

enum CmKeyDigest
```cangjie
public enum CmKeyDigest <: Equatable<CmKeyDigest> & ToString { | CM_DIGEST_NONE | CM_DIGEST_MD5 | CM_DIGEST_SHA1 | CM_DIGEST_SHA224 | CM_DIGEST_SHA256 | CM_DIGEST_SHA384 | CM_DIGEST_SHA512 | ... }
```
功能： 表示签名、验签使用的摘要算法。

#### CM_DIGEST_MD5

```cangjie
CM_DIGEST_MD5
```
功能： MD5摘要算法。

#### CM_DIGEST_NONE

```cangjie
CM_DIGEST_NONE
```
功能： 不需要摘要算法，选用此项时，需要业务传入已经计算过摘要的数据进行签名、验签。

#### CM_DIGEST_SHA1

```cangjie
CM_DIGEST_SHA1
```
功能： SHA1摘要算法。

#### CM_DIGEST_SHA224

```cangjie
CM_DIGEST_SHA224
```
功能： SHA224摘要算法。

#### CM_DIGEST_SHA256

```cangjie
CM_DIGEST_SHA256
```
功能： SHA256摘要算法。

#### CM_DIGEST_SHA384

```cangjie
CM_DIGEST_SHA384
```
功能： SHA384摘要算法。

#### CM_DIGEST_SHA512

```cangjie
CM_DIGEST_SHA512
```
功能： SHA512摘要算法。


### CmKeyPadding

enum CmKeyPadding
```cangjie
public enum CmKeyPadding <: Equatable<CmKeyPadding> & ToString { | CM_PADDING_NONE | CM_PADDING_PSS | CM_PADDING_PKCS1_V1_5 | ... }
```
功能： 表示签名、验签使用的填充方式。

#### CM_PADDING_NONE

```cangjie
CM_PADDING_NONE
```
功能： 无填充。

#### CM_PADDING_PKCS1_V1_5

```cangjie
CM_PADDING_PKCS1_V1_5
```
功能： PSS方式填充。

#### CM_PADDING_PSS

```cangjie
CM_PADDING_PSS
```
功能： PKCS1_V1_5方式填充。


### CmKeyPurpose

enum CmKeyPurpose
```cangjie
public enum CmKeyPurpose <: Equatable<CmKeyPurpose> & ToString { | CM_KEY_PURPOSE_SIGN | CM_KEY_PURPOSE_VERIFY | ... }
```
功能： 表示密钥使用目的的枚举，用于签名、验签。

#### CM_KEY_PURPOSE_SIGN

```cangjie
CM_KEY_PURPOSE_SIGN
```
功能： 签名。

#### CM_KEY_PURPOSE_VERIFY

```cangjie
CM_KEY_PURPOSE_VERIFY
```
功能： 验签。



