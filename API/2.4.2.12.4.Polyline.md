# Polyline

折线绘制组件。

## Component Constructors

### public init(width!: Length, height!: Length)

```cangjie
public init(width!: Length, height!: Length)
```
功能： 在宽度为width、高度为height的填充区域内绘制一条折线。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 命名参数。 填充区域宽度，取值范围≥0。 默认单位：vp。 异常值按照取0处理。 | width | - |
| Length | 是 | 命名参数。 填充区域宽度，取值范围≥0。 默认单位：vp。 异常值按照取0处理。 | height | - |

### public init(width!: Int64, height!: Int64)

```cangjie
public init(width!: Int64, height!: Int64)
```
功能： 在宽度为width、高度为height的填充区域内绘制一条折线。需要设置 width 或 height 属性参数不为0才能显示出来。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int64 | 是 | 命名参数。 填充区域宽度，取值范围≥0。 单位：vp。 异常值按照取0处理。 | width | - |
| Int64 | 是 | 命名参数。 填充区域宽度，取值范围≥0。 单位：vp。 异常值按照取0处理。 | height | - |

### public init(width!: Float64, height!: Float64)

```cangjie
public init(width!: Float64, height!: Float64)
```
功能： 在宽度为width、高度为height的填充区域内绘制一条折线。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Float64 | 是 | 命名参数。 填充区域宽度，取值范围≥0。 单位：vp。 异常值按照取0处理。 | width | - |
| Float64 | 是 | 命名参数。 填充区域宽度，取值范围≥0。 单位：vp。 异常值按照取0处理。 | height | - |

### public init()

```cangjie
public init()
```
功能： 在宽度为0、高度为0的填充区域内绘制一条折线。需要设置 width 或 height 属性参数不为0才能显示出来。


## Functions

### public func points(pointList: Array<(Int64, Int64)>): This

```cangjie
public func points(pointList: Array<(Int64, Int64)>): This
```
功能： 设置折线的顶点坐标列表。异常值按照初始值处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array<(Int64,Int64)> | 是 | 折线的顶点坐标列表。 初始值：[]。 默认单位：vp。 | pointList | - |

### public func points(pointList: Array<(Float64, Float64)>): This

```cangjie
public func points(pointList: Array<(Float64, Float64)>): This
```
功能： 设置折线的顶点坐标列表。异常值按照初始值处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array<(Float64,Float64)> | 是 | 折线的顶点坐标列表。 初始值：[]。 默认单位：vp。 | pointList | - |


## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 func build() {
 Column(10) {
 // 在 100 * 100 的矩形框中绘制一个折线，起点(0, 0)，经过(50, 100)，终点(100, 100)
 Polyline(width: 100, height: 100)
 .points([(0, 0), (50, 100), (100, 100)])
 .fill(Color.GREEN)
 // 在 100 * 100 的矩形框中绘制一个折线，起点(0, 0)，经过(0, 100)和(100, 100)，终点(100, 200)
 Polyline()
 .width(100)
 .height(100)
 .points([(0, 0), (0, 100), (100, 100), (100, 200)])
 .fillOpacity(0)
 .strokeWidth(5)
 .stroke(Color.BLUE)
 }.width(100.percent).margin(10)
 }
}
```

