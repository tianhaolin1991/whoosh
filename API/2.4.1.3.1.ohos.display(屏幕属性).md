# ohos.display(屏幕属性)

屏幕属性提供管理显示设备的一些基础能力，包括获取默认显示设备的信息，获取所有显示设备的信息以及监听显示设备的插拔行为。

```cangjie
import ohos.display.*
```
## Functions

### public func getAllDisplayPhysicalResolution(): Array<DisplayPhysicalResolution>

```cangjie
public func getAllDisplayPhysicalResolution(): Array<DisplayPhysicalResolution>
```
功能： 获取当前折叠设备的显示模式以及对应的物理屏幕分辨率信息对象。

**Returns:**

| type | description |
| --- | --- |
| Array< DisplayPhysicalResolution > | 返回当前所有的DisplayPhysicalResolution对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码如下表。

| code | description |
| --- | --- |
| 120 | ERROR: Failed to get all display physical resolution. |


**Example:**

```cangjie
import ohos.display.*
func getAllDisplayPhysicalResolutionExample() {
 try {
 let displayPhysicalResolutions: Array<DisplayPhysicalResolution> = getAllDisplayPhysicalResolution()
 AppLog.info("displayPhysicalResolutions size: ${displayPhysicalResolutions.size}")
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
}
```

### public func getAllDisplays(): Array<Display>

```cangjie
public func getAllDisplays(): Array<Display>
```
功能： 获取当前所有的Display对象。

**Returns:**

| type | description |
| --- | --- |
| Array< Display > | 返回当前所有的display对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 屏幕错误码 。

| code | description |
| --- | --- |
| 1400003 | ERROR: Failed to get all displays. |


**Example:**

```cangjie
import ohos.display.*
func getAllDisplaysExample() {
 try {
 let displayClass: Array<Display> = getAllDisplays()
 if (displayClass.size > 0) {
 println(displayClass[0].name)
 }
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
}
```

### public func getCurrentFoldCreaseRegion(): FoldCreaseRegion

```cangjie
public func getCurrentFoldCreaseRegion(): FoldCreaseRegion
```
功能： 在当前显示模式下获取折叠折痕区域。

**Returns:**

| type | description |
| --- | --- |
| FoldCreaseRegion | FoldCreaseRegion对象，返回设备在当前显示模式下的折叠折痕区域。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 屏幕错误码 。

| code | description |
| --- | --- |
| 1400003 | ERROR: Failed to get current fold crease region. |


**Example:**

```cangjie
import ohos.display.*
func getCurrentFoldCreaseRegionExample() {
 try {
 let region = getCurrentFoldCreaseRegion()
 println(region.displayId)
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
}
```

### public func getDefaultDisplaySync(): Display

```cangjie
public func getDefaultDisplaySync(): Display
```
功能： 获取当前默认的Display对象。

**Returns:**

| type | description |
| --- | --- |
| Display | 返回默认的Display对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 屏幕错误码 。

| code | description |
| --- | --- |
| 1400001 | ERROR: Failed to get default display. |
| 1400003 | ERROR: Failed to get default display. |


**Example:**

```cangjie
import ohos.display.*
func getDefaultDisplaySyncExample() {
 try {
 let displayClass: Display = getDefaultDisplaySync()
 println(displayClass.name)
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
}
```

### public func getFoldDisplayMode(): FoldDisplayMode

```cangjie
public func getFoldDisplayMode(): FoldDisplayMode
```
功能： 获取可折叠设备的显示模式。

**Returns:**

| type | description |
| --- | --- |
| FoldDisplayMode | FoldDisplayMode对象，返回当前可折叠设备的显示模式。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 屏幕错误码 。

| code | description |
| --- | --- |
| 1400003 | This display manager service works abnormally. |


**Example:**

```cangjie
import ohos.display.*
func getFoldDisplayModeExample() {
 try {
 let mode = getFoldDisplayMode()
 match (mode) {
 case FoldDisplayMode.FOLD_DISPLAY_MODE_UNKNOWN => AppLog.info("Unkown mode.")
 case FoldDisplayMode.FOLD_DISPLAY_MODE_FULL => AppLog.info("Full mode.")
 case FoldDisplayMode.FOLD_DISPLAY_MODE_MAIN => AppLog.info("Main mode.")
 case FoldDisplayMode.FOLD_DISPLAY_MODE_SUB => AppLog.info("Sub mode.")
 case FoldDisplayMode.FOLD_DISPLAY_MODE_COORDINATION => AppLog.info("Coordination mode.")
 case _ => throw Exception("can not get display mode.")
 }
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
}
```

### public func getFoldStatus(): FoldStatus

```cangjie
public func getFoldStatus(): FoldStatus
```
功能： 获取可折叠设备的当前折叠状态。

**Returns:**

| type | description |
| --- | --- |
| FoldStatus | FoldStatus对象，返回当前可折叠设备的折叠状态。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 屏幕错误码 。

| code | description |
| --- | --- |
| 1400003 | This display manager service works abnormally. |


**Example:**

```cangjie
import ohos.display.*
func getFoldStatusExample() {
 try {
 let status = getFoldStatus()
 match (status) {
 case FoldStatus.FOLD_STATUS_UNKNOWN => AppLog.info("Unkown status.")
 case FoldStatus.FOLD_STATUS_EXPANDED => AppLog.info("Expanded.")
 case FoldStatus.FOLD_STATUS_FOLDED => AppLog.info("Folded.")
 case FoldStatus.FOLD_STATUS_HALF_FOLDED => AppLog.info("Half folded.")
 case _ => throw Exception("can not get fold status.")
 }
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
}
```

### public func hasPrivateWindow(displayId: UInt32): Bool

```cangjie
public func hasPrivateWindow(displayId: UInt32): Bool
```
功能： 查询指定Display对象上是否有可见的隐私窗口。可通过 setWindowPrivacyMode() 接口设置隐私窗口。隐私窗口内容将无法被截屏或录屏。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| UInt32 | 是 | 显示设备的id，该参数仅支持整数输入。 | id | - |

**Returns:**

| type | description |
| --- | --- |
| Bool | 查询的Display对象上是否有可见的隐私窗口。 true表示此Display对象上有可见的隐私窗口，false表示此Display对象上没有可见的隐私窗口。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 屏幕错误码 。

| code | description |
| --- | --- |
| 1400003 | This display manager service works abnormally. |


**Example:**

```cangjie
import ohos.display.*
func hasPrivateWindowExample() {
 try {
 let displayClass = getDefaultDisplaySync()
 var ret: Bool = true
 try {
 ret = hasPrivateWindow(displayClass.id)
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
 if (ret) {
 AppLog.info("There has privateWindow.")
 } else {
 AppLog.info("There has no privateWindow.")
 }
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
}
```

### public func isCaptured(): Bool

```cangjie
public func isCaptured(): Bool
```
功能： 检查设备是否正在截屏、投屏、录屏。

**Returns:**

| type | description |
| --- | --- |
| Bool | boolean值，返回当前设备是否有截屏、投屏或者录屏。true表示有截屏、投屏、录屏，否则返回false。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 屏幕错误码 。

| code | description |
| --- | --- |
| 1400003 | This display manager service works abnormally. |


**Example:**

```cangjie
import ohos.display.*
func isCapturedExample() {
 try {
 var ret: Bool = false
 try {
 ret = isCaptured()
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
 if (ret) {
 AppLog.info("The device is foldable.")
 } else {
 AppLog.info("The device is not foldable.")
 }
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
}
```

### public func isFoldable(): Bool

```cangjie
public func isFoldable(): Bool
```
功能： 检查设备是否可折叠。

**Returns:**

| type | description |
| --- | --- |
| Bool | Bool对象，返回当前设备是否可折叠的结果。true表示可折叠，false表示不可折叠。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 屏幕错误码 。

| code | description |
| --- | --- |
| 1400003 | This display manager service works abnormally. |


**Example:**

```cangjie
import ohos.display.*
func isFoldableExample() {
 try {
 let displayClass = getDefaultDisplaySync()
 var ret: Bool = false
 try {
 ret = isFoldable()
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
 if (ret) {
 AppLog.info("The device is foldable.")
 } else {
 AppLog.info("The device is not foldable.")
 }
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
}
```

### public func off(`type`: ListenerType): Unit

```cangjie
public func off(`type`: ListenerType): Unit
```
功能： 关闭显示设备变化的监听。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ListenerType | 是 | 监听事件类型。 | `type` | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 401 | Parameter error. Possible causes: 1.Mandatory parameters are left unspecified. 2.Incorrect parameter types. |


**Example:**

```cangjie
import ohos.display.*
var temp: Unit = off(LISTNER_TYPE_CHANGE)
```

### public func off(`type`: ListenerType, callback: Callback1Argument<UInt64>): Unit

```cangjie
public func off(`type`: ListenerType, callback: Callback1Argument<UInt64>): Unit
```
功能： 关闭显示设备变化的监听。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ListenerType | 是 | 监听事件。 type为"LISTNER_TYPE_CHANGE"，表示改变显示设备事件。例如：显示器方向改变。 | `type` | - |
| Callback1Argument<UInt64> | 是 | 需要取消注册的回调函数。 | callback | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 202 | Parameter error: type is not supported. |
| 401 | Parameter error: type is not supported. |


**Example:**

```cangjie
import ohos.display.*
class TestCallback <: Callback1Argument<UInt64> {
 public init() {}
 public open func invoke(value: UInt64): Unit {
 AppLog.info("Display change, ID: ${value}")
 }
}
let testCallback = TestCallback()
var temp: Unit = off(LISTNER_TYPE_CHANGE, testCallback)
```

### public func off(`type`: ListenerType, callback: Callback1Argument<FoldStatus>): Unit

```cangjie
public func off(`type`: ListenerType, callback: Callback1Argument<FoldStatus>): Unit
```
功能： 关闭折叠设备折叠状态变化的监听。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 监听事件。固定为'LISTNER_TYPE_FOLD_STATUS_CHANGE'，表示折叠设备折叠状态发生变化。 | `type` | - |
| Callback1Argument< FoldStatus > | 是 | 回调函数。表示折叠设备折叠状态。 | callback | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 202 | Parameter error: type is not supported. |
| 401 | Parameter error: type is not supported. |


**Example:**

```cangjie
import ohos.display.*
class TestCallback <: Callback1Argument<FoldStatus> {
 public init() {}
 public open func invoke(value: FoldStatus): Unit {
 AppLog.info(
 "Display fold status changed, current fold status: " + match (value) {
 case FOLD_STATUS_UNKNOWN => "FOLD_STATUS_UNKNOWN"
 case FOLD_STATUS_EXPANDED => "FOLD_STATUS_EXPANDED"
 case FOLD_STATUS_FOLDED => "FOLD_STATUS_FOLDED"
 case FOLD_STATUS_HALF_FOLDED => "FOLD_STATUS_HALF_FOLDED"
 case _ => "Failed to get fold status."
 })
 }
}
let testCallback = TestCallback()
var temp: Unit = on(LISTNER_TYPE_FOLD_STATUS_CHANGE, testCallback)
```

### public func off(`type`: ListenerType, callback: Callback1Argument<Array<Float32>>): Unit

```cangjie
public func off(`type`: ListenerType, callback: Callback1Argument<Array<Float32>>): Unit
```
功能： 关闭折叠设备折叠角度变化的监听。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 监听事件。监听事件，固定为'LISTNER_TYPE_CAPTURE_STATUS_CHANGE'，表示折叠设备折叠角度发生变化。 | `type` | - |
| Callback1Argument<Array<Float32>> | 否 | 需要取消注册的回调函数。 | callback | 注册折叠角度变化监听的所有回调函数 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 202 | Parameter error: type is not supported. |
| 401 | Parameter error: type is not supported. |


**Example:**

```cangjie
import ohos.display.*
class TestCallback <: Callback1Argument<Array<Float32>> {
 public init() {}
 public open func invoke(value: Array<Float32>): Unit {
 AppLog.info("Listening fold angles length: " + value
 .size
 .toString())
 }
}
let testCallback = TestCallback()
var temp: Unit = off(LISTNER_TYPE_FOLD_ANGLE_CHANGE, testCallback)
```

### public func off(`type`: ListenerType, callback: Callback1Argument<Bool>): Unit

```cangjie
public func off(`type`: ListenerType, callback: Callback1Argument<Bool>): Unit
```
功能： 关闭屏幕截屏、投屏、录屏状态变化的监听。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 监听事件。固定为'LISTNER_TYPE_CAPTURE_STATUS_CHANGE'，表示设备截屏、投屏或者录屏状态发生变化。 | `type` | - |
| Callback1Argument<Bool> | 是 | 需要取消注册的回调函数。 | callback | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 屏幕错误码 。

| code | description |
| --- | --- |
| 202 | Parameter error: type is not supported. |
| 401 | Parameter error: type is not supported. |


**Example:**

```cangjie
import ohos.display.*
class TestCallback <: Callback1Argument<Bool> {
 public init() {}
 public open func invoke(value: Bool): Unit {
 AppLog.info("Listening fold capture status: " + value.toString())
 }
}
let testCallback = TestCallback()
var temp: Unit = off(LISTNER_TYPE_CAPTURE_STATUS_CHANGE, testCallback)
```

### public func off(`type`: ListenerType, callback: Callback1Argument<FoldDisplayMode>): Unit

```cangjie
public func off(`type`: ListenerType, callback: Callback1Argument<FoldDisplayMode>): Unit
```
功能： 关闭折叠设备屏幕显示模式变化的监听。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 监听事件。固定为'LISTNER_TYPE_FOLD_DISPLAY_MODE_CHANGE'，表示折叠设备折叠状态发生变化。 | `type` | - |
| Callback1Argument< FoldDisplayMode > | 是 | 需要取消注册的回调函数。 | callback | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 屏幕错误码 。

| code | description |
| --- | --- |
| 202 | Parameter error: type is not supported. |
| 401 | Parameter error: type is not supported. |


**Example:**

```cangjie
import ohos.display.*
class TestCallback <: Callback1Argument<FoldDisplayMode> {
 public init() {}
 public open func invoke(value: FoldDisplayMode): Unit {
 AppLog.info(
 "Display fold status changed, current fold status: " + match (value) {
 case FOLD_DISPLAY_MODE_UNKNOWN => "FOLD_DISPLAY_MODE_UNKNOWN"
 case FOLD_DISPLAY_MODE_FULL => "FOLD_DISPLAY_MODE_FULL"
 case FOLD_DISPLAY_MODE_MAIN => "FOLD_DISPLAY_MODE_MAIN"
 case FOLD_DISPLAY_MODE_SUB => "FOLD_DISPLAY_MODE_SUB"
 case FOLD_DISPLAY_MODE_COORDINATION => "FOLD_DISPLAY_MODE_COORDINATION"
 case _ => "Failed to get fold display mode."
 })
 }
}
let testCallback = TestCallback()
var temp: Unit = off(LISTNER_TYPE_FOLD_DISPLAY_MODE_CHANGE, testCallback)
```

### public func on(`type`: ListenerType, callback: Callback1Argument<UInt64>): Unit

```cangjie
public func on(`type`: ListenerType, callback: Callback1Argument<UInt64>): Unit
```
功能： 开启显示设备变化的监听。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ListenerType | 是 | 监听事件。 type为"LISTNER_TYPE_CHANGE"，表示改变显示设备事件。 | `type` | - |
| Callback1Argument<UInt64> | 是 | 回调函数。返回监听到的显示设备的id，该参数应为整数。 | callback | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 202 | Parameter error:type is not supported. |
| 401 | Parameter error:type is not supported. |


**Example:**

```cangjie
import ohos.display.*
class TestCallback <: Callback1Argument<UInt64> {
 public init() {}
 public open func invoke(value: UInt64): Unit {
 AppLog.info("Display change, ID: ${value}")
 }
}
let testCallback = TestCallback()
var temp: Unit = on(LISTNER_TYPE_CHANGE, testCallback)
```

### public func on(`type`: ListenerType, callback: Callback1Argument<FoldStatus>): Unit

```cangjie
public func on(`type`: ListenerType, callback: Callback1Argument<FoldStatus>): Unit
```
功能： 开启折叠设备折叠状态变化的监听。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 监听事件。固定为'LISTNER_TYPE_FOLD_STATUS_CHANGE'，表示折叠设备折叠状态发生变化。 | `type` | - |
| Callback1Argument< FoldStatus > | 是 | 回调函数。表示折叠设备折叠状态。 | callback | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 202 | Parameter error: type is not supported. |
| 401 | Parameter error: type is not supported. |


**Example:**

```cangjie
import ohos.display.*
class TestCallback <: Callback1Argument<FoldStatus> {
 public init(){}
 public open func invoke(value: FoldStatus): Unit {
 AppLog.info("Display fold status changed, current fold status: " + match(value) {
 case FOLD_STATUS_UNKNOWN => "FOLD_STATUS_UNKNOWN"
 case FOLD_STATUS_EXPANDED => "FOLD_STATUS_EXPANDED"
 case FOLD_STATUS_FOLDED => "FOLD_STATUS_FOLDED"
 case FOLD_STATUS_HALF_FOLDED => "FOLD_STATUS_HALF_FOLDED"
 case _ => "Failed to get fold status."
 })
 }
}
let testCallback = TestCallback()
var temp: Unit = on(LISTNER_TYPE_FOLD_STATUS_CHANGE, testCallback)
```

### public func on(`type`: ListenerType, callback: Callback1Argument<Array<Float32>>): Unit

```cangjie
public func on(`type`: ListenerType, callback: Callback1Argument<Array<Float32>>): Unit
```
功能： 开启折叠设备折叠角度变化的监听。如果是双折轴设备，则有两个角度值；在充电口朝下的状态下，从右到左分别是折轴一和折轴二。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 监听事件。监听事件，固定为'LISTNER_TYPE_FOLD_ANGLE_CHANGE'，表示折叠设备折叠角度发生变化。 | `type` | - |
| Callback1Argument<Array<Float32>> | 是 | 回调函数。表示折叠设备屏幕折叠角度值（0度~180度）。如果是双折轴设备，则数组返回两个角度值，第一个值是折轴一的折叠角度值，第二个值是折轴二的折叠角度值。 | callback | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 202 | Parameter error: type is not supported. |
| 401 | Parameter error: type is not supported. |


**Example:**

```cangjie
import ohos.display.*
class TestCallback <: Callback1Argument<Array<Float32>> {
 public init(){}
 public open func invoke(value: Array<Float32>): Unit {
 AppLog.info("Listening fold angles length: " + value.size.toString())
 }
}
let testCallback = TestCallback()
var temp: Unit = on(LISTNER_TYPE_FOLD_ANGLE_CHANGE, testCallback)
```

### public func on(`type`: ListenerType, callback: Callback1Argument<Bool>): Unit

```cangjie
public func on(`type`: ListenerType, callback: Callback1Argument<Bool>): Unit
```
功能： 开启屏幕截屏、投屏、录屏状态变化的监听。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 监听事件。监听事件，固定为'LISTNER_TYPE_CAPTURE_STATUS_CHANGE'表示设备截屏、投屏或者录屏状态发生变化。 | `type` | - |
| Callback1Argument<Bool> | 是 | 回调函数。表示设备截屏、投屏、录屏状态发生变化。true表示设备开始截屏、投屏或者录屏，false表示结束截屏、投屏、录屏。 | callback | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 202 | Parameter error: type is not supported. |
| 401 | Parameter error: type is not supported. |


**Example:**

```cangjie
import ohos.display.*
class TestCallback <: Callback1Argument<Bool> {
 public init() {}
 public open func invoke(value: Bool): Unit {
 AppLog.info("Listening fold capture status: " + value.toString())
 }
}
let testCallback = TestCallback()
var temp: Unit = on(LISTNER_TYPE_CAPTURE_STATUS_CHANGE, testCallback)
```

### public func on(`type`: ListenerType, callback: Callback1Argument<FoldDisplayMode>): Unit

```cangjie
public func on(`type`: ListenerType, callback: Callback1Argument<FoldDisplayMode>): Unit
```
功能： 开启折叠设备屏幕显示模式变化的监听。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 监听事件。固定为'LISTNER_TYPE_FOLD_DISPLAY_MODE_CHANGE'，表示折叠设备折叠状态发生变化。 | `type` | - |
| Callback1Argument< FoldDisplayMode > | 是 | 回调函数。表示折叠设备屏幕显示模式。 | callback | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 202 | Parameter error: type is not supported. |
| 401 | Parameter error: type is not supported. |


**Example:**

```cangjie
import ohos.display.*
class TestCallback <: Callback1Argument<FoldDisplayMode> {
 public init() {}
 public open func invoke(value: FoldDisplayMode): Unit {
 AppLog.info(
 "Display fold status changed, current fold status: " + match (value) {
 case FOLD_DISPLAY_MODE_UNKNOWN => "FOLD_DISPLAY_MODE_UNKNOWN"
 case FOLD_DISPLAY_MODE_FULL => "FOLD_DISPLAY_MODE_FULL"
 case FOLD_DISPLAY_MODE_MAIN => "FOLD_DISPLAY_MODE_MAIN"
 case FOLD_DISPLAY_MODE_SUB => "FOLD_DISPLAY_MODE_SUB"
 case FOLD_DISPLAY_MODE_COORDINATION => "FOLD_DISPLAY_MODE_COORDINATION"
 case _ => "Failed to get fold display mode."
 })
 }
}
let testCallback = TestCallback()
var temp: Unit = on(LISTNER_TYPE_FOLD_DISPLAY_MODE_CHANGE, testCallback)
```

### public func setFoldDisplayMode(mode: FoldDisplayMode): Unit

```cangjie
public func setFoldDisplayMode(mode: FoldDisplayMode): Unit
```
功能： 设置可折叠设备的显示模式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| FoldDisplayMode | 是 | 可折叠设备的显示模式。 | mode | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 屏幕错误码 。

| code | description |
| --- | --- |
| 1400003 | This display manager service works abnormally. |


**Example:**

```cangjie
import ohos.display.*
func setFoldDisplayModeExample() {
 try {
 let mode = FoldDisplayMode.FOLD_DISPLAY_MODE_FULL
 setFoldDisplayMode(mode)
 } catch (exception: Exception) {
 AppLog.error(exception.toString())
 }
}
```


## Classes

### Display

class Display
```cangjie
public class Display {}
```
功能： 设置屏幕实例。描述Display对象的属性和方法。

#### prop alive

```cangjie
public prop alive: Bool
```
功能： 设置显示设备是否启用。true表示设备启用，false表示设备未启用。
Type: Bool; Readable: true; Writable: false

#### prop availableHeight

```cangjie
public prop availableHeight: UInt32
```
功能： 2in1设备上屏幕的可用区域高度。
Type: UInt32; Readable: true; Writable: false

#### prop availableWidth

```cangjie
public prop availableWidth: UInt32
```
功能： 设置2in1设备上屏幕的可用区域宽度。
Type: UInt32; Readable: true; Writable: false

#### prop colorSpaces

```cangjie
public prop colorSpaces: Array<ColorSpace>
```
功能： 设置显示设备支持的所有色域类型。
Type: Array< color_manager.ColorSpace >; Readable: true; Writable: false

#### prop densityDPI

```cangjie
public prop densityDPI: Float32
```
功能： 设置显示设备屏幕的物理像素密度，表示每英寸上的像素点数。
Type: Float32; Readable: true; Writable: false

#### prop densityPixels

```cangjie
public prop densityPixels: Float32
```
功能： 设置显示设备的逻辑密度，是像素单位无关的缩放系数。
Type: Float32; Readable: true; Writable: false

#### prop hdrFormats

```cangjie
public prop hdrFormats: Array<HDRFormat>
```
功能： 设置显示设备支持的所有HDR格式。
Type: Array< HDRFormat >; Readable: true; Writable: false

#### prop height

```cangjie
public prop height: Int32
```
功能： 显示设备的屏幕高度。
Type: Int32; Readable: true; Writable: false

#### prop id

```cangjie
public prop id: UInt32
```
功能： 设置显示设备的id号，该参数应为大于等于0的整数。
Type: UInt32; Readable: true; Writable: false

#### prop name

```cangjie
public prop name: String
```
功能： 设置显示设备的名称。
Type: String; Readable: true; Writable: false

#### prop orientation

```cangjie
public prop orientation: Orientation
```
功能： 设置屏幕当前显示的方向。
Type: Orientation; Readable: true; Writable: false

#### prop refreshRate

```cangjie
public prop refreshRate: UInt32
```
功能： 设置显示设备的刷新率。
Type: UInt32; Readable: true; Writable: false

#### prop rotation

```cangjie
public prop rotation: UInt32
```
功能： 设置显示设备的屏幕顺时针旋转角度。
Type: UInt32; Readable: true; Writable: false

#### prop scaledDensity

```cangjie
public prop scaledDensity: Float32
```
功能： 显示设备的显示字体的缩放因子。该参数为浮点数，通常与densityPixels相同。
Type: Float32; Readable: true; Writable: false

#### prop state

```cangjie
public prop state: DisplayState
```
功能： 设置显示设备的状态。
Type: DisplayState; Readable: true; Writable: false

#### prop width

```cangjie
public prop width: Int32
```
功能： 设置显示设备的屏幕宽度。
Type: Int32; Readable: true; Writable: false

#### prop xDPI

```cangjie
public prop xDPI: Float32
```
功能： 设置x方向中每英寸屏幕的确切物理像素值。
Type: Float32; Readable: true; Writable: false

#### prop yDPI

```cangjie
public prop yDPI: Float32
```
功能： 设置y方向中每英寸屏幕的确切物理像素值。
Type: Float32; Readable: true; Writable: false


### CutoutInfo

class CutoutInfo
```cangjie
public class CutoutInfo { public CutoutInfo( public let boundingRects: Array<Rect>, public let waterfallDisplayAreaRects: WaterfallDisplayAreaRects ) }
```
功能： 挖孔屏、刘海屏、瀑布屏等不可用屏幕区域信息。

#### let boudingRects

```cangjie
public let boundingRects: Array<Rect>
```
功能： 设置挖孔、刘海等区域的边界矩形。
Type: Array< Rect >; Readable: true; Writable: false

#### let waterfallDisplayAreaRects

```cangjie
public let waterfallDisplayAreaRects: WaterfallDisplayAreaRects
```
功能： 设置瀑布屏曲面部分显示区域。
Type: WaterfallDisplayAreaRects; Readable: true; Writable: false


### Rect

class Rect
```cangjie
public class Rect { public Rect( public var left: Int32, public var top: Int32, public var width: UInt32, public var height: UInt32 ) }
```
功能： 矩形区域信息。

#### var height

```cangjie
public var height: UInt32
```
功能： 设置矩形区域的高度。
Type: UInt32; Readable: true; Writable: true

#### var left

```cangjie
public var left: Int32
```
功能： 设置矩形区域的左边界。
Type: Int32; Readable: true; Writable: true

#### var top

```cangjie
public var top: Int32
```
功能： 设置矩形区域的上边界。
Type: Int32; Readable: true; Writable: true

#### var width

```cangjie
public var width: UInt32
```
功能： 设置矩形区域的宽度。
Type: UInt32; Readable: true; Writable: true


### DisplayPhysicalResolution

class DisplayPhysicalResolution
```cangjie
public class DisplayPhysicalResolution { public DisplayPhysicalResolution( public let foldDisplayMode: FoldDisplayMode, public let physicalWidth: UInt32, public let physicalHeight: UInt32 ) }
```
功能： 折叠设备的显示模式以及对应的物理屏幕分辨率信息。

#### let foldDisplayMode

```cangjie
public let foldDisplayMode: FoldDisplayMode
```
功能： 设置折叠设备的显示模式。
Type: FoldDisplayMode; Readable: true; Writable: false

#### let physicalWidth

```cangjie
public let physicalWidth: UInt32
```
功能： 设置折叠设备的宽度。
Type: UInt32; Readable: true; Writable: false

#### let physicalHeight

```cangjie
public let physicalHeight: UInt32
```
功能： 设置折叠设备的高度。
Type: UInt32; Readable: true; Writable: false


### FoldCreaseRegion

class FoldCreaseRegion
```cangjie
public class FoldCreaseRegion { public FoldCreaseRegion( public let displayId: UInt32, public let creaseRects: Array<Rect> ) }
```
功能： 折叠折痕区域。

#### let creaseRects

```cangjie
public let creaseRects: Array<Rect>
```
功能： 设置折痕区域。
Type: Array< Rect >; Readable: true; Writable: false

#### let displayId

```cangjie
public let displayId: UInt32
```
功能： 设置显示器ID，用于识别折痕所在的屏幕。
Type: UInt32; Readable: true; Writable: false


### WaterfallDisplayAreaRects

class WaterfallDisplayAreaRects
```cangjie
public class WaterfallDisplayAreaRects { public WaterfallDisplayAreaRects( public let left: Rect, public let top: Rect, public let right: Rect, public let bottom: Rect ) }
```
功能： 瀑布屏曲面部分显示区域信息。

#### let bottom

```cangjie
public let bottom: Rect
```
功能： 设置瀑布曲面区域底部矩形区域。
Type: Rect; Readable: true; Writable: false

#### let left

```cangjie
public let left: Rect
```
功能： 设置瀑布曲面区域的左侧矩形区域。
Type: Rect; Readable: true; Writable: false

#### let right

```cangjie
public let right: Rect
```
功能： 设置瀑布曲面区域右侧矩形区域。
Type: Rect; Readable: true; Writable: false

#### let top

```cangjie
public let top: Rect
```
功能： 设置瀑布曲面区域的顶部矩形区域。
Type: Rect; Readable: true; Writable: false



## Enums

### Orientation

enum Orientation
```cangjie
public enum Orientation { | PORTRAIT | LANDSCAPE | PORTRAIT_INVERTED | LANDSCAPE_INVERTED | ... }
```
功能： 显示设备当前的显示方向类型。

#### LANDSCAPE

```cangjie
LANDSCAPE
```
功能： 表示设备当前以横屏方式显示。

#### LANDSCAPE_INVERTED

```cangjie
LANDSCAPE_INVERTED
```
功能： 表示设备当前以反向横屏方式显示。

#### PORTRAIT

```cangjie
PORTRAIT
```
功能： 表示设备当前以竖屏方式显示。

#### PORTRAIT_INVERTED

```cangjie
PORTRAIT_INVERTED
```
功能： 表示设备当前以反向竖屏方式显示。


### DisplayState

enum DisplayState
```cangjie
public enum DisplayState { | STATE_UNKNOWN | STATE_OFF | STATE_ON | STATE_DOZE | STATE_DOZE_SUSPEND | STATE_VR | STATE_ON_SUSPEND | ... }
```
功能： 显示设备的状态类型。

#### STATE_DOZE

```cangjie
STATE_DOZE
```
功能： 表示显示设备为低电耗模式。

#### STATE_DOZE_SUSPEND

```cangjie
STATE_DOZE_SUSPEND
```
功能： 表示显示设备为睡眠模式，CPU为挂起状态。

#### STATE_OFF

```cangjie
STATE_OFF
```
功能： 表示显示设备状态为关闭。

#### STATE_ON

```cangjie
STATE_ON
```
功能： 表示显示设备状态为开启。

#### STATE_ON_SUSPEND

```cangjie
STATE_ON_SUSPEND
```
功能： 表示显示设备为开启状态，CPU为挂起状态。

#### STATE_UNKNOWN

```cangjie
STATE_UNKNOWN
```
功能： 表示显示设备状态未知。

#### STATE_VR

```cangjie
STATE_VR
```
功能： 表示显示设备为VR模式。


### FoldDisplayMode

enum FoldDisplayMode
```cangjie
public enum FoldDisplayMode { | FOLD_DISPLAY_MODE_UNKNOWN | FOLD_DISPLAY_MODE_FULL | FOLD_DISPLAY_MODE_MAIN | FOLD_DISPLAY_MODE_SUB | FOLD_DISPLAY_MODE_COORDINATION | ... }
```
功能： 可折叠设备的显示模式类型。

#### FOLD_DISPLAY_MODE_COORDINATION

```cangjie
FOLD_DISPLAY_MODE_COORDINATION
```
功能： 表示设备当前双屏协同显示。

#### FOLD_DISPLAY_MODE_FULL

```cangjie
FOLD_DISPLAY_MODE_FULL
```
功能： 表示设备当前全屏显示。

#### FOLD_DISPLAY_MODE_MAIN

```cangjie
FOLD_DISPLAY_MODE_MAIN
```
功能： 表示设备当前主屏幕显示。

#### FOLD_DISPLAY_MODE_SUB

```cangjie
FOLD_DISPLAY_MODE_SUB
```
功能： 表示设备当前子屏幕显示。

#### FOLD_DISPLAY_MODE_UNKNOWN

```cangjie
FOLD_DISPLAY_MODE_UNKNOWN
```
功能： 表示设备当前折叠显示模式未知。


### FoldStatus

enum FoldStatus
```cangjie
public enum FoldStatus { | FOLD_STATUS_UNKNOWN | FOLD_STATUS_EXPANDED | FOLD_STATUS_FOLDED | FOLD_STATUS_HALF_FOLDED | ... }
```
功能： 当前可折叠设备的折叠状态类型。

#### FOLD_STATUS_EXPANDED

```cangjie
FOLD_STATUS_EXPANDED
```
功能： 表示设备当前折叠状态为完全展开。如果是双折轴设备，则表示折轴一折叠状态为完全展开，折轴二折叠状态为折叠。

#### FOLD_STATUS_FOLDED

```cangjie
FOLD_STATUS_FOLDED
```
功能： 表示设备当前折叠状态为折叠。如果是双折轴设备，则表示折轴一折叠状态为折叠，折轴二折叠状态为折叠。

#### FOLD_STATUS_HALF_FOLDED

```cangjie
FOLD_STATUS_HALF_FOLDED
```
功能： 表示设备当前折叠状态为半折叠。半折叠指完全展开和折叠之间的状态。如果是双折轴设备，则表示折轴一折叠状态为半折叠，折轴二折叠状态为折叠。

#### FOLD_STATUS_UNKNOWN

```cangjie
FOLD_STATUS_UNKNOWN
```
功能： 表示设备当前折叠状态未知。


### HDRFormat

enum HDRFormat
```cangjie
public enum HDRFormat { | NONE | VIDEO_HLG | VIDEO_HDR10 | VIDEO_HDR_VIVID | IMAGE_HDR_VIVID_DUAL | IMAGE_HDR_VIVID_SINGLE | IMAGE_HDR_ISO_DUAL | IMAGE_HDR_ISO_SINGLE | ... }
```
功能： HDR格式类型。

#### IMAGE_HDR_ISO_DUAL

```cangjie
IMAGE_HDR_ISO_DUAL
```
功能： 设置支持图片的HDR_ISO格式，以dual JPEG格式存储。

#### IMAGE_HDR_ISO_SINGLE

```cangjie
IMAGE_HDR_ISO_SINGLE
```
功能： 设置支持图片的HDR_ISO格式，以single HEIF格式存储。

#### IMAGE_HDR_VIVID_DUAL

```cangjie
IMAGE_HDR_VIVID_DUAL
```
功能： 设置支持图片的HDR_VIVID格式，以dual JPEG格式存储。

#### IMAGE_HDR_VIVID_SINGLE

```cangjie
IMAGE_HDR_VIVID_SINGLE
```
功能： 设置支持图片的HDR_VIVID格式，以single HEIF格式存储。

#### NONE

```cangjie
NONE
```
功能： 设置不支持HDR类型。

#### VIDEO_HDR10

```cangjie
VIDEO_HDR10
```
功能： 设置支持视频的HDR10格式。

#### VIDEO_HLG

```cangjie
VIDEO_HLG
```
功能： 设置支持视频的HLG格式。

#### VIDEO_HDR_VIVID

```cangjie
VIDEO_HDR_VIVID
```
功能： 设置支持视频的HDR_VIVID格式。


### ListenerType

enum ListenerType
```cangjie
public enum ListenerType <: ToString { | LISTNER_TYPE_ADD | LISTNER_TYPE_REMOVE | LISTNER_TYPE_CHANGE | LISTNER_TYPE_FOLD_STATUS_CHANGE | LISTNER_TYPE_FOLD_ANGLE_CHANGE | LISTNER_TYPE_CAPTURE_STATUS_CHANGE | LISTNER_TYPE_FOLD_DISPLAY_MODE_CHANGE | LISTNER_TYPE_AVAILABLE_AREA_CHANGE | ... }
```
功能： 设置监听事件类型。

#### LISTNER_TYPE_ADD

```cangjie
LISTNER_TYPE_ADD
```
功能： 表示增加显示设备事件。

#### LISTNER_TYPE_AVAILABLE_AREA_CHANGE

```cangjie
LISTNER_TYPE_AVAILABLE_AREA_CHANGE
```
功能： 表示折叠设备屏幕显示模式发生变化。

#### LISTNER_TYPE_CAPTURE_STATUS_CHANGE

```cangjie
LISTNER_TYPE_CAPTURE_STATUS_CHANGE
```
功能： 表示设备截屏、投屏或者录屏状态发生变化。

#### LISTNER_TYPE_CHANGE

```cangjie
LISTNER_TYPE_CHANGE
```
功能： 表示改变显示设备事件。

#### LISTNER_TYPE_FOLD_ANGLE_CHANGE

```cangjie
LISTNER_TYPE_FOLD_ANGLE_CHANGE
```
功能： 表示折叠设备折叠角度发生变化。

#### LISTNER_TYPE_FOLD_DISPLAY_MODE_CHANGE

```cangjie
LISTNER_TYPE_FOLD_DISPLAY_MODE_CHANGE
```
功能： 表示折叠设备屏幕显示模式发生变化。

#### LISTNER_TYPE_FOLD_STATUS_CHANGE

```cangjie
LISTNER_TYPE_FOLD_STATUS_CHANGE
```
功能： 表示折叠设备折叠状态发生变化。

#### LISTNER_TYPE_REMOVE

```cangjie
LISTNER_TYPE_REMOVE
```
功能： 表示移除显示设备事件。



