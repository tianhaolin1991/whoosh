# Hyperlink

超链接组件，组件宽高范围内点击实现跳转

## Component Constructors

### public init(address: String, subcomponent: ()->Unit)

```cangjie
public init(address: String, subcomponent: ()->Unit)
```
功能： 创建Hyperlink组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | Hyperlink组件跳转的网页。 | address | - |
| ()->Unit | 是 | Image子组件。 | subcomponent | - |

### public init(address!: String, content!: String="")

```cangjie
public init(address!: String, content!: String="")
```
功能： 创建Hyperlink组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 命名参数。 Hyperlink组件跳转的网页。 | address | - |
| String | 否 | 命名参数。 Hyperlink组件中超链接显示文本。 说明： 组件内有子组件时，不显示超链接文本。 | content | "" |

### public init(address!: String, content!: AppResource)

```cangjie
public init(address!: String, content!: AppResource)
```
功能： 创建Hyperlink组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 命名参数。 Hyperlink组件跳转的网页。 | address | - |
| AppResource | 是 | 命名参数。 Hyperlink组件中超链接显示文本。 说明： 组件内有子组件时，不显示超链接文本。 | content | - |

### public init(address: AppResource, subcomponent: ()->Unit)

```cangjie
public init(address: AppResource, subcomponent: ()->Unit)
```
功能： 创建Hyperlink组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | Hyperlink组件跳转的网页。 | address | - |
| ()->Unit | 是 | Image子组件。 | subcomponent | - |

### public init(address!: AppResource, content!: String="")

```cangjie
public init(address!: AppResource, content!: String="")
```
功能： 创建Hyperlink组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 命名参数。 Hyperlink组件跳转的网页。 | address | - |
| String | 否 | 命名参数。 Hyperlink组件中超链接显示文本。 说明： 组件内有子组件时，不显示超链接文本。 | content | "" |

### public init(address!: AppResource, content!: AppResource)

```cangjie
public init(address!: AppResource, content!: AppResource)
```
功能： 创建Hyperlink组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 命名参数。 Hyperlink组件跳转的网页。 | address | - |
| AppResource | 是 | 命名参数。 Hyperlink组件中超链接显示文本。 说明： 组件内有子组件时，不显示超链接文本。 | content | - |


## Functions

### public func color(value: ResourceColor): This

```cangjie
public func color(value: ResourceColor): This
```
功能： 设置超链接文本的颜色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ResourceColor | 是 | 超链接文本的颜色。 phone初始值：0xff0a59f7。 | value | - |


## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*
import std.collection.*
import ohos.resource_manager.__GenerateResource__

@Entry
@Component
class EntryView {
 func build() {
 Column() {
 Column() {
 Hyperlink(address: "https://www.huawei.com/", content: "jumper to huawei").color(Color.BLUE)
 }
 Column() {
 Hyperlink("https://www.huawei.com/") {
 Image(@r(app.media.startIcon))
 .width(100)
 .height(100)
 }
 }
 }
 }
}
```

