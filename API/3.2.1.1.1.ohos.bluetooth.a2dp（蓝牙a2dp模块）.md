# ohos.bluetooth.a2dp（蓝牙a2dp模块）

a2dp模块提供了访问蓝牙音频接口的方法。

```cangjie
import kit.ConnectivityKit.*
```
## Functions

### public func createA2dpSrcProfile(): A2dpSourceProfile

```cangjie
public func createA2dpSrcProfile(): A2dpSourceProfile
```
功能： 创建a2dp profile实例。

**Returns:**

| type | description |
| --- | --- |
| A2dpSourceProfile | 返回该profile的实例。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 401 | Invalid parameter. Possible causes: 1. Mandatory parameters are left unspecified. 2. Incorrect parameter types. 3. Parameter verification failed. |
| 801 | Capability not supported. |


**Example:**

```cangjie
// index.cj

import ohos.base.*
import kit.ConnectivityKit.*
import ohos.hilog.Hilog

try {
 let a2dpProfile = createA2dpSrcProfile()
} catch (e: BusinessException) {
 Hilog.info(0, "Bluetooth", "errCode: ${e.code}, errMessage: ${e.message}")
}
```


## Classes

### A2dpSourceProfile

class A2dpSourceProfile
```cangjie
public class A2dpSourceProfile <: BaseProfile {}
```
功能： 使用A2dpSourceProfile方法之前需要创建该类的实例进行操作，通过 createA2dpSrcProfile() 方法构造此实例。


### CodecInfo

class CodecInfo
```cangjie
public class CodecInfo {}
```
功能： 编码器信息。

#### var codecBitsPerSample

```cangjie
public var codecBitsPerSample: CodecBitsPerSample = CODEC_BITS_PER_SAMPLE_NONE
```
功能： 表示每个采样点的位数，初始值为CODEC_BITS_PER_SAMPLE_NONE。
Type: CodecBitsPerSample; Readable: true; Writable: true

#### var codecChannelMode

```cangjie
public var codecChannelMode: CodecChannelMode = CODEC_CHANNEL_MODE_NONE
```
功能： 表示编码器的声道模式，初始值为CODEC_CHANNEL_MODE_NONE。
Type: CodecChannelMode; Readable: true; Writable: true

#### var codecSampleRate

```cangjie
public var codecSampleRate: CodecSampleRate = CODEC_SAMPLE_RATE_NONE
```
功能： 表示编码器的采样率，初始值为CODEC_BITS_PER_SAMPLE_NONE。
Type: CodecSampleRate; Readable: true; Writable: true

#### var codecType

```cangjie
public var codecType: CodecType = CODEC_TYPE_SBC
```
功能： 表示编码器类型，初始值为CODEC_TYPE_SBC。
Type: CodecType; Readable: true; Writable: true



## Enums

### CodecBitsPerSample

enum CodecBitsPerSample
```cangjie
public enum CodecBitsPerSample <: Equatable<CodecBitsPerSample> & ToString { | CODEC_BITS_PER_SAMPLE_NONE | CODEC_BITS_PER_SAMPLE_16 | CODEC_BITS_PER_SAMPLE_24 | CODEC_BITS_PER_SAMPLE_32 | ... }
```
功能： 蓝牙编码器每个采样点的位数。

#### CODEC_BITS_PER_SAMPLE_16

```cangjie
CODEC_BITS_PER_SAMPLE_16
```
功能： 16位采样点的位数。

#### CODEC_BITS_PER_SAMPLE_24

```cangjie
CODEC_BITS_PER_SAMPLE_24
```
功能： 24位采样点的位数。

#### CODEC_BITS_PER_SAMPLE_32

```cangjie
CODEC_BITS_PER_SAMPLE_32
```
功能： 32位采样点的位数。

#### CODEC_BITS_PER_SAMPLE_NONE

```cangjie
CODEC_BITS_PER_SAMPLE_NONE
```
功能： 未知采样点的位数。


### CodecChannelMode

enum CodecChannelMode
```cangjie
public enum CodecChannelMode <: Equatable<CodecChannelMode> & ToString { | CODEC_CHANNEL_MODE_NONE | CODEC_CHANNEL_MODE_MONO | CODEC_CHANNEL_MODE_STEREO | ... }
```
功能： 蓝牙编码器的声道模式。

#### CODEC_CHANNEL_MODE_MONO

```cangjie
CODEC_CHANNEL_MODE_MONO
```
功能： 单声道。

#### CODEC_CHANNEL_MODE_NONE

```cangjie
CODEC_CHANNEL_MODE_NONE
```
功能： 未知声道。

#### CODEC_CHANNEL_MODE_STEREO

```cangjie
CODEC_CHANNEL_MODE_STEREO
```
功能： 双声道。


### CodecSampleRate

enum CodecSampleRate
```cangjie
public enum CodecSampleRate <: Equatable<CodecSampleRate> & ToString { | CODEC_SAMPLE_RATE_NONE | CODEC_SAMPLE_RATE_44100 | CODEC_SAMPLE_RATE_48000 | CODEC_SAMPLE_RATE_88200 | CODEC_SAMPLE_RATE_96000 | CODEC_SAMPLE_RATE_176400 | CODEC_SAMPLE_RATE_192000 | ... }
```
功能： 蓝牙编码器的采样率。

#### CODEC_SAMPLE_RATE_176400

```cangjie
CODEC_SAMPLE_RATE_176400
```
功能： 176.4k位采样率。

#### CODEC_SAMPLE_RATE_192000

```cangjie
CODEC_SAMPLE_RATE_192000
```
功能： 192k位采样率。

#### CODEC_SAMPLE_RATE_44100

```cangjie
CODEC_SAMPLE_RATE_44100
```
功能： 44.1k采样率。

#### CODEC_SAMPLE_RATE_48000

```cangjie
CODEC_SAMPLE_RATE_48000
```
功能： 48k采样率。

#### CODEC_SAMPLE_RATE_88200

```cangjie
CODEC_SAMPLE_RATE_88200
```
功能： 88.2k采样率。

#### CODEC_SAMPLE_RATE_96000

```cangjie
CODEC_SAMPLE_RATE_96000
```
功能： 96k位采样率。

#### CODEC_SAMPLE_RATE_NONE

```cangjie
CODEC_SAMPLE_RATE_NONE
```
功能： 未知采样率。


### CodecType

enum CodecType
```cangjie
public enum CodecType <: Equatable<CodecType> & ToString { | CODEC_TYPE_INVALID | CODEC_TYPE_SBC | CODEC_TYPE_AAC | CODEC_TYPE_L2HC | ... }
```
功能： 蓝牙编码器类型。

#### CODEC_TYPE_AAC

```cangjie
CODEC_TYPE_AAC
```
功能： AAC。

#### CODEC_TYPE_INVALID

```cangjie
CODEC_TYPE_INVALID
```
功能： 未知编码类型。

#### CODEC_TYPE_L2HC

```cangjie
CODEC_TYPE_L2HC
```
功能： L2HC。

#### CODEC_TYPE_SBC

```cangjie
CODEC_TYPE_SBC
```
功能： SBC。


### PlayingState

enum PlayingState
```cangjie
public enum PlayingState <: Equatable<PlayingState> & ToString { | STATE_NOT_PLAYING | STATE_PLAYING | ... }
```
功能： 蓝牙A2DP播放状态。

#### STATE_NOT_PLAYING

```cangjie
STATE_NOT_PLAYING
```
功能： 表示未播放。

#### STATE_PLAYING

```cangjie
STATE_PLAYING
```
功能： 表示正在播放。



