# XComponent

提供用于图形绘制和媒体数据写入的Surface，XComponent负责将其嵌入到视图中，支持应用自定义Surface位置和大小。具体指南请参考 自定义渲染 (XComponent) 文档。

## Component Constructors

### public init(id!: String, `type`!: XComponentType, controller!: XComponentController)

```cangjie
public init(id!: String, `type`!: XComponentType, controller!: XComponentController)
```
功能： 创建一个XComponent组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 命名参数。 组件的唯一标识，支持最大的字符串长度128。 | id | - |
| XComponentType | 是 | 命名参数。 用于指定XComponent组件类型。 | `type` | - |
| XComponentController | 是 | 命名参数。 给组件绑定一个控制器，通过控制器调用组件方法，仅XComponent类型为"SURFACE"时有效。 | controller | - |


## Functions

### public func enableSecure(isSecure: Bool): This

```cangjie
public func enableSecure(isSecure: Bool): This
```
功能： 防止组件内自绘制内容被截屏、录屏。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否开启隐私图层模式。 | isSecure | - |


## Classes

### XComponentController

class XComponentController
```cangjie
public open class XComponentController { public init() }
```
功能： XComponent组件的控制器，可以将此对象绑定至XComponent组件，然后通过控制器来调用组件方法。


### SurfaceRect

class SurfaceRect
```cangjie
public class SurfaceRect { public var offsetX: Float32 public var offsetY: Float32 public var surfaceWidth: Float32 public var surfaceHeight: Float32 public init(offsetX: Float32, offsetY: Float32, surfaceWidth: Float32, surfaceHeight: Float32) }
```
功能： 用于描述XComponent持有Surface的显示区域。

#### var offsetX

```cangjie
public var offsetX: Float32
```
功能： Surface显示区域相对于XComponent组件左上角的x轴坐标，单位：px。
Type: Float32; Readable: true; Writable: true

#### var offsetY

```cangjie
public var offsetY: Float32
```
功能： Surface显示区域相对于XComponent组件左上角的y轴坐标，单位：px。
Type: Float32; Readable: true; Writable: true

#### var surfaceHeight

```cangjie
public var surfaceHeight: Float32
```
功能： Surface显示区域的高度，单位：px。
Type: Float32; Readable: true; Writable: true

#### var surfaceWidth

```cangjie
public var surfaceWidth: Float32
```
功能： Surface显示区域的宽度，单位：px。
Type: Float32; Readable: true; Writable: true


### SurfaceRotationOptions

class SurfaceRotationOptions
```cangjie
public class SurfaceRotationOptions { public var lock: Bool public init(lock: Bool) }
```
功能： 用于描述XComponent持有Surface在屏幕旋转时是否锁定方向的设置。

#### var lock

```cangjie
public var lock: Bool
```
功能： Surface在屏幕旋转时是否锁定方向。
Type: Bool; Readable: true; Writable: true



