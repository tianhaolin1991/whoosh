# Button

按钮组件，可快速创建不同样式的按钮。

## Component Constructors

### public init()

```cangjie
public init()
```
功能： 创建一个空按钮组件。

### public init(content: () -> Unit)

```cangjie
public init(content: () -> Unit)
```
功能： 创建包含子组件的按钮。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ()->Unit | 是 | 按钮包含的子组件。 | content | - |

### public init(label: String)

```cangjie
public init(label: String)
```
功能： 使用文本内容创建相应的按钮组件，此时Button无法包含子组件。 文本内容默认单行显示。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 按钮文本内容。当文本字符的长度超过按钮本身的宽度时，文本将会被截断。 | label | - |

### public init(label: AppResource)

```cangjie
public init(label: AppResource)
```
功能： 使用文本内容创建相应的按钮组件，此时Button无法包含子组件。 文本内容默认单行显示。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 按钮文本内容。当文本字符的长度超过按钮本身的宽度时，文本将会被截断。 | label | - |

### public init(options: ButtonOptions)

```cangjie
public init(options: ButtonOptions)
```
功能： 使用文本内容创建相应的按钮组件，此时Button无法包含子组件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ButtonOptions | 是 | 配置按钮的显示样式。 | options | - |

### public init(options: ButtonOptions, content: () -> Unit)

```cangjie
public init(options: ButtonOptions, content: () -> Unit)
```
功能： 创建可以包含子组件且有显示样式的按钮。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ButtonOptions | 是 | 配置按钮的显示样式。 | options | - |
| ()->Unit | 是 | 按钮包含的子组件。 | content | - |

### public init(label: String, options: ButtonOptions)

```cangjie
public init(label: String, options: ButtonOptions)
```
功能： 使用文本内容创建相应的按钮组件，此时Button无法包含子组件。 文本内容默认单行显示。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 按钮文本内容。 说明 ： 当文本字符的长度超过按钮本身的宽度时，文本将会被截断。 | label | - |
| ButtonOptions | 是 | 配置按钮的显示样式。 | options | - |

### public init(label: AppResource, options: ButtonOptions)

```cangjie
public init(label: AppResource, options: ButtonOptions)
```
功能： 使用文本内容创建相应的按钮组件，此时Button无法包含子组件。 文本内容默认单行显示。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AppResource | 是 | 设置文本内容。 说明： 当文本字符的长度超过按钮本身的宽度时，文本将会被截断。 | label | - |
| ButtonOptions | 是 | 配置按钮的显示样式。 | options | - |


## Functions

### public func buttonStyle(value: ButtonStyleMode): This

```cangjie
public func buttonStyle(value: ButtonStyleMode): This
```
功能： 设置Button组件的样式和重要程度。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ButtonStyleMode | 是 | Button组件的样式和重要程度。 初始值：ButtonStyleMode.EMPHASIZED。 | value | - |

### public func controlSize(value: ControlSize): This

```cangjie
public func controlSize(value: ControlSize): This
```
功能： 设置Button组件的尺寸。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ControlSize | 是 | Button组件的尺寸。 初始值：ControlSize.Normal。 | value | - |

### public func labelStyle( overflow!: TextOverflow = TextOverflow.Ellipsis, maxLines!: UInt32 = 1, minFontSize!: Option<Length> = None, maxFontSize!: Option<Length> = None, heightAdaptivePolicy!: TextHeightAdaptivePolicy = TextHeightAdaptivePolicy.MAX_LINES_FIRST, font!: Fonts = Fonts() ): This

```cangjie
public func labelStyle( overflow!: TextOverflow = TextOverflow.Ellipsis, maxLines!: UInt32 = 1, minFontSize!: Option<Length> = None, maxFontSize!: Option<Length> = None, heightAdaptivePolicy!: TextHeightAdaptivePolicy = TextHeightAdaptivePolicy.MAX_LINES_FIRST, font!: Fonts = Fonts() ): This
```
功能： 设置Button组件label文本和字体的样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| TextOverflow | 否 | 命名参数。 设置label文本超长时的显示方式。文本截断是按字截断。例如，英文以单词为最小单位进行截断，若需要以字母为单位进行截断，可在字母间添加零宽空格。 | overflow | TextOverflow.Ellipsis |
| UInt32 | 否 | 命名参数。 设置label文本的最大行数。默认情况下，文本是自动折行的，如果指定此参数，则文本最多不会超过指定的行。如果有多余的文本，可以通过overflow来指定截断方式。 | maxLines | 1 |
| Option< Length > | 否 | 命名参数。 设置label文本最小显示字号。需配合maxFontSize以及maxLines或布局大小限制使用。 说明 ： minFontSize小于或等于0时，自适应字号不生效。 | minFontSize | None |
| Option< Length > | 否 | 命名参数。 设置label文本最大显示字号。需配合minFontSize以及maxLines或布局大小限制使用。 | maxFontSize | None |
| TextHeightAdaptivePolicy | 否 | 命名参数。 设置label文本自适应高度的方式。 | heightAdaptivePolicy | TextHeightAdaptivePolicy.MAX_LINES_FIRST |
| Fonts | 否 | 命名参数。 设置label文本字体样式。 | font | Fonts() |

### public func labelStyle( overflow!: TextOverflow = TextOverflow.Ellipsis, maxLines!: UInt32 = 1, minFontSize!: AppResource, maxFontSize!: AppResource, heightAdaptivePolicy!: TextHeightAdaptivePolicy = TextHeightAdaptivePolicy.MAX_LINES_FIRST, font!: Fonts = Fonts() ): This

```cangjie
public func labelStyle( overflow!: TextOverflow = TextOverflow.Ellipsis, maxLines!: UInt32 = 1, minFontSize!: AppResource, maxFontSize!: AppResource, heightAdaptivePolicy!: TextHeightAdaptivePolicy = TextHeightAdaptivePolicy.MAX_LINES_FIRST, font!: Fonts = Fonts() ): This
```
功能： 设置Button组件label文本和字体的样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| TextOverflow | 否 | 命名参数。 设置label文本超长时的显示方式。文本截断是按字截断。例如，英文以单词为最小单位进行截断，若需要以字母为单位进行截断，可在字母间添加零宽空格。 | overflow | TextOverflow.Ellipsis |
| UInt32 | 否 | 命名参数。 设置label文本的最大行数。默认情况下，文本是自动折行的，如果指定此参数，则文本最多不会超过指定的行。如果有多余的文本，可以通过overflow来指定截断方式。 | maxLines | 1 |
| AppResource | 是 | 命名参数。 设置label文本最小显示字号。需配合maxFontSize以及maxLines或布局大小限制使用。 | minFontSize | - |
| AppResource | 是 | 命名参数。 设置label文本最大显示字号。需配合minFontSize以及maxLines或布局大小限制使用。 | maxFontSize | - |
| TextHeightAdaptivePolicy | 否 | 命名参数。 设置label文本自适应高度的方式。 | heightAdaptivePolicy | TextHeightAdaptivePolicy.MAX_LINES_FIRST |
| Fonts | 否 | 命名参数。 设置label文本字体样式。 | font | Fonts() |

### public func labelStyle( overflow!: TextOverflow = TextOverflow.Ellipsis, maxLines!: UInt32 = 1, minFontSize!: AppResource, maxFontSize!: Option<Length> = None, heightAdaptivePolicy!: TextHeightAdaptivePolicy = TextHeightAdaptivePolicy.MAX_LINES_FIRST, font!: Fonts = Fonts() ): This

```cangjie
public func labelStyle( overflow!: TextOverflow = TextOverflow.Ellipsis, maxLines!: UInt32 = 1, minFontSize!: AppResource, maxFontSize!: Option<Length> = None, heightAdaptivePolicy!: TextHeightAdaptivePolicy = TextHeightAdaptivePolicy.MAX_LINES_FIRST, font!: Fonts = Fonts() ): This
```
功能： 设置Button组件label文本和字体的样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| TextOverflow | 否 | 命名参数。 设置label文本超长时的显示方式。文本截断是按字截断。例如，英文以单词为最小单位进行截断，若需要以字母为单位进行截断，可在字母间添加零宽空格。 | overflow | TextOverflow.Ellipsis |
| UInt32 | 否 | 命名参数。 设置label文本的最大行数。默认情况下，文本是自动折行的，如果指定此参数，则文本最多不会超过指定的行。如果有多余的文本，可以通过overflow来指定截断方式。 | maxLines | 1 |
| AppResource | 是 | 命名参数。 设置label文本最小显示字号。需配合maxFontSize以及maxLines或布局大小限制使用。 | minFontSize | - |
| Option< Length > | 否 | 命名参数。 设置label文本最大显示字号。需配合minFontSize以及maxLines或布局大小限制使用。 | maxFontSize | None |
| TextHeightAdaptivePolicy | 否 | 命名参数。 设置label文本自适应高度的方式。 | heightAdaptivePolicy | TextHeightAdaptivePolicy.MAX_LINES_FIRST |
| Fonts | 否 | 命名参数。 设置label文本字体样式。 | font | Fonts() |

### public func labelStyle( overflow!: TextOverflow = TextOverflow.Ellipsis, maxLines!: UInt32 = 1, minFontSize!: Option<Length> = None, maxFontSize!: AppResource, heightAdaptivePolicy!: TextHeightAdaptivePolicy = TextHeightAdaptivePolicy.MAX_LINES_FIRST, font!: Fonts = Fonts() ): This

```cangjie
public func labelStyle( overflow!: TextOverflow = TextOverflow.Ellipsis, maxLines!: UInt32 = 1, minFontSize!: Option<Length> = None, maxFontSize!: AppResource, heightAdaptivePolicy!: TextHeightAdaptivePolicy = TextHeightAdaptivePolicy.MAX_LINES_FIRST, font!: Fonts = Fonts() ): This
```
功能： 设置Button组件label文本和字体的样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| TextOverflow | 否 | 命名参数。 设置label文本超长时的显示方式。文本截断是按字截断。例如，英文以单词为最小单位进行截断，若需要以字母为单位进行截断，可在字母间添加零宽空格。 | overflow | TextOverflow.Ellipsis |
| UInt32 | 否 | 命名参数。 设置label文本的最大行数。默认情况下，文本是自动折行的，如果指定此参数，则文本最多不会超过指定的行。如果有多余的文本，可以通过overflow来指定截断方式。 | maxLines | 1 |
| Option< Length > | 否 | 命名参数。 设置label文本最小显示字号。需配合maxFontSize以及maxLines或布局大小限制使用。 说明 ： minFontSize小于或等于0时，自适应字号不生效。 | minFontSize | None |
| AppResource | 是 | 命名参数。 设置label文本最大显示字号。需配合minFontSize以及maxLines或布局大小限制使用。 | maxFontSize | - |
| TextHeightAdaptivePolicy | 否 | 命名参数。 设置label文本自适应高度的方式。 | heightAdaptivePolicy | TextHeightAdaptivePolicy.MAX_LINES_FIRST |
| Fonts | 否 | 命名参数。 设置label文本字体样式。 | font | Fonts() |

### public func role(value: ButtonRole): This

```cangjie
public func role(value: ButtonRole): This
```
功能： 设置Button组件的角色。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ButtonRole | 是 | 设置Button组件的角色。 初始值：BottonRole.Normal。 | value | - |

### public func shape(shapeType: ShapeType): This

```cangjie
public func shape(shapeType: ShapeType): This
```
功能： 设置Button组件的形状。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ShapeType | 是 | 形状类型。 | shapeType | - |

### public func shape(buttonType: ButtonType): This

```cangjie
public func shape(buttonType: ButtonType): This
```
功能： 设置Button组件的形状。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ButtonType | 是 | 按键形状类型。 | buttonType | - |

### public func stateEffect(value: Bool): This

```cangjie
public func stateEffect(value: Bool): This
```
功能： 设置是否开启按压态显示效果。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 按钮按下时是否开启按压态显示效果，当设置为false时，按压效果关闭。 初始值：true | value | - |

### public func onClick(callback: () -> Unit): This

```cangjie
public func onClick(callback: () -> Unit): This
```
功能： 回调函数，按钮被点击时触发该回调。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| () -> Unit | 是 | 回调函数，组件被点击时触发该回调。 | callback | - |


## Classes

### ButtonOptions

class ButtonOptions
```cangjie
public class ButtonOptions { public var shape: ButtonType public var stateEffect: Bool public var buttonStyle: ButtonStyleMode public var controlSize: ControlSize public var role: ButtonRole public init( shape!: ButtonType = ButtonType.Capsule, stateEffect!: Bool = true, buttonStyle!: ButtonStyleMode = ButtonStyleMode.EMPHASIZED, controlSize!: ControlSize = ControlSize.NORMAL, role!: ButtonRole = ButtonRole.NORMAL ) }
```
功能： 配置按钮的显示样式。

#### var buttonStyle

```cangjie
public var buttonStyle: ButtonStyleMode
```
功能： 描述按钮的样式和重要程度。
Type: ButtonStyleMode; Readable: true; Writable: true

#### var controlSize

```cangjie
public var controlSize: ControlSize
```
功能： 描述按钮的尺寸。
Type: ControlSize; Readable: true; Writable: true

#### var role

```cangjie
public var role: ButtonRole
```
功能： 描述按钮的角色。
Type: ButtonRole; Readable: true; Writable: true

#### var shape

```cangjie
public var shape: ButtonType
```
功能： 描述按钮的形状。
Type: ButtonType; Readable: true; Writable: true

#### var stateEffect

```cangjie
public var stateEffect: Bool
```
功能： 按钮按下时是否开启按压态显示效果。
Type: Bool; Readable: true; Writable: true



## Enums

### ButtonRole

enum ButtonRole
```cangjie
public enum ButtonRole { | NORMAL | ERROR }
```
功能： 按钮的角色。

#### ERROR

```cangjie
ERROR
```
功能： 警示按钮。

#### NORMAL

```cangjie
NORMAL
```
功能： 正常按钮。


### ButtonStyleMode

enum ButtonStyleMode
```cangjie
public enum ButtonStyleMode { | NORMAL | EMPHASIZED | TEXTUAL }
```
功能： 按钮的重要程度。

#### EMPHASIZED

```cangjie
EMPHASIZED
```
功能： 强调按钮（用于强调当前操作）。

#### NORMAL

```cangjie
NORMAL
```
功能： 普通按钮（一般界面操作）。

#### TEXTUAL

```cangjie
TEXTUAL
```
功能： 文本按钮（纯文本，无背景颜色）。


### ButtonType

enum ButtonType
```cangjie
public enum ButtonType { | Normal | Capsule | Circle | ROUNDED_RECTANGLE }
```
功能： 按键形状类型。

#### Capsule

```cangjie
Capsule
```
功能： 胶囊型按钮（圆角默认为高度的一半）。

#### Circle

```cangjie
Circle
```
功能： 圆形按钮。

#### Normal

```cangjie
Normal
```
功能： 普通按钮（默认不带圆角）。

#### ROUNDED_RECTANGLE

```cangjie
ROUNDED_RECTANGLE
```
功能： 圆角矩形按钮。



## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 func build() {
 Flex(FlexParams(direction: FlexDirection.Column, alignItems: ItemAlign.Start, justifyContent: FlexAlign.SpaceBetween)) {
 Text("Common button").fontSize(9).fontColor(0xCCCCCC)

 Flex(FlexParams(alignItems: ItemAlign.Center, justifyContent: FlexAlign.SpaceBetween)) {
 Button("Ok").shape(ButtonType.Normal).stateEffect(true).borderRadius(8)
 .width(90).role(ButtonRole.NORMAL)
 Button(ButtonOptions()) {
 Row() {
 LoadingProgress().width(20).height(20).color(Color.WHITE)
 Text("loading").fontSize(12).fontColor(0xffffff).margin(left: 5, right: 12)
 }.alignItems(VerticalAlign.Center).width(90).height(40)
 }.shape(ButtonType.Normal).stateEffect(true).borderRadius(8).width(90).role(ButtonRole.NORMAL)
 Button("Disable").shape(ButtonType.Normal).stateEffect(true).opacity(0.5)
 .borderRadius(8).width(90).role(ButtonRole.ERROR)
 }

 Text("Capsule button").fontSize(9).fontColor(0xCCCCCC)

 Flex(FlexParams(alignItems: ItemAlign.Center, justifyContent: FlexAlign.SpaceBetween)) {
 Button("Ok").shape(ButtonType.Capsule).stateEffect(true).borderRadius(8)
 .width(90).role(ButtonRole.NORMAL)
 Button(ButtonOptions()) {
 Row() {
 LoadingProgress().width(20).height(20).color(Color.WHITE)
 Text("loading").fontSize(12).fontColor(0xffffff).margin(left: 5, right: 12)
 }.alignItems(VerticalAlign.Center).width(90).height(40)
 }.shape(ButtonType.Capsule).stateEffect(true).borderRadius(8).width(90)
 .onClick { evt =>
 nativeLog("The login is successful")
 }.role(ButtonRole.NORMAL)
 Button("Disable").shape(ButtonType.Capsule).stateEffect(true).opacity(0.5)
 .borderRadius(8).width(90).role(ButtonRole.ERROR)
 }

 Text("Circle button").fontSize(9).fontColor(0xCCCCCC)

 Flex(FlexParams(alignItems: ItemAlign.Center, wrap: FlexWrap.Wrap)) {
 Button("YES").shape(ButtonType.Circle).stateEffect(true).width(55)
 .height(55).role(ButtonRole.NORMAL)
 Button("NO").shape(ButtonType.Capsule).stateEffect(true).width(55)
 .height(55).margin(left: 20).role(ButtonRole.ERROR).fontSize(15)
 }
 }.height(400).padding(left: 35, right: 35, top: 35)
 }
}
```
```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 @State var count: UInt32 = 0
 func build() {
 Column() {
 Text('${this.count}')
 .fontSize(30)
 .onClick{ evt =>
 this.count ++
 }
 if (this.count <= 0) {
 Button('count is negative').fontSize(30).height(50)
 } else if (this.count % 2 == 0) {
 Button('count is even').fontSize(30).height(50)
 } else {
 Button('count is odd').fontSize(30).height(50)
 }
 }.height(100.percent).width(100.percent).justifyContent(FlexAlign.Center)
 }
}
```
```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 @State var txt: String = 'overflowTextOverlengthTextOverflow.Clip'
 func build() {
 Column() {
 Button(this.txt)
 .width(210)
 .height(100)
 .backgroundColor(0x317aff)
 .labelStyle(overflow: TextOverflow.Clip,
 maxLines: 1,
 minFontSize: 20.fp,
 maxFontSize: 20.fp,
 font: Fonts(
 size: 20.fp,
 weight: FontWeight.Bolder,
 family: 'cursive',
 style: FontStyle.Italic
 )
 )
 .fontSize(40)
 }.height(100.percent).width(100.percent).justifyContent(FlexAlign.Center)
 }
}
```
```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 @State var txt: String = 'overflowTextOverlengthTextOverflow.Clip'
 func build() {
 Flex(FlexParams(direction: FlexDirection.Column, alignItems: ItemAlign.Start, justifyContent: FlexAlign.SpaceBetween)) {
 Text('Normal size button').fontSize(9).fontColor(0xCCCCCC)
 Flex(FlexParams(alignItems: ItemAlign.Center, wrap: FlexWrap.Wrap)) {
 Button('Emphasized').buttonStyle(ButtonStyleMode.EMPHASIZED)
 Button('Normal').buttonStyle(ButtonStyleMode.NORMAL)
 Button('Textual').buttonStyle(ButtonStyleMode.TEXTUAL)
 }

 Text('Small size button').fontSize(9).fontColor(0xCCCCCC)
 Flex(FlexParams(alignItems: ItemAlign.Center, wrap: FlexWrap.Wrap)) {
 Button('Emphasized'). controlSize(ControlSize.SMALL).buttonStyle(ButtonStyleMode.EMPHASIZED)
 Button('Normal').controlSize(ControlSize.SMALL).buttonStyle(ButtonStyleMode.NORMAL)
 Button('Textual').controlSize(ControlSize.SMALL).buttonStyle(ButtonStyleMode.TEXTUAL)
 }
 }.height(400).padding(left: 35, right: 35, top: 35)
 }
}
```
```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 @State var txt: String = 'overflowTextOverlengthTextOverflow.Clip'
 func build() {
 Flex(FlexParams(direction: FlexDirection.Column, alignItems: ItemAlign.Start, justifyContent: FlexAlign.SpaceBetween)) {
 Text('Role is Normal button').fontSize(9).fontColor(0xCCCCCC)
 Flex(FlexParams(alignItems: ItemAlign.Center, wrap: FlexWrap.Wrap)) {
 Button('Emphasized').buttonStyle(ButtonStyleMode.EMPHASIZED).role(ButtonRole.NORMAL)
 Button('Normal').buttonStyle(ButtonStyleMode.NORMAL).role(ButtonRole.NORMAL)
 Button('Textual').buttonStyle(ButtonStyleMode.TEXTUAL).role(ButtonRole.NORMAL);
 }

 Text('Role is Error button').fontSize(9).fontColor(0xCCCCCC)
 Flex(FlexParams(alignItems: ItemAlign.Center, wrap: FlexWrap.Wrap)) {
 Button('Emphasized').buttonStyle(ButtonStyleMode.EMPHASIZED).role(ButtonRole.ERROR)
 Button('Normal').buttonStyle(ButtonStyleMode.NORMAL).role(ButtonRole.ERROR)
 Button('Textual').buttonStyle(ButtonStyleMode.TEXTUAL).role(ButtonRole.ERROR);
 }
 }.height(400).padding(left: 35, right: 35, top: 35)
 }
}
```

