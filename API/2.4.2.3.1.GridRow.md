# GridRow

栅格布局可以为布局提供规律性的结构，解决多尺寸多设备的动态布局问题，保证不同设备上各个模块的布局一致性。

## Component Constructors

### public init(child: () -> Unit)

```cangjie
public init(child: () -> Unit)
```
功能： 创建一个可包含子组件的GridRow容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ()->Unit | 是 | GridRow容器的子组件。 | child | - |

### public init( columns!: Int32, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )

```cangjie
public init( columns!: Int32, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )
```
功能： 创建一个可包含子组件的GridRow容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 命名参数。 布局列数设置。 取值为大于0的整数，初始值：12。 | columns | - |
| BreakPoints | 否 | 命名参数。 断点值的断点数列以及基于窗口或容器尺寸的相应参照。 初始值： { value: ["320vp", "600vp", "840vp"],reference: BreakpointsReference.WindowSize } | breakpoints | BreakPoints() |
| GridRowDirection | 否 | 命名参数。 栅格布局排列方向。 | direction | GridRowDirection.GridRowRow |
| ()->Unit | 是 | 命名参数。 GridRow 容器的子组件。 | child | - |

### public init( columns!: GridRowColumnOption, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )

```cangjie
public init( columns!: GridRowColumnOption, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )
```
功能： 创建一个可包含子组件的GridRow容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| GridRowColumnOption | 是 | 命名参数。 布局列数设置。 | columns | - |
| BreakPoints | 否 | 命名参数。 断点值的断点数列以及基于窗口或容器尺寸的相应参照。 初始值： { value: ["320vp", "600vp", "840vp"],reference: BreakpointsReference.WindowSize } | breakpoints | BreakPoints() |
| GridRowDirection | 否 | 命名参数。 栅格布局排列方向。 | direction | GridRowDirection.GridRowRow |
| ()->Unit | 是 | 命名参数。 GridRow 容器的子组件。 | child | - |

### public init( gutter!: Length, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )

```cangjie
public init( gutter!: Length, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )
```
功能： 创建一个可包含子组件的GridRow容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 命名参数。 栅格布局间距，x代表水平方向。 初始值：0。 | gutter | - |
| BreakPoints | 否 | 命名参数。 断点值的断点数列以及基于窗口或容器尺寸的相应参照。 初始值： { value: ["320vp", "600vp", "840vp"],reference: BreakpointsReference.WindowSize } | breakpoints | BreakPoints() |
| GridRowDirection | 否 | 命名参数。 栅格布局排列方向。 | direction | GridRowDirection.GridRowRow |
| ()->Unit | 是 | 命名参数。 GridRow容器的子组件。 | child | - |

### public init( gutter!: GutterOption, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )

```cangjie
public init( gutter!: GutterOption, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )
```
功能： 创建一个可包含子组件的GridRow容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| GutterOption | 是 | 命名参数。 栅格布局间距，x代表水平方向。 | gutter | - |
| BreakPoints | 否 | 命名参数。 断点值的断点数列以及基于窗口或容器尺寸的相应参照。 初始值： { value: ["320vp", "600vp", "840vp"],reference: BreakpointsReference.WindowSize } | breakpoints | BreakPoints() |
| GridRowDirection | 否 | 命名参数。 栅格布局排列方向。 | direction | GridRowDirection.GridRowRow |
| ()->Unit | 是 | 命名参数。 GridRow容器的子组件。 | child | - |

### public init( gutter!: Length, columns!: Int32, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )

```cangjie
public init( gutter!: Length, columns!: Int32, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )
```
功能： 创建一个可包含子组件的GridRow容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 命名参数。 栅格布局间距，x代表水平方向。 初始值：0。 | gutter | - |
| Int32 | 是 | 命名参数。 布局列数设置。 取值为大于0的整数，初始值：12。 | columns | - |
| BreakPoints | 否 | 命名参数。 断点值的断点数列以及基于窗口或容器尺寸的相应参照。 初始值： { value: ["320vp", "600vp", "840vp"],reference: BreakpointsReference.WindowSize } | breakpoints | BreakPoints() |
| GridRowDirection | 否 | 命名参数。 栅格布局排列方向。 | direction | GridRowDirection.GridRowRow |
| ()->Unit | 是 | 命名参数。 GridRow容器的子组件。 | child | - |

### public init( gutter!: Length, columns!: GridRowColumnOption, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )

```cangjie
public init( gutter!: Length, columns!: GridRowColumnOption, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )
```
功能： 创建一个可包含子组件的GridRow容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 命名参数。 栅格布局间距，x代表水平方向。 初始值：0。 | gutter | - |
| GridRowColumnOption | 是 | 命名参数。 布局列数设置。 | columns | - |
| BreakPoints | 否 | 命名参数。 断点值的断点数列以及基于窗口或容器尺寸的相应参照。 初始值： { value: ["320vp", "600vp", "840vp"],reference: BreakpointsReference.WindowSize } | breakpoints | BreakPoints() |
| GridRowDirection | 否 | 命名参数。 栅格布局排列方向。 | direction | GridRowDirection.GridRowRow |
| ()->Unit | 是 | 命名参数。 GridRow 容器的子组件。 | child | - |

### public init( gutter!: GutterOption, columns!: Int32, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )

```cangjie
public init( gutter!: GutterOption, columns!: Int32, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )
```
功能： 创建一个可包含子组件的GridRow容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| GutterOption | 是 | 命名参数。 栅格布局间距，x代表水平方向。 | gutter | - |
| Int32 | 是 | 命名参数。 布局列数设置。 取值为大于0的整数，初始值：12。 | columns | - |
| BreakPoints | 否 | 命名参数。 断点值的断点数列以及基于窗口或容器尺寸的相应参照。 初始值： { value: ["320vp", "600vp", "840vp"],reference: BreakpointsReference.WindowSize } | breakpoints | BreakPoints() |
| GridRowDirection | 否 | 命名参数。 栅格布局排列方向。 | direction | GridRowDirection.GridRowRow |
| ()->Unit | 是 | 命名参数。 GridRow 容器的子组件。 | child | - |

### public init( gutter!: GutterOption, columns!: GridRowColumnOption, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )

```cangjie
public init( gutter!: GutterOption, columns!: GridRowColumnOption, breakpoints!: BreakPoints = BreakPoints(), direction!: GridRowDirection = GridRowDirection.GridRowRow, child!: () -> Unit )
```
功能： 创建一个可包含子组件的GridRow容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| GutterOption | 是 | 命名参数。 栅格布局间距，x代表水平方向。 | gutter | - |
| GridRowColumnOption | 是 | 命名参数。 布局列数设置。 | columns | - |
| BreakPoints | 否 | 命名参数。 断点值的断点数列以及基于窗口或容器尺寸的相应参照。 初始值： { value: ["320vp", "600vp", "840vp"],reference: BreakpointsReference.WindowSize } | breakpoints | BreakPoints() |
| GridRowDirection | 否 | 命名参数。 栅格布局排列方向。 | direction | GridRowDirection.GridRowRow |
| ()->Unit | 是 | 命名参数。 GridRow容器的子组件。 | child | - |


## Functions

### public func alignItems(value: ItemAlign): This

```cangjie
public func alignItems(value: ItemAlign): This
```
功能： 设置GridRow中的GridCol垂直主轴方向对齐方式。GridCol本身也可通过alignSelf( ItemAlign )设置自身对齐方式。当上述两种对齐方式都设置时，以GridCol自身设置为准。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ItemAlign | 是 | GridRow中的GridCol垂直主轴方向对齐方式。 初始值：ItemAlign.Start 说明： ItemAlign支持的枚举：ItemAlign.Start、ItemAlign.Center、ItemAlign.End、ItemAlign.Stretch。 | value | - |

### public func onBreakpointChange(callback: (String)->Unit): This

```cangjie
public func onBreakpointChange(callback: (String)->Unit): This
```
功能： 断点发生变化时触发回调。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| (String)->Unit | 是 | 断点发生变化时触发回调取值为"xs"、"sm"、"md"、"lg"、"xl"、"xxl" | callback | - |


## Classes

### BreakPoints

class BreakPoints
```cangjie
public class BreakPoints { public BreakPoints( public var value!: Array<Length> = [320.vp, 520.vp, 840.vp], public var reference!: BreakpointsReference = BreakpointsReference.WindowSize ) }
```
功能： 构建栅格容器组件的断点。

#### var reference

```cangjie
public var reference: BreakpointsReference = BreakpointsReference.WindowSize
```
功能： 断点切换参照物。
Type: BreakpointsReference; Readable: true; Writable: true

#### var value

```cangjie
public var value: Array<Length>=[320.vp, 520.vp, 840.vp]
```
功能： 断点位置的单调递增数组设置。
Type: Array< Length >; Readable: true; Writable: true


### GridRowSizeOption

class GridRowSizeOption
```cangjie
public class GridRowSizeOption { public var xs: Length public var sm: Length public var md: Length public var lg: Length public var xl: Length public var xxl: Length public GridRowSizeOption( xs!: Length = 0.vp, sm!: Length = 0.vp, md!: Length = 0.vp, lg!: Length = 0.vp, xl!: Length = 0.vp, xxl!: Length = 0.vp ) }
```
功能： 栅格在不同宽度设备类型下，gutter的大小。

#### var lg

```cangjie
public var lg: Length
```
功能： 大宽度类型设备。
Type: Length; Readable: true; Writable: true

#### var md

```cangjie
public var md: Length
```
功能： 中等宽度类型设备。
Type: Length; Readable: true; Writable: true

#### var sm

```cangjie
public var sm: Length
```
功能： 小宽度类型设备。
Type: Length; Readable: true; Writable: true

#### var xl

```cangjie
public var xl: Length
```
功能： 特大宽度类型设备。
Type: Length; Readable: true; Writable: true

#### var xs

```cangjie
public var xs: Length
```
功能： 最小宽度类型设备。
Type: Length; Readable: true; Writable: true

#### var xxl

```cangjie
public var xxl: Length
```
功能： 超大宽度类型设备。
Type: Length; Readable: true; Writable: true


### GutterOption

class GutterOption
```cangjie
public class GutterOption { public init(x!: Length, y!: Length) public init(x!: GridRowSizeOption, y!: GridRowSizeOption) }
```
功能： 栅格布局间距类型，用于描述栅格子组件不同方向的间距。



## Structs

### GridRowColumnOption

struct GridRowColumnOption
```cangjie
public struct GridRowColumnOption { public GridRowColumnOption( xs!: Int32 = 12, sm!: Int32 = 12, md!: Int32 = 12, lg!: Int32 = 12, xl!: Int32 = 12, xxl!: Int32 = 12 ) }
```
功能： 栅格在不同宽度设备类型下，栅格列数。



## Enums

### BreakpointsReference

enum BreakpointsReference
```cangjie
public enum BreakpointsReference { | WindowSize | ComponentSize }
```
功能： 设置以窗口为参照或以容器为参照。

#### ComponentSize

```cangjie
ComponentSize
```
功能： 以容器为参照。

#### WindowSize

```cangjie
WindowSize
```
功能： 以窗口为参照。


### GridRowDirection

enum GridRowDirection
```cangjie
public enum GridRowDirection { | GridRowRow | RowReverse }
```
功能： 栅格元素按照行或列方向排列。

#### GridRowRow

```cangjie
GridRowRow
```
功能： 栅格元素按照行方向排列。

#### RowReverse

```cangjie
RowReverse
```
功能： 栅格元素按照逆序行方向排列。



## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 var bgColors: Array<Color> = [Color.RED, Color.GREEN, Color.BLUE, Color.GRAY, Color.RED, Color.GREEN, Color.BLUE, Color.GRAY]
 var currentBp: String = ""
 func build() {
 Column {
 GridRow(
 //设置栅格在不同宽度设备类型下对应的显示栅格列数。
 //xs:最小宽度类型设备 sm:小宽度类型设备 md:中等宽度类型设备。
 //lg:大宽度类型设备 xl:特大宽度类型设备 xxl:超大宽度类型设备。
 columns: GridRowColumnOption(xs: 6, sm: 7, md: 8, lg: 9, xl: 10, xxl: 11),
 //设置栅格布局间距，x代表水平方向，y代表垂直方向。
 gutter: GutterOption(x: 5.vp, y: 10.vp),
 //设置断点值的断点数列以及基于窗口或容器尺寸的相应参照。
 breakpoints: BreakPoints(
 //启用xs,sm,md,lg四个断点
 value: [200.vp, 300.vp, 400.vp], //设置断点位置的单调递增数组。
 reference: BreakpointsReference.WindowSize
 ), //设置为以窗口为参照。
 //设置栅格布局排列方向,按照行方向排列。
 direction: GridRowDirection.GridRowRow
 ) {
 //循环渲染出bgColors对应颜色的栅格
 ForEach(
 bgColors,
 itemGeneratorFunc: {
 color: Color, index: Int64 => GridCol() {
 Row()
 .width(100.percent)
 .height(20.vp)
 }
 .borderWidth(2.vp)
 .borderColor(color)
 .span(1)
 }
 )
 }
 .width(100.percent)
 .height(200)
 .onBreakpointChange({bp => currentBp = bp})
 .alignItems(ItemAlign.Center) //设置GridRow中的GridCol垂直主轴方向对齐方式。这里设置为居中对齐。

 GridRow(
 //设置布局列数为5列
 columns: 5,
 //设置栅格布局间距，水平方向为5vp，垂直方向10vp。
 gutter: GutterOption(x: 5.vp, y: 10.vp),
 //设置断点值的断点数列以及基于窗口或容器尺寸的相应参照。
 breakpoints: BreakPoints(
 //启用xs,sm,md,lg四个断点
 value: [400.vp, 600.vp, 800.vp], //设置断点位置的单调递增数组。
 reference: BreakpointsReference.WindowSize //设置为以窗口为参照。
 ),
 direction: GridRowDirection.GridRowRow
 ) {
 ForEach(
 bgColors,
 itemGeneratorFunc: {
 color: Color, index: Int64 => GridCol() {
 Row()
 .width(100.percent)
 .height(20.vp)
 }
 .borderWidth(2.vp)
 .borderColor(color)
 .span(GridColColumnOption(xs: 2, sm: 3, md: 4, lg: 5, xl: 6, xxl: 7))
 }
 )
 }
 .width(100.percent)
 .height(100.percent)
 .onBreakpointChange({bp => currentBp = bp})
 .alignItems(ItemAlign.Center)
 }
 .margin(left: 10, right: 10, top: 5, bottom: 5)
 .height(400)
 }
}
```

