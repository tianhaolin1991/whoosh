# ohos.pasteboard（剪贴板）

本模块主要提供管理系统剪贴板的能力，为系统复制、粘贴功能提供支持。系统剪贴板支持对文本、HTML、URI、PixelMap等内容的操作。

```cangjie
import kit.BasicServicesKit.*
```
## Functions

### public func createData(mimeType: String, value: String): PasteData

```cangjie
public func createData(mimeType: String, value: String): PasteData
```
功能： 构建一个剪贴板内容对象。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 剪贴板数据对应的MIME类型，可以是常量中已定义的类型，也可以自定义。mimeType取值可以为 MIMETYPE_TEXT_HTML 、 MIMETYPE_TEXT_PLAIN 、 MIMETYPE_TEXT_URI 。 | mimeType | - |
| String | 是 | 字符串数据内容。 | value | - |

**Returns:**

| type | description |
| --- | --- |
| PasteData | 返回剪贴板内容对象。 |

**Example:**

```cangjie
// index.cj

import kit.BasicServicesKit.*

let dataText = "hello"
let pasteData = createData(MIMETYPE_TEXT_PLAIN, dataText)
```

### public func createData(mimeType: String, value: PixelMap): PasteData

```cangjie
public func createData(mimeType: String, value: PixelMap): PasteData
```
功能： 构建一个剪贴板内容对象。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 剪贴板数据对应的MIME类型，可以是常量中已定义的类型，也可以自定义。取值为 MIMETYPE_PIXELMAP 。 | mimeType | - |
| PixelMap | 是 | PixelMap类型的数据内容。 | value | - |

**Returns:**

| type | description |
| --- | --- |
| PasteData | 返回剪贴板内容对象。 |

**Example:**

```cangjie
// index.cj

import kit.BasicServicesKit.*
import ohos.image.*

var globalPixelMap: PixelMap
var color: Array<UInt8> = Array<UInt8>(96) {index: Int64 => UInt8(index + 1)}
var opts = InitializationOptions(editable: true, pixelFormat: PixelMapFormat.RGBA_8888, size: Size(height: 6, width: 8))
globalPixelMap = createPixelMap(color, opts)
let pasteData = createData(MIMETYPE_TEXT_PLAIN, globalPixelMap)
```

### public func createData(mimeType: String, value: Array<UInt8>): PasteData

```cangjie
public func createData(mimeType: String, value: Array<UInt8>): PasteData
```
功能： 构建一个剪贴板内容对象。定义的字符串。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 剪贴板数据对应的MIME类型，为自定义String。 | mimeType | - |
| Array<UInt8> | 是 | 数据内容。 | value | - |

**Returns:**

| type | description |
| --- | --- |
| PasteData | 返回剪贴板内容对象。 |

**Example:**

```cangjie
// index.cj

import kit.BasicServicesKit.*

var dataText = Array<UInt8>(96, repeat: 0)
let pasteData = createData("app/xml", dataText)
```

### public func createRecord(mimeType: String, value: String): PasteDataRecord

```cangjie
public func createRecord(mimeType: String, value: String): PasteDataRecord
```
功能： 创建一条数据内容条目。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 剪贴板数据对应的MIME类型，可以是常量中已定义的类型，也可以自定义。mimeType取值可以为 MIMETYPE_TEXT_HTML 、 MIMETYPE_TEXT_PLAIN 、 MIMETYPE_TEXT_URI 。 | mimeType | - |
| String | 是 | 字符串数据内容。 | value | - |

**Returns:**

| type | description |
| --- | --- |
| PasteDataRecord | 返回新建的数据内容条目。 |

**Example:**

```cangjie
// index.cj

import kit.BasicServicesKit.*

let dataText = "word"
let pasteDataRecord = createRecord(MIMETYPE_TEXT_PLAIN, dataText)
```

### public func createRecord(mimeType: String, value: PixelMap): PasteDataRecord

```cangjie
public func createRecord(mimeType: String, value: PixelMap): PasteDataRecord
```
功能： 创建一条数据内容条目。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 剪贴板数据对应的MIME类型，可以是常量中已定义的类型，也可以自定义。取值为 MIMETYPE_PIXELMAP 。 | mimeType | - |
| PixelMap | 是 | PixelMap类型的数据内容。 | value | - |

**Returns:**

| type | description |
| --- | --- |
| PasteDataRecord | 返回新建的数据内容条目。 |

**Example:**

```cangjie
// index.cj

import ohos.image.*
import kit.BasicServicesKit.*

var globalPixelMap: PixelMap
var color: Array<UInt8> = Array<UInt8>(96) {index: Int64 => UInt8(index + 1)}
var opts = InitializationOptions(editable: true, pixelFormat: PixelMapFormat.RGBA_8888, size: Size(height: 6, width: 8))
globalPixelMap = createPixelMap(color, opts)
let pasteDataRecord1 = createRecord(MIMETYPE_PIXELMAP, globalPixelMap)
```

### public func createRecord(mimeType: String, value: Array<UInt8>): PasteDataRecord

```cangjie
public func createRecord(mimeType: String, value: Array<UInt8>): PasteDataRecord
```
功能： 创建一条数据内容条目。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 剪贴板数据对应的MIME类型，为自定义String。 | mimeType | - |
| Array<UInt8> | 是 | 数据内容。 | value | - |

**Returns:**

| type | description |
| --- | --- |
| PasteDataRecord | 返回新建的数据内容条目。 |

**Example:**

```cangjie
// index.cj

import kit.BasicServicesKit.*

var dataText = Array<UInt8>(96, repeat: 0)
let pasteData = createData("app/xml", dataText)
```

### public func getSystemPasteboard(): SystemPasteboard

```cangjie
public func getSystemPasteboard(): SystemPasteboard
```
功能： 获取系统剪贴板对象。

**Returns:**

| type | description |
| --- | --- |
| SystemPasteboard | 系统剪贴板对象。 |

**Example:**

```cangjie
// index.cj

import kit.BasicServicesKit.*

let sysBoard = getSystemPasteboard()
```


## Classes

### PasteData

class PasteData
```cangjie
public class PasteData {}
```
功能： 剪贴板内容对象。剪贴板内容包含一个或者多个内容条目（PasteDataRecord）以及属性描述对象（PasteDataProperty）。


### PasteDataRecord

class PasteDataRecord
```cangjie
public class PasteDataRecord { public let htmlText: String public let mimeType: String public let plainText: String public let uri: String public let pixelMap: PixelMap }
```
功能： 对于剪贴板中内容记录的抽象定义，称之为条目。剪贴板内容部分由一个或者多个条目构成，例如一条文本内容、一份HTML、一个URI。

#### let htmlText

```cangjie
public let htmlText: String
```
功能： HTML内容。
Type: String; Readable: true; Writable: false

#### let mimeType

```cangjie
public let mimeType: String
```
功能： 数据类型。
Type: String; Readable: true; Writable: false

#### let pixelMap

```cangjie
public let pixelMap: PixelMap
```
功能： PixelMap内容。
Type: PixelMap; Readable: true; Writable: false

#### let plainText

```cangjie
public let plainText: String
```
功能： 纯文本内容。
Type: String; Readable: true; Writable: false

#### let uri

```cangjie
public let uri: String
```
功能： URI内容。
Type: String; Readable: true; Writable: false


### SystemPasteboard

class SystemPasteboard
```cangjie
public class SystemPasteboard {}
```
功能： 系统剪贴板。



## Structs

### PasteDataProperty

struct PasteDataProperty
```cangjie
public struct PasteDataProperty { public PasteDataProperty( public var mimeTypes: Array<String>, public var tag: String, public var timestamp: Int64, public var localOnly!: Bool = false, public var shareOption!: ShareOption = CROSSDEVICE ) }
```
功能： 定义了剪贴板中所有内容条目的属性，包含时间戳、数据类型、粘贴范围以及一些附加数据等。 该属性必须通过setProperty方法，才能设置到剪贴板中。



## Enums

### ShareOption

enum ShareOption
```cangjie
public enum ShareOption { | INAPP | LOCALDEVICE | CROSSDEVICE | ... }
```
功能： 可粘贴数据的范围类型枚举。

#### CROSSDEVICE

```cangjie
CROSSDEVICE
```
功能： 表示允许跨设备在任何应用内粘贴。

#### INAPP

```cangjie
INAPP
```
功能： 表示仅允许同应用内粘贴。

#### LOCALDEVICE

```cangjie
LOCALDEVICE
```
功能： 表示允许在此设备中任何应用内粘贴。



