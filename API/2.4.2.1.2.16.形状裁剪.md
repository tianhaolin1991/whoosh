# 形状裁剪

用于对组件进行裁剪、遮罩处理。

## Functions

### public func clip(isClip: Bool): This

```cangjie
public func clip(isClip: Bool): This
```
功能： 是否对子组件超出当前组件范围外的区域进行裁剪。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否按照父容器边缘轮廓进行裁剪。 初始值：false 说明： true表示进行裁剪，false表示不进行裁剪。 设置为true后，子组件超出当前组件范围外的区域将不响应绑定的手势事件。 | isClip | - |

### public func clip(shape: CircleShape): This

```cangjie
public func clip(shape: CircleShape): This
```
功能： 按 CircleShape 的形状对当前组件进行裁剪。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| CircleShape | 是 | 对当前组件进行裁剪的指定的形状。 | shape | - |

### public func clip(shape: EllipseShape): This

```cangjie
public func clip(shape: EllipseShape): This
```
功能： 按 EllipseShape 的形状对当前组件进行裁剪。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| EllipseShape | 是 | 对当前组件进行裁剪的指定的形状。 | shape | - |

### public func clip(shape: RectShape): This

```cangjie
public func clip(shape: RectShape): This
```
功能： 按 RectShape 的形状对当前组件进行裁剪。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| RectShape | 是 | 对当前组件进行裁剪的指定的形状。 | shape | - |

### public func clip(shape: PathShape): This

```cangjie
public func clip(shape: PathShape): This
```
功能： 按 PathShape 的形状对当前组件进行裁剪。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| PathShape | 是 | 对当前组件进行裁剪的指定的形状。 | shape | - |

### public func mask(shape: CircleShape): This

```cangjie
public func mask(shape: CircleShape): This
```
功能： 为组件上添加 CircleShape 形状的遮罩。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| CircleShape | 是 | 对当前组件进行遮罩指定的形状。 | shape | - |

### public func mask(shape: EllipseShape): This

```cangjie
public func mask(shape: EllipseShape): This
```
功能： 为组件上添加 EllipseShape 形状的遮罩。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| EllipseShape | 是 | 对当前组件进行遮罩指定的形状。 | shape | - |

### public func mask(shape: RectShape): This

```cangjie
public func mask(shape: RectShape): This
```
功能： 为组件上添加 RectShape 形状的遮罩。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| RectShape | 是 | 对当前组件进行遮罩指定的形状。 | shape | - |

### public func mask(shape: PathShape): This

```cangjie
public func mask(shape: PathShape): This
```
功能： 为组件上添加 PathShape 形状的遮罩。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| PathShape | 是 | 对当前组件进行遮罩指定的形状。 | shape | - |

### public func mask(value: ProgressMask): This

```cangjie
public func mask(value: ProgressMask): This
```
功能： 为组件上添加 ProgressMask 形状的遮罩。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ProgressMask | 是 | 在当前组件上加上可动态设置进度、最大值和颜色的遮罩。 | value | - |


## Classes

### ShapeAbstract

class ShapeAbstract
```cangjie
sealed abstract class ShapeAbstract {}
```
功能： CircleShape，RectShape，PathShape，EllipseShape的基类，重新定义了fill，height，offset，size，width等相关的公共成员方法。


### CircleShape

class CircleShape
```cangjie
public class CircleShape <: ShapeAbstract { public init() public init(width!: Length, height!: Length) }
```
功能： 用于clip和mask接口的圆形形状。


### EllipseShape

class EllipseShape
```cangjie
public class EllipseShape <: ShapeAbstract { public init() public init(width!: Length, height!: Length) }
```
功能： 用于clip和mask接口的椭圆形形状。


### RectShape

class RectShape
```cangjie
public class RectShape <: ShapeAbstract { public init() public init(width!: Length, height!: Length) }
```
功能： 用于clip和mask接口的矩形形状。


### PathShape

class PathShape
```cangjie
public class PathShape <: ShapeAbstract { public init() public init(commands!: String) public init(width!: Length, height!: Length, commands!: String = "") }
```
功能： 用于clip和mask接口的自定义绘制路径形状。


### ProgressMask

class ProgressMask
```cangjie
public class ProgressMask { public init(value!:Float32,total!:Float32,color!:Color) }
```
功能： 设置遮罩的进度、最大值和遮罩颜色的形状。



## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*
import kit.LocalizationKit.*

@Entry
@Component
class EntryView {
 func build() {
 Column {
 Text("clip").fontSize(12)
 Row() {
 Image(@r(app.media.startIcon)).width(300).height(180)
 }.clip(true).borderRadius(20)
 Text("first end").fontSize(12)
 Image(@r(app.media.startIcon)).width(300).height(180).clip(CircleShape(width: 280, height:160))
 Text("secend end").fontSize(12)
 Text("mask").fontSize(12)
 Image(@r(app.media.startIcon)).mask(RectShape(width: 500, height: 140).fill(Color.GRAY))
 .width(500).height(140)
 Text("first end").fontSize(12)
 Image(@r(app.media.startIcon)).mask(CircleShape(width: 140, height: 140).fill(Color.GRAY))
 .width(500).height(140)
 Text("secend end").fontSize(12)
 }
 }
}
```

