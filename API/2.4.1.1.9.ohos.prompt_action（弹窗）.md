# ohos.prompt_action（弹窗）

创建并显示文本提示框、对话框和操作菜单。

```cangjie
import kit.UIKit.*
```
## Classes

### PromptAction

class PromptAction
```cangjie
public class PromptAction {}
```
功能： 创建弹窗和响应弹窗动作。


### ActionMenuOptions

class ActionMenuOptions
```cangjie
public open class ActionMenuOptions { public ActionMenuOptions( public let title!: String = '', public let buttons!: Array<ButtonInfo>, public let showInSubWindow!: Bool = false, public let isModal!: Bool = true ) }
```
功能： 操作菜单的选项。

#### let title

```cangjie
public let title: String = ""
```
功能： 表示标题文本。
Type: String; Readable: true; Writable: false

#### let buttons

```cangjie
public let buttons: Array<ButtonInfo>
```
功能： 表示菜单中菜单项按钮的数组，结构为：ButtonInfo("button", Color.BLACK)，支持1-6个按钮。按钮数量大于6个时，仅显示前6个按钮，之后的按钮不显示。
Type: Array< ButtonInfo >; Readable: true; Writable: false

#### let showInSubWindow

```cangjie
public let showInSubWindow: Bool = false
```
功能： 表示某弹框需要显示在主窗口之外时，是否在子窗口显示此弹窗。
Type: Bool; Readable: true; Writable: false

#### let isModal

```cangjie
public let isModal: Bool = true
```
功能： 表示弹窗是否为模态窗口，模态窗口有蒙层，非模态窗口无蒙层。
Type: Bool; Readable: true; Writable: false


### BaseDialogOptions

class BaseDialogOptions
```cangjie
public open class BaseDialogOptions { public let maskRect: Rectangle = Rectangle(x: 0.vp, y: 0.vp, width: 100.percent, height: 100.percent), public let alignment: DialogAlignment = DialogAlignment.Default, public let offset: Offset = Offset(0.vp, 0.vp), public let isModal: Bool = true, public let showInSubWindow: Bool = false, public let maskColor: Color = Color(0x33000000), public let transition: TransitionEffect = TransitionEffect.OPACITY, public let onDidAppear: () -> Unit = { => }, public let onDidDisappear: () -> Unit = { => }, public let onWillAppear: () -> Unit = { => }, public let onWillDisappear: () -> Unit = { => }, public let keyboardAvoidMode: KeyboardAvoidMode = KeyboardAvoidMode.DEFAULT, public let enableHoverMode: Bool = false, public let hoverModeArea: HoverModeAreaType = HoverModeAreaType.BOTTOM_SCREEN }
```
功能： 弹窗的选项。

#### let maskRect

```cangjie
public let maskRect: Rectangle = Rectangle(x: 0.vp, y: 0.vp, width: 100.percent, height: 100.percent)
```
功能： 表示弹窗遮蔽层区域。
Type: Rectangle; Readable: true; Writable: false

#### let alignment

```cangjie
public let alignment: DialogAlignment = DialogAlignment.Default
```
功能： 表示弹窗在竖直方向上的对齐方式。
Type: DialogAlignment; Readable: true; Writable: false

#### let offset

```cangjie
public let offset: Offset = Offset(0.vp, 0.vp)
```
功能： 表示弹窗相对alignment所在位置的偏移量。
Type: offset; Readable: true; Writable: false

#### let isModal

```cangjie
public let isModal: Bool = true
```
功能： 表示弹窗是否为模态窗口。
Type: Bool; Readable: true; Writable: false

#### let showInSubWindow

```cangjie
public let showInSubWindow: Bool = false
```
功能： 表示弹框需要显示在主窗口之外时，是否在子窗口显示此弹窗。
Type: Bool; Readable: true; Writable: false

#### let maskColor

```cangjie
public let maskColor: Color = Color(0x33000000)
```
功能： 表示自定义蒙层颜色。
Type: Color; Readable: true; Writable: false

#### let transition

```cangjie
public let transition: TransitionEffect = TransitionEffect.OPACITY
```
功能： 表示弹窗显示和退出的过渡效果。
Type: TransitionEffect; Readable: true; Writable: false

#### let onDidAppear

```cangjie
public let onDidAppear: () -> Unit = { => }
```
功能： 表示弹窗弹出时的事件回调。
Type: () -> Unit; Readable: true; Writable: false

#### let onDidDisappear

```cangjie
public let onDidDisappear: () -> Unit = { => }
```
功能： 表示弹窗消失时的事件回调。
Type: () -> Unit; Readable: true; Writable: false

#### let onWillAppear

```cangjie
public let onWillAppear: () -> Unit = { => }
```
功能： 表示弹窗显示动效前的事件回调。
Type: () -> Unit; Readable: true; Writable: false

#### let onWillDisappear

```cangjie
public let onWillDisappear: () -> Unit = { => }
```
功能： 表示弹窗退出动效前的事件回调。
Type: () -> Unit; Readable: true; Writable: false

#### let keyboardAvoidMode

```cangjie
public let keyboardAvoidMode: KeyboardAvoidMode = KeyboardAvoidMode.DEFAULT
```
功能： 表示用于设置弹窗是否在拉起软键盘时进行自动避让。
Type: KeyboardAvoidMode; Readable: true; Writable: false

#### let enableHoverMode

```cangjie
public let enableHoverMode: Bool = false
```
功能： 表示是否响应悬停态。
Type: Bool; Readable: true; Writable: false

#### let hoverModeArea

```cangjie
public let hoverModeArea: HoverModeAreaType = HoverModeAreaType.BOTTOM_SCREEN
```
功能： 表示悬停态下弹窗默认展示区域。
Type: HoverModeAreaType; Readable: true; Writable: false


### CustomDialogOptions

class CustomDialogOptions
```cangjie
public class CustomDialogOptions <: BaseDialogOptions { public let builder:() -> Unit = {=>} public let backgroundColor: UInt32 = Color.TRANSPARENT.toUInt32() public let cornerRadius: BorderRadiuses= BorderRadiuses(topLeft: 32.vp, topRight: 32.vp, bottomLeft: 32.vp, bottomRight: 32.vp) public let borderWidth: EdgeWidths = EdgeWidths() public let borderColor: EdgeColor = EdgeColor() public let borderStyle: Option<BorderStyle> = None public let borderEdgeStyle: Option<EdgeStyles> = None public let width: Length = 400.vp public let height: Length = 100.vp public let shadowOption: Option<ShadowOptions> = None public let shadowStyle: Option<ShadowStyle> = None public let backgroundBlurStyle: BlurStyle = BlurStyle.COMPONENT_ULTRA_THICK public init( maskRect!: Rectangle = Rectangle(x: 0.vp, y: 0.vp, width: 100.percent, height: 100.percent), alignment!: DialogAlignment = DialogAlignment.Default, offset!: Offset = Offset(0.vp, 0.vp), isModal!: Bool = true, showInSubWindow!: Bool = false, builder!: ()-> Unit ) public init( maskRect!: Rectangle = Rectangle(x: 0.vp, y: 0.vp, width: 100.percent, height: 100.percent), alignment!: DialogAlignment = DialogAlignment.Default, offset!: Offset = Offset(0.vp, 0.vp), isModal!: Bool = true, showInSubWindow!: Bool = false, builder!: ()-> Unit, autoCancel!: Bool, // 5.1 start maskColor!: Color = Color(0x33000000), transition!: TransitionEffect = TransitionEffect.OPACITY, onDidAppear!: () -> Unit = { => }, onDidDisappear!: () -> Unit = { => }, onWillAppear!: () -> Unit = { => }, onWillDisappear!: () -> Unit = { => }, keyboardAvoidMode!: KeyboardAvoidMode = KeyboardAvoidMode.DEFAULT, enableHoverMode!: Bool = false, hoverModeArea!: HoverModeAreaType = HoverModeAreaType.BOTTOM_SCREEN, backgroundColor!: Color = Color.TRANSPARENT, cornerRadius!: BorderRadiuses = BorderRadiuses(topLeft: 32.vp, topRight: 32.vp, bottomLeft: 32.vp, bottomRight: 32.vp), borderWidth!: EdgeWidths = EdgeWidths(top: 0.vp, right: 0.vp, bottom: 0.vp, left: 0.vp), borderColor!: EdgeColor = EdgeColor(top: Color.BLACK, right: Color.BLACK, bottom: Color.BLACK, left: Color.BLACK), borderStyle!: Option<BorderStyle> = Option.None, borderEdgeStyle!: Option<EdgeStyles> = Option.None, width!: Length = 400.vp, height!: Length = 100.vp, shadowOption!: Option<ShadowOptions> = Option.None, shadowStyle!: Option<ShadowStyle> = Option.None, backgroundBlurStyle!: BlurStyle = BlurStyle.COMPONENT_ULTRA_THICK ) public init() }
```
功能： 自定义弹窗的内容，继承自BaseDialogOptions。

#### let builder

```cangjie
public let builder:() -> Unit = {=>}
```
功能： 表示自定义弹窗的内容。
Type: ()->Unit; Readable: true; Writable: false

#### let backgroundColor

```cangjie
public let backgroundColor: UInt32 = Color.TRANSPARENT.toUInt32()
```
功能： 表示弹窗背板颜色。
Type: UInt32; Readable: true; Writable: false

#### let cornerRadius

```cangjie
public let cornerRadius: BorderRadiuses = BorderRadiuses(topLeft: 32.vp, topRight: 32.vp, bottomLeft: 32.vp, bottomRight: 32.vp)
```
功能： 表示背板的圆角半径。可分别设置4个圆角的半径。圆角大小受组件尺寸限制，最大值为组件宽或高的一半，若值为负，则按照默认值处理。百分比参数方式：以父元素弹窗宽和高的百分比来设置弹窗的圆角。
Type: BorderRadiuses; Readable: true; Writable: false

#### let borderWidth

```cangjie
public let borderWidth: EdgeWidths = EdgeWidths()
```
功能： 表示弹窗背板的边框宽度。可分别设置4个边框宽度。百分比参数方式：以父元素弹窗宽的百分比来设置弹窗的边框宽度。当弹窗左边框和右边框大于弹窗宽度，弹窗上边框和下边框大于弹窗高度，显示可能不符合预期。
Type: EdgeWidths; Readable: true; Writable: false

#### let borderColor

```cangjie
public let borderColor: EdgeColor = EdgeColor()
```
功能： 表示弹窗背板的边框颜色。如果使用borderColor属性，需要和borderWidth属性一起使用。
Type: EdgeColor; Readable: true; Writable: false

#### let borderStyle

```cangjie
public let borderStyle: Option<BorderStyle> = None
```
功能： 表示弹窗背板的边框样式。如果使用borderStyle属性，需要和borderWidth属性一起使用。
Type: Option< BorderStyle >; Readable: true; Writable: false

#### let borderEdgeStyle

```cangjie
public let borderEdgeStyle: Option<EdgeStyles> = None
```
功能： 表示弹窗背板的边框样式。如果使用borderStyle属性，需要和borderWidth属性一起使用。
Type: Option< EdgeStyles >; Readable: true; Writable: false

#### let width

```cangjie
public let width: Length = 400.vp
```
功能： 表示弹窗背板的宽度。
Type: Length; Readable: true; Writable: false

#### let height

```cangjie
public let height: Length = 100.vp
```
功能： 表示弹窗背板的高度。
Type: Length; Readable: true; Writable: false

#### let shadowOption

```cangjie
public let shadowOption: Option<ShadowOptions> = None
```
功能： 表示弹窗背板阴影。
Type: Option< ShadowOptions >; Readable: true; Writable: false

#### let shadowStyle

```cangjie
public let shadowStyle: Option<ShadowStyle> = None
```
功能： 表示弹窗背板阴影。
Type: Option< ShadowStyle >; Readable: true; Writable: false

#### let backgroundBlurStyle

```cangjie
public let backgroundBlurStyle: BlurStyle = BlurStyle.COMPONENT_ULTRA_THICK
```
功能： 表示弹窗背板模糊材质。
Type: BlurStyle; Readable: true; Writable: false


### ShowDialogOptions

class ShowDialogOptions
```cangjie
public open class ShowDialogOptions { public ShowDialogOptions( public let title!: String = '', public let message!: String = '', public let buttons!: Array<ButtonInfo> = [ButtonInfo("button", Color(0x31463146))], public let alignment!: DialogAlignment = DialogAlignment.Default, public let offset!: Offset = Offset(0.vp, 0.vp), public let maskRect!: Rectangle = Rectangle(x: 0.vp, y: 0.vp, width: 100.percent, height: 100.percent), public let showInSubWindow!: Bool = false, public let isModal!: Bool = true, public let backgroundColor!: Color = Color.TRANSPARENT, public let backgroundBlurStyle!: BlurStyle = BlurStyle.COMPONENT_ULTRA_THICK, public let shadowOption!: Option<ShadowOptions> = Option.None, public let shadowStyle!: Option<ShadowStyle> = Option.None, public let enableHoverMode!: Bool = false, public let hoverModeArea!: HoverModeAreaType = HoverModeAreaType.BOTTOM_SCREEN ) }
```
功能： 对话框的选项。

#### let title

```cangjie
public let title: String = ""
```
功能： 表示标题文本。
Type: String; Readable: true; Writable: false

#### let message

```cangjie
public let message: String = ""
```
功能： 表示内容文本。
Type: String; Readable: true; Writable: false

#### let buttons

```cangjie
public let buttons: Array<ButtonInfo> = [ButtonInfo("button", Color(0x31463146))]
```
功能： 表示对话框中按钮的数组。
Type: Array< ButtonInfo >; Readable: true; Writable: false

#### let alignment

```cangjie
public let alignment: DialogAlignment = DialogAlignment.Default
```
功能： 表示弹窗在竖直方向上的对齐方式。
Type: DialogAlignment; Readable: true; Writable: false

#### let offset

```cangjie
public let offset: Offset = Offset(0.vp, 0.vp)
```
功能： 表示弹窗相对alignment所在位置的偏移量。
Type: Offset; Readable: true; Writable: false

#### let maskRect

```cangjie
public let maskRect: Rectangle = Rectangle(x: 0.vp, y: 0.vp, width: 100.percent, height: 100.percent)
```
功能： 表示弹窗遮蔽层区域。
Type: Rectangle; Readable: true; Writable: false

#### let showInSubWindow

```cangjie
public let showInSubWindow: Bool = false
```
功能： 表示弹框需要显示在主窗口之外时，是否在子窗口显示此弹窗。
Type: Bool; Readable: true; Writable: false

#### let isModal

```cangjie
public let isModal: Bool = true
```
功能： 表示弹窗是否为模态窗口。
Type: Bool; Readable: true; Writable: false

#### let backgroundColor

```cangjie
public let backgroundColor: Color = Color.TRANSPARENT
```
功能： 表示弹窗背板颜色。
Type: Color; Readable: true; Writable: false

#### let backgroundBlurStyle

```cangjie
public let backgroundBlurStyle: BlurStyle = BlurStyle.COMPONENT_ULTRA_THICK
```
功能： 表示弹窗背板模糊材质。
Type: BlurStyle; Readable: true; Writable: false

#### let shadowOption

```cangjie
public let shadowOption: Option<ShadowOptions> = Option.None
```
功能： 表示弹窗背板阴影。
Type: Option< ShadowOptions >; Readable: true; Writable: false

#### let shadowStyle

```cangjie
public let shadowStyle: Option<ShadowStyle> = Option.None
```
功能： 表示弹窗背板阴影。
Type: Option< ShadowStyle >; Readable: true; Writable: false

#### let enableHoverMode

```cangjie
public let enableHoverMode: Bool = false
```
功能： 是否响应悬停态。
Type: Bool; Readable: true; Writable: false

#### let hoverModeArea

```cangjie
public let hoverModeArea: HoverModeAreaType = HoverModeAreaType.BOTTOM_SCREEN
```
功能： 悬停态下弹窗默认展示区域。
Type: HoverModeAreaType; Readable: true; Writable: false


### ShowToastOptions

class ShowToastOptions
```cangjie
public class ShowToastOptions { public ShowToastOptions( public let message!: String = 'ShowToast', public let duration!: UInt32 = 1500, public let bottom!: String = '80vp', public let showMode!: ToastShowMode = ToastShowMode.Default, public let alignment!: Alignment = Alignment.Bottom, public let offset!: Offset = Offset(0.vp, 0.vp), public let backgroundColor!: Color = Color.TRANSPARENT, public let textColor!: Color = Color.BLACK, public let backgroundBlurStyle!: BlurStyle = BlurStyle.COMPONENT_ULTRA_THICK, public let shadowOption!: Option<ShadowOptions> = Option.None, public let shadowStyle!: Option<ShadowStyle> = Option.None, public let enableHoverMode!: Bool = false, public let hoverModeArea!: HoverModeAreaType = HoverModeAreaType.BOTTOM_SCREEN ) }
```
功能： 文本提示框的选项。

#### let message

```cangjie
public let message: String = "ShowToast"
```
功能： 表示显示的文本信息。
Type: String; Readable: true; Writable: false

#### let duration

```cangjie
public let duration: UInt32 = 1500
```
功能： 表示弹窗持续时间。
Type: UInt32; Readable: true; Writable: false

#### let bottom

```cangjie
public let bottom: String = "80vp"
```
功能： 表示弹窗底部边框距离导航条的高度。
Type: String; Readable: true; Writable: false

#### let showMode

```cangjie
public let showMode: ToastShowMode = ToastShowMode.Default
```
功能： 表示弹窗是否显示在应用之上。
Type: ToastShowMode; Readable: true; Writable: false

#### let alignment

```cangjie
public let alignment: Alignment = Alignment.Bottom
```
功能： 表示弹窗对齐方式。
Type: Alignment; Readable: true; Writable: false

#### let offset

```cangjie
public let offset: Offset = Offset(0.vp, 0.vp)
```
功能： 表示弹窗在对齐方式上的偏移。
Type: Offset; Readable: true; Writable: false

#### let backgroundColor

```cangjie
public let backgroundColor: Color = Color.TRANSPARENT
```
功能： 表示文本提示框背板颜色。
Type: Color; Readable: true; Writable: false

#### let textColor

```cangjie
public let textColor: Color = Color.BLACK
```
功能： 表示文本提示框文本颜色。
Type: Color; Readable: true; Writable: false

#### let backgroundBlurStyle

```cangjie
public let backgroundBlurStyle: BlurStyle = BlurStyle.COMPONENT_ULTRA_THICK
```
功能： 表示文本提示框背板模糊材质。
Type: BlurStyle; Readable: true; Writable: false

#### let shadowOption

```cangjie
public let shadowOption: Option<ShadowOptions> = Option.None
```
功能： 表示文本提示框背板阴影。
Type: Option< ShadowOptions >; Readable: true; Writable: false

#### let shadowStyle

```cangjie
public let shadowStyle: Option<ShadowStyle> = Option.None
```
功能： 表示文本提示框背板阴影。
Type: Option< ShadowStyle >; Readable: true; Writable: false

#### let enableHoverMode

```cangjie
public let enableHoverMode: Bool = false
```
功能： 表示弹窗是否响应悬停态。
Type: Bool; Readable: true; Writable: false

#### let hoverModeArea

```cangjie
public let hoverModeArea: HoverModeAreaType = HoverModeAreaType.BOTTOM_SCREEN
```
功能： 表示响应悬停态时，弹窗的显示区域。
Type: HoverModeAreaType; Readable: true; Writable: false


### ButtonInfo

class ButtonInfo
```cangjie
public class ButtonInfo { public let text: String public let color: UInt32 public let primary: Bool public init(text: String, color: UInt32, primary!: Bool = false) public init(text: String, color: Color, primary!: Bool = false) }
```
功能： 菜单中的菜单项按钮。

#### let color

```cangjie
public let color: UInt32
```
功能： 表示按钮文本颜色。
Type: UInt32; Readable: true; Writable: false

#### let primary

```cangjie
public let primary: Bool
```
功能： 表示在弹窗获焦且未进行tab键走焦时，按钮是否默认响应Enter键。多个Button时，只允许一个Button的该字段配置为true，否则所有Button均不响应。多重弹窗可自动获焦连续响应。
Type: Bool; Readable: true; Writable: false

#### let text

```cangjie
public let text: String
```
功能： 表示按钮文本内容。
Type: String; Readable: true; Writable: false


### EdgeColor

class EdgeColor
```cangjie
public class EdgeColor { public var top: Color public var right: Color public var bottom: Color public var left: Color public init(top!: Color = Color.BLACK, right!: Color = Color.BLACK, bottom!: Color = Color.BLACK, left!: Color = Color.BLACK) }
```
功能： 边框颜色，用于描述组件边框四条边的颜色。

#### var bottom

```cangjie
public var bottom: Color
```
功能： 设置组件下边框颜色。
Type: Color; Readable: true; Writable: true

#### var left

```cangjie
public var left: Color
```
功能： 设置组件左边框颜色。
Type: Color; Readable: true; Writable: true

#### var right

```cangjie
public var right: Color
```
功能： 设置组件右边框颜色。
Type: Color; Readable: true; Writable: true

#### var top

```cangjie
public var top: Color
```
功能： 设置组件上边框颜色。
Type: Color; Readable: true; Writable: true



## Enums

### HoverModeAreaType

enum HoverModeAreaType
```cangjie
public enum HoverModeAreaType { | TOP_SCREEN | BOTTOM_SCREEN }
```
功能： 悬停态显示区域类型。

#### BOTTOM_SCREEN

```cangjie
BOTTOM_SCREEN
```
功能： 下半屏。

#### TOP_SCREEN

```cangjie
TOP_SCREEN
```
功能： 上半屏。


### KeyboardAvoidMode

enum KeyboardAvoidMode
```cangjie
public enum KeyboardAvoidMode { | DEFAULT | NONE }
```
功能： 弹窗是否在拉起软键盘时进行自动避让。

#### DEFAULT

```cangjie
DEFAULT
```
功能： 默认避让软键盘并在到达极限高度之后进行高度压缩。

#### NONE

```cangjie
NONE
```
功能： 不避让软键盘。


### ShadowStyle

enum ShadowStyle
```cangjie
public enum ShadowStyle { | OUTER_DEFAULT_XS | OUTER_DEFAULT_SM | OUTER_DEFAULT_MD | OUTER_DEFAULT_LG | OUTER_FLOATING_SM | OUTER_FLOATING_MD }
```
功能： 阴影类型。

#### OUTER_DEFAULT_LG

```cangjie
OUTER_DEFAULT_LG
```
功能： 大阴影。

#### OUTER_DEFAULT_MD

```cangjie
OUTER_DEFAULT_MD
```
功能： 中阴影。

#### OUTER_DEFAULT_SM

```cangjie
OUTER_DEFAULT_SM
```
功能： 小阴影。

#### OUTER_DEFAULT_XS

```cangjie
OUTER_DEFAULT_XS
```
功能： 超小阴影。

#### OUTER_FLOATING_MD

```cangjie
OUTER_FLOATING_MD
```
功能： 浮动中阴影。

#### OUTER_FLOATING_SM

```cangjie
OUTER_FLOATING_SM
```
功能： 浮动小阴影。


### ToastShowMode

enum ToastShowMode
```cangjie
public enum ToastShowMode { | Default | TopMost }
```
功能： 设置弹窗显示模式，默认显示在应用内，支持显示在应用之上。

#### Default

```cangjie
Default
```
功能： Toast 显示在应用内。

#### TopMost

```cangjie
TopMost
```
功能： Toast 显示在应用之上。



## Types

### ShowActionMenuCallBack

type ShowActionMenuCallBack
```cangjie
public type ShowActionMenuCallBack = AsyncCallback<Int32>
```
功能： 菜单响应结果回调。Int32类型参数为选中按钮在buttons数组中的索引，从0开始。


### ShowDialogCallBack

type ShowDialogCallBack
```cangjie
public type ShowDialogCallBack = AsyncCallback<Int32>
```
功能： 对话框响应结果回调。Int32类型参数为选中按钮在buttons数组中的索引。



## Examples

```cangjie
package ohos_app_cangjie_entry

import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 func build() {
 Column {
 Text("Show toast!")
 .fontSize(50.px)
 .margin(10.px)
 .onClick({evt =>
 PromptAction.showToast(message: "hello world")
 })
 }
 }
}
```
```cangjie
package ohos_app_cangjie_entry

import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 @State var index0: Int32 = 0
 func build() {
 Column {
 Text("Show dialog! index: " + index0.toString())
 .fontSize(50.px)
 .margin(10.px)
 .onClick({evt =>
 let buttons: Array<ButtonInfo> = [
 ButtonInfo("yes", Color.BLACK),
 ButtonInfo("no", Color.BLACK)
 ]
 PromptAction.showDialog(title: "select", message: "yes or no", buttons: buttons, callback: { err: Option<AsyncError>, i: Option<Int32> =>
 try {
 match (err) {
 case Some(e) => AppLog.error("error: errcode is ${e.code}")
 case _ => index0 = i.getOrThrow()
 }
 } catch (e: Exception) {
 AppLog.error(e.toString())
 }
 })
 })
 }
 }
}
```
```cangjie
package ohos_app_cangjie_entry

import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 @State var index1: Int32 = 0
 func build() {
 Column {
 Text("Show action menu! index: " + index1.toString())
 .fontSize(50.px)
 .margin(10.px)
 .onClick({evt =>
 let buttons: Array<ButtonInfo> = [
 ButtonInfo("a", Color.BLACK),
 ButtonInfo("b", Color.BLACK)
 ]
 PromptAction.showActionMenu(title: "menu", buttons: buttons, callback: { err: Option<AsyncError>, i: Option<Int32> =>
 try {
 match (err) {
 case Some(e) => AppLog.error("error: errcode is ${e.code}")
 case _ => index1 = i.getOrThrow()
 }
 } catch (e: Exception) {
 AppLog.error(e.toString())
 }
 })
 })
 }
 }
}
```
```cangjie
package ohos_app_cangjie_entry

import kit.UIKit.*
import ohos.state_macro_manage.*

var customdialogId: Int32 = 0

@Entry
@Component
public class EntryView {
 @Builder
 func CustomDialog() {
 Column() {
 Text("Hello")
 .height(50.vp)
 Button("Close")
 .onClick({
 => PromptAction.closeCustomDialog(customdialogId)
 })
 }
 .margin(10.vp)
 }

 func build() {
 Flex(FlexParams(justifyContent: FlexAlign.Center ,alignItems: ItemAlign.Center)) {
 Button("openDialog")
 .onClick({
 => PromptAction.openCustomDialog(
 CustomDialogOptions(builder: bind(this.CustomDialog, this)),
 {
 id => customdialogId = id
 }
 )
 })
 }
 .height(70.percent)
 }
}
```

