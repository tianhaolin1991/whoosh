# ohos.image（图片处理）

本模块提供图片处理效果，包括通过属性创建PixelMap、读取图像像素数据、读取区域内的图片数据等。

```cangjie
import kit.ImageKit.*
```
## Functions

### public func CreateIncrementalSource(buf: Array<UInt8>, options!: SourceOptions = SourceOptions()): ImageSource

```cangjie
public func CreateIncrementalSource(buf: Array<UInt8>, options!: SourceOptions = SourceOptions()): ImageSource
```
功能： 通过缓冲区以增量的方式创建图片源实例，IncrementalSource不支持读写Exif信息。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array<UInt8> | 是 | 增量数据， 这个参数实际不生效。 | buf | - |
| SourceOptions | 否 | 命名参数。 图片属性，包括图片序号与默认属性值。 | options | SourceOptions() |

**Returns:**

| type | description |
| --- | --- |
| ImageSource | 返回图片源。 |

**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

let buf = Array<UInt8>(1, repeat: 0)
let imageSource = CreateIncrementalSource(buf)
```

### public func createImageCreator(width: Int32, height: Int32, format: Int32, capacity: Int32): ImageCreator

```cangjie
public func createImageCreator(width: Int32, height: Int32, format: Int32, capacity: Int32): ImageCreator
```
功能： 创建ImageCreator实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 图像的默认宽度。 | width | - |
| Int32 | 是 | 图像的默认高度。 | height | - |
| Int32 | 是 | 图像格式，如YCBCR_422_SP，JPEG。 | format | - |
| Int32 | 是 | 同时访问的最大图像数。 | capacity | - |

**Returns:**

| type | description |
| --- | --- |
| ImageCreator | 返回ImageCreator实例。 |

**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

var imageCreator = createImageCreator(8192, 8, 4, 8)
```

### public func createImageCreator(size: Size, format: Int32, capacity: Int32): ImageCreator

```cangjie
public func createImageCreator(size: Size, format: Int32, capacity: Int32): ImageCreator
```
功能： 创建ImageCreator实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Size | 是 | 图像的默认大小。 | size | - |
| Int32 | 是 | 图像格式，如YCBCR_422_SP，JPEG。 | format | - |
| Int32 | 是 | 同时访问的最大图像数。 | capacity | - |

**Returns:**

| type | description |
| --- | --- |
| ImageCreator | 返回ImageCreator实例。 |

**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

let size = Size(height: 8192, width: 8)
let imageCreator = createImageCreator(size, 4, 8)
```

### public func createImagePacker(): ImagePacker

```cangjie
public func createImagePacker(): ImagePacker
```
功能： 创建ImagePacker实例。

**Returns:**

| type | description |
| --- | --- |
| ImagePacker | 返回ImagePacker实例。 |

**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

let imagePacker : ImagePacker = createImagePacker()
```

### public func createImageReceiver(width: Int32, height: Int32, format: ImageFormat, capacity: Int32): ImageReceiver

```cangjie
public func createImageReceiver(width: Int32, height: Int32, format: ImageFormat, capacity: Int32): ImageReceiver
```
功能： 通过宽、高、图片格式、容量创建ImageReceiver实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 图像的默认宽度。 | width | - |
| Int32 | 是 | 图像的默认高度。 | height | - |
| ImageFormat | 是 | 图像格式，取值为 ImageFormat 常量（目前仅支持ImageFormat:JPEG）。 | format | - |
| Int32 | 是 | 同时访问的最大图像数。 | capacity | - |

**Returns:**

| type | description |
| --- | --- |
| ImageReceiver | 如果操作成功，则返回ImageReceiver实例。 |

**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

let receiver : ImageReceiver = createImageReceiver(8192, 8, ImageFormat.JPEG, 8)
```

### public func createImageSource(uri: String): ImageSource

```cangjie
public func createImageSource(uri: String): ImageSource
```
功能： 通过传入的URI创建图片源实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 图片路径，当前仅支持应用沙箱路径。 当前支持格式有：.jpg .png .gif .bmp .webp RAW SVG 。 | uri | - |

**Returns:**

| type | description |
| --- | --- |
| ImageSource | 返回ImageSource类实例 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 Image错误码 。

| code | description |
| --- | --- |
| 62980104 | Image initialization abnormal. |
| 62980115 | Invalid image parameter. |


**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

let path: String = "../test.jpg"
let imageSourceApi: ImageSource = createImageSource(path)
```

### public func createImageSource(uri: String, options: SourceOptions): ImageSource

```cangjie
public func createImageSource(uri: String, options: SourceOptions): ImageSource
```
功能： 通过传入的URI创建图片源实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 图片路径，当前仅支持应用沙箱路径。 当前支持格式有：.jpg .png .gif .bmp .webp RAW SVG 。 | uri | - |
| SourceOptions | 是 | 图片属性，包括图片序号与默认属性值。 | options | - |

**Returns:**

| type | description |
| --- | --- |
| ImageSource | 返回ImageSource类实例 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 Image错误码 。

| code | description |
| --- | --- |
| 62980104 | Image initialization abnormal. |
| 62980115 | Invalid image parameter. |


**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

let sourceOptions: SourceOptions = SourceOptions(sourceDensity: 120)
let imageSource: ImageSource = createImageSource("test.png", sourceOptions)
```

### public func createImageSource(fd: Int32): ImageSource

```cangjie
public func createImageSource(fd: Int32): ImageSource
```
功能： 通过传入文件描述符来创建图片源实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 文件描述符fd。 | fd | - |

**Returns:**

| type | description |
| --- | --- |
| ImageSource | 返回ImageSource类实例。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 Image错误码 。

| code | description |
| --- | --- |
| 62980104 | Image initialization abnormal. |
| 62980115 | Invalid image parameter. |


**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

let imageSourceApi : ImageSource = createImageSource(0)
```

### public func createImageSource(fd: Int32, options: SourceOptions): ImageSource

```cangjie
public func createImageSource(fd: Int32, options: SourceOptions): ImageSource
```
功能： 通过传入文件描述符来创建图片源实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 文件描述符fd。 | fd | - |
| SourceOptions | 是 | 图片属性，包括图片序号与默认属性值。 | options | - |

**Returns:**

| type | description |
| --- | --- |
| ImageSource | 返回ImageSource类实例。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 Image错误码 。

| code | description |
| --- | --- |
| 62980104 | Image initialization abnormal. |
| 62980115 | Invalid image parameter. |


**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

let sourceOptions: SourceOptions = SourceOptions(sourceDensity: 120)
let imageSource: ImageSource = createImageSource(0, sourceOptions)
```

### public func createImageSource(buf: Array<UInt8>): ImageSource

```cangjie
public func createImageSource(buf: Array<UInt8>): ImageSource
```
功能： 通过缓冲区创建图片源实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array<UInt8> | 是 | 图像缓冲区数组。 | buf | - |

**Returns:**

| type | description |
| --- | --- |
| ImageSource | 返回ImageSource类实例。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 Image错误码 。

| code | description |
| --- | --- |
| 62980104 | Image initialization abnormal. |
| 62980115 | Invalid image parameter. |


**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

let buf: Array<UInt8> = Array<UInt8>(96, repeat: 0) //96为需要创建的像素buffer大小，取值为：height * width *4
let imageSourceApi: ImageSource = createImageSource(buf)
```

### public func createImageSource(buf: Array<UInt8>, options: SourceOptions): ImageSource

```cangjie
public func createImageSource(buf: Array<UInt8>, options: SourceOptions): ImageSource
```
功能： 通过缓冲区创建图片源实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array<UInt8> | 是 | 图像缓冲区数组。 | buf | - |
| SourceOptions | 是 | 图片属性，包括图片序号与默认属性值。 | options | - |

**Returns:**

| type | description |
| --- | --- |
| ImageSource | 返回ImageSource类实例。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 Image错误码 。

| code | description |
| --- | --- |
| 62980104 | Image initialization abnormal. |
| 62980115 | Invalid image parameter. |


**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

let data: Array<UInt8> = Array<UInt8>(112, repeat: 0)
let sourceOptions: SourceOptions = SourceOptions(sourceDensity: 120)
let imageSourceApi: ImageSource = createImageSource(data, sourceOptions)
```

### public func createImageSource(rawfile: RawFileDescriptor, options!: SourceOptions = SourceOptions()): ImageSource

```cangjie
public func createImageSource(rawfile: RawFileDescriptor, options!: SourceOptions = SourceOptions()): ImageSource
```
功能： 通过图像资源文件的RawFileDescriptor创建图片源实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| RawFileDescriptor | 是 | 图像资源文件的RawFileDescriptor。 | rawfile | - |
| SourceOptions | 否 | 命名参数。 图片属性，包括图片像素密度、像素格式和图片尺寸。 | options | SourceOptions() |

**Returns:**

| type | description |
| --- | --- |
| ImageSource | 返回ImageSource类实例。 |

**Example:**

```cangjie
// index.cj

import kit.ImageKit.*
import ohos.resource_manager.ResourceManager
import ohos.base.*

let resourceManager = ResourceManager.getResourceManager(Global.getStageContext()) // 需获取Context应用上下文，详见本文使用说明
try {
 let rawfd = resourceManager.getRawFd("test.png")
 createImageSource(rawfd)
} catch (e: BusinessException) {
 let code = e.code
 let message = e.message
 AppLog.info("getRawFd failed, error code: ${code}, message: ${message}.")
}
```

### public func createPixelMap(colors: Array<UInt8>, opts: InitializationOptions): PixelMap

```cangjie
public func createPixelMap(colors: Array<UInt8>, opts: InitializationOptions): PixelMap
```
功能： 通过属性创建PixelMap，默认采用BGRA_8888格式处理数据，目前只支持BGRA_8888格式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array<UInt8> | 是 | BGRA_8888格式的颜色数组。 | colors | - |
| InitializationOptions | 是 | 创建像素的属性，包括透明度，尺寸，缩略值，像素格式和是否可编辑。 | opts | - |

**Returns:**

| type | description |
| --- | --- |
| PixelMap | 返回Pixelmap。 当创建的pixelmap大小超过原图大小时，返回原图pixelmap大小。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 Image错误码 。

| code | description |
| --- | --- |
| 62980096 | If the operation failed. |


**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

let color: Array<UInt8> = Array<UInt8>(96, repeat: 0) //96为需要创建的像素buffer大小，取值为：height * width *4
let opts: InitializationOptions = InitializationOptions(editable: true, pixelFormat: RGBA_8888,
 size: Size(height: 4, width: 6))
let pixelMap = createPixelMap(color, opts)
```

### public func createPixelMap(opts: InitializationOptions): PixelMap

```cangjie
public func createPixelMap(opts: InitializationOptions): PixelMap
```
功能： 通过属性创建PixelMap。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| InitializationOptions | 是 | 创建像素的属性，包括透明度、尺寸、缩略值、像素格式和是否可编辑。 | opts | - |

**Returns:**

| type | description |
| --- | --- |
| PixelMap | 成功同步返回PixelMap对象，失败抛出异常。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 401 | Parameter error. Possible causes: 1.Mandatory parameters are left unspecified. 2.Incorrect parameter types. 3.Parameter verification failed |


**Example:**

```cangjie
// index.cj

import kit.ImageKit.*

let opts = InitializationOptions(editable: true, pixelFormat: RGBA_8888,
 size: Size(height: 4, width: 6))
let pixelMap = createPixelMap(opts)
```

### public func createPremultipliedPixelMap(src: PixelMap, dst: PixelMap): Unit

```cangjie
public func createPremultipliedPixelMap(src: PixelMap, dst: PixelMap): Unit
```
功能： 将PixelMap的透明通道非预乘模式转变为预乘模式，转换后的数据存入目标PixelMap。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| PixelMap | 是 | 源PixelMap对象。 | src | - |
| PixelMap | 是 | 目标PixelMap对象。 | dst | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 Image错误码 。

| code | description |
| --- | --- |
| 401 | Parameter error. Possible causes: 1.Mandatory parameters are left unspecified. 2.Incorrect parameter types. 3.Parameter verification failed |
| 62980103 | The image data is not supported |
| 62980246 | Failed to read the pixelMap |
| 62980248 | Pixelmap not allow modify |


### public func createUnpremultipliedPixelMap(src: PixelMap, dst: PixelMap): Unit

```cangjie
public func createUnpremultipliedPixelMap(src: PixelMap, dst: PixelMap): Unit
```
功能： 将PixelMap的透明通道预乘模式转变为非预乘模式，转换后的数据存入目标PixelMap。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| PixelMap | 是 | 源PixelMap对象。 | src | - |
| PixelMap | 是 | 目标PixelMap对象。 | dst | - |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 Image错误码 。

| code | description |
| --- | --- |
| 401 | Parameter error. Possible causes: 1.Mandatory parameters are left unspecified. 2.Incorrect parameter types. 3.Parameter verification failed |
| 62980103 | The image data is not supported |
| 62980246 | Failed to read the pixelMap |
| 62980248 | Pixelmap not allow modify |



## Classes

### DecodingOptions

class DecodingOptions
```cangjie
public class DecodingOptions { public let sampleSize: UInt32 public let rotate: UInt32 public let editable: Bool public let desiredSize: Size public let desiredRegion: Region public let desiredPixelFormat: PixelMapFormat public let index: UInt32 public let fitDensity: Int32 public let desiredColorSpace: ?ColorSpaceManager public let desiredDynamicRange: DecodingDynamicRange public init(sampleSize!: UInt32 = 1, rotate!: UInt32 = 0, editable!: Bool = false, desiredSize!: Size = Size(height: 0, width: 0), desiredRegion!: Region = Region(Size(height: 0, width: 0), 0, 0), desiredPixelFormat!: PixelMapFormat = UNKNOWN, index!: UInt32 = 0, fitDensity!: Int32 = 0, desiredColorSpace!: ?ColorSpaceManager = None) public init(desiredDynamicRange: DecodingDynamicRange, sampleSize!: UInt32 = 1, rotate!: UInt32 = 0, editable!: Bool = false, desiredSize!: Size = Size(height: 0, width: 0), desiredRegion!: Region = Region(Size(height: 0, width: 0), 0, 0), desiredPixelFormat!: PixelMapFormat = UNKNOWN, index!: UInt32 = 0, fitDensity!: Int32 = 0, desiredColorSpace!: ?ColorSpaceManager = None) }
```
功能： 图像解码设置选项。

#### let desiredColorSpace

```cangjie
public let desiredColorSpace: ?ColorSpaceManager = None
```
功能： 目标色彩空间。
Type: ? ColorSpaceManager; Readable: true; Writable: false

#### let desiredDynamicRange

```cangjie
public let desiredDynamicRange: DecodingDynamicRange = SDR
```
功能： 目标动态范围，默认值为SDR。
Type: DecodingDynamicRange; Readable: true; Writable: false

#### let desiredPixelFormat

```cangjie
public let desiredPixelFormat: PixelMapFormat = UNKNOWN
```
功能： 解码的像素格式。
Type: PixelMapFormat; Readable: true; Writable: false

#### let desiredRegion

```cangjie
public let desiredRegion: Region = Region(Size(height: 0, width: 0), 0, 0)
```
功能： 解码区域。
Type: Region; Readable: true; Writable: false

#### let desiredSize

```cangjie
public let desiredSize: Size = Size(height: 0, width: 0)
```
功能： 期望输出大小。
Type: Size; Readable: true; Writable: false

#### let editable

```cangjie
public let editable: Bool = false
```
功能： 是否可编辑。当取值为false时，图片不可二次编辑，如crop等操作将失败。
Type: Bool; Readable: true; Writable: false

#### let fitDensity

```cangjie
public let fitDensity: Int32 = 0
```
功能： 图像像素密度，单位为ppi。
Type: Int32; Readable: true; Writable: false

#### let index

```cangjie
public let index: UInt32 = 0
```
功能： 解码图片序号。
Type: UInt32; Readable: true; Writable: false

#### let rotate

```cangjie
public let rotate: UInt32 = 0
```
功能： 旋转角度。
Type: UInt32; Readable: true; Writable: false

#### let sampleSize

```cangjie
public let sampleSize: UInt32 = 1
```
功能： 缩略图采样大小，当前只能取1。
Type: UInt32; Readable: true; Writable: false


### GainmapChannel

class GainmapChannel
```cangjie
public class GainmapChannel { public GainmapChannel( public var gainmapMax: Float32, public var gainmapMin: Float32, public var gamma: Float32, public var baseOffset: Float32, public var alternateOffset: Float32 ) }
```
功能： Gainmap图单个通道的数据内容，参考ISO 21496-1。

#### var alternateOffset

```cangjie
public var alternateOffset: Float32
```
功能： 提取的可选择图像偏移量，参考ISO 21496-1。
Type: Float32; Readable: true; Writable: true

#### var baseOffset

```cangjie
public var baseOffset: Float32
```
功能： 基础图的偏移，参考ISO 21496-1。
Type: Float32; Readable: true; Writable: true

#### var gainmapMax

```cangjie
public var gainmapMax: Float32
```
功能： 增强图像的最大值，参考ISO 21496-1。
Type: Float32; Readable: true; Writable: true

#### var gainmapMin

```cangjie
public var gainmapMin: Float32
```
功能： 增强图像的最小值，参考ISO 21496-1。
Type: Float32; Readable: true; Writable: true

#### var gamma

```cangjie
public var gamma: Float32
```
功能： gamma值，参考ISO 21496-1。
Type: Float32; Readable: true; Writable: true


### HdrGainmapMetadata

class HdrGainmapMetadata
```cangjie
public class HdrGainmapMetadata { public HdrGainmapMetadata( public var writerVersion: UInt16, public var miniVersion: UInt16, public var gainmapChannelCount: UInt8, public var useBaseColorFlag: Bool, public var baseHeadroom: Float32, public var alternateHeadroom: Float32, public var channels: Array<GainmapChannel> ) }
```
功能： Gainmap使用的元数据值， HdrMetadataKey 中HDR_GAINMAP_METADATA关键字对应的值，参考ISO 21496-1。

#### var alternateHeadroom

```cangjie
public var alternateHeadroom: Float32
```
功能： 提取的可选择图像提亮比，参考ISO 21496-1。
Type: Float32; Readable: true; Writable: true

#### var baseHeadroom

```cangjie
public var baseHeadroom: Float32
```
功能： 基础图提亮比，参考ISO 21496-1。
Type: Float32; Readable: true; Writable: true

#### var channels

```cangjie
public var channels: Array<GainmapChannel>
```
功能： 各通道的数据，长度为3，参考ISO 21496-1。
Type: Array< GainmapChannel >; Readable: true; Writable: true

#### var gainmapChannelCount

```cangjie
public var gainmapChannelCount: UInt8
```
功能： Gainmap的颜色通道数，值为3时RGB通道的元数据值不同，值为1时各通道元数据值相同，参考ISO 21496-1。
Type: UInt8; Readable: true; Writable: true

#### var miniVersion

```cangjie
public var miniVersion: UInt16
```
功能： 元数据解析需要理解的最小版本。
Type: UInt16; Readable: true; Writable: true

#### var useBaseColorFlag

```cangjie
public var useBaseColorFlag: Bool
```
功能： 是否使用基础图的色彩空间，参考ISO 21496-1。
Type: Bool; Readable: true; Writable: true

#### var writerVersion

```cangjie
public var writerVersion: UInt16
```
功能： 元数据编写器使用的版本。
Type: UInt16; Readable: true; Writable: true


### HdrStaticMetadata

class HdrStaticMetadata
```cangjie
public class HdrStaticMetadata { public HdrStaticMetadata( public var displayPrimariesX: Array<Float32>, public var displayPrimariesY: Array<Float32>, public var whitePointX: Float32, public var whitePointY: Float32, public var maxLuminance: Float32, public var minLuminance: Float32, public var maxContentLightLevel: Float32, public var maxFrameAverageLightLevel: Float32 ) }
```
功能： 静态元数据值， HdrMetadataKey 中HDR_STATIC_METADATA关键字对应的值。

#### var displayPrimariesX

```cangjie
public var displayPrimariesX: Array<Float32>
```
功能： 归一化后显示设备三基色的X坐标，数组的长度为3，以0.00002为单位，范围[0.0, 1.0]。
Type: Array<Float32>; Readable: true; Writable: true

#### var displayPrimariesY

```cangjie
public var displayPrimariesY: Array<Float32>
```
功能： 归一化后显示设备三基色的Y坐标，数组的长度为3，以0.00002为单位，范围[0.0, 1.0]。
Type: Array<Float32>; Readable: true; Writable: true

#### var maxContentLightLevel

```cangjie
public var maxContentLightLevel: Float32
```
功能： 显示内容的最大亮度。以1为单位，最大值为65535。
Type: Float32; Readable: true; Writable: true

#### var maxFrameAverageLightLevel

```cangjie
public var maxFrameAverageLightLevel: Float32
```
功能： 显示内容的最大平均亮度，以1为单位，最大值为65535。
Type: Float32; Readable: true; Writable: true

#### var maxLuminance

```cangjie
public var maxLuminance: Float32
```
功能： 图像主监视器最大亮度。以1为单位，最大值为65535。
Type: Float32; Readable: true; Writable: true

#### var minLuminance

```cangjie
public var minLuminance: Float32
```
功能： 图像主监视器最小亮度。以0.0001为单位，最大值6.55535。
Type: Float32; Readable: true; Writable: true

#### var whitePointX

```cangjie
public var whitePointX: Float32
```
功能： 归一化后白点值的X坐标，以0.00002为单位，范围[0.0, 1.0]。
Type: Float32; Readable: true; Writable: true

#### var whitePointY

```cangjie
public var whitePointY: Float32
```
功能： 归一化后白点值的Y坐标，以0.00002为单位，范围[0.0, 1.0]。
Type: Float32; Readable: true; Writable: true


### Image

class Image
```cangjie
public class Image {}
```
功能： 提供基本的图像操作，包括获取图像信息、读写图像数据。

#### prop clipRect

```cangjie
public prop clipRect: Region
```
功能： 要裁剪的图像区域。
Type: Region; Readable: true; Writable: false

#### prop format

```cangjie
public prop format: Int32
```
功能： 图像格式，参考 PixelMapFormat 。
Type: Int32; Readable: true; Writable: false

#### prop size

```cangjie
public prop size: Size
```
功能： 图像大小。
Type: Size; Readable: true; Writable: false

#### prop timestamp

```cangjie
public prop timestamp: Int64
```
功能： 图像时间戳。时间戳以纳秒为单位，通常是单调递增的。时间戳的具体含义和基准取决于图像的生产者，在相机预览/拍照场景，生产者就是相机。来自不同生产者的图像的时间戳可能有不同的含义和基准，因此可能无法进行比较。如果要获取某张照片的生成时间，可以通过 getImageProperty 接口读取相关的EXIF信息。
Type: Int64; Readable: true; Writable: false


### ImageCreator

class ImageCreator
```cangjie
public class ImageCreator {}
```
功能： 图像创建模块，用于请求图像原生数据区域，并开放给应用编译原生图像数据的能力。 在调用以下方法前需要先创建ImageCreator实例。

#### prop capacity

```cangjie
public prop capacity: Int32
```
功能： 同时访问的图像数。
Type: Int32; Readable: true; Writable: false

#### prop format

```cangjie
public prop format: Int32
```
功能： 图像格式。
Type: Int32; Readable: true; Writable: false


### ImageInfo

class ImageInfo
```cangjie
public class ImageInfo { public var size: Size, public var density: Int32, public var stride: Int32, public var pixelFormat: PixelMapFormat, public var alphaType: AlphaType, public var mimeType: String, public var isHdr: Bool public init(size: Size, density: Int32) public init(size: Size, density: Int32, stride: Int32, pixelFormat: PixelMapFormat, alphaType: AlphaType, mimeType: String, isHdr: Bool) }
```
功能： 表示图片信息。

#### var alphaType

```cangjie
public var alphaType: AlphaType
```
功能： 透明度。
Type: AlphaType; Readable: true; Writable: true

#### var density

```cangjie
public var density: Int32
```
功能： 像素密度，单位为ppi。
Type: Int32; Readable: true; Writable: true

#### var isHdr

```cangjie
public var isHdr: Bool
```
功能： 图片是否为高动态范围（HDR）。对于 ImageSource ，代表源图片是否为HDR；对于 PixelMap ，代表解码后的pixelmap是否为HDR。
Type: Bool; Readable: true; Writable: true

#### var mimeType

```cangjie
public var mimeType: String
```
功能： 图片真实格式（MIME type）。
Type: String; Readable: true; Writable: true

#### var pixelFormat

```cangjie
public var pixelFormat: PixelMapFormat
```
功能： 像素格式。
Type: PixelMapFormat; Readable: true; Writable: true

#### var size

```cangjie
public var size: Size
```
功能： 图片大小。
Type: Size; Readable: true; Writable: true

#### var stride

```cangjie
public var stride: Int32
```
功能： 跨距，内存中每行像素所占的空间。stride >= region.size.width*4。
Type: Int32; Readable: true; Writable: true


### ImagePacker

class ImagePacker
```cangjie
public class ImagePacker {}
```
功能： 图片打包器类，用于图片压缩和打包。在调用ImagePacker的方法前，需要先通过 createImagePacker 构建一个ImagePacker实例，当前支持格式有：jpeg、webp、png。

#### prop supportedFormats

```cangjie
public prop supportedFormats: Array<String>
```
功能： 图片打包支持的格式jpeg、webp、png。
Type: Array<String>; Readable: true; Writable: false


### ImageReceiver

class ImageReceiver
```cangjie
public class ImageReceiver {}
```
功能： 图像接收类，用于获取组件surface id，接收最新的图片和读取下一张图片，以及释放ImageReceiver实例。

#### prop capacity

```cangjie
public prop capacity: Int32
```
功能： 同时访问的图像数。
Type: Int32; Readable: true; Writable: false

#### prop format

```cangjie
public prop format: ImageFormat
```
功能： 图像格式。
Type: ImageFormat; Readable: true; Writable: false

#### prop size

```cangjie
public prop size: Size
```
功能： 图片大小。
Type: Size; Readable: true; Writable: false


### ImageSource

class ImageSource
```cangjie
public class ImageSource {}
```
功能： 图片源类，用于获取图片相关信息。在调用ImageSource的方法前，需要先通过createImageSource构建一个ImageSource实例。

#### prop supportedFormats

```cangjie
public prop supportedFormats: Array<String>
```
功能： 支持的图片格式，包括：png，jpeg，bmp，gif，webp，RAW。
Type: Array<String>; Readable: true; Writable: false


### InitializationOptions

class InitializationOptions
```cangjie
public class InitializationOptions { public var alphaType: AlphaType public var editable: Bool public var srcPixelFormat: PixelMapFormat public var pixelFormat: PixelMapFormat public var scaleMode: ScaleMode public var size: Size public init(alphaType!: AlphaType = AlphaType.PREMUL, editable!: Bool = false, pixelFormat!: PixelMapFormat = PixelMapFormat.BGRA_8888, scaleMode!: ScaleMode = ScaleMode.FIT_TARGET_SIZE, size!: Size) public init(srcPixelFormat: PixelMapFormat, alphaType!: AlphaType = AlphaType.PREMUL, editable!: Bool = false, pixelFormat!: PixelMapFormat = PixelMapFormat.BGRA_8888, scaleMode!: ScaleMode = ScaleMode.FIT_TARGET_SIZE, size!: Size) }
```
功能： PixelMap的初始化选项。

#### var alphaType

```cangjie
public var alphaType: AlphaType = AlphaType.PREMUL
```
功能： 透明度。
Type: AlphaType; Readable: true; Writable: true

#### var editable

```cangjie
public var editable: Bool = false
```
功能： 是否可编辑。
Type: Bool; Readable: true; Writable: true

#### var pixelFormat

```cangjie
public var pixelFormat: PixelMapFormat = PixelMapFormat.BGRA_8888
```
功能： 像素格式。
Type: PixelMapFormat; Readable: true; Writable: true

#### var scaleMode

```cangjie
public var scaleMode: ScaleMode = ScaleMode.FIT_TARGET_SIZE
```
功能： 缩略值。
Type: ScaleMode; Readable: true; Writable: true

#### var size

```cangjie
public var size: Size
```
功能： 创建图片大小。
Type: Size; Readable: true; Writable: true

#### var srcPixelFormat

```cangjie
public var srcPixelFormat: PixelMapFormat = PixelMapFormat.BGRA_8888
```
功能： 传入的buffer数据的像素格式。默认值为BGRA_8888。
Type: PixelMapFormat; Readable: true; Writable: true


### PackingOption

class PackingOption
```cangjie
public class PackingOption { public var format: String public var quality: UInt8 public var bufferSize: UInt64 public var desiredDynamicRange: PackingDynamicRange public var needsPackProperties: Bool public init(format: String, quality: UInt8, bufferSize!: UInt64 = 25 * 1024 * 1024) public init(format: String, quality: UInt8, desiredDynamicRange: PackingDynamicRange, needsPackProperties: Bool, bufferSize!: UInt64 = 25 * 1024 * 1024) }
```
功能： 表示图片打包选项。

#### var bufferSize

```cangjie
public var bufferSize: UInt64 = 25 * 1024 * 1024
```
功能： 接收编码数据的缓冲区大小，单位为Byte。如果不设置大小，默认为25M。如果编码图片超过25M，需要指定大小。bufferSize需大于编码后图片大小。使用 packToFile 不受此参数限制。
Type: UInt64; Readable: true; Writable: true

#### var desiredDynamicRange

```cangjie
public var desiredDynamicRange: PackingDynamicRange = SDR
```
功能： 目标动态范围。默认值为SDR。
Type: PackingDynamicRange; Readable: true; Writable: true

#### var format

```cangjie
public var format: String
```
功能： 目标格式。当前只支持"image/jpeg"、"image/webp"、"image/png"和"image/heif"（不同硬件设备支持情况不同）。
Type: String; Readable: true; Writable: true

#### var needsPackProperties

```cangjie
public var needsPackProperties: Bool = false
```
功能： 是否需要编码图片属性信息，例如EXIF。默认值为false。
Type: Bool; Readable: true; Writable: true

#### var quality

```cangjie
public var quality: UInt8
```
功能： JPEG编码中设定输出图片质量的参数，取值范围为0-100。0质量最低，100质量最高，质量越高生成图片所占空间越大。
Type: UInt8; Readable: true; Writable: true


### PixelMap

class PixelMap
```cangjie
public class PixelMap {}
```
功能： 图像像素类，用于读取或写入图像数据以及获取图像信息。在调用PixelMap的方法前，需要先通过 createPixelMap 创建一个PixelMap实例。目前pixelmap序列化大小最大128MB，超过会送显失败。大小计算方式为(宽*高*每像素占用字节数)。

#### prop isEditable

```cangjie
public prop isEditable: Bool
```
功能： 设定图像像素是否可被编辑。
Type: Bool; Readable: true; Writable: false

#### prop isStrideAlignment

```cangjie
public prop isStrideAlignment: Bool
```
功能： 设定图像内存是否为DMA内存。
Type: Bool; Readable: true; Writable: false



## Structs

### Component

struct Component
```cangjie
public struct Component { public Component( public let componentType: ComponentType, public let rowStride: Int32, public let pixelStride: Int32, public let byteBuffer: Array<UInt8> ) }
```
功能： 描述图像颜色分量。


### ImagePropertyOptions

struct ImagePropertyOptions
```cangjie
public struct ImagePropertyOptions { public ImagePropertyOptions( public let index!: UInt32 = 0, public let defaultValue!: String = "" ) }
```
功能： 表示查询图片属性的索引。


### PositionArea

struct PositionArea
```cangjie
public struct PositionArea { public PositionArea( public let pixels: Array<UInt8>, public let offset: UInt32, public let stride: UInt32, public let region: Region ) }
```
功能： 表示图片指定区域内的数据。


### Region

struct Region
```cangjie
public struct Region { public Region( public var size: Size, public var x: Int32, public var y: Int32 ) }
```
功能： 表示区域信息。


### Size

struct Size
```cangjie
public struct Size { public Size( public var height!: Int32 = 0, public var width!: Int32 = 0 ) }
```
功能： 表示图片尺寸。


### SourceOptions

struct SourceOptions
```cangjie
public struct SourceOptions { public SourceOptions( public var sourceDensity!: Int32 = 0, public var sourcePixelFormat!: PixelMapFormat = PixelMapFormat.UNKNOWN, public var sourceSize!: Size = Size(height: 0, width: 0) ) }
```
功能： ImageSource的初始化选项。



## Enums

### AlphaType

enum AlphaType
```cangjie
public enum AlphaType <: Equatable<AlphaType> & ToString { | UNKNOWN | OPAQUE | PREMUL | UNPREMUL | ... }
```
功能： 图像的透明度类型。

#### OPAQUE

```cangjie
OPAQUE
```
功能： 没有alpha或图片不透明。

#### PREMUL

```cangjie
PREMUL
```
功能： RGB前乘alpha。

#### UNKNOWN

```cangjie
UNKNOWN
```
功能： 未知透明度。

#### UNPREMUL

```cangjie
UNPREMUL
```
功能： RGB不前乘alpha。


### AntiAliasingLevel

enum AntiAliasingLevel
```cangjie
public enum AntiAliasingLevel <: Equatable<AntiAliasingLevel> & ToString { | NONE | LOW | MEDIUM | HIGH | ... }
```
功能： 缩放时的缩放算法。

#### HIGH

```cangjie
HIGH
```
功能： cubic缩放算法。

#### LOW

```cangjie
LOW
```
功能： 双线性缩放算法。

#### MEDIUM

```cangjie
MEDIUM
```
功能： 双线性缩放算法，同步开启mipmap。

#### NONE

```cangjie
NONE
```
功能： 默认为最近邻缩放算法。


### ComponentType

enum ComponentType
```cangjie
public enum ComponentType <: Equatable<ComponentType> & ToString { | YUV_Y | YUV_U | YUV_V | JPEG | ... }
```
功能： 图像的组件类型。

#### JPEG

```cangjie
JPEG
```
功能： JPEG 类型。

#### YUV_U

```cangjie
YUV_U
```
功能： 色度信息。

#### YUV_V

```cangjie
YUV_V
```
功能： 色度信息。

#### YUV_Y

```cangjie
YUV_Y
```
功能： 亮度信息。


### DecodingDynamicRange

enum DecodingDynamicRange
```cangjie
public enum DecodingDynamicRange <: Equatable<DecodingDynamicRange> & ToString { | AUTO | SDR | HDR | ... }
```
功能： 描述解码时期望的图像动态范围。

#### AUTO

```cangjie
AUTO
```
功能： 自适应，根据图片信息处理。即如果图片本身为HDR图片，则会按照HDR内容解码；反之按照SDR内容解码。通过 CreateIncrementalSource 创建的imagesource会解码为SDR内容。

#### HDR

```cangjie
HDR
```
功能： 按照高动态范围处理图片。通过 CreateIncrementalSource 创建的imagesource会解码为SDR内容。

#### SDR

```cangjie
SDR
```
功能： 按照标准动态范围处理图片。


### HdrMetadataKey

enum HdrMetadataKey
```cangjie
public enum HdrMetadataKey <: Equatable<HdrMetadataKey> & ToString { | HDR_METADATA_TYPE | HDR_STATIC_METADATA | HDR_DYNAMIC_METADATA | HDR_GAINMAP_METADATA | ... }
```
功能： pixelmap 使用的HDR相关元数据信息的关键字。

#### HDR_DYNAMIC_METADATA

```cangjie
HDR_DYNAMIC_METADATA
```
功能： 动态元数据。

#### HDR_GAINMAP_METADATA

```cangjie
HDR_GAINMAP_METADATA
```
功能： Gainmap使用的元数据。

#### HDR_METADATA_TYPE

```cangjie
HDR_METADATA_TYPE
```
功能： pixelmap 使用的元数据类型。

#### HDR_STATIC_METADATA

```cangjie
HDR_STATIC_METADATA
```
功能： 静态元数据。


### HdrMetadataType

enum HdrMetadataType
```cangjie
public enum HdrMetadataType <: Equatable<HdrMetadataType> & ToString { | NONE | BASE | GAINMAP | ALTERNATE | ... }
```
功能： HdrMetadataKey 中HDR_METADATA_TYPE关键字对应的值。

#### ALTERNATE

```cangjie
ALTERNATE
```
功能： 表示用于合成后HDR图的元数据。

#### BASE

```cangjie
BASE
```
功能： 表示用于基础图的元数据。

#### GAINMAP

```cangjie
GAINMAP
```
功能： 表示用于Gainmap图的元数据。

#### NONE

```cangjie
NONE
```
功能： 无元数据内容。


### ImageFormat

enum ImageFormat
```cangjie
public enum ImageFormat <: Equatable<ImageFormat> & ToString { | YCBCR_422_SP | JPEG | ... }
```
功能： 图片格式。

#### JPEG

```cangjie
JPEG
```
功能： JPEG编码格式。

#### YCBCR_422_SP

```cangjie
YCBCR_422_SP
```
功能： YCBCR422半平面格式。


### PackingDynamicRange

enum PackingDynamicRange
```cangjie
public enum PackingDynamicRange <: Equatable<PackingDynamicRange> & ToString { | AUTO | SDR | ... }
```
功能： 描述编码时期望的图像动态范围。

#### AUTO

```cangjie
AUTO
```
功能： 自适应，根据 pixelmap 内容处理。即如果pixelmap本身为HDR，则会按照HDR内容进行编码；反之按照SDR内容编码。

#### SDR

```cangjie
SDR
```
功能： 按照标准动态范围处理图片。

#### prop value

```cangjie
public prop value: Int32
```
功能： 获取枚举值的值。


### PixelMapFormat

enum PixelMapFormat
```cangjie
public enum PixelMapFormat <: Equatable<PixelMapFormat> & ToString { | UNKNOWN | RGB_565 | RGBA_8888 | BGRA_8888 | RGB_888 | ALPHA_8 | RGBA_F16 | NV21 | NV12 | RGBA_1010102 | YCBCR_P010 | YCRCB_P010 | ... }
```
功能： 图片像素格式。

#### ALPHA_8

```cangjie
ALPHA_8
```
功能： 格式为ALPHA_8。

#### BGRA_8888

```cangjie
BGRA_8888
```
功能： 格式为BGRA_8888。

#### NV12

```cangjie
NV12
```
功能： 格式为NV12。

#### NV21

```cangjie
NV21
```
功能： 格式为NV21。

#### RGBA_1010102

```cangjie
RGBA_1010102
```
功能： 格式为RGBA_1010102。

#### RGBA_8888

```cangjie
RGBA_8888
```
功能： 格式为RGBA_8888。

#### RGBA_F16

```cangjie
RGBA_F16
```
功能： 格式为RGBA_F16。

#### RGB_565

```cangjie
RGB_565
```
功能： 格式为RGB_565。

#### RGB_888

```cangjie
RGB_888
```
功能： 格式为RGB_888。

#### UNKNOWN

```cangjie
UNKNOWN
```
功能： 未知格式。

#### YCBCR_P010

```cangjie
YCBCR_P010
```
功能： 格式为YCBCR_P010。

#### YCRCB_P010

```cangjie
YCRCB_P010
```
功能： 格式为YCRCB_P010。


### PropertyKey

enum PropertyKey
```cangjie
public enum PropertyKey <: ToString & Equatable<PropertyKey> { | NEW_SUBFILE_TYPE | SUBFILE_TYPE | IMAGE_WIDTH | IMAGE_LENGTH | BITS_PER_SAMPLE | COMPRESSION | PHOTOMETRIC_INTERPRETATION | IMAGE_DESCRIPTION | MAKE | MODEL | STRIP_OFFSETS | ORIENTATION | SAMPLES_PER_PIXEL | ROWS_PER_STRIP | STRIP_BYTE_COUNTS | X_RESOLUTION | Y_RESOLUTION | PLANAR_CONFIGURATION | RESOLUTION_UNIT | TRANSFER_FUNCTION | SOFTWARE | DATE_TIME | ARTIST | WHITE_POINT | PRIMARY_CHROMATICITIES | PHOTO_MODE | JPEG_INTERCHANGE_FORMAT | JPEG_INTERCHANGE_FORMAT_LENGTH | YCBCR_COEFFICIENTS | YCBCR_SUB_SAMPLING | YCBCR_POSITIONING | REFERENCE_BLACK_WHITE | COPYRIGHT | EXPOSURE_TIME | F_NUMBER | EXPOSURE_PROGRAM | SPECTRAL_SENSITIVITY | GPS_VERSION_ID | GPS_LATITUDE_REF | GPS_LATITUDE | GPS_LONGITUDE_REF | GPS_LONGITUDE | GPS_ALTITUDE_REF | GPS_ALTITUDE | GPS_TIME_STAMP | GPS_SATELLITES | GPS_STATUS | GPS_MEASURE_MODE | GPS_DOP | GPS_SPEED_REF | GPS_SPEED | GPS_TRACK_REF | GPS_TRACK | GPS_IMG_DIRECTION_REF | GPS_IMG_DIRECTION | GPS_MAP_DATUM | GPS_DEST_LATITUDE_REF | GPS_DEST_LATITUDE | GPS_DEST_LONGITUDE_REF | GPS_DEST_LONGITUDE | GPS_DEST_BEARING_REF | GPS_DEST_BEARING | GPS_DEST_DISTANCE_REF | GPS_DEST_DISTANCE | GPS_PROCESSING_METHOD | GPS_AREA_INFORMATION | GPS_DATE_STAMP | GPS_DIFFERENTIAL | GPS_H_POSITIONING_ERROR | ISO_SPEED_RATINGS | PHOTOGRAPHIC_SENSITIVITY | OECF | SENSITIVITY_TYPE | STANDARD_OUTPUT_SENSITIVITY | RECOMMENDED_EXPOSURE_INDEX | ISO_SPEED | ISO_SPEED_LATITUDE_YYY | ISO_SPEED_LATITUDE_ZZZ | EXIF_VERSION | DATE_TIME_ORIGINAL | DATE_TIME_DIGITIZED | OFFSET_TIME | OFFSET_TIME_ORIGINAL | OFFSET_TIME_DIGITIZED | COMPONENTS_CONFIGURATION | COMPRESSED_BITS_PER_PIXEL | SHUTTER_SPEED | APERTURE_VALUE | BRIGHTNESS_VALUE | EXPOSURE_BIAS_VALUE | MAX_APERTURE_VALUE | SUBJECT_DISTANCE | METERING_MODE | LIGHT_SOURCE | FLASH | FOCAL_LENGTH | SUBJECT_AREA | MAKER_NOTE | SCENE_POINTER | SCENE_VERSION | SCENE_FOOD_CONF | SCENE_STAGE_CONF | SCENE_BLUE_SKY_CONF | SCENE_GREEN_PLANT_CONF | SCENE_BEACH_CONF | SCENE_SNOW_CONF | SCENE_SUNSET_CONF | SCENE_FLOWERS_CONF | SCENE_NIGHT_CONF | SCENE_TEXT_CONF | FACE_POINTER | FACE_VERSION | FACE_COUNT | FACE_CONF | FACE_SMILE_SCORE | FACE_RECT | FACE_LEYE_CENTER | FACE_REYE_CENTER | FACE_MOUTH_CENTER | CAPTURE_MODE | BURST_NUMBER | FRONT_CAMERA | ROLL_ANGLE | PITCH_ANGLE | PHYSICAL_APERTURE | FOCUS_MODE | USER_COMMENT | SUBSEC_TIME | SUBSEC_TIME_ORIGINAL | SUBSEC_TIME_DIGITIZED | FLASHPIX_VERSION | COLOR_SPACE | PIXEL_X_DIMENSION | PIXEL_Y_DIMENSION | RELATED_SOUND_FILE | FLASH_ENERGY | SPATIAL_FREQUENCY_RESPONSE | FOCAL_PLANE_X_RESOLUTION | FOCAL_PLANE_Y_RESOLUTION | FOCAL_PLANE_RESOLUTION_UNIT | SUBJECT_LOCATION | EXPOSURE_INDEX | SENSING_METHOD | FILE_SOURCE | SCENE_TYPE | CFA_PATTERN | CUSTOM_RENDERED | EXPOSURE_MODE | WHITE_BALANCE | DIGITAL_ZOOM_RATIO | FOCAL_LENGTH_IN_35_MM_FILM | SCENE_CAPTURE_TYPE | GAIN_CONTROL | CONTRAST | SATURATION | SHARPNESS | DEVICE_SETTING_DESCRIPTION | SUBJECT_DISTANCE_RANGE | IMAGE_UNIQUE_ID | CAMERA_OWNER_NAME | BODY_SERIAL_NUMBER | LENS_SPECIFICATION | LENS_MAKE | LENS_MODEL | LENS_SERIAL_NUMBER | COMPOSITE_IMAGE | SOURCE_IMAGE_NUMBER_OF_COMPOSITE_IMAGE | SOURCE_EXPOSURE_TIMES_OF_COMPOSITE_IMAGE | GAMMA | DNG_VERSION | DEFAULT_CROP_SIZE | GIF_LOOP_COUNT | IS_XMAGE_SUPPORTED | XMAGE_MODE | XMAGE_LEFT | XMAGE_TOP | XMAGE_RIGHT | XMAGE_BOTTOM | CLOUD_ENHANCEMENT_MODE | WIND_SNAPSHOT_MODE | ... }
```
功能： Exif（Exchangeable image file format）图片信息。

#### APERTURE_VALUE

```cangjie
APERTURE_VALUE
```
功能： 光圈值。

#### ARTIST

```cangjie
ARTIST
```
功能： 创建图像的用户名称。

#### BITS_PER_SAMPLE

```cangjie
BITS_PER_SAMPLE
```
功能： 每个像素比特数。

#### BODY_SERIAL_NUMBER

```cangjie
BODY_SERIAL_NUMBER
```
功能： 相机机身的序列号。

#### BRIGHTNESS_VALUE

```cangjie
BRIGHTNESS_VALUE
```
功能： 图像的亮度值，以APEX单位表示。

#### BURST_NUMBER

```cangjie
BURST_NUMBER
```
功能： 连拍次数。

#### CAMERA_OWNER_NAME

```cangjie
CAMERA_OWNER_NAME
```
功能： 相机所有者的姓名。

#### CAPTURE_MODE

```cangjie
CAPTURE_MODE
```
功能： 捕获模式。

#### CFA_PATTERN

```cangjie
CFA_PATTERN
```
功能： 图像传感器的色彩滤光片（CFA）几何图案。

#### CLOUD_ENHANCEMENT_MODE

```cangjie
CLOUD_ENHANCEMENT_MODE
```
功能： 云增强模式。

#### COLOR_SPACE

```cangjie
COLOR_SPACE
```
功能： 色彩空间信息标签，通常记录为色彩空间指定符。

#### COMPONENTS_CONFIGURATION

```cangjie
COMPONENTS_CONFIGURATION
```
功能： 压缩数据的特定信息。

#### COMPOSITE_IMAGE

```cangjie
COMPOSITE_IMAGE
```
功能： 表示图像是否为合成图像。

#### COMPRESSED_BITS_PER_PIXEL

```cangjie
COMPRESSED_BITS_PER_PIXEL
```
功能： 用于压缩图像的压缩模式，单位为每像素位数。

#### COMPRESSION

```cangjie
COMPRESSION
```
功能： 图像压缩方案。

#### CONTRAST

```cangjie
CONTRAST
```
功能： 相机应用的对比度处理方向。

#### COPYRIGHT

```cangjie
COPYRIGHT
```
功能： 图像的版权信息。

#### CUSTOM_RENDERED

```cangjie
CUSTOM_RENDERED
```
功能： 指示图像数据上的特殊处理。

#### DATE_TIME

```cangjie
DATE_TIME
```
功能： 日期时间。

#### DATE_TIME_DIGITIZED

```cangjie
DATE_TIME_DIGITIZED
```
功能： 图像作为数字数据存储的日期和时间，格式为YYYY:MM:DD HH:MM:SS。

#### DATE_TIME_ORIGINAL

```cangjie
DATE_TIME_ORIGINAL
```
功能： 拍摄时间，例如2022:09:06 15:48:00。

#### DEFAULT_CROP_SIZE

```cangjie
DEFAULT_CROP_SIZE
```
功能： DefaultCropSize指定了原始坐标中的最终图像大小，考虑了额外的边缘像素。

#### DEVICE_SETTING_DESCRIPTION

```cangjie
DEVICE_SETTING_DESCRIPTION
```
功能： 特定相机模型的拍照条件信息。

#### DIGITAL_ZOOM_RATIO

```cangjie
DIGITAL_ZOOM_RATIO
```
功能： 捕获时的数字变焦比率。

#### DNG_VERSION

```cangjie
DNG_VERSION
```
功能： DNG版本标签编码了符合DNG规范的四级版本号。

#### EXIF_VERSION

```cangjie
EXIF_VERSION
```
功能： 支持的Exif标准版本。

#### EXPOSURE_BIAS_VALUE

```cangjie
EXPOSURE_BIAS_VALUE
```
功能： 曝光偏差值。

#### EXPOSURE_INDEX

```cangjie
EXPOSURE_INDEX
```
功能： 捕获时选定的曝光指数。

#### EXPOSURE_MODE

```cangjie
EXPOSURE_MODE
```
功能： 拍摄时设置的曝光模式。

#### EXPOSURE_PROGRAM

```cangjie
EXPOSURE_PROGRAM
```
功能： 拍照时相机用来设置曝光的程序的类别。

#### EXPOSURE_TIME

```cangjie
EXPOSURE_TIME
```
功能： 曝光时间，例如1/33 sec。

#### FACE_CONF

```cangjie
FACE_CONF
```
功能： 人脸置信度。

#### FACE_COUNT

```cangjie
FACE_COUNT
```
功能： 人脸数量。

#### FACE_LEYE_CENTER

```cangjie
FACE_LEYE_CENTER
```
功能： 左眼中心。

#### FACE_MOUTH_CENTER

```cangjie
FACE_MOUTH_CENTER
```
功能： 嘴中心。

#### FACE_POINTER

```cangjie
FACE_POINTER
```
功能： 脸部指针。

#### FACE_RECT

```cangjie
FACE_RECT
```
功能： 脸部矩形。

#### FACE_REYE_CENTER

```cangjie
FACE_REYE_CENTER
```
功能： 右眼中心。

#### FACE_SMILE_SCORE

```cangjie
FACE_SMILE_SCORE
```
功能： FaceCount张人脸的笑脸分数。

#### FACE_VERSION

```cangjie
FACE_VERSION
```
功能： 人脸算法版本信息。

#### FILE_SOURCE

```cangjie
FILE_SOURCE
```
功能： 表明图像来源。

#### FLASH

```cangjie
FLASH
```
功能： 闪光灯,记录闪光灯状态。

#### FLASHPIX_VERSION

```cangjie
FLASHPIX_VERSION
```
功能： 该标签表示FPXR文件支持的Flashpix格式版本，增强了设备兼容性。

#### FLASH_ENERGY

```cangjie
FLASH_ENERGY
```
功能： 图像捕获时的闪光能量，以BCPS表示。

#### FOCAL_LENGTH

```cangjie
FOCAL_LENGTH
```
功能： 焦距。

#### FOCAL_LENGTH_IN_35_MM_FILM

```cangjie
FOCAL_LENGTH_IN_35_MM_FILM
```
功能： 焦距35毫米胶片。

#### FOCAL_PLANE_RESOLUTION_UNIT

```cangjie
FOCAL_PLANE_RESOLUTION_UNIT
```
功能： 测量FocalPlaneXResolution和FocalPlaneYResolution的单位。

#### FOCAL_PLANE_X_RESOLUTION

```cangjie
FOCAL_PLANE_X_RESOLUTION
```
功能： 图像宽度中每FocalPlaneResolutionUnit的像素。

#### FOCAL_PLANE_Y_RESOLUTION

```cangjie
FOCAL_PLANE_Y_RESOLUTION
```
功能： 图像高度中每FocalPlaneResolutionUnit的像素。

#### FOCUS_MODE

```cangjie
FOCUS_MODE
```
功能： 对焦模式。

#### FRONT_CAMERA

```cangjie
FRONT_CAMERA
```
功能： 是否是前置相机自拍。

#### F_NUMBER

```cangjie
F_NUMBER
```
功能： 光圈值，例如f/1.8。

#### GAIN_CONTROL

```cangjie
GAIN_CONTROL
```
功能： 整体图像增益调整的程度。

#### GAMMA

```cangjie
GAMMA
```
功能： 表示系数伽马的值。

#### GIF_LOOP_COUNT

```cangjie
GIF_LOOP_COUNT
```
功能： GIF图片循环次数。0表示无限循环，其他值表示循环次数。

#### GPS_ALTITUDE

```cangjie
GPS_ALTITUDE
```
功能： 基于GPSAltitudeRef的高度。

#### GPS_ALTITUDE_REF

```cangjie
GPS_ALTITUDE_REF
```
功能： 用于GPS高度的参照高度。

#### GPS_AREA_INFORMATION

```cangjie
GPS_AREA_INFORMATION
```
功能： 记录GPS区域名的字符字符串。

#### GPS_DATE_STAMP

```cangjie
GPS_DATE_STAMP
```
功能： GPS日期戳。

#### GPS_DEST_BEARING

```cangjie
GPS_DEST_BEARING
```
功能： 目的地方位。

#### GPS_DEST_BEARING_REF

```cangjie
GPS_DEST_BEARING_REF
```
功能： 指向目的地点的方位参照。

#### GPS_DEST_DISTANCE

```cangjie
GPS_DEST_DISTANCE
```
功能： 到目的地点的距离。

#### GPS_DEST_DISTANCE_REF

```cangjie
GPS_DEST_DISTANCE_REF
```
功能： 目标点距离的测量单位。

#### GPS_DEST_LATITUDE

```cangjie
GPS_DEST_LATITUDE
```
功能： 目的地点的纬度。

#### GPS_DEST_LATITUDE_REF

```cangjie
GPS_DEST_LATITUDE_REF
```
功能： 目的地点的纬度参照。

#### GPS_DEST_LONGITUDE

```cangjie
GPS_DEST_LONGITUDE
```
功能： 目的地点的经度。

#### GPS_DEST_LONGITUDE_REF

```cangjie
GPS_DEST_LONGITUDE_REF
```
功能： 目的地点的经度参照。

#### GPS_DIFFERENTIAL

```cangjie
GPS_DIFFERENTIAL
```
功能： 此字段表示GPS数据是否应用了差分校正，对于精确的位置准确性至关重要。

#### GPS_DOP

```cangjie
GPS_DOP
```
功能： GPS DOP（数据精度等级）。

#### GPS_H_POSITIONING_ERROR

```cangjie
GPS_H_POSITIONING_ERROR
```
功能： 此标签指示水平定位误差，单位为米。

#### GPS_IMG_DIRECTION

```cangjie
GPS_IMG_DIRECTION
```
功能： 拍摄时图像的方向。

#### GPS_IMG_DIRECTION_REF

```cangjie
GPS_IMG_DIRECTION_REF
```
功能： 图像方向的参照。

#### GPS_LATITUDE

```cangjie
GPS_LATITUDE
```
功能： 图片纬度。修改时应按"度,分,秒"格式传入，如"39,54,7.542"。

#### GPS_LATITUDE_REF

```cangjie
GPS_LATITUDE_REF
```
功能： 纬度引用，例如N或S。

#### GPS_LONGITUDE

```cangjie
GPS_LONGITUDE
```
功能： 图片经度。修改时应按"度,分,秒"格式传入，如"116,19,42.16"。

#### GPS_LONGITUDE_REF

```cangjie
GPS_LONGITUDE_REF
```
功能： 经度引用，例如W或E。

#### GPS_MAP_DATUM

```cangjie
GPS_MAP_DATUM
```
功能： GPS接收器使用的大地测量数据。

#### GPS_MEASURE_MODE

```cangjie
GPS_MEASURE_MODE
```
功能： GPS测量模式。

#### GPS_PROCESSING_METHOD

```cangjie
GPS_PROCESSING_METHOD
```
功能： 记录定位方法名的字符字符串。

#### GPS_SATELLITES

```cangjie
GPS_SATELLITES
```
功能： 用于测量的GPS卫星。

#### GPS_SPEED

```cangjie
GPS_SPEED
```
功能： GPS接收器的移动速度。

#### GPS_SPEED_REF

```cangjie
GPS_SPEED_REF
```
功能： 用来表示GPS接收器移动速度的单位。

#### GPS_STATUS

```cangjie
GPS_STATUS
```
功能： 录制图像时GPS接收器的状态。

#### GPS_TIME_STAMP

```cangjie
GPS_TIME_STAMP
```
功能： GPS时间戳。

#### GPS_TRACK

```cangjie
GPS_TRACK
```
功能： GPS接收机的移动方向。

#### GPS_TRACK_REF

```cangjie
GPS_TRACK_REF
```
功能： GPS接收机移动方向的参照。

#### GPS_VERSION_ID

```cangjie
GPS_VERSION_ID
```
功能： GPSInfoIFD的版本。

#### IMAGE_DESCRIPTION

```cangjie
IMAGE_DESCRIPTION
```
功能： 图像信息描述。

#### IMAGE_LENGTH

```cangjie
IMAGE_LENGTH
```
功能： 图片长度。

#### IMAGE_UNIQUE_ID

```cangjie
IMAGE_UNIQUE_ID
```
功能： 为每张图片唯一分配的标识符。

#### IMAGE_WIDTH

```cangjie
IMAGE_WIDTH
```
功能： 图片宽度。

#### ISO_SPEED

```cangjie
ISO_SPEED
```
功能： ISO速度等级。

#### ISO_SPEED_LATITUDE_YYY

```cangjie
ISO_SPEED_LATITUDE_YYY
```
功能： 该标签指示摄像机或输入设备的ISO速度纬度yyy值，该值在ISO 12232中定义。

#### ISO_SPEED_LATITUDE_ZZZ

```cangjie
ISO_SPEED_LATITUDE_ZZZ
```
功能： 该标签指示摄像机或输入设备的ISO速度纬度zzz值，该值在ISO 12232中定义。

#### ISO_SPEED_RATINGS

```cangjie
ISO_SPEED_RATINGS
```
功能： ISO感光度，例如400。

#### IS_XMAGE_SUPPORTED

```cangjie
IS_XMAGE_SUPPORTED
```
功能： 是否支持XMAGE。

#### JPEG_INTERCHANGE_FORMAT

```cangjie
JPEG_INTERCHANGE_FORMAT
```
功能： JPEG压缩缩略图数据开始字节（SOI）的偏移。

#### JPEG_INTERCHANGE_FORMAT_LENGTH

```cangjie
JPEG_INTERCHANGE_FORMAT_LENGTH
```
功能： JPEG压缩缩略图数据的字节数。

#### LENS_MAKE

```cangjie
LENS_MAKE
```
功能： 镜头的制造商。

#### LENS_MODEL

```cangjie
LENS_MODEL
```
功能： 镜头的型号名称。

#### LENS_SERIAL_NUMBER

```cangjie
LENS_SERIAL_NUMBER
```
功能： 镜头的序列号。

#### LENS_SPECIFICATION

```cangjie
LENS_SPECIFICATION
```
功能： 使用的镜头规格。

#### LIGHT_SOURCE

```cangjie
LIGHT_SOURCE
```
功能： 光源。

#### MAKE

```cangjie
MAKE
```
功能： 生产商。

#### MAKER_NOTE

```cangjie
MAKER_NOTE
```
功能： Exif/DCF制造商使用的标签，用于记录任何所需信息。

#### MAX_APERTURE_VALUE

```cangjie
MAX_APERTURE_VALUE
```
功能： 最小F数镜头。

#### METERING_MODE

```cangjie
METERING_MODE
```
功能： 测光模式。

#### MODEL

```cangjie
MODEL
```
功能： 设备型号。

#### NEW_SUBFILE_TYPE

```cangjie
NEW_SUBFILE_TYPE
```
功能： 在Exif中，"NewSubfileType"字段用于标识子文件的数据类型，如全分辨率图像、缩略图或多帧图像的一部分。其值是位掩码，0代表全分辨率图像，1代表缩略图，2代表多帧图像的一部分。

#### OECF

```cangjie
OECF
```
功能： 表示ISO 14524中规定的光电转换函数（OECF）。

#### OFFSET_TIME

```cangjie
OFFSET_TIME
```
功能： 在Exif中，OffsetTime字段表示与UTC（协调世界时）的时间偏移，格式为±HH:MM，用于确定照片拍摄的本地时间。

#### OFFSET_TIME_DIGITIZED

```cangjie
OFFSET_TIME_DIGITIZED
```
功能： 此标签记录图像数字化时的UTC偏移量，有助于准确调整时间戳。

#### OFFSET_TIME_ORIGINAL

```cangjie
OFFSET_TIME_ORIGINAL
```
功能： 此标签记录原始图像创建时的UTC偏移量，对于时间敏感的应用至关重要。

#### ORIENTATION

```cangjie
ORIENTATION
```
功能： 图片方向。 - 1：Top-left，图像未旋转。 - 2：Top-right，镜像水平翻转。 - 3：Bottom-right，图像旋转180°。 - 4：Bottom-left，镜像垂直翻转。 - 5：Left-top，镜像水平翻转再顺时针旋转270°。 - 6：Right-top，顺时针旋转90°。 - 7：Right-bottom，镜像水平翻转再顺时针旋转90°。 - 8：Left-bottom，顺时针旋转270°。 - 未定义值返回Unknown Value。

#### PHOTOGRAPHIC_SENSITIVITY

```cangjie
PHOTOGRAPHIC_SENSITIVITY
```
功能： 此标签指示拍摄图像时相机或输入设备的灵敏度。

#### PHOTOMETRIC_INTERPRETATION

```cangjie
PHOTOMETRIC_INTERPRETATION
```
功能： 像素构成，例如RGB或YCbCr。

#### PHOTO_MODE

```cangjie
PHOTO_MODE
```
功能： 拍照模式。

#### PHYSICAL_APERTURE

```cangjie
PHYSICAL_APERTURE
```
功能： 物理孔径，光圈大小。

#### PITCH_ANGLE

```cangjie
PITCH_ANGLE
```
功能： 俯仰角度。

#### PIXEL_X_DIMENSION

```cangjie
PIXEL_X_DIMENSION
```
功能： 像素X尺寸。

#### PIXEL_Y_DIMENSION

```cangjie
PIXEL_Y_DIMENSION
```
功能： 像素Y尺寸。

#### PLANAR_CONFIGURATION

```cangjie
PLANAR_CONFIGURATION
```
功能： 表示像素组件的记录格式，chunky格式或是planar格式。

#### PRIMARY_CHROMATICITIES

```cangjie
PRIMARY_CHROMATICITIES
```
功能： 图像的主要颜色的色度。

#### RECOMMENDED_EXPOSURE_INDEX

```cangjie
RECOMMENDED_EXPOSURE_INDEX
```
功能： 推荐曝光指数。

#### REFERENCE_BLACK_WHITE

```cangjie
REFERENCE_BLACK_WHITE
```
功能： 参考黑点值和参考白点值。

#### RELATED_SOUND_FILE

```cangjie
RELATED_SOUND_FILE
```
功能： 与图像数据相关的音频文件的名称。

#### RESOLUTION_UNIT

```cangjie
RESOLUTION_UNIT
```
功能： 用于测量XResolution和YResolution的单位。

#### ROLL_ANGLE

```cangjie
ROLL_ANGLE
```
功能： 滚动角度。

#### ROWS_PER_STRIP

```cangjie
ROWS_PER_STRIP
```
功能： 每个strip的图像数据行数。

#### SAMPLES_PER_PIXEL

```cangjie
SAMPLES_PER_PIXEL
```
功能： 每个像素的分量数。由于该标准适用于RGB和YCbCr图像，因此该标签的值设置为3。在JPEG压缩数据中，使用JPEG标记代替该标签。

#### SATURATION

```cangjie
SATURATION
```
功能： 相机应用的饱和度处理方向。

#### SCENE_BEACH_CONF

```cangjie
SCENE_BEACH_CONF
```
功能： 拍照场景：沙滩。

#### SCENE_BLUE_SKY_CONF

```cangjie
SCENE_BLUE_SKY_CONF
```
功能： 拍照场景：蓝天。

#### SCENE_CAPTURE_TYPE

```cangjie
SCENE_CAPTURE_TYPE
```
功能： 捕获的场景类型。

#### SCENE_FLOWERS_CONF

```cangjie
SCENE_FLOWERS_CONF
```
功能： 拍照场景：花。

#### SCENE_FOOD_CONF

```cangjie
SCENE_FOOD_CONF
```
功能： 拍照场景：食物。

#### SCENE_GREEN_PLANT_CONF

```cangjie
SCENE_GREEN_PLANT_CONF
```
功能： 拍照场景：绿植。

#### SCENE_NIGHT_CONF

```cangjie
SCENE_NIGHT_CONF
```
功能： 拍照场景：夜晚。

#### SCENE_POINTER

```cangjie
SCENE_POINTER
```
功能： 场景指针。

#### SCENE_SNOW_CONF

```cangjie
SCENE_SNOW_CONF
```
功能： 拍照场景：下雪。

#### SCENE_STAGE_CONF

```cangjie
SCENE_STAGE_CONF
```
功能： 拍照场景：舞台。

#### SCENE_SUNSET_CONF

```cangjie
SCENE_SUNSET_CONF
```
功能： 拍照场景：日落。

#### SCENE_TEXT_CONF

```cangjie
SCENE_TEXT_CONF
```
功能： 拍照场景：文本。

#### SCENE_TYPE

```cangjie
SCENE_TYPE
```
功能： 拍摄场景模式，例如人像、风光、运动、夜景等。

#### SCENE_VERSION

```cangjie
SCENE_VERSION
```
功能： 场景算法版本信息。

#### SENSING_METHOD

```cangjie
SENSING_METHOD
```
功能： 相机上的图像传感器类型。

#### SENSITIVITY_TYPE

```cangjie
SENSITIVITY_TYPE
```
功能： 灵敏度类型。

#### SHARPNESS

```cangjie
SHARPNESS
```
功能： 相机应用的锐度处理方向。

#### SHUTTER_SPEED

```cangjie
SHUTTER_SPEED
```
功能： 快门速度，以APEX（摄影曝光的加法系统）值表示。

#### SOFTWARE

```cangjie
SOFTWARE
```
功能： 用于生成图像的软件的名称和版本。

#### SOURCE_EXPOSURE_TIMES_OF_COMPOSITE_IMAGE

```cangjie
SOURCE_EXPOSURE_TIMES_OF_COMPOSITE_IMAGE
```
功能： 合成图像的源图像曝光时间。

#### SOURCE_IMAGE_NUMBER_OF_COMPOSITE_IMAGE

```cangjie
SOURCE_IMAGE_NUMBER_OF_COMPOSITE_IMAGE
```
功能： 用于合成图像的源图像数量。

#### SPATIAL_FREQUENCY_RESPONSE

```cangjie
SPATIAL_FREQUENCY_RESPONSE
```
功能： 相机或输入设备的空间频率表。

#### SPECTRAL_SENSITIVITY

```cangjie
SPECTRAL_SENSITIVITY
```
功能： 表示所用相机的每个通道的光谱灵敏度。

#### STANDARD_OUTPUT_SENSITIVITY

```cangjie
STANDARD_OUTPUT_SENSITIVITY
```
功能： 标准输出灵敏度。

#### STRIP_BYTE_COUNTS

```cangjie
STRIP_BYTE_COUNTS
```
功能： 每个图像数据带的总字节数。

#### STRIP_OFFSETS

```cangjie
STRIP_OFFSETS
```
功能： 每个strip的字节偏移量。

#### SUBFILE_TYPE

```cangjie
SUBFILE_TYPE
```
功能： 此标签指示此子文件中的数据类型。标签已弃用，请使用NewSubfileType替代。

#### SUBJECT_AREA

```cangjie
SUBJECT_AREA
```
功能： 该标签指示整个场景中主要主体的位置和区域。

#### SUBJECT_DISTANCE

```cangjie
SUBJECT_DISTANCE
```
功能： 测量单位为米的主体距离。

#### SUBJECT_DISTANCE_RANGE

```cangjie
SUBJECT_DISTANCE_RANGE
```
功能： 表示主体到相机的距离范围。

#### SUBJECT_LOCATION

```cangjie
SUBJECT_LOCATION
```
功能： 主要对象相对于左边缘的位置。

#### SUBSEC_TIME

```cangjie
SUBSEC_TIME
```
功能： 用于为DateTime标签记录秒的分数的标签。

#### SUBSEC_TIME_DIGITIZED

```cangjie
SUBSEC_TIME_DIGITIZED
```
功能： 用于为DateTimeDigitized标签记录秒的分数的标签。

#### SUBSEC_TIME_ORIGINAL

```cangjie
SUBSEC_TIME_ORIGINAL
```
功能： 用于为DateTimeOriginal标签记录秒的分数的标签。

#### TRANSFER_FUNCTION

```cangjie
TRANSFER_FUNCTION
```
功能： 图像的传递函数，通常用于颜色校正。

#### USER_COMMENT

```cangjie
USER_COMMENT
```
功能： 用户注释。

#### WHITE_BALANCE

```cangjie
WHITE_BALANCE
```
功能： 白平衡。

#### WHITE_POINT

```cangjie
WHITE_POINT
```
功能： 图像的白点色度。

#### WIND_SNAPSHOT_MODE

```cangjie
WIND_SNAPSHOT_MODE
```
功能： 运动快拍模式。

#### XMAGE_BOTTOM

```cangjie
XMAGE_BOTTOM
```
功能： 水印区域Y2坐标。

#### XMAGE_LEFT

```cangjie
XMAGE_LEFT
```
功能： 水印区域X1坐标。

#### XMAGE_MODE

```cangjie
XMAGE_MODE
```
功能： XMAGE水印模式。

#### XMAGE_RIGHT

```cangjie
XMAGE_RIGHT
```
功能： 水印区域X2坐标。

#### XMAGE_TOP

```cangjie
XMAGE_TOP
```
功能： 水印区域Y1坐标。

#### X_RESOLUTION

```cangjie
X_RESOLUTION
```
功能： 图像宽度方向的分辨率。

#### YCBCR_COEFFICIENTS

```cangjie
YCBCR_COEFFICIENTS
```
功能： 从RGB到YCbCr图像数据的转换矩阵系数。

#### YCBCR_POSITIONING

```cangjie
YCBCR_POSITIONING
```
功能： 色度分量相对于亮度分量的位置。

#### YCBCR_SUB_SAMPLING

```cangjie
YCBCR_SUB_SAMPLING
```
功能： 色度分量与亮度分量的采样比率。

#### Y_RESOLUTION

```cangjie
Y_RESOLUTION
```
功能： 图像高度方向的分辨率。


### ScaleMode

enum ScaleMode
```cangjie
public enum ScaleMode <: Equatable<ScaleMode> & ToString { | FIT_TARGET_SIZE | CENTER_CROP | ... }
```
功能： 图像的缩放模式。

#### CENTER_CROP

```cangjie
CENTER_CROP
```
功能： 缩放图像以填充目标图像区域并居中裁剪区域外的效果。

#### FIT_TARGET_SIZE

```cangjie
FIT_TARGET_SIZE
```
功能： 图像适合目标尺寸的效果。



