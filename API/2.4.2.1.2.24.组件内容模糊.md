# 组件内容模糊

为当前组件添加内容模糊效果。

## Functions

### public func foregroundBlurStyle(value: ForegroundBlurStyle, options!: Option<ForegroundBlurStyleOptions> = None)：This

```cangjie
public func foregroundBlurStyle(value: ForegroundBlurStyle, options!: Option<ForegroundBlurStyleOptions> = None)：This
```
功能： 为当前组件提供内容模糊能力。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ForegroundBlurStyle | 是 | 内容模糊样式。 | value | - |
| Option< ForegroundBlurStyleOptions > | 否 | 命名参数。 可选参数，内容模糊选项。 | options | None |


## Classes

### BlurStyleOptions

class BlurStyleOptions
```cangjie
public open class BlurStyleOptions { public BlurStyleOptions ( public let adaptiveColor: AdaptiveColor, public let blurOptions: BlurOptions, public let colorMode: ThemeColorMode, public let scale: Float32 ) }
```
功能： 内容模糊选项。

#### let adaptiveColor

```cangjie
public let adaptiveColor: AdaptiveColor = AdaptiveColor.DEFAULT
```
功能： 内容模糊效果使用的取色模式。
Type: AdaptiveColor; Readable: true; Writable: false

#### let blurOptions

```cangjie
public let blurOptions: BlurOptions = BlurOptions([0.0, 0.0])
```
功能： 灰阶模糊参数。
Type: BlurOptions; Readable: true; Writable: false

#### let colorMode

```cangjie
public let colorMode: ThemeColorMode = hemeColorMode.SYSTEM
```
功能： 内容模糊效果使用的深浅色模式。
Type: ThemeColorMode; Readable: true; Writable: false

#### let scale

```cangjie
public let scale: Float32
```
功能： 内容模糊效果程度。取值范围：[0.0, 1.0]。
Type: Float32; Readable: true; Writable: false


### BlurOptions

class BlurOptions
```cangjie
public class BlurOptions { public BlurOptions(public var grayscale: VArray<Float32, $2>) }
```
功能： 灰阶模糊参数。

#### var grayscale

```cangjie
public var grayscale: VArray<Float32, $2>
```
功能： 灰阶模糊参数，参数取值范围[0, 127]。
Type: VArray<Float32, $2>; Readable: true; Writable: true


### ForegroundBlurStyleOptions

class ForegroundBlurStyleOptions
```cangjie
public class ForegroundBlurStyleOptions { public ForegroundBlurStyleOptions( public var adaptiveColor!: AdaptiveColor = DEFAULT, public var blurOptions!: BlurOptions = BlurOptions([0.0, 0.0]), public var colorMode!: ThemeColorMode = SYSTEM, public var scale!: Float32 = 1.0 ) }
```
功能： 内容模糊选项。

#### var adaptiveColor

```cangjie
public var adaptiveColor: AdaptiveColor = DEFAULT
```
功能： 内容模糊效果使用的取色模式。
Type: AdaptiveColor; Readable: true; Writable: true

#### var blurOptions

```cangjie
public var blurOptions: BlurOptions = BlurOptions([0.0, 0.0])
```
功能： 灰阶模糊参数。
Type: BlurOptions; Readable: true; Writable: true

#### var colorMode

```cangjie
public var colorMode: ThemeColorMode = SYSTEM
```
功能： 内容模糊效果使用的深浅色模式。
Type: ThemeColorMode; Readable: true; Writable: true

#### var scale

```cangjie
public var scale: Float32 = 1.0
```
功能： 内容模糊效果程度。取值范围：[0.0, 1.0]。
Type: Float32; Readable: true; Writable: true



## Enums

### AdaptiveColor

enum AdaptiveColor
```cangjie
public enum AdaptiveColor { | DEFAULT | AVERAGE }
```
功能： 取色模式。

#### DEFAULT

```cangjie
DEFAULT
```
功能： 不使用取色模糊。使用默认的颜色作为蒙版颜色。采用非DEFAULT方式较耗时。

#### AVERAGE

```cangjie
AVERAGE
```
功能： 使用取色模糊。将取色区域的颜色平均值作为蒙版颜色。



## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*
import kit.LocalizationKit.*

@Entry
@Component
class EntryView {
 func build() {
 Column {
 Text("Thin Material")
 .fontSize(30)
 .fontColor(0xCCCCCC)

 Image(@r(app.media.startIcon)).foregroundBlurStyle(
 ForegroundBlurStyle.THIN,
 options: ForegroundBlurStyleOptions(colorMode: ThemeColorMode.LIGHT,
 adaptiveColor: AdaptiveColor.DEFAULT, blurOptions: BlurOptions([100.0, 100.0]), scale: 0.5)
 )
 }
 .width(100.percent)
 .height(100.percent)
 }
}
```

