# ohos.ability_access_ctrl（程序访问控制管理）

程序访问控制提供程序的权限管理能力，包括鉴权、授权和取消授权等。

```cangjie
import kit.AbilityKit.*
```
## Classes

### AbilityAccessCtrl

class AbilityAccessCtrl
```cangjie
public class AbilityAccessCtrl {}
```
功能： 此类用于创建管理访问控制模块的实例。


### AccessCtrlPermissionRequestResult

class AccessCtrlPermissionRequestResult
```cangjie
public class AccessCtrlPermissionRequestResult { public AccessCtrlPermissionRequestResult( public let permissions: Array<String>, public let authResults: Array<Int32>, public let dialogShownResults!: ?Array<Bool> = None ) }
```
功能： 权限请求结果对象，在调用 requestPermissionsFromUser 申请权限时返回此对象表明此次权限申请的结果。

#### let authResults

```cangjie
public let authResults: Array<Int32>
```
功能： 相应请求权限的结果。
Type: Array<Int32>; Readable: true; Writable: false

#### let dialogShownResults

```cangjie
public let dialogShownResults: ?Array<Bool> = None
```
功能： 此权限申请是否有弹窗。
Type: ?Array<Bool>; Readable: true; Writable: false

#### let permissions

```cangjie
public let permissions: Array<String>
```
功能： 用户传入的权限。
Type: Array<String>; Readable: true; Writable: false


### AtManager

class AtManager
```cangjie
public class AtManager {}
```
功能： 管理访问控制模块的实例。



## Enums

### GrantStatus

enum GrantStatus
```cangjie
public enum GrantStatus <: Equatable<GrantStatus> & ToString { | PERMISSION_DENIED | PERMISSION_GRANTED | ... }
```
功能： 表示授权状态。

#### PERMISSION_DENIED

```cangjie
PERMISSION_DENIED
```
功能： 未授权。

#### PERMISSION_GRANTED

```cangjie
PERMISSION_GRANTED
```
功能： 已授权。


### SwitchType

enum SwitchType
```cangjie
public enum SwitchType <: Equatable<SwitchType> & ToString { | CAMERA | MICROPHONE | LOCATION | ... }
```
功能： 表示全局开关类型。

#### CAMERA

```cangjie
CAMERA
```
功能： 相机全局开关。

#### LOCATION

```cangjie
LOCATION
```
功能： 位置全局开关。

#### MICROPHONE

```cangjie
MICROPHONE
```
功能： 麦克风全局开关。



## Types

### Permissions

type Permissions
```cangjie
public type Permissions = String
```
功能： 权限名，为一个字符串。



