# Rect

矩形绘制组件。

## Component Constructors

### public init(width!: Length, height!: Length)

```cangjie
public init(width!: Length, height!: Length)
```
功能： 绘制一个宽度为width，高度为height的矩形。异常值按照初始值处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 命名参数。 矩形宽度，取值范围≥0。 初始值：0。 默认单位：vp。 | width | - |
| Length | 是 | 命名参数。 矩形高度，取值范围≥0。 初始值：0。 默认单位：vp。 | height | - |

### public init()

```cangjie
public init()
```
功能： 绘制一个宽度为0，高度为0的矩形。需要设置 width 或 height 属性参数不为0才能显示出来。


## Functions

### public func radius(value: Length): This

```cangjie
public func radius(value: Length): This
```
功能： 设置圆角半径大小，取值范围≥0。异常值按照初始值处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 圆角半径大小。 初始值：0。 默认单位：vp。 | value | - |

### public func radius<T>(radiusArray: Array<T>) where T <: Length

```cangjie
public func radius<T>(radiusArray: Array<T>) where T <: Length
```
功能： 设置圆角半径大小，取值范围≥0。异常值按照初始值处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array<T> | 是 | 圆角半径大小。 初始值：0。 默认单位：vp。 | radiusArray | - |

### public func radius<T>(radiusArray: Array<(T, T)>) where T <: Length

```cangjie
public func radius<T>(radiusArray: Array<(T, T)>) where T <: Length
```
功能： 设置圆角半径大小，取值范围≥0。异常值按照初始值处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Array<(T,T)> | 是 | 圆角半径大小。 初始值：0。 默认单位：vp。 | radiusArray | - |

### public func radiusHeight(value: Length): This

```cangjie
public func radiusHeight(value: Length): This
```
功能： 设置圆角的高度，仅设置高时宽高一致。异常值按照初始值处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 设置圆角的高度。 初始值：0。 默认单位：vp。 | value | - |

### public func radiusWidth(value: Length): This

```cangjie
public func radiusWidth(value: Length): This
```
功能： 设置圆角的宽度，仅设置宽时宽高一致。异常值按照初始值处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 圆角的宽度。 初始值：0。 默认单位：vp。 | value | - |


## Examples

```cangjie
package ohos_app_cangjie_entry
import kit.UIKit.*
import ohos.state_macro_manage.*

@Entry
@Component
class EntryView {
 func build() {
 Column(10) {
 Text("normal").fontSize(11).fontColor(0xCCCCCC).width(90.percent)
 // 绘制90% * 50的矩形
 Column(5) {
 Text("normal").fontSize(9).fontColor(0xCCCCCC).width(90.percent)
 // 绘制90% * 50矩形
 Rect().width(90.percent).height(50).fill(Color.GREEN)
 // 绘制90% * 50的矩形框
 Rect()
 .width(90.percent)
 .height(50)
 .fillOpacity(0)
 .stroke(Color.RED)
 .strokeWidth(3)

 Text("with rounded corners").fontSize(11).fontColor(0xCCCCCC).width(90.percent)
 // 绘制90% * 80的矩形, 圆角宽高分别为40、20
 Rect()
 .width(90.percent)
 .height(50)
 .radiusHeight(20)
 .radiusWidth(40)
 .fill(Color.GREEN)
 // 绘制90% * 80的矩形, 圆角宽高为20
 Rect()
 .width(90.percent)
 .height(80)
 .radius(20)
 .fill(Color.GREEN)
 .stroke(Color.TRANSPARENT)
 }.width(100.percent).margin(top: 10)
 // 绘制90% * 50矩形, 左上圆角宽高40,右上圆角宽高20,右下圆角宽高40,左下圆角宽高20
 Rect()
 .width(90.percent)
 .height(80)
 .radius([(40, 40), (20, 20), (40, 40), (20, 20)])
 .fill(Color.GREEN)
 }.width(100.percent).margin(top: 5)
 }
}
```

