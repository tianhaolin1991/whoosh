# ImageBitmap

ImageBitmap对象可以存储canvas渲染的像素数据。

```cangjie
import kit.UIKit.*
```
## Classes

### ImageBitmap

class ImageBitmap
```cangjie
public class ImageBitmap { public init(src: String) public init(src: String, width: Float64, height: Float64) public init(src: String, width: Int64, height: Int64) public init(date: PixelMap, unit!: LengthMetricsUnit = LengthMetricsUnit.DEFAULT) public init(date: String, unit: LengthMetricsUnit) }
```
功能： ImageBitmap对象可以存储canvas渲染的像素数据。

#### prop height

```cangjie
public prop height: Float64
```
功能： ImageBitmap的像素高度。默认单位为vp。
Type: Float64; Readable: true; Writable: false

#### prop width

```cangjie
public prop width: Float64
```
功能： ImageBitmap的像素宽度。默认单位为vp。
Type: Float64; Readable: true; Writable: false



