# Swiper

滑块视图容器，提供子组件滑动轮播显示的能力。

## Component Constructors

### public init(controller: SwiperController, subcomponent: () -> Unit)

```cangjie
public init(controller: SwiperController, subcomponent: () -> Unit)
```
功能： 创建一个包含Swiper控制器和子组件的Swiper对象。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| SwiperController | 是 | 给组件绑定一个控制器，用来控制组件翻页。 | controller | - |
| ()->Unit | 是 | Swiper容器的子组件。 | subcomponent | - |

### public init(subcomponent: () -> Unit)

```cangjie
public init(subcomponent: () -> Unit)
```
功能： 创建一个可包含子组件的Swiper容器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ()->Unit | 是 | Swiper容器的子组件。 | subcomponent | - |


## Functions

### public func autoPlay(value: Bool): This

```cangjie
public func autoPlay(value: Bool): This
```
功能： 设置子组件是否自动播放。 loop 为false时，自动轮播到最后一页时停止轮播。手势切换后不是最后一页时继续播放。当Swiper不可见时会停止轮播。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 子组件是否自动播放。 初始值：false，不自动轮播。 | value | - |

### public func cachedCount(value: Int32): This

```cangjie
public func cachedCount(value: Int32): This
```
功能： 设置预加载子组件个数，以当前页面为基准，加载当前显示页面的前后个数。例如cachedCount=1时，会将当前显示的页面的前面一页和后面一页的子组件都预加载。如果设置为按组翻页，即displayCount的swipeByGroup参数设为true，预加载时会以组为基本单位。例如cachedCount=1，swipeByGroup=true时，会将当前组的前面一组和后面一组的子组件都预加载。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 预加载子组件个数。 初始值：1。 取值范围：[0, +∞)，设置小于0的值时，按照初始值处理。 | value | - |

### public func curve(value: Curve): This

```cangjie
public func curve(value: Curve): This
```
功能： 设置Swiper的动画曲线，默认为弹簧插值曲线。常用曲线参考 Curve枚举说明 。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Curve | 是 | Swiper的动画曲线。 | value | - |

### public func disableSwipe(value: Bool): This

```cangjie
public func disableSwipe(value: Bool): This
```
功能： 设置禁用组件滑动切换功能。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否禁用组件滑动切换功能。设置为true禁用，false不禁用。 初始值：false。 | value | - |

### public func displayArrow(value: Bool, isHoverShow!: Bool = false): This

```cangjie
public func displayArrow(value: Bool, isHoverShow!: Bool = false): This
```
功能： 设置导航点箭头样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 支持设置箭头和底板样式。设置为false不显示箭头和底板，true显示默认的箭头和底板样式。 初始值：false。 | value | - |
| Bool | 否 | 命名参数。 设置鼠标悬停时是否显示箭头。 说明： 1、isHoverShow为false时，常驻显示箭头。 2、isHoverShow为true时，有导航点时鼠标悬停在导航点和箭头范围内显示箭头，无导航点时鼠标悬停在Swiper显示范围内显示箭头。 3、箭头显示时，支持点击翻页。 | isHoverShow | false |

### public func displayArrow(value: ArrowStyle, isHoverShow!: Bool = false): This

```cangjie
public func displayArrow(value: ArrowStyle, isHoverShow!: Bool = false): This
```
功能： 设置导航点箭头样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| ArrowStyle | 是 | 支持设置箭头和底板样式。 | value | - |
| Bool | 否 | 命名参数。 设置鼠标悬停时是否显示箭头。 说明： 1、isHoverShow为false时，常驻显示箭头。 2、isHoverShow为true时，有导航点时鼠标悬停在导航点和箭头范围内显示箭头，无导航点时鼠标悬停在Swiper显示范围内显示箭头。 3、箭头显示时，支持点击翻页。 | isHoverShow | false |

### public func displayCount(value: Int32): This

```cangjie
public func displayCount(value: Int32): This
```
功能： 设置Swiper视窗内元素显示个数。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 视窗内显示的子元素个数。设置小于等于0的值时，按照初始值处理。 初始值：1。 取值范围：(0, +∞)，设置小于等于0的值时，按照初始值处理。 | value | - |

### public func displayCount(value: Int32, swipeByGroup: Bool): This

```cangjie
public func displayCount(value: Int32, swipeByGroup: Bool): This
```
功能： 设置Swiper视窗内元素显示个数。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Int32 | 是 | 视窗内显示的子元素个数。设置小于等于0的值时，按照初始值处理。 初始值：1。 取值范围：(0, +∞)，设置小于等于0的值时，按照初始值处理。 | value | - |
| Bool | 是 | 是否按组进行翻页。如果设为true，在翻页时会按组进行翻页，每组内子元素的数量为displayCount value的值；如果为false，则为默认翻页行为，即按照子元素进行翻页。 初始值：false。 | swipeByGroup | - |

### public func displayCount(value: SwiperAutoFill, swipeByGroup: Bool): This

```cangjie
public func displayCount(value: SwiperAutoFill, swipeByGroup: Bool): This
```
功能： 设置Swiper视窗内元素显示个数。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| SwiperAutoFill | 是 | 视窗内显示的子元素个数。 | value | - |
| Bool | 是 | 是否按组进行翻页。如果设为true，在翻页时会按组进行翻页，每组内子元素的数量为displayCount value的值；如果为false，则为默认翻页行为，即按照子元素进行翻页。 初始值：false。 | swipeByGroup | - |

### public func displayCount(value: SwiperAutoFill): This

```cangjie
public func displayCount(value: SwiperAutoFill): This
```
功能： 设置Swiper视窗内元素显示个数。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| SwiperAutoFill | 是 | 视窗内显示的子元素个数。 | value | - |

### public func displayMode(value: SwiperDisplayMode): This

```cangjie
public func displayMode(value: SwiperDisplayMode): This
```
功能： 设置主轴方向上元素排列的模式，优先以displayCount设置的个数显示，displayCount未设置时本属性生效。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| SwiperDisplayMode | 是 | 主轴方向上元素排列的模式。 初始值：SwiperDisplayMode.STRETCH。 | value | - |

### public func duration(value: UInt32): This

```cangjie
public func duration(value: UInt32): This
```
功能： 设置子组件切换的动画时长。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| UInt32 | 是 | 子组件切换的动画时长，单位为毫秒。设置小于0的值时，按照初始值处理。 初始值：400。 取值范围：[0, +∞)，设置小于0的值时，按照初始值处理。 | value | - |

### public func effectMode(value: EdgeEffect): This

```cangjie
public func effectMode(value: EdgeEffect): This
```
功能： 设置边缘滑动效果， loop = false时生效。调用SwiperController.changeIndex()、SwiperController.showNext()和SwiperController.showPrevious()接口跳转至首尾页时不生效回弹。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| EdgeEffect | 是 | 边缘滑动效果。 初始值：EdgeEffect.Spring。 | value | - |

### public func index(value: UInt32): This

```cangjie
public func index(value: UInt32): This
```
功能： 设置当前在容器中显示的子组件的索引值。设置大于等于子组件数量时，按照初始值0处理。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| UInt32 | 是 | 当前在容器中显示的子组件的索引值。 说明： 设置的值小于0或大于最大页面索引时，取0。 初始值：0。 | value | - |

### public func indicator(value: Bool): This

```cangjie
public func indicator(value: Bool): This
```
功能： 设置可选导航点指示器样式。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 可选导航点指示器样式。 - boolean：是否启用导航点指示器。设置为true启用，false不启用。 初始值：true。 | value | - |

### public func indicator(value: DotIndicator): This

```cangjie
public func indicator(value: DotIndicator): This
```
功能： 设置外部绑定的导航点组件控制器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| DotIndicator | 是 | 可选导航点指示器样式。 - DotIndicator：圆点指示器样式。 | value | - |

### public func indicator(value: DigitIndicator): This

```cangjie
public func indicator(value: DigitIndicator): This
```
功能： 设置外部绑定的导航点组件控制器。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| DigitIndicator | 是 | 可选导航点指示器样式。 - DigitIndicator：数字指示器样式。 | value | - |

### public func indicatorInteractive(value: Bool): This

```cangjie
public func indicatorInteractive(value: Bool): This
```
功能： 设置禁用组件导航点交互功能。设置为true时表示导航点可交互。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 导航点是否可交互。true为导航点可交互，false为导航点不可交互。 初始值：true。 | value | - |

### public func indicatorStyle( left!: Length = (-1.0).vp, top!: Length = (-1.0).vp, right!: Length = (-1.0).vp, bottom!: Length = (-1.0).vp, size!: Length = (10.0).vp, color!: ResourceColor = Color(0x0c000000), selectedColor!: ResourceColor = Color(0xff0a59f7), mask!: Bool = false ): This

```cangjie
public func indicatorStyle( left!: Length = (-1.0).vp, top!: Length = (-1.0).vp, right!: Length = (-1.0).vp, bottom!: Length = (-1.0).vp, size!: Length = (10.0).vp, color!: ResourceColor = Color(0x0c000000), selectedColor!: ResourceColor = Color(0xff0a59f7), mask!: Bool = false ): This
```
功能： 设置导航点样式。建议使用 indicator 代替。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 否 | 命名参数。 设置导航点距离Swiper组件左边的距离。 未设置left和right时，进行自适应大小布局，按照指示器本身大小和Swiper的大小在主轴方向上进行居中对齐。设置为0时，按照0位置布局计算。 优先级：高于right属性 取值范围：[0,Swiper宽度-导航点区域宽度]，超出该范围时，取最近的的边界值。 | left | (- 1.0).vp |
| Length | 否 | 命名参数。 设置导航点距离Swiper组件顶部的距离。 未设置top和bottom时，进行自适应大小布局，按照指示器本身大小和Swiper的大小，在交叉轴方向上，位于底部，效果与设置bottom=0一致。设置为0时：按照0位置布局计算。 优先级：高于bottom属性 取值范围：[0,Swiper高度-导航点区域高度]，超出该范围时，取最近的边界值。 | top | (- 1.0).vp |
| Length | 否 | 命名参数。 设置导航点距离Swiper组件右边的距离。 未设置left和right时，进行自适应大小布局，按照指示器本身大小和Swiper的大小在主轴方向上进行居中对齐。设置为0时：按照0位置布局计算。 优先级：低于left属性 取值范围：[0,Swiper宽度-导航点区域宽度]，超出该范围时，取最近的边界值。 | right | (- 1.0).vp |
| Length | 否 | 命名参数。 设置导航点距离Swiper组件底部的距离。 未设置top和bottom时，进行自适应大小布局，按照指示器本身大小和Swiper的大小，在交叉轴方向上，位于底部，效果与设置bottom=0一致。设置为0时：按照0位置布局计算。 优先级：低于top属性 取值范围：[0,Swiper高度-导航点区域高度]，超出该范围时，取最近的边界值。 | bottom | (- 1.0).vp |
| Length | 否 | 命名参数。 设置导航点的直径，不支持设置百分比。 | size | (10.0).vp |
| ResourceColor | 否 | 命名参数。 设置导航点的颜色。 | color | Color(0x0c000000) |
| ResourceColor | 否 | 命名参数。 设置选中的导航点的颜色。 | selectedColor | Color(0xff0a59f7) |
| Bool | 否 | 命名参数。 设置是否显示导航点蒙层样式。设置为true时显示导航点蒙层样式，为false时不显示。 | mask | false |

### public func interval(value: UInt32): This

```cangjie
public func interval(value: UInt32): This
```
功能： 设置使用自动播放时播放的时间间隔。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| UInt32 | 是 | 自动播放时播放的时间间隔。当小于 duration 属性值时，翻页完成后会立即开始下一次轮播。 初始值：3000。 单位：毫秒。 设置小于0的值时，按照初始值处理。 | value | - |

### public func itemSpace(value: Length): This

```cangjie
public func itemSpace(value: Length): This
```
功能： 设置子组件与子组件之间间隙。不支持设置百分比。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 子组件与子组件之间间隙。 类型为Int64、Float64时，默认单位vp。 当设置数值小于0或超出Swiper组件宽度范围时，按照初始值处理。 初始值：0。 | value | - |

### public func loop(value: Bool): This

```cangjie
public func loop(value: Bool): This
```
功能： 设置是否开启循环。设置为true时表示开启循环，在LazyForEach懒循环加载模式下，加载的组件数量建议大于5个。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否开启循环。true为开启循环，false为不开启循环 初始值：true。 | value | - |

### public func nestedScroll(value: SwiperNestedScrollMode): This

```cangjie
public func nestedScroll(value: SwiperNestedScrollMode): This
```
功能： 设置Swiper组件和父组件的嵌套滚动模式。loop为true时Swiper组件没有边缘，不会触发父组件嵌套滚动。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| SwiperNestedScrollMode | 是 | Swiper组件和父组件的嵌套滚动模式。 初始值：SwiperNestedScrollMode.SELF_ONLY。 | value | - |

### public func nextMargin(value: Length, ignoreBlank !: Bool = false): This

```cangjie
public func nextMargin(value: Length, ignoreBlank !: Bool = false): This
```
功能： 设置后边距，用于露出后一项的一小部分。仅当Swiper子组件的布局方式为拉伸时生效，主要包括两种场景：1、displayMode属性设置为SwiperDisplayMode.STRETCH；2、displayCount属性设置为Int32类型。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 后边距。不支持设置百分比。 初始值：0。 | value | - |
| Bool | 否 | 命名参数。 非loop场景下尾页不显示nextMargin。在非loop场景下，设置为true时，尾页不显示空白的nextMargin，尾页的右边缘与Swiper视窗右边缘对齐；设置false时，尾页显示空白nextMargin，尾页的右边缘与Swiper视窗右边缘的距离为nextMargin。 初始值：false。 说明： 尾页场景下，prevMargin和nextMargin的值相加作为左边边距显示前一个页面。 | ignoreBlank | false |

### public func prevMargin(value: Length, ignoreBlank !: Bool = false): This

```cangjie
public func prevMargin(value: Length, ignoreBlank !: Bool = false): This
```
功能： 设置前边距，用于露出前一项的一小部分。仅当Swiper子组件的布局方式为拉伸时生效，主要包括两种场景：1、displayMode属性设置为SwiperDisplayMode.STRETCH；2、displayCount属性设置为Int32类型。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Length | 是 | 前边距。不支持设置百分比。 初始值：0。 | value | - |
| Bool | 否 | 命名参数。 非loop场景下首页不显示prevMargin。在非loop场景下，设置为true时，首页不显示空白的prevMargin，首页的左边缘与Swiper视窗左边缘对齐；设置false时，首页显示空白prevMargin，首页的左边缘与Swiper视窗左边缘的距离为prevMargin。 初始值：false。 说明： 首页场景下，prevMargin和nextMargin的值相加作为右边边距显示后一个页面。 | ignoreBlank | false |

### public func vertical(value: Bool): This

```cangjie
public func vertical(value: Bool): This
```
功能： 设置是否纵向滑动。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| Bool | 是 | 是否为纵向滑动。true为纵向滑动，false为横向滑动。 初始值：false。 | value | - |

### public func customContentTransition(transition: SwiperContentAnimatedTransition): This

```cangjie
public func customContentTransition(transition: SwiperContentAnimatedTransition): This
```
功能： 自定义Swiper页面切换动画。在页面跟手滑动和离手后执行切换动画的过程中，会对视窗内所有页面逐帧触发回调，开发者可以在回调中设置透明度、缩放比例、位移等属性来自定义切换动画。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| SwiperContentAnimatedTransition | 是 | Swiper自定义切换动画相关信息。 | transition | - |

### public func onAnimationEnd(callback: (Int32, SwiperAnimationEvent) -> Unit): This

```cangjie
public func onAnimationEnd(callback: (Int32, SwiperAnimationEvent) -> Unit): This
```
功能： 当Swiper切换动效结束时触发该事件，包括动画过程中手势中断，通过SwiperController调用finishAnimation。参数为动画结束后的index值，多列Swiper时，index为最左侧组件的索引。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| (Int32, SwiperAnimationEvent )->Unit | 是 | 回调函数，切换动画结束时触发。 参数一：当前显示元素的索引。 参数二：动画相关信息，只返回主轴方向上当前显示元素相对于Swiper起始位置的位移。 | callback | - |

### public func onAnimationStart(callback: (Int32, Int32, SwiperAnimationEvent) -> Unit): This

```cangjie
public func onAnimationStart(callback: (Int32, Int32, SwiperAnimationEvent) -> Unit): This
```
功能： 切换动画开始时触发该事件。参数为动画开始前的index值（不是最终结束动画的index值），多列Swiper时，index为最左侧组件的索引。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| (Int32,Int32, SwiperAnimationEvent )->Unit | 是 | 回调函数，切换动画开始时触发。 参数一：当前显示元素的索引。 参数二：切换动画目标元素的索引。 参数三：动画相关信息，包括主轴方向上当前显示元素和目标元素相对Swiper起始位置的位移，以及离手速度。 | callback | - |

### public func onChange(callback: (Int32) -> Unit): This

```cangjie
public func onChange(callback: (Int32) -> Unit): This
```
功能： 当前显示的子组件索引变化时触发该事件，返回值为当前显示的子组件的索引值。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| (Int32)->Unit | 是 | 回调函数，当前显示的子组件索引变化时触发该事件。 参数一：当前显示元素的索引。 | callback | - |

### public func onContentDidScroll(callback: (Int32, Int32, Float64, Float64) -> Unit): This

```cangjie
public func onContentDidScroll(callback: (Int32, Int32, Float64, Float64) -> Unit): This
```
功能： 监听Swiper页面滑动事件。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| (Int32,Int32,Float64,Float64)->Unit | 是 | 回调函数，Swiper滑动时触发。 参数一：当前显示元素的索引。 参数二：视窗内页面的索引。 参数三：index页面相对于Swiper主轴起始位置（selectedIndex对应页面的起始位置）的移动比例。 参数四：index对应页面在主轴方向上的长度。 | callback | - |

### public func onGestureSwipe(callback: (Int32, SwiperAnimationEvent) -> Unit): This

```cangjie
public func onGestureSwipe(callback: (Int32, SwiperAnimationEvent) -> Unit): This
```
功能： 在页面跟手滑动过程中，逐帧触发该事件。多列Swiper时，index为最左侧组件的索引。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| (Int32, SwiperAnimationEvent )->Unit | 是 | 回调函数，切页面跟手滑动过程中，逐帧触发。 参数一：当前显示元素的索引。 参数二：动画相关信息，只返回主轴方向上当前显示元素相对于Swiper起始位置的位移。 | callback | - |


## Classes

### DigitIndicator

class DigitIndicator
```cangjie
public class DigitIndicator <: Indicator { public init() }
```
功能： 构造数字指示器的样式。


### DotIndicator

class DotIndicator
```cangjie
public class DotIndicator <: Indicator { public init() }
```
功能： DotIndicator的构造函数。


### FontOptions

class FontOptions
```cangjie
public class FontOptions { public var size: Length public var weight: FontWeight public init(size!: Length = 14.vp, weight!: FontWeight = FontWeight.Normal) }
```
功能： Swiper组件数字导航点的字体样式。

#### var size

```cangjie
public var size: Length
```
功能： 数字导航点指示器的字体大小，不支持设置百分比。
Type: Length; Readable: true; Writable: true

#### var weight

```cangjie
public var weight: FontWeight
```
功能： 数字导航点选中指示器的字体粗细。
Type: FontWeight; Readable: true; Writable: true


### Indicator

class Indicator
```cangjie
public open class Indicator {}
```
功能： 设置导航点距离Swiper组件距离。由于导航点有默认交互区域，交互区域高度为32.vp，所以无法让显示部分完全贴底。


### SwiperController

class SwiperController
```cangjie
public class SwiperController { public init() }
```
功能： SwiperController是Swiper容器组件的控制器，可以定义该类型的对象并绑定至Swiper组件，实现控制子组件的翻页。


### ArrowStyle

class ArrowStyle
```cangjie
public class ArrowStyle { public var showBackground: Bool public var isSidebarMiddle: Bool public var backgroundSize: Length public var backgroundColor: UInt32 public var arrowSize: Length public var arrowColor: UInt32 public init(showBackground!: Bool = false, isSidebarMiddle!: Bool = false, backgroundSize!: Length = 24.vp, backgroundColor!: ResourceColor = Color(0x00000000), arrowSize!: Length = 18.vp, arrowColor!: ResourceColor = Color(0x182431)) }
```
功能： 左右箭头属性。

#### var arrowColor

```cangjie
public var arrowColor: UInt32
```
功能： 设置箭头颜色。
Type: UInt32; Readable: true; Writable: true

#### var arrowSize

```cangjie
public var arrowSize: Length
```
功能： 设置箭头大小。
Type: Length; Readable: true; Writable: true

#### var backgroundColor

```cangjie
public var backgroundColor: UInt32
```
功能： 设置底板颜色。
Type: UInt32; Readable: true; Writable: true

#### var backgroundSize

```cangjie
public var backgroundSize: Length
```
功能： 设置底板大小。
Type: Length; Readable: true; Writable: true

#### var isSidebarMiddle

```cangjie
public var isSidebarMiddle: Bool
```
功能： 设置箭头显示位置。
Type: Bool; Readable: true; Writable: true

#### var showBackground

```cangjie
public var showBackground: Bool
```
功能： 设置箭头底板是否显示。
Type: Bool; Readable: true; Writable: true


### SwiperAnimationEvent

class SwiperAnimationEvent
```cangjie
public class SwiperAnimationEvent { public SwiperAnimationEvent( public var currentOffset: Float64, public var targetOffset: Float64, public var velocity: Float64 ) }
```
功能： Swiper组件动画相关信息集合。

#### var currentOffset

```cangjie
public var currentOffset: Float64
```
功能： Swiper当前显示元素在主轴方向上，相对于Swiper起始位置的位移。单位vp，初始值为0.0。
Type: Float64; Readable: true; Writable: true

#### var targetOffset

```cangjie
public var targetOffset: Float64
```
功能： Swiper当前显示元素在主轴方向上，相对于Swiper起始位置的位移。单位vp，初始值为0.0。
Type: Float64; Readable: true; Writable: true

#### var velocity

```cangjie
public var velocity: Float64
```
功能： Swiper当前显示元素在主轴方向上，相对于Swiper起始位置的位移。单位vp，初始值为0.0。
Type: Float64; Readable: true; Writable: true


### SwiperAutoFill

class SwiperAutoFill
```cangjie
public class SwiperAutoFill { public var minSize: Length public init(minSize: Float64) public init(minSize: Int64) }
```
功能： 自适应属性。

#### var minSize

```cangjie
public var minSize: Length
```
功能： 设置元素显示最小宽度。
Type: Length; Readable: true; Writable: true


### SwiperContentAnimatedTransition

class SwiperContentAnimatedTransition
```cangjie
public class SwiperContentAnimatedTransition { public SwiperContentAnimatedTransition( public var timeout: Int32, public var transition: (SwiperContentTransitionProxy) -> Unit ) }
```
功能： Swiper自定义切换动画相关信息。

#### var timeout

```cangjie
public var timeout: Int32
```
功能： Swiper自定义切换动画超时时间。从页面执行默认动画（页面滑动）至移出视窗外的第一帧开始计时，如果到达该时间后，开发者仍未调用 SwiperContentTransitionProxy 的finishTransition接口通知Swiper组件此页面的自定义动画已结束，那么组件就会认为此页面的自定义动画已结束，立即将该页面节点下渲染树。单位ms。
Type: Int32; Readable: true; Writable: true

#### var transition

```cangjie
public var transition:(SwiperContentTransitionProxy) -> Unit
```
功能： 自定义切换动画具体内容。
Type: ( SwiperContentTransitionProxy )->Unit; Readable: true; Writable: true


### SwiperContentTransitionProxy

class SwiperContentTransitionProxy
```cangjie
public class SwiperContentTransitionProxy { public let selectedIndex: Int32 public let index: Int32 public let position: Float64 public let mainAxisLength: Float64 }
```
功能： Swiper自定义切换动画执行过程中，返回给开发者的proxy对象。开发者可通过该对象获取自定义动画视窗内的页面信息，同时，也可以通过调用该对象的finishTransition接口通知Swiper组件页面自定义动画已结束。

#### let index

```cangjie
public let index: Int32
```
功能： 视窗内页面的索引。
Type: Int32; Readable: true; Writable: false

#### let mainAxisLength

```cangjie
public let mainAxisLength: Float64
```
功能： index对应页面在主轴方向上的长度，单位vp。
Type: Float64; Readable: true; Writable: false

#### let position

```cangjie
public let position: Float64
```
功能： index页面相对于Swiper主轴起始位置（selectedIndex对应页面的起始位置）的移动比例。
Type: Float64; Readable: true; Writable: false

#### let selectedIndex

```cangjie
public let selectedIndex: Int32
```
功能： 当前选中页面的索引。
Type: Int32; Readable: true; Writable: false



## Enums

### SwiperDisplayMode

enum SwiperDisplayMode
```cangjie
public enum SwiperDisplayMode { | STRETCH | AUTO_LINEAR }
```
功能： Swiper在主轴上的尺寸大小模式枚举。

#### STRETCH

```cangjie
STRETCH
```
功能： Swiper滑动一页的宽度为Swiper组件自身的宽度。

#### AUTO_LINEAR deprecated

```cangjie
AUTO_LINEAR
```
功能： Swiper滑动一页的宽度为视窗内最左侧子组件的宽度。已废弃，建议使用 Scroller.scrollto 代替。



## Examples

```cangjie
package ohos_app_cangjie_entry

import kit.UIKit.*
import ohos.state_macro_manage.*
import std.collection.ArrayList

class MyDataSource<T> <: IDataSource<T> {
 private var list: ArrayList<T> = ArrayList<T>([])

 MyDataSource(list: ArrayList<T>) {
 this.list = list
 }

 public func totalCount(): Int64 {
 return this.list.size
 }

 public func getData(index: Int64): T {
 return this.list[index]
 }

 public func onRegisterDataChangeListener(listener: DataChangeListener): Unit {
 }

 public func onUnregisterDataChangeListener(listener: DataChangeListener): Unit {
 }
}

@Entry
@Component
class EntryView {
 private var swiperController: SwiperController = SwiperController()
 private var data: MyDataSource<Int64> = MyDataSource<Int64>(ArrayList<Int64>([]))

 protected override func aboutToAppear() {
 var list: ArrayList<Int64> = ArrayList<Int64>([])
 for (i in 0..10) {
 list.add(i)
 }
 this.data = MyDataSource<Int64>(list)
 }

 func build() {
 Column(5) {
 Swiper(this.swiperController) {
 LazyForEach(
 this.data,
 itemGeneratorFunc: {
 item: Int64, idx: Int64 => Text(item.toString())
 .width(90.percent)
 .height(160)
 .backgroundColor(0xAFEEEE)
 .textAlign(TextAlign.Center)
 .fontSize(30)
 }
 )
 }
 .cachedCount(2)
 .index(1)
 .autoPlay(true)
 .interval(4000)
 .loop(true)
 .indicatorInteractive(true)
 .duration(1000)
 .itemSpace(0)
 .indicator( // 设置圆点导航点样式
 DotIndicator()
 .itemWidth(15)
 .itemHeight(15)
 .selectedItemWidth(15)
 .selectedItemHeight(15)
 .color(Color.GRAY)
 .selectedColor(Color.BLUE))
 .displayArrow( // 设置导航点箭头样式
 ArrowStyle(
 showBackground: true,
 isSidebarMiddle: true,
 backgroundSize: 24,
 backgroundColor: Color.WHITE,
 arrowSize: 18,
 arrowColor: Color.BLUE
 ),
 isHoverShow: true
 )
 .curve(Curve.Linear)

 Row(12) {
 Button("showNext").onClick({
 _ => this
 .swiperController
 .showNext()
 })

 Button("showPrevious").onClick({
 _ => this
 .swiperController
 .showPrevious()
 })
 }.margin(5)

 Row(5) {
 Button("FAST 0").onClick({
 _ => this
 .swiperController
 .changeIndex(0, true)
 })

 Button("FAST 3").onClick({
 _ => this
 .swiperController
 .changeIndex(3, true)
 })

 Button("FAST 9").onClick({
 _ => this
 .swiperController
 .changeIndex(9, true)
 })
 }.margin(5)
 }
 .width(100.percent)
 .margin(top: 5)
 }
}
```

