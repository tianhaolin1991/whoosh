# ohos.crypto（加解密算法库框架）

为屏蔽底层硬件和算法库，向上提供统一的密码算法库加解密相关接口。

```cangjie
import kit.CryptoArchitectureKit.*
```
## Functions

### public func createAsyKeyGenerator(algName: String): AsyKeyGenerator

```cangjie
public func createAsyKeyGenerator(algName: String): AsyKeyGenerator
```
功能： 通过指定算法名称的字符串，获取相应的非对称密钥生成器实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 非对称密钥生成支持的算法名 。 | algName | - |

**Returns:**

| type | description |
| --- | --- |
| AsyKeyGenerator | 返回非对称密钥生成器的对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 crypto framework错误码 。

| code | description |
| --- | --- |
| 401 | invalid parameters. Possible causes: 1. Mandatory parameters are left unspecified; 2. Incorrect parameter types; 3. Parameter verification failed. |
| 801 | this operation is not supported. |
| 17620001 | memory error. |


**Example:**

```cangjie
// index.cj

import kit.CryptoArchitectureKit.*

let asyKeyGenerator = createAsyKeyGenerator('ECC256')
```

### public func createAsyKeyGeneratorBySpec(asyKeySpec: AsyKeySpec): AsyKeyGeneratorBySpec

```cangjie
public func createAsyKeyGeneratorBySpec(asyKeySpec: AsyKeySpec): AsyKeyGeneratorBySpec
```
功能： 通过指定密钥参数，获取相应的非对称密钥生成器实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| AsyKeySpec | 是 | 密钥参数。非对称密钥生成器根据指定的这些参数生成公/私钥。 | asyKeySpec | - |

**Returns:**

| type | description |
| --- | --- |
| AsyKeyGeneratorBySpec | 返回非对称密钥生成器实例。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 crypto framework错误码 。

| code | description |
| --- | --- |
| 401 | invalid parameters. Possible causes: 1. Mandatory parameters are left unspecified; 2. Incorrect parameter types; 3. Parameter verification failed. |
| 801 | this operation is not supported. |
| 17620001 | memory error. |


**Example:**

```cangjie
// index.cj

import kit.CryptoArchitectureKit.*
import std.math.numeric.BigInt

let dsaCommonSpec = DSACommonParamsSpec(
 algName: "DSA",
 specType: KEY_PAIR_SPEC,
 p: BigInt(
 "",
 base: 16
 ),
 q: BigInt("d23304044019d5d382cfeabf351636c7ab219694ac845051f60b047b", base: 16),
 g: BigInt(
 "",
 base: 16
 )
)

let generator = createAsyKeyGenerator("DSA1024")
let keyPair = generator.generateKeyPair()

let dsaKeyPairSpec = DSAKeyPairSpec(
 params: dsaCommonSpec,
 sk: BigInt(keyPair
 .priKey
 .getEncoded()
 .data),
 pk: BigInt(keyPair
 .pubKey
 .getEncoded()
 .data),
)
let asyKeyGeneratorBySpec = createAsyKeyGeneratorBySpec(dsaKeyPairSpec)
```

### public func createCipher(transformation: String): Cipher

```cangjie
public func createCipher(transformation: String): Cipher
```
功能： 通过指定算法名称，获取相应的 Cipher 实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 待生成Cipher的算法名称（含密钥长度）、加密模式以及填充方法的组合。 | transformation | - |

**Returns:**

| type | description |
| --- | --- |
| Cipher | 返回加解密生成器的对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 crypto framework错误码 。

| code | description |
| --- | --- |
| 401 | invalid parameters. |
| 801 | this operation is not supported. |
| 17620001 | memory error. |


**Example:**

```cangjie
// index.cj

import kit.CryptoArchitectureKit.*

let cipherAlgName = "3DES192|ECB|PKCS7"
let cipher = createCipher(cipherAlgName)
```

### public func createKdf(algName: String): Kdf

```cangjie
public func createKdf(algName: String): Kdf
```
功能： 密钥派生函数（key derivation function）实例生成。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 指定密钥派生算法（包含HMAC配套的散列函数）：目前支持PBKDF2、HKDF算法，如"PBKDF2MagIc_StrINgSHA256", "HKDFMagIc_StrINgSHA256"。 | algName | - |

**Returns:**

| type | description |
| --- | --- |
| Kdf | 返回由输入算法指定生成的Kdf对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 crypto framework错误码 。

| code | description |
| --- | --- |
| 401 | invalid parameters. Possible causes: 1. Mandatory parameters are left unspecified; 2. Incorrect parameter types; 3. Parameter verification failed. |
| 801 | this operation is not supported. |
| 17620001 | memory error. |


**Example:**

```cangjie
// index.cj
import kit.CryptoArchitectureKit.*

let kdf = createKdf('PBKDF2|SHA256')
```

### public func createKeyAgreement(algName: String): KeyAgreement

```cangjie
public func createKeyAgreement(algName: String): KeyAgreement
```
功能： KeyAgreement实例生成。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 指定密钥协商算法：目前支持ECC，X25519和DH。 | algName | - |

**Returns:**

| type | description |
| --- | --- |
| KeyAgreement | 返回由输入算法指定生成的KeyAgreement对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 crypto framework错误码 。

| code | description |
| --- | --- |
| 401 | invalid parameters. Possible causes: 1. Mandatory parameters are left unspecified; 2. Incorrect parameter types; 3. Parameter verification failed. |
| 801 | this operation is not supported. |
| 17620001 | memory error. |


**Example:**

```cangjie
// index.cj
import kit.CryptoArchitectureKit.*

let keyAgreement = createKeyAgreement('ECC256')
```

### public func createMac(algName: String): Mac

```cangjie
public func createMac(algName: String): Mac
```
功能： 生成Mac实例，用于进行消息认证码的计算与操作。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 指定摘要算法。 | algName | - |

**Returns:**

| type | description |
| --- | --- |
| Mac | 返回由输入算法指定生成的Mac对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 crypto framework错误码 。

| code | description |
| --- | --- |
| 401 | invalid parameters. |
| 17620001 | memory error. |


**Example:**

```cangjie
// index.cj
import kit.CryptoArchitectureKit.*

var mac = createMac("SHA256")
```

### public func createMd(algName: String): Md

```cangjie
public func createMd(algName: String): Md
```
功能： 生成Md实例，用于进行消息摘要的计算与操作。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 指定摘要算法。 | algName | - |

**Returns:**

| type | description |
| --- | --- |
| Md | 返回由输入算法指定生成的 Md 对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 crypto framework错误码 。

| code | description |
| --- | --- |
| 401 | invalid parameters. |
| 17620001 | memory error. |


**Example:**

```cangjie
// index.cj
import kit.CryptoArchitectureKit.*

let md = createMd("SHA256")
```

### public func createRandom(): Random

```cangjie
public func createRandom(): Random
```
功能： 生成Random实例，用于进行随机数的计算与设置种子。

**Returns:**

| type | description |
| --- | --- |
| Random | 返回由输入算法指定生成的 Random 对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 crypto framework错误码 。

| code | description |
| --- | --- |
| 17620001 | memory error. |


**Example:**

```cangjie
// index.cj
import kit.CryptoArchitectureKit.*

let rand = createRandom()
```

### public func createSign(algName: String): Sign

```cangjie
public func createSign(algName: String): Sign
```
功能： Sign实例生成。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 指定签名算法：RSA，ECC，DSA或SM2。使用RSA PKCS1模式时需要设置摘要，使用RSA PSS模式时需要设置摘要和掩码摘要。 | algName | - |

**Returns:**

| type | description |
| --- | --- |
| Sign | 返回由输入算法指定生成的Sign对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 crypto framework错误码 。

| code | description |
| --- | --- |
| 401 | invalid parameters. |
| 801 | this operation is not supported. |
| 17620001 | memory error. |


**Example:**

```cangjie
// index.cj
import kit.CryptoArchitectureKit.*

var sign = createSign("ECC224|SHA256")
```

### public func createSymKeyGenerator(algName: String): SymKeyGenerator

```cangjie
public func createSymKeyGenerator(algName: String): SymKeyGenerator
```
功能： 通过指定算法名称的字符串，获取相应的对称密钥生成器实例。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 待生成对称密钥生成器的算法名称。具体取值详见 对称密钥生成和转换规格 一节中的“字符串参数”。 | algName | - |

**Returns:**

| type | description |
| --- | --- |
| SymKeyGenerator | 返回对称密钥生成器的对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 。

| code | description |
| --- | --- |
| 801 | this operation is not supported. |


**Example:**

```cangjie
// index.cj

import kit.CryptoArchitectureKit.*

let symKeyGenerator = createSymKeyGenerator("3DES192")
```

### public func createVerify(algName: String): Verify

```cangjie
public func createVerify(algName: String): Verify
```
功能： Verify实例生成。

**Parameters:**

| type | required | description | name | default |
| --- | --- | --- | --- | --- |
| String | 是 | 指定签名算法：RSA，ECC，DSA，SM2或ED25519。使用RSA PKCS1模式时需要设置摘要，使用RSA PSS模式时需要设置摘要和掩码摘要。 使用RSA算法验签时，通过设置Recover参数可支持对签名后数据进行验签恢复。 | algName | - |

**Returns:**

| type | description |
| --- | --- |
| Verify | 返回由输入算法指定生成的Verify对象。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 crypto framework错误码 。

| code | description |
| --- | --- |
| 401 | invalid parameters. Possible causes: 1. Mandatory parameters are left unspecified; 2. Incorrect parameter types; 3. Parameter verification failed. |
| 801 | this operation is not supported. |
| 17620001 | memory error. |


**Example:**

```cangjie
// index.cj

import kit.CryptoArchitectureKit.*

let verifyer1 = createVerify('RSA1024|PKCS1|SHA256')
let verifyer2 = createVerify('RSA1024|PSS|SHA256|MGF1_SHA256')
let verifyer3 = createVerify('RSA1024|PKCS1|SHA256|Recover')
```


## Interfaces

### AsyKeySpec

interface AsyKeySpec
```cangjie
public interface AsyKeySpec { mut prop algName: String mut prop specType: AsyKeySpecType }
```
功能： 指定非对称密钥参数的基本接口，用于创建密钥生成器。在指定非对称密钥参数时需要构造其子类对象，并将子类对象传入 createAsyKeyGeneratorBySpec() 方法创建密钥生成器。构造子类对象时，所有BigInt类型的密钥参数均采用大端写法，并使用正数。

#### prop algName

```cangjie
mut prop algName: String
```
功能： 指定非对称密钥的算法名称，比如"RSA"、"DSA"、"ECC"、"SM2"、"Ed25519"、"X25519"、"DH"。
Type: String; Readable: true; Writable: true

#### prop specType

```cangjie
mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型，用于区分公/私钥参数。
Type: AsyKeySpecType; Readable: true; Writable: true


### ECField

interface ECField
```cangjie
public interface ECField { mut prop fieldType: String }
```
功能： 指定椭圆曲线的域。当前只支持Fp域。

#### prop fieldType

```cangjie
mut prop fieldType: String
```
功能： 指定椭圆曲线域的类型，当前只支持"Fp"。
Type: String; Readable: true; Writable: true


### KdfSpec

interface KdfSpec
```cangjie
public interface KdfSpec { mut prop algName: String }
```
功能： 密钥派生函数参数，使用密钥派生函数进行密钥派生时，需要构建其子类对象并作为输入。

#### prop algName

```cangjie
mut prop algName: String
```
功能： 指明密钥派生函数的算法名，如"PBKDF2"。
Type: String; Readable: true; Writable: true


### Key

interface Key
```cangjie
public interface Key { prop format: String prop algName: String func getEncoded(): DataBlob }
```
功能： 密钥（接口），在运行密码算法（如加解密）时需要提前生成其子类对象，并传入 Cipher 实例的 init() 方法。

#### prop algName

```cangjie
prop algName: String
```
功能： 密钥对应的算法名（含长度）。
Type: String; Readable: true; Writable: false

#### prop format

```cangjie
prop format: String
```
功能： 密钥的格式。
Type: String; Readable: true; Writable: false

#### func getEncoded(): DataBlob

```cangjie
func getEncoded(): DataBlob
```
功能： 同步方法，获取密钥数据的字节流。密钥可以为对称密钥，公钥或者私钥。其中，公钥格式满足ASN.1语法、X.509规范、DER编码格式；私钥格式满足ASN.1语法，PKCS#8规范、DER编码方式。

**Returns:**

| type | description |
| --- | --- |
| DataBlob | 用于查看密钥的具体内容。 |

**Exceptions:**

- **BusinessException**: BusinessException：对应错误码的详细介绍请参见 通用错误码 和 crypto framework错误码 。

| code | description |
| --- | --- |
| 801 | this operation is not supported. |
| 17620001 | memory error. |
| 17630001 | crypto operation error. |


**Example:**

```cangjie
// index.cj

import kit.CryptoArchitectureKit.*

let generator = createSymKeyGenerator("3DES192") // The key is generated by a key generator. The generation process is omitted here.
let key = generator.generateSymKey()
let encodedKey = key.getEncoded()
```


### ParamsSpec

interface ParamsSpec
```cangjie
public interface ParamsSpec { mut prop algName: String mut prop iv: DataBlob }
```
功能： 加解密参数，在进行对称加解密时需要构造其子类对象，并将子类对象传入 init() 方法。

#### prop algName

```cangjie
mut prop algName: String
```
功能： 指明对称加解密参数的算法模式。可选值如下: - IvParamsSpec: 适用于CBCMagIc_StrINgCTRMagIc_StrINgOFBMagIc_StrINgCFB模式。 - GcmParamsSpec: 适用于GCM模式。 - CcmParamsSpec: 适用于CCM模式。
Type: String; Readable: true; Writable: true

#### prop iv

```cangjie
mut prop iv: DataBlob
```
功能： 指明加解密参数iv。常见取值如下： - AES的CBCMagIc_StrINgCTRMagIc_StrINgOFBMagIc_StrINgCFB模式：iv长度为16字节 - 3DES的CBCMagIc_StrINgOFBMagIc_StrINgCFB模式：iv长度为8字节 - SM4的CBCMagIc_StrINgCTRMagIc_StrINgOFBMagIc_StrINgCFB模式：iv长度为16字节。
Type: DataBlob; Readable: true; Writable: true



## Classes

### AsyKeyGenerator

class AsyKeyGenerator
```cangjie
public class AsyKeyGenerator {}
```
功能： 非对称密钥生成器。在使用该类的方法前，需要先使用createAsyKeyGenerator()方法构建一个AsyKeyGenerator实例。

#### prop algName

```cangjie
public prop algName: String
```
功能： 非对称密钥生成器指定的算法名称。
Type: String; Readable: true; Writable: false


### AsyKeyGeneratorBySpec

class AsyKeyGeneratorBySpec
```cangjie
public class AsyKeyGeneratorBySpec {}
```
功能： 非对称密钥生成器。在使用该类的方法前，需要先使用 createAsyKeyGeneratorBySpec() 方法构建一个AsyKeyGeneratorBySpec实例。

#### prop algName

```cangjie
public prop algName: String
```
功能： 非对称密钥生成器的算法名。
Type: String; Readable: true; Writable: false


### Cipher

class Cipher
```cangjie
public class Cipher {}
```
功能： 提供加解密的算法操作功能，按序调用本类中的 init() 、 update() 、 doFinal() 方法，可以实现对称加密/对称解密/非对称加密/非对称解密。

#### prop algName

```cangjie
public prop algName: String
```
功能： 加解密生成器指定的算法名称。
Type: String; Readable: true; Writable: false


### DHCommonParamsSpec

class DHCommonParamsSpec
```cangjie
public class DHCommonParamsSpec <: AsyKeySpec { public init(algName!: String, specType!: AsyKeySpecType, p!: BigInt, g!: BigInt, l!: IntNative) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定DH算法中公私钥包含的参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop g

```cangjie
public mut prop g: BigInt
```
功能： 指定DH算法中参数g。
Type: BigInt; Readable: true; Writable: true

#### prop l

```cangjie
public mut prop l: IntNative
```
功能： 指定DH算法中私钥的长度，单位为bit。
Type: IntNative; Readable: true; Writable: true

#### prop p

```cangjie
public mut prop p: BigInt
```
功能： 指定DH算法中大素数p。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### DHKeyPairSpec

class DHKeyPairSpec
```cangjie
public class DHKeyPairSpec <: AsyKeySpec { public init(params!: DHCommonParamsSpec, sk!: BigInt, pk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定DH算法中公私钥包含的全量参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop params

```cangjie
public mut prop params: DHCommonParamsSpec
```
功能： 指定DH算法中公私钥都包含的公共参数。
Type: DHCommonParamsSpec; Readable: true; Writable: true

#### prop pk

```cangjie
public mut prop pk: BigInt
```
功能： 指定DH算法的公钥pk。
Type: BigInt; Readable: true; Writable: true

#### prop sk

```cangjie
public mut prop sk: BigInt
```
功能： 指定DH算法的私钥sk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### DHKeyUtil

class DHKeyUtil
```cangjie
public class DHKeyUtil {}
```
功能： 根据素数P的长度和私钥长度（bit位数）生成DH公共密钥参数。


### DHPriKeySpec

class DHPriKeySpec
```cangjie
public class DHPriKeySpec <: AsyKeySpec { public init(params!: DHCommonParamsSpec, sk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定DH算法中私钥包含的参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop params

```cangjie
public mut prop params: DHCommonParamsSpec
```
功能： 指定DH算法中公私钥都包含的公共参数。
Type: DHCommonParamsSpec; Readable: true; Writable: true

#### prop sk

```cangjie
public mut prop sk: BigInt
```
功能： 指定DH算法的私钥sk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### DHPubKeySpec

class DHPubKeySpec
```cangjie
public class DHPubKeySpec <: AsyKeySpec { public init(params!: DHCommonParamsSpec, pk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定DH算法中公钥包含的参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop params

```cangjie
public mut prop params: DHCommonParamsSpec
```
功能： 指定DH算法中公私钥都包含的公共参数。
Type: DHCommonParamsSpec; Readable: true; Writable: true

#### prop pk

```cangjie
public mut prop pk: BigInt
```
功能： 指定DH算法的公钥pk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### DSACommonParamsSpec

class DSACommonParamsSpec
```cangjie
public class DSACommonParamsSpec <: AsyKeySpec { public init(algName!: String, specType!: AsyKeySpecType, p!: BigInt, q!: BigInt, g!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定DSA算法中公私钥包含的公共参数，随机生成公/私钥。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop g

```cangjie
public mut prop g: BigInt
```
功能： 指定DSA算法的参数g。
Type: BigInt; Readable: true; Writable: true

#### prop p

```cangjie
public mut prop p: BigInt
```
功能： 指定DSA算法的素模数p。
Type: BigInt; Readable: true; Writable: true

#### prop q

```cangjie
public mut prop q: BigInt
```
功能： 指定DSA算法中密钥参数q（p-1的素因子）。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### DSAKeyPairSpec

class DSAKeyPairSpec
```cangjie
public class DSAKeyPairSpec <: AsyKeySpec { public init(params!: DSACommonParamsSpec, sk!: BigInt, pk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定DSA算法中公私钥包含的全量参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop params

```cangjie
public mut prop params: DSACommonParamsSpec
```
功能： 指定DSA算法中公私钥都包含的公共参数。
Type: DSACommonParamsSpec; Readable: true; Writable: true

#### prop pk

```cangjie
public mut prop pk: BigInt
```
功能： 指定DSA算法的公钥pk。
Type: BigInt; Readable: true; Writable: true

#### prop sk

```cangjie
public mut prop sk: BigInt
```
功能： 指定DSA算法的私钥sk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### DSAPubKeySpec

class DSAPubKeySpec
```cangjie
public class DSAPubKeySpec <: AsyKeySpec { public init(params!: DSACommonParamsSpec, pk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定DSA算法中公钥包含的参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop params

```cangjie
public mut prop params: DSACommonParamsSpec
```
功能： 指定DSA算法中公私钥都包含的公共参数。
Type: DSACommonParamsSpec; Readable: true; Writable: true

#### prop pk

```cangjie
public mut prop pk: BigInt
```
功能： 指定DSA算法的公钥。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### ECCCommonParamsSpec

class ECCCommonParamsSpec
```cangjie
public class ECCCommonParamsSpec <: AsyKeySpec { public init(algName!: String, specType!: AsyKeySpecType, field!: ECField, a!: BigInt, b!: BigInt, g!: Point, n!: BigInt, h!: Int32) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定ECC算法中公私钥包含的公共参数，随机生成公/私钥。

#### prop a

```cangjie
public mut prop a: BigInt
```
功能： 指定椭圆曲线的第一个系数a。
Type: BigInt; Readable: true; Writable: true

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop b

```cangjie
public mut prop b: BigInt
```
功能： 指定椭圆曲线的第二个系数b。
Type: BigInt; Readable: true; Writable: true

#### prop field

```cangjie
public mut prop field: ECField
```
功能： 指定椭圆曲线的域（当前只支持Fp域）。
Type: ECField; Readable: true; Writable: true

#### prop g

```cangjie
public mut prop g: Point
```
功能： 指定基点g。
Type: Point; Readable: true; Writable: true

#### prop h

```cangjie
public mut prop h: Int32
```
功能： 指定余因子h。
Type: Int32; Readable: true; Writable: true

#### prop n

```cangjie
public mut prop n: BigInt
```
功能： 指定基点g的阶数n。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### ECCKeyPairSpec

class ECCKeyPairSpec
```cangjie
public class ECCKeyPairSpec <: AsyKeySpec { public init(params!: ECCCommonParamsSpec, sk!: BigInt, pk!: Point) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定ECC算法中公钥包含的参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop params

```cangjie
public mut prop params: ECCCommonParamsSpec
```
功能： 指定ECC算法中公私钥都包含的公共参数。
Type: ECCCommonParamsSpec; Readable: true; Writable: true

#### prop pk

```cangjie
public mut prop pk: Point
```
功能： 指定ECC算法的公钥pk。
Type: Point; Readable: true; Writable: true

#### prop sk

```cangjie
public mut prop sk: BigInt
```
功能： 指定ECC算法的私钥sk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### ECCKeyUtil

class ECCKeyUtil
```cangjie
public class ECCKeyUtil {}
```
功能： 根据椭圆曲线名生成相应的非对称公共密钥参数。


### ECCPriKeySpec

class ECCPriKeySpec
```cangjie
public class ECCPriKeySpec <: AsyKeySpec { public init(params!: ECCCommonParamsSpec, sk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定ECC算法中私钥包含的参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop params

```cangjie
public mut prop params: ECCCommonParamsSpec
```
功能： 指定ECC算法中公私钥都包含的公共参数。
Type: ECCCommonParamsSpec; Readable: true; Writable: true

#### prop sk

```cangjie
public mut prop sk: BigInt
```
功能： 指定ECC算法的私钥sk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### ECCPubKeySpec

class ECCPubKeySpec
```cangjie
public class ECCPubKeySpec <: AsyKeySpec { public init(params!: ECCCommonParamsSpec, pk!: Point) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定ECC算法中公钥包含的参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop params

```cangjie
public mut prop params: ECCCommonParamsSpec
```
功能： 指定ECC算法中公私钥都包含的公共参数。
Type: ECCCommonParamsSpec; Readable: true; Writable: true

#### prop pk

```cangjie
public mut prop pk: Point
```
功能： 指定ECC算法的公钥pk。
Type: Point; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### ECFieldFp

class ECFieldFp
```cangjie
public class ECFieldFp <: ECField { public init(fieldType!: String, p!: BigInt) }
```
功能： 指定椭圆曲线素数域。该结构体是 ECField 的子类。

#### prop fieldType

```cangjie
public mut prop fieldType: String
```
功能： 指定椭圆曲线域的类型，当前只支持"Fp"。
Type: String; Readable: true; Writable: true

#### prop p

```cangjie
public mut prop p: BigInt
```
功能： 指定素数p。
Type: BigInt; Readable: true; Writable: true


### ED25519KeyPairSpec

class ED25519KeyPairSpec
```cangjie
public class ED25519KeyPairSpec <: AsyKeySpec { public init(algName!: String, specType!: AsyKeySpecType, sk!: BigInt, pk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定ED25519算法中公私钥包含的全量参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop pk

```cangjie
public mut prop pk: BigInt
```
功能： 指定ED25519算法的公钥pk。
Type: BigInt; Readable: true; Writable: true

#### prop sk

```cangjie
public mut prop sk: BigInt
```
功能： 指定ED25519算法的私钥sk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### ED25519PriKeySpec

class ED25519PriKeySpec
```cangjie
public class ED25519PriKeySpec <: AsyKeySpec { public init(algName!: String, specType!: AsyKeySpecType, sk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定ED25519算法中私钥包含的参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop sk

```cangjie
public mut prop sk: BigInt
```
功能： 指定ED25519算法的私钥sk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### ED25519PubKeySpec

class ED25519PubKeySpec
```cangjie
public class ED25519PubKeySpec <: AsyKeySpec { public init(algName!: String, specType!: AsyKeySpecType, pk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定ED25519算法中公钥包含的参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop pk

```cangjie
public mut prop pk: BigInt
```
功能： 指定ED25519算法的公钥pk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### HKDFSpec

class HKDFSpec
```cangjie
public class HKDFSpec <: KdfSpec { public init(algName!: String, key!: Array<UInt8>, salt!: Array<UInt8>, info!: Array<UInt8>, keySize!: Int32) }
```
功能： 密钥派生函数参数 KdfSpec 的子类，作为HKDF密钥派生函数进行密钥派生时的输入。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指明密钥派生函数的算法名。
Type: String; Readable: true; Writable: true

#### prop info

```cangjie
public mut prop info: Array<UInt8>
```
功能： 拓展信息。
Type: Array<UInt8>; Readable: true; Writable: true

#### prop key

```cangjie
public mut prop key: Array<UInt8>
```
功能： 密钥材料。
Type: Array<UInt8>; Readable: true; Writable: true

#### prop keySize

```cangjie
public mut prop keySize: Int32
```
功能： 派生得到的密钥字节长度。
Type: Int32; Readable: true; Writable: true

#### prop salt

```cangjie
public mut prop salt: Array<UInt8>
```
功能： 盐值。
Type: Array<UInt8>; Readable: true; Writable: true


### Kdf

class Kdf
```cangjie
public class Kdf {}
```
功能： 密钥派生函数（key derivation function）类，使用密钥派生方法之前需要创建该类的实例进行操作，通过createKdf(algName: String): Kdf方法构造此实例。

#### prop algName

```cangjie
public prop algName: String
```
功能： 密钥派生函数的算法名称。
Type: String; Readable: true; Writable: false


### KeyAgreement

class KeyAgreement
```cangjie
public class KeyAgreement {}
```
功能： KeyAgreement类，使用密钥协商方法之前需要创建该类的实例进行操作，通过 createKeyAgreement(algName: String): KeyAgreement 方法构造此实例。

#### prop algName

```cangjie
public prop algName: String
```
功能： 指定密钥协商算法：目前仅支持ECC，X25519和DH。
Type: String; Readable: true; Writable: false


### KeyPair

class KeyPair
```cangjie
public class KeyPair {}
```
功能： 非对称密钥对，包含：公钥与私钥。

#### prop priKey

```cangjie
public prop priKey: PriKey
```
功能： 私钥。
Type: PriKey; Readable: true; Writable: false

#### prop pubKey

```cangjie
public prop pubKey: PubKey
```
功能： 公钥。
Type: PubKey; Readable: true; Writable: false


### Mac

class Mac
```cangjie
public class Mac {}
```
功能： Mac类，调用Mac方法可以进行MAC（Message Authentication Code）加密计算。调用前，需要通过 createMac 构造Mac实例。

#### prop algName

```cangjie
public prop algName: String
```
功能： 代表指定的摘要算法名。
Type: String; Readable: true; Writable: false


### Md

class Md
```cangjie
public class Md {}
```
功能： Md类，调用Md方法可以进行MD（Message Digest）摘要计算。调用前，需要通过 createMd 构造Md实例。

#### prop algName

```cangjie
public prop algName: String
```
功能： 代表指定的摘要算法名。
Type: String; Readable: true; Writable: false


### PBKDF2Spec

class PBKDF2Spec
```cangjie
public class PBKDF2Spec <: KdfSpec { public init(algName!: String, password!: Array<UInt8>, salt!: Array<UInt8>, iterations!: Int32, keySize!: Int32) }
```
功能： 密钥派生函数参数 KdfSpec 的子类，作为PBKDF2密钥派生函数进行密钥派生时的输入。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定密钥派生函数的算法名。
Type: String; Readable: true; Writable: true

#### prop iterations

```cangjie
public mut prop iterations: Int32
```
功能： 迭代次数，需要为正整数。
Type: Int32; Readable: true; Writable: true

#### prop keySize

```cangjie
public mut prop keySize: Int32
```
功能： 派生得到的密钥字节长度。
Type: Int32; Readable: true; Writable: true

#### prop password

```cangjie
public mut prop password: Array<UInt8>
```
功能： 用户输入的原始密码。
Type: Array<UInt8>; Readable: true; Writable: true

#### prop salt

```cangjie
public mut prop salt: Array<UInt8>
```
功能： 盐值。
Type: Array<UInt8>; Readable: true; Writable: true


### Point

class Point
```cangjie
public class Point { public init(x!: BigInt, y!: BigInt) }
```
功能： 指定椭圆曲线上的一个点。

#### prop x

```cangjie
public mut prop x: BigInt
```
功能： 指定椭圆曲线上，点的x坐标。
Type: BigInt; Readable: true; Writable: true

#### prop y

```cangjie
public mut prop y: BigInt
```
功能： 指定椭圆曲线上，点的y坐标。
Type: BigInt; Readable: true; Writable: true


### PriKey

class PriKey
```cangjie
public class PriKey <: Key {}
```
功能： 私钥，是 Key 的子类，在非对称加解密、签名、密钥协商时需要将其作为输入使用。


### PubKey

class PubKey
```cangjie
public class PubKey <: Key {}
```
功能： 公钥，是 Key 的子类，在非对称加解密、验签、密钥协商时需要将其对象作为输入使用。


### RSACommonParamsSpec

class RSACommonParamsSpec
```cangjie
public class RSACommonParamsSpec <: AsyKeySpec { public init(algName!: String, specType!: AsyKeySpecType, n!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定RSA算法中公私钥包含的公共参数，随机生成公/私钥。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop n

```cangjie
public mut prop n: BigInt
```
功能： 指定模数n。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型，用于区分公/私钥参数。
Type: AsyKeySpecType; Readable: true; Writable: true


### RSAKeyPairSpec

class RSAKeyPairSpec
```cangjie
public class RSAKeyPairSpec <: AsyKeySpec { public init(params!: RSACommonParamsSpec, sk!: BigInt, pk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定RSA算法中公私钥包含的全量参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop params

```cangjie
public mut prop params: RSACommonParamsSpec
```
功能： 指定RSA算法中公私钥都包含的公共参数。
Type: RSACommonParamsSpec; Readable: true; Writable: true

#### prop pk

```cangjie
public mut prop pk: BigInt
```
功能： 指定RSA算法的公钥pk。
Type: BigInt; Readable: true; Writable: true

#### prop sk

```cangjie
public mut prop sk: BigInt
```
功能： 指定RSA算法的公钥sk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### RSAPubKeySpec

class RSAPubKeySpec
```cangjie
public class RSAPubKeySpec <: AsyKeySpec { public init(params!: RSACommonParamsSpec, pk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定RSA算法中公钥包含的参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop params

```cangjie
public mut prop params: RSACommonParamsSpec
```
功能： 指定RSA算法中公私钥都包含的公共参数。
Type: RSACommonParamsSpec; Readable: true; Writable: true

#### prop pk

```cangjie
public mut prop pk: BigInt
```
功能： 指定RSA算法的公钥pk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### Random

class Random
```cangjie
public class Random {}
```
功能： Random类，调用Random方法可以进行随机数计算。调用前，需要通过 createRandom 构造Random实例。

#### prop algName

```cangjie
public prop algName: String
```
功能： 代表当前使用的随机数生成算法，目前只支持“CTR_DRBG"。
Type: String; Readable: true; Writable: false


### SM2CipherTextSpec

class SM2CipherTextSpec
```cangjie
public class SM2CipherTextSpec { public init(xCoordinate!: BigInt, yCoordinate!: BigInt, cipherTextData!: Array<UInt8>, hashData!: Array<UInt8>) }
```
功能： SM2密文参数，使用SM2密文格式转换函数进行格式转换时，需要用到此对象。可以通过指定此参数，生成符合国密标准的ASN.1格式的SM2密文，反之，也可以从ASN.1格式的SM2密文中获取具体参数。

#### prop cipherTextData

```cangjie
public mut prop cipherTextData: Array<UInt8>
```
功能： 密文。
Type: Array<UInt8>; Readable: true; Writable: true

#### prop hashData

```cangjie
public mut prop hashData: Array<UInt8>
```
功能： 杂凑值。
Type: Array<UInt8>; Readable: true; Writable: true

#### prop xCoordinate

```cangjie
public mut prop xCoordinate: BigInt
```
功能： x分量。
Type: BigInt; Readable: true; Writable: true

#### prop yCoordinate

```cangjie
public mut prop yCoordinate: BigInt
```
功能： y分量。
Type: BigInt; Readable: true; Writable: true


### SM2CryptoUtil

class SM2CryptoUtil
```cangjie
public class SM2CryptoUtil {}
```
功能： 用于SM2密码学运算的工具类。


### Sign

class Sign
```cangjie
public class Sign {}
```
功能： Sign类，使用Sign方法之前需要创建该类的实例进行操作，通过 createSign 方法构造此实例。按序调用本类中的init、update、sign方法完成签名操作。

#### prop algName

```cangjie
public prop algName: String
```
功能： 签名指定的算法名称。
Type: String; Readable: true; Writable: false


### SymKey

class SymKey
```cangjie
public class SymKey <: Key {}
```
功能： 对称密钥，是 Key 的子类，在对称加解密时需要将其对象传入 Cipher 实例的 init() 方法使用。


### SymKeyGenerator

class SymKeyGenerator
```cangjie
public class SymKeyGenerator {}
```
功能： 对称密钥生成器。

#### prop algName

```cangjie
public prop algName: String
```
功能： 对称密钥生成器指定的算法名称。
Type: String; Readable: true; Writable: false


### Verify

class Verify
```cangjie
public class Verify {}
```
功能： Verify类，使用Verify方法之前需要创建该类的实例进行操作，通过 createVerify(algName: String): Verify 方法构造此实例。按序调用本类中的init、update、verify方法完成签名操作。

#### prop algName

```cangjie
public prop algName: String
```
功能： 验签指定的算法名称。
Type: String; Readable: true; Writable: false


### X25519KeyPairSpec

class X25519KeyPairSpec
```cangjie
public class X25519KeyPairSpec <: AsyKeySpec { public init(algName!: String, specType!: AsyKeySpecType, sk!: BigInt, pk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定X25519算法中公私钥包含的全量参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop pk

```cangjie
public mut prop pk: BigInt
```
功能： 指定X25519算法的公钥pk。
Type: BigInt; Readable: true; Writable: true

#### prop sk

```cangjie
public mut prop sk: BigInt
```
功能： 指定X25519算法的私钥sk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### X25519PriKeySpec

class X25519PriKeySpec
```cangjie
public class X25519PriKeySpec <: AsyKeySpec { public init(algName!: String, specType!: AsyKeySpecType, sk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定X25519算法中私钥包含的参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop sk

```cangjie
public mut prop sk: BigInt
```
功能： 指定X25519算法的私钥sk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true


### X25519PubKeySpec

class X25519PubKeySpec
```cangjie
public class X25519PubKeySpec <: AsyKeySpec { public init(algName!: String, specType!: AsyKeySpecType, pk!: BigInt) }
```
功能： 密钥参数 AsyKeySpec 的子类，用于指定X25519算法中公钥包含的参数。

#### prop algName

```cangjie
public mut prop algName: String
```
功能： 指定非对称密钥的算法名称。
Type: String; Readable: true; Writable: true

#### prop pk

```cangjie
public mut prop pk: BigInt
```
功能： 指定X25519算法的公钥pk。
Type: BigInt; Readable: true; Writable: true

#### prop specType

```cangjie
public mut prop specType: AsyKeySpecType
```
功能： 指定密钥参数类型。
Type: AsyKeySpecType; Readable: true; Writable: true



## Structs

### CcmParamsSpec

struct CcmParamsSpec
```cangjie
public struct CcmParamsSpec <: ParamsSpec { public init(algName: String, iv: DataBlob, add: DataBlob, authTag: DataBlob) }
```
功能： 加解密参数 ParamsSpec 的子类，用于在对称加解密时作为 init() 方法的参数。


### DataBlob

struct DataBlob
```cangjie
public struct DataBlob { public DataBlob( public let data: Array<UInt8>) }
```
功能： 存储数组的数据类型。


### GcmParamsSpec

struct GcmParamsSpec
```cangjie
public struct GcmParamsSpec <: ParamsSpec { public init(algName: String, iv: DataBlob, add: DataBlob, authTag: DataBlob) }
```
功能： 加解密参数 ParamsSpec 的子类，用于在对称加解密时作为 init() 方法的参数。


### IvParamsSpec

struct IvParamsSpec
```cangjie
public struct IvParamsSpec <: ParamsSpec { public init(algName: String, iv: DataBlob) }
```
功能： 加解密参数 ParamsSpec 的子类，用于在对称加解密时作为 init() 方法的参数。



## Enums

### AsyKeySpecItem

enum AsyKeySpecItem
```cangjie
public enum AsyKeySpecItem <: Equatable<AsyKeySpecItem> & ToString { | DSA_P_BN | DSA_Q_BN | DSA_G_BN | DSA_SK_BN | DSA_PK_BN | ECC_FP_P_BN | ECC_A_BN | ECC_B_BN | ECC_G_X_BN | ECC_G_Y_BN | ECC_N_BN | ECC_H_NUM | ECC_SK_BN | ECC_PK_X_BN | ECC_PK_Y_BN | ECC_FIELD_TYPE_STR | ECC_FIELD_SIZE_NUM | ECC_CURVE_NAME_STR | RSA_N_BN | RSA_SK_BN | RSA_PK_BN | DH_P_BN | DH_G_BN | DH_L_NUM | DH_SK_BN | DH_PK_BN | ED25519_SK_BN | ED25519_PK_BN | X25519_SK_BN | X25519_PK_BN | ... }
```
功能： 表示密钥参数。

#### DH_G_BN

```cangjie
DH_G_BN
```
功能： DH算法中的参数g。

#### DH_L_NUM

```cangjie
DH_L_NUM
```
功能： DH算法中私钥长度，单位为bit。

#### DH_PK_BN

```cangjie
DH_PK_BN
```
功能： DH算法中的公钥pk。

#### DH_P_BN

```cangjie
DH_P_BN
```
功能： DH算法中的素数p。

#### DH_SK_BN

```cangjie
DH_SK_BN
```
功能： DH算法中的私钥sk。

#### DSA_G_BN

```cangjie
DSA_G_BN
```
功能： DSA算法的参数g。

#### DSA_PK_BN

```cangjie
DSA_PK_BN
```
功能： DSA算法的公钥pk。

#### DSA_P_BN

```cangjie
DSA_P_BN
```
功能： DSA算法的素模数p。

#### DSA_Q_BN

```cangjie
DSA_Q_BN
```
功能： DSA算法中密钥参数q（p-1的素因子）。

#### DSA_SK_BN

```cangjie
DSA_SK_BN
```
功能： DSA算法的私钥sk。

#### ECC_A_BN

```cangjie
ECC_A_BN
```
功能： ECC算法中椭圆曲线的第一个系数a。

#### ECC_B_BN

```cangjie
ECC_B_BN
```
功能： ECC算法中椭圆曲线的第二个系数b。

#### ECC_CURVE_NAME_STR

```cangjie
ECC_CURVE_NAME_STR
```
功能： ECC算法中的SECG(Standards for Efficient Cryptography Group)曲线名称。

#### ECC_FIELD_SIZE_NUM

```cangjie
ECC_FIELD_SIZE_NUM
```
功能： ECC算法中域的大小，单位为bits（注：对于Fp域，域的大小为素数p的bits长度）。

#### ECC_FIELD_TYPE_STR

```cangjie
ECC_FIELD_TYPE_STR
```
功能： ECC算法中，椭圆曲线的域类型（当前只支持Fp域）。

#### ECC_FP_P_BN

```cangjie
ECC_FP_P_BN
```
功能： ECC算法中表示椭圆曲线Fp域的素数p。

#### ECC_G_X_BN

```cangjie
ECC_G_X_BN
```
功能： ECC算法中基点g的x坐标。

#### ECC_G_Y_BN

```cangjie
ECC_G_Y_BN
```
功能： ECC算法中基点g的y坐标。

#### ECC_H_NUM

```cangjie
ECC_H_NUM
```
功能： ECC算法中的余因子h。

#### ECC_N_BN

```cangjie
ECC_N_BN
```
功能： ECC算法中基点g的阶n。

#### ECC_PK_X_BN

```cangjie
ECC_PK_X_BN
```
功能： ECC算法中，公钥pk（椭圆曲线上的一个点）的x坐标。

#### ECC_PK_Y_BN

```cangjie
ECC_PK_Y_BN
```
功能： ECC算法中，公钥pk（椭圆曲线上的一个点）的y坐标。

#### ECC_SK_BN

```cangjie
ECC_SK_BN
```
功能： ECC算法中的私钥sk。

#### ED25519_PK_BN

```cangjie
ED25519_PK_BN
```
功能： ED25519算法中的公钥pk。

#### ED25519_SK_BN

```cangjie
ED25519_SK_BN
```
功能： ED25519算法中的私钥sk。

#### RSA_N_BN

```cangjie
RSA_N_BN
```
功能： RSA算法中的模数n。

#### RSA_PK_BN

```cangjie
RSA_PK_BN
```
功能： RSA算法中的公钥pk（即公钥指数e）。

#### RSA_SK_BN

```cangjie
RSA_SK_BN
```
功能： RSA算法中的私钥sk（即私钥指数d）。

#### X25519_PK_BN

```cangjie
X25519_PK_BN
```
功能： X25519算法中的公钥pk。

#### X25519_SK_BN

```cangjie
X25519_SK_BN
```
功能： X25519算法中的私钥sk。


### AsyKeySpecType

enum AsyKeySpecType
```cangjie
public enum AsyKeySpecType <: Equatable<AsyKeySpecType> & ToString { | COMMON_PARAMS_SPEC | PRIVATE_KEY_SPEC | PUBLIC_KEY_SPEC | KEY_PAIR_SPEC | ... }
```
功能： 表示密钥参数类型。

#### COMMON_PARAMS_SPEC

```cangjie
COMMON_PARAMS_SPEC
```
功能： 表示公私钥中包含的公共参数。使用此类型的参数可以调用 generateKeyPair 随机生成密钥对。

#### KEY_PAIR_SPEC

```cangjie
KEY_PAIR_SPEC
```
功能： 表示公私钥中包含的全量参数。使用此类型的参数可以调用 generateKeyPair 生成指定的密钥对。

#### PRIVATE_KEY_SPEC

```cangjie
PRIVATE_KEY_SPEC
```
功能： 表示私钥中包含的参数。使用此类型的参数可以调用 generatePriKey 生成指定的私钥。

#### PUBLIC_KEY_SPEC

```cangjie
PUBLIC_KEY_SPEC
```
功能： 表示公钥中包含的参数。使用此类型的参数可以调用 generatePubKey 生成指定的公钥。


### CipherSpecItem

enum CipherSpecItem
```cangjie
public enum CipherSpecItem <: Equatable<CipherSpecItem> & ToString { | OAEP_MD_NAME_STR | OAEP_MGF_NAME_STR | OAEP_MGF1_MD_STR | OAEP_MGF1_PSRC_UINT8ARR | SM2_MD_NAME_STR | ... }
```
功能： 表示加解密参数的枚举，这些加解密参数支持通过setCipherSpec接口设置/通过getCipherSpec接口获取。

#### OAEP_MD_NAME_STR

```cangjie
OAEP_MD_NAME_STR
```
功能： 表示RSA算法中，使用PKCS1_OAEP模式时，消息摘要功能的算法名。

#### OAEP_MGF1_MD_STR

```cangjie
OAEP_MGF1_MD_STR
```
功能： 表示RSA算法中，使用PKCS1_OAEP模式时，MGF1掩码生成功能的消息摘要算法。

#### OAEP_MGF1_PSRC_UINT8ARR

```cangjie
OAEP_MGF1_PSRC_UINT8ARR
```
功能： 表示RSA算法中，使用PKCS1_OAEP模式时，pSource的字节流。

#### OAEP_MGF_NAME_STR

```cangjie
OAEP_MGF_NAME_STR
```
功能： 表示RSA算法中，使用PKCS1_OAEP模式时，掩码生成算法（目前仅支持MGF1）。

#### SM2_MD_NAME_STR

```cangjie
SM2_MD_NAME_STR
```
功能： 表示SM2算法中，使用的摘要算法名。


### CryptoMode

enum CryptoMode
```cangjie
public enum CryptoMode <: Equatable<CryptoMode> & ToString { | ENCRYPT_MODE | DECRYPT_MODE | ... }
```
功能： 表示加解密操作。

#### DECRYPT_MODE

```cangjie
DECRYPT_MODE
```
功能： 表示进行解密操作。

#### ENCRYPT_MODE

```cangjie
ENCRYPT_MODE
```
功能： 表示进行加密操作。


### Result

enum Result
```cangjie
public enum Result <: ToString { | INVALID_PARAMS | NOT_SUPPORT | ERR_OUT_OF_MEMORY | ERR_RUNTIME_ERROR | ERR_CRYPTO_OPERATION | ... }
```
功能： 表示执行结果。

#### ERR_CRYPTO_OPERATION

```cangjie
ERR_CRYPTO_OPERATION
```
功能： 调用三方算法库API出错。

#### ERR_OUT_OF_MEMORY

```cangjie
ERR_OUT_OF_MEMORY
```
功能： 内存错误。

#### ERR_RUNTIME_ERROR

```cangjie
ERR_RUNTIME_ERROR
```
功能： 运行时外部错误。

#### INVALID_PARAMS

```cangjie
INVALID_PARAMS
```
功能： 非法入参。

#### NOT_SUPPORT

```cangjie
NOT_SUPPORT
```
功能： 操作不支持。


### ResultSpec

enum ResultSpec
```cangjie
public enum ResultSpec <: ToString { | BIGINT(BigInt) | NUMBER(Int32) | STRING(String) | UINT8ARR(Array<UInt8>) | ... }
```
功能： 表示返回值。

#### BIGINT(BigInt)

```cangjie
BIGINT(BigInt)
```
功能： 封装BigInt类型。

#### NUMBER(Int32)

```cangjie
NUMBER(Int32)
```
功能： 封装Int32类型。

#### STRING(String)

```cangjie
STRING(String)
```
功能： 封装String类型。

#### UINT8ARR(Array<UInt8>)

```cangjie
UINT8ARR(Array<UInt8>)
```
功能： 封装Array<UInt8>类型。


### SignSpecItem

enum SignSpecItem
```cangjie
public enum SignSpecItem <: Equatable<SignSpecItem> & ToString { | PSS_MD_NAME_STR | PSS_MGF_NAME_STR | PSS_MGF1_MD_STR | PSS_SALT_LEN_NUM | PSS_TRAILER_FIELD_NUM | SM2_USER_ID_UINT8ARR | ... }
```
功能： 表示签名验签参数的枚举，这些签名验签参数支持通过setSignSpec、setVerifySpec接口设置/通过getSignSpec、getVerifySpec接口获取。

#### PSS_MD_NAME_STR

```cangjie
PSS_MD_NAME_STR
```
功能： 表示RSA算法中，使用PSS模式时，消息摘要功能的算法名。

#### PSS_MGF1_MD_STR

```cangjie
PSS_MGF1_MD_STR
```
功能： 表示RSA算法中，使用PSS模式时，MGF1掩码生成功能的消息摘要参数。

#### PSS_MGF_NAME_STR

```cangjie
PSS_MGF_NAME_STR
```
功能： 表示RSA算法中，使用PSS模式时，掩码生成算法（目前仅支持MGF1）。

#### PSS_SALT_LEN_NUM

```cangjie
PSS_SALT_LEN_NUM
```
功能： 表示RSA算法中，使用PSS模式时，盐值的长度，长度以字节为单位。

#### PSS_TRAILER_FIELD_NUM

```cangjie
PSS_TRAILER_FIELD_NUM
```
功能： 表示RSA算法中，使用PSS模式时，用于编码操作的整数。

#### SM2_USER_ID_UINT8ARR

```cangjie
SM2_USER_ID_UINT8ARR
```
功能： 表示SM2算法中，用户身份标识字段。



